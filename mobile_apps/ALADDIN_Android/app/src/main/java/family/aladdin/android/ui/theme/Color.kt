package family.aladdin.android.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * 🎨 ALADDIN Color Palette
 * Все цвета взяты из iOS Colors.swift и HTML wireframes
 */

// MARK: - Primary Colors

/** Основной синий цвет (кнопки, акценты) */
val PrimaryBlue = Color(0xFF2E5BFF)

/** Вторичный синий (для градиентов) */
val SecondaryBlue = Color(0xFF60A5FA)

/** Вторичный золотой цвет (акценты, иконки) */
val SecondaryGold = Color(0xFFFCD34D)

// MARK: - Semantic Colors

/** Успех, OK статус 🟢 */
val SuccessGreen = Color(0xFF10B981)

/** Опасность, ошибка, угроза 🔴 */
val DangerRed = Color(0xFFEF4444)

/** Предупреждение ⚠️ */
val WarningOrange = Color(0xFFF59E0B)

/** Информация ℹ️ */
val InfoBlue = Color(0xFF3B82F6)

// MARK: - Background Colors

/** Основной фон приложения (тёмный) */
val BackgroundDark = Color(0xFF0F172A)

/** Средний фон (для inputs, toggles) */
val BackgroundMedium = Color(0xFF1E293B)

/** Фон карточек и модальных окон */
val SurfaceDark = Color(0xFF1E293B)

/** Градиент фона - начало */
val GradientStart = Color(0xFF0A1128)

/** Градиент фона - середина */
val GradientMiddle = Color(0xFF1E3A5F)

/** Градиент фона - конец */
val GradientEnd = Color(0xFF2E5090)

// MARK: - Text Colors

/** Основной текст (белый) */
val TextPrimary = Color.White

/** Вторичный текст (серый) */
val TextSecondary = Color(0xFF94A3B8)

/** Третичный текст (более серый) */
val TextTertiary = Color(0xFF64748B)

// MARK: - Unicorn Colors (для детского интерфейса)

/** Фиолетовый цвет единорога */
val UnicornPurple = Color(0xFF9333EA)

/** Розовый цвет единорога */
val UnicornPink = Color(0xFFEC4899)

/** Голубой цвет единорога */
val UnicornBlue = Color(0xFF3B82F6)



