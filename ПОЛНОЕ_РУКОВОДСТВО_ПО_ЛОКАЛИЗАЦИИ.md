

    subtitle: localizationManager.localized("vpn_subtitle"),
    showBackButton: true
)
```

**В словаре:**
```swift
.russian: [
    "vpn_title": "🔒 ALADDIN VPN",
    "vpn_subtitle": "Защита интернета",
],
.english: [
    "vpn_title": "🔒 ALADDIN VPN",
    "vpn_subtitle": "Protect your internet",
],
```

### 3. Паттерн: Условные строки

**Пример из VPNScreen:**
```swift
Text(viewModel.isVPNEnabled 
    ? localizationManager.localized("vpn_protected") 
    : localizationManager.localized("vpn_not_protected"))
```

**В словаре:**
```swift
.russian: [
    "vpn_protected": "ЗАЩИЩЕНО",
    "vpn_not_protected": "НЕ ЗАЩИЩЕНО",
],
.english: [
    "vpn_protected": "PROTECTED",
    "vpn_not_protected": "NOT PROTECTED",
],
```

### 4. Паттерн: Accessibility Labels

**Пример:**
```swift
Button(action: {}) {
    Text(localizationManager.localized("vpn_connect"))
}
.accessibilityLabel(localizationManager.localized("vpn_connect_action"))
.accessibilityHint(localizationManager.localized("vpn_toggle_hint"))
```

**В словаре:**
```swift
.russian: [
    "vpn_connect": "ПОДКЛЮЧИТЬ",
    "vpn_connect_action": "Подключить VPN",
    "vpn_toggle_hint": "Нажмите для переключения VPN",
],
.english: [
    "vpn_connect": "CONNECT",
    "vpn_connect_action": "Connect VPN",
    "vpn_toggle_hint": "Tap to toggle VPN connection",
],
```

### 5. Паттерн: Форматированные строки с параметрами

**Пример:**
```swift
Text(String(format: localizationManager.localized("family_info"), members, devices))
```

**В словаре:**
```swift
.russian: [
    "family_info": "%d членов • %d устройств",
],
.english: [
    "family_info": "%d members • %d devices",
],
```

**Использование:**
```swift
localizationManager.localized("family_info", members, devices)
```

### 6. Паттерн: Enum с локализацией

**Пример из ChildRewardsScreen:**
```swift
enum RewardTab {
    case shop
    case history
    case achievements
    
    func localizedTitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .shop: return localizationManager.localized("child_rewards_tab_shop")
        case .history: return localizationManager.localized("child_rewards_tab_history")
        case .achievements: return localizationManager.localized("child_rewards_tab_achievements")
        }
    }
}
```

**Использование:**
```swift
Text(RewardTab.shop.localizedTitle(localizationManager))
```

### 7. Паттерн: Enum с локализованными массивами

**Пример из TariffsScreen:**
```swift
enum ThreatCategory {
    case cyberThreats
    case fraud
    // ...
    
    func localizedThreats(_ localizationManager: LocalizationManager) -> [String] {
        switch self {
        case .cyberThreats:
            return [
                localizationManager.localized("tariffs_threat_cyber_1"),
                localizationManager.localized("tariffs_threat_cyber_2"),
                // ...
            ]
        case .fraud:
            return [
                localizationManager.localized("tariffs_threat_fraud_1"),
                // ...
            ]
        }
    }
}
```

**Использование:**
```swift
ForEach(Array(category.localizedThreats(localizationManager).enumerated()), id: \.offset) { index, threat in
    Text(threat)
}
```

### 8. Паттерн: Локализация вложенных структур

**Пример из PrivacyPolicyScreen:**
```swift
enum PrivacyMainSection: String, CaseIterable {
    case general
    case principles
    // ...
    
    func localizedTitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .general:
            return localizationManager.localized("privacy_policy_section_general")
        case .principles:
            return localizationManager.localized("privacy_policy_section_principles")
        // ...
        }
    }
    
    func localizedSubtitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .general:
            return localizationManager.localized("privacy_policy_section_general_subtitle")
        // ...
        }
    }
    
    func localizedContent(_ localizationManager: LocalizationManager) -> [String] {
        switch self {
        case .general:
            return [
                localizationManager.localized("privacy_policy_section_general_content_1"),
                localizationManager.localized("privacy_policy_section_general_content_2"),
                // ...
            ]
        // ...
        }
    }
}
```

**Использование:**
```swift
VStack(alignment: .leading) {
    Text(section.localizedTitle(localizationManager))
    Text(section.localizedSubtitle(localizationManager))
    ForEach(section.localizedContent(localizationManager), id: \.self) { content in
        Text(content)
    }
}
```

---

## 🔄 ПОШАГОВАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ

### ЭТАП 1: ПОДГОТОВКА

#### Шаг 1.1: Добавить EnvironmentObject
```swift
struct MyScreen: View {
    @EnvironmentObject private var localizationManager: LocalizationManager
    // ...
}
```

#### Шаг 1.2: Добавить .id() модификатор для принудительного обновления
```swift
var body: some View {
    // ...
}
.id("my_screen_lang_\(localizationManager.currentLanguage.rawValue)")
```

### ЭТАП 2: ЗАМЕНА ХАРДКОДИРОВАННЫХ СТРОК

#### Шаг 2.1: Найти все хардкодированные строки
```bash
grep -n '"[А-Яа-я]' Screens/MyScreen.swift
```

#### Шаг 2.2: Заменить на вызовы локализации
**До:**
```swift
Text("Привет")
```

**После:**
```swift
Text(localizationManager.localized("hello"))
```

#### Шаг 2.3: Создать ключи по конвенции
**Конвенция:** `screen_section_item`

**Примеры:**
- `vpn_title` - заголовок VPN экрана
- `vpn_subtitle` - подзаголовок VPN экрана
- `vpn_connect` - кнопка подключения
- `vpn_protected` - статус "защищено"
- `vpn_status_hint` - подсказка для accessibility

### ЭТАП 3: РАБОТА С ENUMS

#### Шаг 3.1: Если enum содержит строки
**До:**
```swift
enum Tab {
    case shop
    case history
    
    var title: String {
        switch self {
        case .shop: return "Магазин"
        case .history: return "История"
        }
    }
}
```

**После:**
```swift
enum Tab {
    case shop
    case history
    
    func localizedTitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .shop: return localizationManager.localized("tab_shop")
        case .history: return localizationManager.localized("tab_history")
        }
    }
}
```

#### Шаг 3.2: Если enum содержит массивы
**До:**
```swift
enum Category {
    case threats
    
    var items: [String] {
        switch self {
        case .threats: return ["Угроза 1", "Угроза 2"]
        }
    }
}
```

**После:**
```swift
enum Category {
    case threats
    
    func localizedItems(_ localizationManager: LocalizationManager) -> [String] {
        switch self {
        case .threats:
            return [
                localizationManager.localized("threat_1"),
                localizationManager.localized("threat_2"),
            ]
        }
    }
}
```

### ЭТАП 4: ДОБАВЛЕНИЕ КЛЮЧЕЙ В СЛОВАРИ

#### Шаг 4.1: Найти место в словаре
**Файл:** `Core/Localization/LocalizationManager.swift`

**Структура:**
```swift
lazy var translations: [Language: [String: String]] = [
    .russian: [
        // ... существующие ключи ...
        "my_new_key": "Мой новый текст",
    ],
    .english: [
        // ... существующие ключи ...
        "my_new_key": "My new text",
    ],
]
```

#### Шаг 4.2: Добавить ключи в оба словаря ОДНОВРЕМЕННО
**⚠️ КРИТИЧЕСКИ ВАЖНО:** Всегда добавлять в оба словаря одновременно!

**Правильно:**
```swift
// В .russian:
"my_key": "Мой текст",

// В .english:
"my_key": "My text",
```

**Неправильно:**
```swift
// Только в .russian - ❌ ОШИБКА!
"my_key": "Мой текст",
```

#### Шаг 4.3: Группировать ключи по экранам
```swift
.russian: [
    // My Screen
    "my_screen_title": "Мой экран",
    "my_screen_subtitle": "Подзаголовок",
    "my_screen_button": "Кнопка",
    
    // Next Screen
    "next_screen_title": "Следующий экран",
],
```

### ЭТАП 5: ПРОВЕРКА И ВАЛИДАЦИЯ

#### Шаг 5.1: Проверить на дубликаты
```bash
python3 scripts/check_localization_duplicates.py
```

**Ожидаемый результат:**
```
✅ RUSSIAN: дубликатов не найдено
✅ ENGLISH: дубликатов не найдено
```

#### Шаг 5.2: Проверить наличие всех ключей
```bash
# Извлечь все ключи из экрана
grep -o 'localizationManager\.localized("\([^"]*\)")' Screens/MyScreen.swift | sed 's/localizationManager\.localized("\(.*\)")/\1/' | sort -u

# Проверить каждый ключ
grep -c '"my_key":' Core/Localization/LocalizationManager.swift
# Должно быть: 2 (по одному в каждом словаре)
```

#### Шаг 5.3: Проверить компиляцию
```bash
# В Xcode: Cmd+B
# Или через терминал (если настроено)
```

#### Шаг 5.4: Проверить работу в приложении
1. Запустить приложение
2. Переключить язык
3. Проверить, что все тексты обновились

---

## 📝 ПРИМЕРЫ ИЗ РЕАЛЬНЫХ ЭКРАНОВ

### Пример 1: VPNScreen - Простой экран

**Ключевые элементы:**
1. Заголовок и подзаголовок
2. Условные строки (статус VPN)
3. Кнопки действий
4. Accessibility labels

**Код:**
```swift
struct VPNScreen: View {
    @EnvironmentObject private var localizationManager: LocalizationManager
    
    var body: some View {
        VStack {
            ALADDINNavigationBar(
                title: localizationManager.localized("vpn_title"),
                subtitle: localizationManager.localized("vpn_subtitle")
            )
            
            Text(viewModel.isVPNEnabled 
                ? localizationManager.localized("vpn_protected") 
                : localizationManager.localized("vpn_not_protected"))
            
            Button(action: {}) {
                Text(viewModel.isVPNEnabled 
                    ? localizationManager.localized("vpn_disconnect") 
                    : localizationManager.localized("vpn_connect"))
            }
            .accessibilityLabel(localizationManager.localized("vpn_toggle_hint"))
        }
        .id("vpn_screen_lang_\(localizationManager.currentLanguage.rawValue)")
    }
}
```

### Пример 2: PrivacyPolicyScreen - Сложный экран с Enums

**Ключевые элементы:**
1. Enum с локализованными методами
2. Динамический контент
3. Табы с локализацией

**Код:**
```swift
enum PrivacyTab {
    case main
    case vpn
    
    func localizedTitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .main: return localizationManager.localized("privacy_policy_tab_main")
        case .vpn: return localizationManager.localized("privacy_policy_tab_vpn")
        }
    }
}

enum PrivacyMainSection {
    case general
    
    func localizedTitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .general:
            return localizationManager.localized("privacy_policy_section_general")
        }
    }
    
    func localizedContent(_ localizationManager: LocalizationManager) -> [String] {
        switch self {
        case .general:
            return [
                localizationManager.localized("privacy_policy_section_general_content_1"),
                localizationManager.localized("privacy_policy_section_general_content_2"),
            ]
        }
    }
}

struct PrivacyPolicyScreen: View {
    @EnvironmentObject private var localizationManager: LocalizationManager
    
    var body: some View {
        VStack {
            HStack {
                tabButton(title: PrivacyTab.main.localizedTitle(localizationManager), tab: .main)
                tabButton(title: PrivacyTab.vpn.localizedTitle(localizationManager), tab: .vpn)
            }
            
            ForEach(PrivacyMainSection.allCases, id: \.self) { section in
                VStack {
                    Text(section.localizedTitle(localizationManager))
                    ForEach(section.localizedContent(localizationManager), id: \.self) { content in
                        Text(content)
                    }
                }
            }
        }
        .id("privacy_policy_lang_\(localizationManager.currentLanguage.rawValue)")
    }
}
```

### Пример 3: TariffsScreen - Enum с большими массивами

**Ключевые элементы:**
1. Enum с локализованными массивами угроз
2. Метод `localizedThreats()`

**Код:**
```swift
enum ThreatCategory {
    case cyberThreats
    case fraud
    
    func localizedThreats(_ localizationManager: LocalizationManager) -> [String] {
        switch self {
        case .cyberThreats:
            return [
                localizationManager.localized("tariffs_threat_cyber_1"),
                localizationManager.localized("tariffs_threat_cyber_2"),
                // ... 10 угроз
            ]
        case .fraud:
            return [
                localizationManager.localized("tariffs_threat_fraud_1"),
                // ... 12 угроз
            ]
        }
    }
}

struct TariffsScreen: View {
    @EnvironmentObject private var localizationManager: LocalizationManager
    
    var body: some View {
        ForEach(ThreatCategory.allCases, id: \.self) { category in
            VStack {
                Text(category.localizedTitle(localizationManager))
                ForEach(Array(category.localizedThreats(localizationManager).enumerated()), id: \.offset) { index, threat in
                    Text(threat)
                }
            }
        }
        .id("tariffs_lang_\(localizationManager.currentLanguage.rawValue)")
    }
}
```

### Пример 4: SettingsScreen - Секции с принудительным обновлением

**Ключевые элементы:**
1. Отдельные `.id()` для секций
2. Enum с локализованным `displayName`

**Код:**
```swift
enum ThemeMode {
    case light
    case dark
    case system
    
    func displayName(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .light: return localizationManager.localized("theme_light")
        case .dark: return localizationManager.localized("theme_dark")
        case .system: return localizationManager.localized("theme_system")
        }
    }
}

struct SettingsScreen: View {
    @EnvironmentObject private var localizationManager: LocalizationManager
    
    private var appSection: some View {
        VStack {
            Text(localizationManager.localized("language"))
            Text(localizationManager.localized("dark_theme"))
        }
        .id("app_section_\(localizationManager.currentLanguage.rawValue)")
    }
    
    private var additionalSection: some View {
        VStack {
            Text(localizationManager.localized("help_support"))
        }
        .id("additional_section_\(localizationManager.currentLanguage.rawValue)")
    }
    
    var body: some View {
        VStack {
            appSection
            additionalSection
        }
        .id("settings_lang_\(localizationManager.currentLanguage.rawValue)")
    }
}
```

---

## 🔧 РАБОТА С ENUMS

### Паттерн 1: Enum с одним локализованным значением

```swift
enum Tab {
    case shop
    case history
    
    func localizedTitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .shop: return localizationManager.localized("tab_shop")
        case .history: return localizationManager.localized("tab_history")
        }
    }
}
```

### Паттерн 2: Enum с несколькими локализованными значениями

```swift
enum Section {
    case general
    
    func localizedTitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .general:
            return localizationManager.localized("section_general_title")
        }
    }
    
    func localizedSubtitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .general:
            return localizationManager.localized("section_general_subtitle")
        }
    }
}
```

### Паттерн 3: Enum с локализованным массивом

```swift
enum Category {
    case threats
    
    func localizedItems(_ localizationManager: LocalizationManager) -> [String] {
        switch self {
        case .threats:
            return [
                localizationManager.localized("threat_1"),
                localizationManager.localized("threat_2"),
            ]
        }
    }
}
```

---

## 🔄 РАБОТА С ДИНАМИЧЕСКИМ КОНТЕНТОМ

### Паттерн: Массивы контента

**До:**
```swift
enum Section {
    case general
    
    var content: [String] {
        switch self {
        case .general:
            return [
                "Пункт 1",
                "Пункт 2",
            ]
        }
    }
}

ForEach(section.content, id: \.self) { item in
    Text(item)
}
```

**После:**
```swift
enum Section {
    case general
    
    func localizedContent(_ localizationManager: LocalizationManager) -> [String] {
        switch self {
        case .general:
            return [
                localizationManager.localized("section_general_content_1"),
                localizationManager.localized("section_general_content_2"),
            ]
        }
    }
}

ForEach(section.localizedContent(localizationManager), id: \.self) { item in
    Text(item)
}
```

---

## 🔄 ПРИНУДИТЕЛЬНОЕ ОБНОВЛЕНИЕ UI

### Проблема: SwiftUI не обновляет View при изменении языка

**Решение:** Использовать `.id()` модификатор

### Паттерн 1: Обновление всего экрана

```swift
var body: some View {
    VStack {
        // ...
    }
    .id("my_screen_lang_\(localizationManager.currentLanguage.rawValue)")
}
```

### Паттерн 2: Обновление отдельных секций

```swift
private var appSection: some View {
    VStack {
        // ...
    }
    .id("app_section_\(localizationManager.currentLanguage.rawValue)")
}

private var additionalSection: some View {
    VStack {
        // ...
    }
    .id("additional_section_\(localizationManager.currentLanguage.rawValue)")
}
```

### Паттерн 3: Обновление модальных окон

```swift
.sheet(isPresented: $showModal) {
    MyModal()
        .environmentObject(localizationManager)
        .id("modal_lang_\(localizationManager.currentLanguage.rawValue)")
}
```

---

## ✅ ЧЕКЛИСТЫ И ВАЛИДАЦИЯ

### Чеклист перед началом локализации

- [ ] Найти файл экрана (`Screens/XX_Screen.swift`)
- [ ] Добавить `@EnvironmentObject private var localizationManager: LocalizationManager`
- [ ] Найти все хардкодированные строки
- [ ] Составить список всех ключей, которые нужно добавить

### Чеклист во время локализации

- [ ] Заменить все хардкодированные строки на `localizationManager.localized("key")`
- [ ] Создать методы локализации для enums (если есть)
- [ ] Добавить `.id()` модификатор для принудительного обновления
- [ ] Добавить все ключи в `.russian` словарь
- [ ] Добавить все ключи в `.english` словарь

### Чеклист после локализации

- [ ] Проверить на дубликаты: `python3 scripts/check_localization_duplicates.py`
- [ ] Проверить наличие всех ключей в обоих словарях
- [ ] Проверить компиляцию (Cmd+B в Xcode)
- [ ] Запустить приложение и проверить работу
- [ ] Переключить язык и проверить обновление UI

---

## 🚨 ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ

### Ошибка 1: UI не обновляется при смене языка

**Проблема:**
```swift
var body: some View {
    Text(localizationManager.localized("my_key"))
    // При смене языка текст не обновляется
}
```

**Решение:**
```swift
var body: some View {
    Text(localizationManager.localized("my_key"))
}
.id("my_screen_lang_\(localizationManager.currentLanguage.rawValue)")
```

### Ошибка 2: Enum не локализуется

**Проблема:**
```swift
enum Tab {
    case shop
    
    var title: String {
        return "Магазин" // Хардкод
    }
}
```

**Решение:**
```swift
enum Tab {
    case shop
    
    func localizedTitle(_ localizationManager: LocalizationManager) -> String {
        switch self {
        case .shop: return localizationManager.localized("tab_shop")
        }
    }
}
```

### Ошибка 3: Массив не локализуется

**Проблема:**
```swift
enum Category {
    case threats
    
    var items: [String] {
        return ["Угроза 1", "Угроза 2"] // Хардкод
    }
}
```

**Решение:**
```swift
enum Category {
    case threats
    
    func localizedItems(_ localizationManager: LocalizationManager) -> [String] {
        switch self {
        case .threats:
            return [
                localizationManager.localized("threat_1"),
                localizationManager.localized("threat_2"),
            ]
        }
    }
}
```

### Ошибка 4: Дубликаты ключей

**Проблема:**
```swift
.russian: [
    "my_key": "Текст 1",
    "my_key": "Текст 2", // ❌ ДУБЛИКАТ!
],
```

**Решение:**
```bash
# Проверить перед коммитом
python3 scripts/check_localization_duplicates.py

# Удалить дубликат, оставив только одно значение
```

### Ошибка 5: Ключ добавлен только в один словарь

**Проблема:**
```swift
.russian: [
    "my_key": "Мой текст",
],
.english: [
    // ❌ Ключ отсутствует!
],
```

**Решение:**
```swift
.russian: [
    "my_key": "Мой текст",
],
.english: [
    "my_key": "My text", // ✅ Добавить в оба словаря
],
```

---

## 📊 СТАТИСТИКА И МЕТРИКИ

### Текущее состояние (2025-11-06)

- **Всего экранов локализовано:** 22
- **Всего ключей в русском словаре:** 1465
- **Всего ключей в английском словаре:** 1042
- **Дубликатов:** 0
- **Покрытие:** 100%

### Примеры локализованных экранов

1. ✅ **MainScreen** - 22 ключа
2. ✅ **FamilyScreen** - 117 ключей
3. ✅ **VPNScreen** - 37 ключей
4. ✅ **SettingsScreen** - 29 ключей
5. ✅ **ProfileScreen** - 30 ключей
6. ✅ **PrivacyPolicyScreen** - 55 ключей
7. ✅ **TermsOfServiceScreen** - 87 ключей
8. ✅ **ChildRewardsScreen** - 100+ ключей
9. ✅ **TariffsScreen** - 109 новых ключей (100 угроз)

---

## 🛠️ ИНСТРУМЕНТЫ

### Скрипт проверки дубликатов

```bash
python3 scripts/check_localization_duplicates.py
```

**Результат:**
```
✅ RUSSIAN: дубликатов не найдено (1465 уникальных ключей)
✅ ENGLISH: дубликатов не найдено (1042 уникальных ключей)
```

### Команды для проверки ключей

```bash
# Извлечь все ключи из экрана
grep -o 'localizationManager\.localized("\([^"]*\)")' Screens/MyScreen.swift | sed 's/localizationManager\.localized("\(.*\)")/\1/' | sort -u

# Проверить наличие ключа
grep -c '"my_key":' Core/Localization/LocalizationManager.swift
# Должно быть: 2 (по одному в каждом словаре)
```

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

### Документы проекта

1. **ИНСТРУКЦИЯ_ДЛЯ_ML_МОДЕЛИ_ИСПРАВЛЕНИЕ_ЛОКАЛИЗАЦИИ.md** - пошаговая инструкция
2. **LOCALIZATION_BEST_PRACTICES.md** - лучшие практики
3. **ОТЧЕТ_ПО_ЛОКАЛИЗАЦИИ_ФИНАЛЬНЫЙ.md** - финальный отчет
4. **ПЛАН_ЛОКАЛИЗАЦИИ_ТАРИФОВ_И_AI.md** - план локализации тарифов

### Ключевые файлы

- `Core/Localization/LocalizationManager.swift` - основной менеджер локализации
- `scripts/check_localization_duplicates.py` - скрипт проверки дубликатов

---

## 🎯 КРАТКАЯ ПАМЯТКА

### Алгоритм локализации нового экрана:

1. ✅ Добавить `@EnvironmentObject private var localizationManager: LocalizationManager`
2. ✅ Заменить все хардкодированные строки на `localizationManager.localized("key")`
3. ✅ Создать методы локализации для enums (если есть)
4. ✅ Добавить `.id("screen_lang_\(localizationManager.currentLanguage.rawValue)")`
5. ✅ Добавить все ключи в `.russian` словарь
6. ✅ Добавить все ключи в `.english` словарь
7. ✅ Проверить на дубликаты: `python3 scripts/check_localization_duplicates.py`
8. ✅ Проверить компиляцию и работу в приложении

---

**Дата создания:** 2025-11-06  
**Последнее обновление:** 2025-11-06  
**Статус:** ✅ Полное руководство готово к использованию



