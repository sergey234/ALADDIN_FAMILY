# 🎯 ПЛАН ИСПРАВЛЕНИЙ СТРАНИЦЫ АНАЛИТИКА - ПО КАЖДОМУ ПУНКТУ

**Дата:** 2026-03-13  
**Build:** Текущий  
**Статус:** 🔴 **ГОТОВ К РЕАЛИЗАЦИИ**  

---

## 📋 СТРУКТУРА ПЛАНА

Каждый пункт имеет:
- ✅ Детальное описание проблемы
- ✅ Файлы для изменения
- ✅ Точные строки кода
- ✅ Решение с примерами кода
- ✅ Проверочный чек-лист
- ✅ TODO задачи

---

## 🔴 ПУНКТ 1: "Заблокировано" обрезается до "Заблокир..."

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, верхняя секция с карточками статистики
- **Проблема:** Текст "Заблокировано" обрезается и видно только "Заблокир..."
- **Требование:** Текст должен полностью отображаться на 1 строке без переносов

### 📁 Файлы для изменения:
- `Screens/04_AnalyticsScreen.swift` (строки 160-189, функция `mainStats`)
- `Resources/Localization/ru.lproj/Localizable.strings` (проверить ключ `analytics_blocked_short`)

### 🔧 Решение:

**Шаг 1:** Проверить ключ локализации
```swift
// Найти ключ "analytics_blocked_short" в Localizable.strings
// Убедиться, что значение короткое: "Заблокировано" или "Заблок."
```

**Шаг 2:** Исправить функцию `compactStatCard` в `AnalyticsScreen.swift`
```swift
// ❌ БЫЛО:
Text(label)
    .font(.system(size: 10))
    .foregroundColor(.textSecondary)
    .lineLimit(1)  // Обрезается!

// ✅ СТАЛО:
Text(label)
    .font(.system(size: 10))
    .foregroundColor(.textSecondary)
    .lineLimit(1)
    .minimumScaleFactor(0.7)  // Автоматическое уменьшение шрифта
    .fixedSize(horizontal: false, vertical: true)  // Предотвращение обрезания
```

**Шаг 3:** Проверить ширину карточек
```swift
// Убедиться, что карточки имеют достаточно места
HStack(spacing: Spacing.s) {
    // 4 карточки должны помещаться
    compactStatCard(...)
    compactStatCard(...)
    compactStatCard(...)
    compactStatCard(...)
}
.frame(maxWidth: .infinity)  // ✅ Использовать всю ширину
```

### ✅ Чек-лист:
- [ ] Проверить ключ локализации `analytics_blocked_short`
- [ ] Добавить `.minimumScaleFactor(0.7)` к тексту label
- [ ] Добавить `.fixedSize(horizontal: false, vertical: true)` к тексту label
- [ ] Проверить ширину карточек
- [ ] Протестировать на разных размерах экрана
- [ ] Убедиться, что текст полностью виден

### 📋 TODO для пункта 1:
- [ ] **TODO 1.1:** Найти ключ локализации `analytics_blocked_short` в `Localizable.strings`
- [ ] **TODO 1.2:** Проверить значение ключа - короткое ли оно?
- [ ] **TODO 1.3:** Открыть файл `Screens/04_AnalyticsScreen.swift`
- [ ] **TODO 1.4:** Найти функцию `compactStatCard` (строки 289-314)
- [ ] **TODO 1.5:** Добавить `.minimumScaleFactor(0.7)` к `Text(label)`
- [ ] **TODO 1.6:** Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(label)`
- [ ] **TODO 1.7:** Проверить ширину карточек в `mainStats`
- [ ] **TODO 1.8:** Скомпилировать и протестировать
- [ ] **TODO 1.9:** Убедиться, что "Заблокировано" полностью видно

---

## 🔴 ПУНКТ 2: "Эффективность" обрезается до "Эффектив..."

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, верхняя секция с карточками статистики
- **Проблема:** Текст "Эффективность" обрезается и видно только "Эффектив..."
- **Требование:** Текст должен полностью отображаться на 1 строке без переносов, выровнен

### 📁 Файлы для изменения:
- `Screens/04_AnalyticsScreen.swift` (строки 160-189, функция `mainStats`)
- `Resources/Localization/ru.lproj/Localizable.strings` (проверить ключ `analytics_effectiveness_short`)

### 🔧 Решение:

**Шаг 1:** Проверить ключ локализации
```swift
// Найти ключ "analytics_effectiveness_short" в Localizable.strings
// Убедиться, что значение короткое: "Эффективность" или "Эффект."
```

**Шаг 2:** Исправить функцию `compactStatCard` (то же самое, что в пункте 1)
```swift
// ✅ Применить те же исправления:
Text(label)
    .font(.system(size: 10))
    .foregroundColor(.textSecondary)
    .lineLimit(1)
    .minimumScaleFactor(0.7)  // ✅
    .fixedSize(horizontal: false, vertical: true)  // ✅
```

**Шаг 3:** Выровнять все карточки
```swift
// Убедиться, что все карточки имеют одинаковую ширину
private func compactStatCard(icon: String, value: String, label: String) -> some View {
    VStack(spacing: 4) {
        // ...
        Text(label)
            .font(.system(size: 10))
            .foregroundColor(.textSecondary)
            .lineLimit(1)
            .minimumScaleFactor(0.7)
            .fixedSize(horizontal: false, vertical: true)
            .frame(maxWidth: .infinity)  // ✅ Выравнивание по ширине
            .multilineTextAlignment(.center)  // ✅ Центрирование текста
    }
    .frame(maxWidth: .infinity)  // ✅ Равная ширина для всех карточек
}
```

### ✅ Чек-лист:
- [ ] Проверить ключ локализации `analytics_effectiveness_short`
- [ ] Добавить `.minimumScaleFactor(0.7)` к тексту label
- [ ] Добавить `.fixedSize(horizontal: false, vertical: true)` к тексту label
- [ ] Добавить `.frame(maxWidth: .infinity)` для выравнивания
- [ ] Добавить `.multilineTextAlignment(.center)` для центрирования
- [ ] Протестировать на разных размерах экрана
- [ ] Убедиться, что текст полностью виден и выровнен

### 📋 TODO для пункта 2:
- [ ] **TODO 2.1:** Найти ключ локализации `analytics_effectiveness_short` в `Localizable.strings`
- [ ] **TODO 2.2:** Проверить значение ключа - короткое ли оно?
- [ ] **TODO 2.3:** Открыть файл `Screens/04_AnalyticsScreen.swift`
- [ ] **TODO 2.4:** Найти функцию `compactStatCard` (строки 289-314)
- [ ] **TODO 2.5:** Добавить `.minimumScaleFactor(0.7)` к `Text(label)`
- [ ] **TODO 2.6:** Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(label)`
- [ ] **TODO 2.7:** Добавить `.frame(maxWidth: .infinity)` для выравнивания
- [ ] **TODO 2.8:** Добавить `.multilineTextAlignment(.center)` для центрирования
- [ ] **TODO 2.9:** Скомпилировать и протестировать
- [ ] **TODO 2.10:** Убедиться, что "Эффективность" полностью видно и выровнено

---

## 🔴 ПУНКТ 3: Проверить данные угроз - реальные или MOCK?

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, секция "Детали угроз"
- **Данные:** Вэб угрозы 542, Угрозы в файлах 318, Угрозы в приложениях 187, Сетевые угрозы 200
- **Вопрос:** Это реальные данные с сервера или MOCK данные?
- **Требование:** Проверить всю логику работы данных

### 📁 Файлы для проверки:
- `ViewModels/AnalyticsViewModel.swift` (строки 1-209)
- `Core/Analytics/LocalAnalyticsService.swift` (если есть)
- `Core/Analytics/RemoteAnalyticsService.swift` (если есть)
- `Core/Analytics/AnalyticsService.swift` (проверить протокол)

### 🔧 Решение:

**Шаг 1:** Найти источник данных `threatCategories`
```swift
// В AnalyticsViewModel.swift найти:
@Published private(set) var threatCategories: [ThreatTypeCount] = []

// Найти метод, который заполняет threatCategories:
private func apply(securityAnalytics: SecurityAnalytics) {
    threatCategories = securityAnalytics.blockedThreats
}
```

**Шаг 2:** Проверить `AnalyticsService`
```swift
// Найти протокол AnalyticsService и его реализации:
// - LocalAnalyticsService (MOCK данные)
// - RemoteAnalyticsService (реальные данные с API)
```

**Шаг 3:** Проверить, какой сервис используется
```swift
// В AnalyticsScreen.swift найти:
static func makeViewModel() -> AnalyticsViewModel {
    let service: AnalyticsService = {
        if AppConfig.useMockAPI {
            return LocalAnalyticsService()  // MOCK
        } else {
            return RemoteAnalyticsService()  // Реальные данные
        }
    }()
    return AnalyticsViewModel(service: service)
}
```

**Шаг 4:** Проверить данные в `LocalAnalyticsService`
```swift
// Если используется LocalAnalyticsService, проверить:
// - Откуда берутся данные угроз?
// - Это статические данные или вычисляемые?
// - Какие значения возвращаются?
```

**Шаг 5:** Проверить данные в `RemoteAnalyticsService`
```swift
// Если используется RemoteAnalyticsService, проверить:
// - Какой API endpoint используется?
// - Как обрабатываются данные с сервера?
// - Есть ли fallback на MOCK данные при ошибке?
```

**Шаг 6:** Проверить логику вычисления
```swift
// В AnalyticsViewModel проверить:
// - Как вычисляются значения угроз?
// - Есть ли формулы или проценты?
// - Откуда берутся числа 542, 318, 187, 200?
```

### ✅ Чек-лист:
- [ ] Найти источник данных `threatCategories`
- [ ] Проверить, какой сервис используется (Local или Remote)
- [ ] Проверить `LocalAnalyticsService` - какие данные возвращает?
- [ ] Проверить `RemoteAnalyticsService` - какой API endpoint используется?
- [ ] Проверить логику вычисления значений угроз
- [ ] Проверить, есть ли fallback на MOCK данные
- [ ] Убедиться, что данные корректны

### 📋 TODO для пункта 3:
- [ ] **TODO 3.1:** Открыть файл `ViewModels/AnalyticsViewModel.swift`
- [ ] **TODO 3.2:** Найти свойство `threatCategories` (строка 13)
- [ ] **TODO 3.3:** Найти метод `apply(securityAnalytics:)` (строка 196)
- [ ] **TODO 3.4:** Проследить, откуда приходит `SecurityAnalytics`
- [ ] **TODO 3.5:** Открыть файл `Screens/04_AnalyticsScreen.swift`
- [ ] **TODO 3.6:** Найти функцию `makeViewModel()` (строки 515-534)
- [ ] **TODO 3.7:** Проверить, какой сервис используется (Local или Remote)
- [ ] **TODO 3.8:** Найти файл `Core/Analytics/LocalAnalyticsService.swift`
- [ ] **TODO 3.9:** Проверить метод `fetchSecurityAnalytics` - какие данные возвращает?
- [ ] **TODO 3.10:** Найти файл `Core/Analytics/RemoteAnalyticsService.swift`
- [ ] **TODO 3.11:** Проверить метод `fetchSecurityAnalytics` - какой API endpoint используется?
- [ ] **TODO 3.12:** Проверить логику вычисления значений угроз (542, 318, 187, 200)
- [ ] **TODO 3.13:** Убедиться, что данные реальные или MOCK (задокументировать)
- [ ] **TODO 3.14:** Если данные MOCK, проверить, нужно ли переключить на реальные

---

## 🔴 ПУНКТ 4: "Отчет о вождении" обрезается до "Отчет о вожд..."

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, секция "Компоненты защиты"
- **Проблема:** Текст "Отчет о вождении" обрезается и видно только "Отчет о вожд..."
- **Требование:** Текст должен полностью читаться без переносов на другие строки, уместить все

### 📁 Файлы для изменения:
- `Shared/Components/ComponentReportCard.swift` (строки 37-41, секция с title)
- `Resources/Localization/ru.lproj/Localizable.strings` (проверить ключ `component_driving_reports_agent_title`)

### 🔧 Решение:

**Шаг 1:** Проверить ключ локализации
```swift
// Найти ключ "component_driving_reports_agent_title" в Localizable.strings
// Текущее значение: "Отчеты о вождении"
// Можно сократить до: "Отчеты о вождении" или "Вождение"
```

**Шаг 2:** Изучить реализацию на странице "Защита АЛАДДИН"
```swift
// Найти страницу "Защита АЛАДДИН Экстренная помощь-Защита от угроз"
// Посмотреть, как там реализовано отображение текста на 1 строке
// Применить аналогичный подход
```

**Шаг 3:** Исправить `ComponentReportCard.swift`
```swift
// ❌ БЫЛО:
VStack(alignment: .leading, spacing: Spacing.xs) {
    Text(title)
        .font(.bodyBold)
        .foregroundColor(.textPrimary)
        .lineLimit(1)  // Обрезается!
    
    // Метрики...
}

// ✅ СТАЛО:
VStack(alignment: .leading, spacing: Spacing.xs) {
    Text(title)
        .font(.bodyBold)
        .foregroundColor(.textPrimary)
        .lineLimit(1)
        .minimumScaleFactor(0.8)  // Автоматическое уменьшение шрифта
        .fixedSize(horizontal: false, vertical: true)  // Предотвращение обрезания
    
    // Метрики...
}
```

**Шаг 4:** Проверить ширину карточки
```swift
// Убедиться, что карточка имеет достаточно места
HStack(spacing: Spacing.m) {
    iconWithBadge
    VStack(alignment: .leading, spacing: Spacing.xs) {
        Text(title)
            // ... исправления ...
    }
    Spacer()  // ✅ Занимает все доступное пространство
    Image(systemName: "chevron.right")
}
.padding(Spacing.m)
.frame(maxWidth: .infinity)  // ✅ Использовать всю ширину
```

### ✅ Чек-лист:
- [ ] Проверить ключ локализации `component_driving_reports_agent_title`
- [ ] Найти страницу "Защита АЛАДДИН" и изучить реализацию
- [ ] Добавить `.minimumScaleFactor(0.8)` к `Text(title)`
- [ ] Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)`
- [ ] Проверить ширину карточки
- [ ] Протестировать на разных размерах экрана
- [ ] Убедиться, что текст полностью виден

### 📋 TODO для пункта 4:
- [ ] **TODO 4.1:** Найти ключ локализации `component_driving_reports_agent_title` в `Localizable.strings`
- [ ] **TODO 4.2:** Проверить значение ключа - можно ли сократить?
- [ ] **TODO 4.3:** Найти страницу "Защита АЛАДДИН Экстренная помощь-Защита от угроз"
- [ ] **TODO 4.4:** Изучить реализацию отображения текста на этой странице
- [ ] **TODO 4.5:** Открыть файл `Shared/Components/ComponentReportCard.swift`
- [ ] **TODO 4.6:** Найти секцию с `Text(title)` (строки 37-41)
- [ ] **TODO 4.7:** Добавить `.minimumScaleFactor(0.8)` к `Text(title)`
- [ ] **TODO 4.8:** Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)`
- [ ] **TODO 4.9:** Проверить ширину карточки в `HStack`
- [ ] **TODO 4.10:** Применить аналогичный подход с страницы "Защита АЛАДДИН"
- [ ] **TODO 4.11:** Скомпилировать и протестировать
- [ ] **TODO 4.12:** Убедиться, что "Отчет о вождении" полностью видно

---

## 🔴 ПУНКТ 5: "Мониторинг Дарк Веб" обрезается до "Мониторинг Д..."

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, секция "Компоненты защиты"
- **Проблема:** Текст "Мониторинг Дарк Веб" обрезается и видно только "Мониторинг Д..."
- **Требование:** Текст должен полностью отображаться на 1 строке

### 📁 Файлы для изменения:
- `Shared/Components/ComponentReportCard.swift` (строки 37-41, секция с title)
- `Resources/Localization/ru.lproj/Localizable.strings` (проверить ключ `component_dark_web_monitoring_agent_title`)

### 🔧 Решение:

**Шаг 1:** Проверить ключ локализации
```swift
// Найти ключ "component_dark_web_monitoring_agent_title" в Localizable.strings
// Текущее значение: "Мониторинг Dark Web"
// Можно сократить до: "Дарк Веб" или "Dark Web"
```

**Шаг 2:** Исправить `ComponentReportCard.swift` (то же самое, что в пункте 4)
```swift
// ✅ Применить те же исправления:
Text(title)
    .font(.bodyBold)
    .foregroundColor(.textPrimary)
    .lineLimit(1)
    .minimumScaleFactor(0.8)  // ✅
    .fixedSize(horizontal: false, vertical: true)  // ✅
```

### ✅ Чек-лист:
- [ ] Проверить ключ локализации `component_dark_web_monitoring_agent_title`
- [ ] Добавить `.minimumScaleFactor(0.8)` к `Text(title)`
- [ ] Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)`
- [ ] Протестировать на разных размерах экрана
- [ ] Убедиться, что текст полностью виден

### 📋 TODO для пункта 5:
- [ ] **TODO 5.1:** Найти ключ локализации `component_dark_web_monitoring_agent_title` в `Localizable.strings`
- [ ] **TODO 5.2:** Проверить значение ключа - можно ли сократить?
- [ ] **TODO 5.3:** Открыть файл `Shared/Components/ComponentReportCard.swift`
- [ ] **TODO 5.4:** Найти секцию с `Text(title)` (строки 37-41)
- [ ] **TODO 5.5:** Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 5.6:** Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 5.7:** Скомпилировать и протестировать
- [ ] **TODO 5.8:** Убедиться, что "Мониторинг Дарк Веб" полностью видно

---

## 🔴 ПУНКТ 6: "Защита кражи личности"

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, секция "Компоненты защиты"
- **Проблема:** Нужно убедиться, что текст полностью виден на 1 строке
- **Требование:** Текст должен полностью отображаться на 1 строке без переносов

### 📁 Файлы для изменения:
- `Shared/Components/ComponentReportCard.swift` (строки 37-41, секция с title)
- `Resources/Localization/ru.lproj/Localizable.strings` (проверить ключ `component_russian_identity_theft_protection_agent_title`)

### 🔧 Решение:

**Шаг 1:** Проверить ключ локализации
```swift
// Найти ключ "component_russian_identity_theft_protection_agent_title" в Localizable.strings
// Текущее значение: "Защита от кражи личности (РФ)"
// Можно сократить до: "Защита личности" или "Кража личности"
```

**Шаг 2:** Исправить `ComponentReportCard.swift` (то же самое, что в пункте 4)
```swift
// ✅ Применить те же исправления:
Text(title)
    .font(.bodyBold)
    .foregroundColor(.textPrimary)
    .lineLimit(1)
    .minimumScaleFactor(0.8)  // ✅
    .fixedSize(horizontal: false, vertical: true)  // ✅
```

### ✅ Чек-лист:
- [ ] Проверить ключ локализации `component_russian_identity_theft_protection_agent_title`
- [ ] Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] Протестировать на разных размерах экрана
- [ ] Убедиться, что текст полностью виден

### 📋 TODO для пункта 6:
- [ ] **TODO 6.1:** Найти ключ локализации `component_russian_identity_theft_protection_agent_title` в `Localizable.strings`
- [ ] **TODO 6.2:** Проверить значение ключа - можно ли сократить?
- [ ] **TODO 6.3:** Открыть файл `Shared/Components/ComponentReportCard.swift`
- [ ] **TODO 6.4:** Найти секцию с `Text(title)` (строки 37-41)
- [ ] **TODO 6.5:** Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 6.6:** Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 6.7:** Скомпилировать и протестировать
- [ ] **TODO 6.8:** Убедиться, что "Защита кражи личности" полностью видно

---

## 🔴 ПУНКТ 7: "Пузырь местоположения" обрезается до "Пузырь место..."

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, секция "Компоненты защиты"
- **Проблема:** Текст "Пузырь местоположения" обрезается и видно только "Пузырь место..."
- **Требование:** Текст должен полностью отображаться на 1 строке

### 📁 Файлы для изменения:
- `Shared/Components/ComponentReportCard.swift` (строки 37-41, секция с title)
- `Resources/Localization/ru.lproj/Localizable.strings` (проверить ключ `component_location_bubble_agent_title`)

### 🔧 Решение:

**Шаг 1:** Проверить ключ локализации
```swift
// Найти ключ "component_location_bubble_agent_title" в Localizable.strings
// Текущее значение: "Пузырь местоположения"
// Можно сократить до: "Местоположение" или "Location Bubble"
```

**Шаг 2:** Исправить `ComponentReportCard.swift` (то же самое, что в пункте 4)
```swift
// ✅ Применить те же исправления:
Text(title)
    .font(.bodyBold)
    .foregroundColor(.textPrimary)
    .lineLimit(1)
    .minimumScaleFactor(0.8)  // ✅
    .fixedSize(horizontal: false, vertical: true)  // ✅
```

### ✅ Чек-лист:
- [ ] Проверить ключ локализации `component_location_bubble_agent_title`
- [ ] Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] Протестировать на разных размерах экрана
- [ ] Убедиться, что текст полностью виден

### 📋 TODO для пункта 7:
- [ ] **TODO 7.1:** Найти ключ локализации `component_location_bubble_agent_title` в `Localizable.strings`
- [ ] **TODO 7.2:** Проверить значение ключа - можно ли сократить?
- [ ] **TODO 7.3:** Открыть файл `Shared/Components/ComponentReportCard.swift`
- [ ] **TODO 7.4:** Найти секцию с `Text(title)` (строки 37-41)
- [ ] **TODO 7.5:** Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 7.6:** Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 7.7:** Скомпилировать и протестировать
- [ ] **TODO 7.8:** Убедиться, что "Пузырь местоположения" полностью видно

---

## 🔴 ПУНКТ 8: "Блокировка трекеров" обрезается до "Блокировка тр....."

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, секция "Компоненты защиты"
- **Проблема:** Текст "Блокировка трекеров" обрезается и видно только "Блокировка тр....."
- **Требование:** Текст должен полностью отображаться на 1 строке

### 📁 Файлы для изменения:
- `Shared/Components/ComponentReportCard.swift` (строки 37-41, секция с title)
- `Resources/Localization/ru.lproj/Localizable.strings` (проверить ключ `component_anti_tracker_agent_title`)

### 🔧 Решение:

**Шаг 1:** Проверить ключ локализации
```swift
// Найти ключ "component_anti_tracker_agent_title" в Localizable.strings
// Текущее значение: "Блокировка трекеров"
// Можно сократить до: "Трекеры" или "Блокировка"
```

**Шаг 2:** Исправить `ComponentReportCard.swift` (то же самое, что в пункте 4)
```swift
// ✅ Применить те же исправления:
Text(title)
    .font(.bodyBold)
    .foregroundColor(.textPrimary)
    .lineLimit(1)
    .minimumScaleFactor(0.8)  // ✅
    .fixedSize(horizontal: false, vertical: true)  // ✅
```

### ✅ Чек-лист:
- [ ] Проверить ключ локализации `component_anti_tracker_agent_title`
- [ ] Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] Протестировать на разных размерах экрана
- [ ] Убедиться, что текст полностью виден

### 📋 TODO для пункта 8:
- [ ] **TODO 8.1:** Найти ключ локализации `component_anti_tracker_agent_title` в `Localizable.strings`
- [ ] **TODO 8.2:** Проверить значение ключа - можно ли сократить?
- [ ] **TODO 8.3:** Открыть файл `Shared/Components/ComponentReportCard.swift`
- [ ] **TODO 8.4:** Найти секцию с `Text(title)` (строки 37-41)
- [ ] **TODO 8.5:** Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 8.6:** Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 8.7:** Скомпилировать и протестировать
- [ ] **TODO 8.8:** Убедиться, что "Блокировка трекеров" полностью видно

---

## 🔴 ПУНКТ 9: "АИ категоризация" обрезается до "АИ категориза..."

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, секция "Компоненты защиты"
- **Проблема:** Текст "АИ категоризация" обрезается и видно только "АИ категориза..."
- **Требование:** Текст должен полностью отображаться на 1 строке без переносов

### 📁 Файлы для изменения:
- `Shared/Components/ComponentReportCard.swift` (строки 37-41, секция с title)
- `Resources/Localization/ru.lproj/Localizable.strings` (проверить ключ `component_ai_categories_agent_title`)

### 🔧 Решение:

**Шаг 1:** Проверить ключ локализации
```swift
// Найти ключ "component_ai_categories_agent_title" в Localizable.strings
// Текущее значение: "AI категории"
// Можно сократить до: "AI категории" или "AI"
```

**Шаг 2:** Исправить `ComponentReportCard.swift` (то же самое, что в пункте 4)
```swift
// ✅ Применить те же исправления:
Text(title)
    .font(.bodyBold)
    .foregroundColor(.textPrimary)
    .lineLimit(1)
    .minimumScaleFactor(0.8)  // ✅
    .fixedSize(horizontal: false, vertical: true)  // ✅
```

### ✅ Чек-лист:
- [ ] Проверить ключ локализации `component_ai_categories_agent_title`
- [ ] Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] Протестировать на разных размерах экрана
- [ ] Убедиться, что текст полностью виден

### 📋 TODO для пункта 9:
- [ ] **TODO 9.1:** Найти ключ локализации `component_ai_categories_agent_title` в `Localizable.strings`
- [ ] **TODO 9.2:** Проверить значение ключа - можно ли сократить?
- [ ] **TODO 9.3:** Открыть файл `Shared/Components/ComponentReportCard.swift`
- [ ] **TODO 9.4:** Найти секцию с `Text(title)` (строки 37-41)
- [ ] **TODO 9.5:** Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 9.6:** Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] **TODO 9.7:** Скомпилировать и протестировать
- [ ] **TODO 9.8:** Убедиться, что "АИ категоризация" полностью видно

---

## 🌐 ДОПОЛНИТЕЛЬНО: Английская локализация

### 📝 Описание проблемы:
- **Где:** Страница Аналитика, все секции
- **Проблема:** В английской локализации тексты также обрезаются:
  - "Dark Web Monitoring" → "Dark Web Moni..."
  - "Identity Theft Protection" → "Identity Theft..."
  - И другие
- **Требование:** Все тексты должны полностью отображаться на 1 строке

### 📁 Файлы для изменения:
- `Shared/Components/ComponentReportCard.swift` (строки 37-41, секция с title)
- `Resources/Localization/en.lproj/Localizable.strings` (проверить все ключи компонентов)

### 🔧 Решение:

**Шаг 1:** Проверить все ключи в английской локализации
```swift
// Проверить ключи:
// - component_dark_web_monitoring_agent_title = "Dark Web Monitoring"
// - component_russian_identity_theft_protection_agent_title = "Identity Theft Protection (RU)"
// - component_location_bubble_agent_title = "Location Bubble"
// - component_anti_tracker_agent_title = "Tracker Blocking"
// - component_ai_categories_agent_title = "AI Categories"
// - component_driving_reports_agent_title = "Driving Reports"
```

**Шаг 2:** Исправить `ComponentReportCard.swift` (то же самое, что в пункте 4)
```swift
// ✅ Применить те же исправления для всех текстов:
Text(title)
    .font(.bodyBold)
    .foregroundColor(.textPrimary)
    .lineLimit(1)
    .minimumScaleFactor(0.8)  // ✅
    .fixedSize(horizontal: false, vertical: true)  // ✅
```

**Шаг 3:** Сократить длинные тексты (если нужно)
```swift
// Если тексты все еще обрезаются, можно сократить:
// "Dark Web Monitoring" → "Dark Web"
// "Identity Theft Protection (RU)" → "Identity Theft"
```

### ✅ Чек-лист:
- [ ] Проверить все ключи компонентов в английской локализации
- [ ] Добавить `.minimumScaleFactor(0.8)` к `Text(title)` (если еще не добавлено)
- [ ] Добавить `.fixedSize(horizontal: false, vertical: true)` к `Text(title)` (если еще не добавлено)
- [ ] Сократить длинные тексты, если нужно
- [ ] Протестировать на разных размерах экрана
- [ ] Убедиться, что все тексты полностью видимы

### 📋 TODO для английской локализации:
- [ ] **TODO EN.1:** Открыть файл `Resources/Localization/en.lproj/Localizable.strings`
- [ ] **TODO EN.2:** Проверить все ключи компонентов:
  - `component_dark_web_monitoring_agent_title`
  - `component_russian_identity_theft_protection_agent_title`
  - `component_location_bubble_agent_title`
  - `component_anti_tracker_agent_title`
  - `component_ai_categories_agent_title`
  - `component_driving_reports_agent_title`
- [ ] **TODO EN.3:** Проверить, обрезаются ли тексты в английской локализации
- [ ] **TODO EN.4:** Если обрезаются, сократить тексты или применить `.minimumScaleFactor`
- [ ] **TODO EN.5:** Открыть файл `Shared/Components/ComponentReportCard.swift`
- [ ] **TODO EN.6:** Убедиться, что `.minimumScaleFactor(0.8)` и `.fixedSize` применены
- [ ] **TODO EN.7:** Скомпилировать и протестировать на английском языке
- [ ] **TODO EN.8:** Убедиться, что все тексты полностью видимы

---

## 🔍 ДОПОЛНИТЕЛЬНО: Изучить страницу "Защита АЛАДДИН"

### 📝 Описание задачи:
- **Где:** Страница "Защита АЛАДДИН Экстренная помощь-Защита от угроз"
- **Цель:** Изучить, как там реализовано отображение текста на 1 строке
- **Требование:** Применить аналогичный подход для всех страниц

### 📁 Файлы для изучения:
- `Screens/01_MainScreen.swift` (возможно, там секция "Защита АЛАДДИН")
- `Components/ThreatProtectionCard.swift` (возможно, там реализация)
- Другие файлы с защитой

### 🔧 Решение:

**Шаг 1:** Найти страницу "Защита АЛАДДИН"
```swift
// Поиск по кодовой базе:
// - "Защита АЛАДДИН"
// - "Экстренная помощь"
// - "Защита от угроз"
```

**Шаг 2:** Изучить реализацию
```swift
// Посмотреть, какие модификаторы используются:
// - .lineLimit(1)
// - .minimumScaleFactor(...)
// - .fixedSize(...)
// - .frame(...)
// - Другие модификаторы
```

**Шаг 3:** Применить аналогичный подход
```swift
// Применить те же модификаторы для всех текстов на странице Аналитика
```

### ✅ Чек-лист:
- [ ] Найти страницу "Защита АЛАДДИН Экстренная помощь-Защита от угроз"
- [ ] Изучить реализацию отображения текста
- [ ] Записать используемые модификаторы
- [ ] Применить аналогичный подход для всех страниц

### 📋 TODO для изучения страницы "Защита АЛАДДИН":
- [ ] **TODO PROTECTION.1:** Найти страницу "Защита АЛАДДИН Экстренная помощь-Защита от угроз"
- [ ] **TODO PROTECTION.2:** Открыть файл с реализацией этой страницы
- [ ] **TODO PROTECTION.3:** Изучить, какие модификаторы используются для текста
- [ ] **TODO PROTECTION.4:** Записать список модификаторов:
  - `.lineLimit(...)`
  - `.minimumScaleFactor(...)`
  - `.fixedSize(...)`
  - `.frame(...)`
  - Другие
- [ ] **TODO PROTECTION.5:** Применить аналогичный подход для `AnalyticsScreen.swift`
- [ ] **TODO PROTECTION.6:** Применить аналогичный подход для `ComponentReportCard.swift`
- [ ] **TODO PROTECTION.7:** Скомпилировать и протестировать
- [ ] **TODO PROTECTION.8:** Убедиться, что все тексты полностью видимы

---

## 🔢 ДОПОЛНИТЕЛЬНО: Проверить все цифры на странице Аналитика

### 📝 Описание задачи:
- **Где:** Страница Аналитика, все секции
- **Вопрос:** Это фейковые цифры или реальные данные?
- **Требование:** Проверить все цифры и определить их источник

### 📁 Файлы для проверки:
- `ViewModels/AnalyticsViewModel.swift` (строки 1-209)
- `Core/Analytics/LocalAnalyticsService.swift` (если есть)
- `Core/Analytics/RemoteAnalyticsService.swift` (если есть)
- `Screens/04_AnalyticsScreen.swift` (строки 515-534, функция `makeViewModel`)

### 🔧 Решение:

**Шаг 1:** Проверить все свойства ViewModel
```swift
// В AnalyticsViewModel.swift проверить:
@Published private(set) var threatsBlocked: Int = 0
@Published private(set) var threatsDetected: Int = 0
@Published private(set) var itemsScanned: Int = 0
@Published private(set) var protectionLevel: Double = 0
@Published private(set) var threatCategories: [ThreatTypeCount] = []
```

**Шаг 2:** Проверить источник данных
```swift
// В AnalyticsScreen.swift проверить:
static func makeViewModel() -> AnalyticsViewModel {
    let service: AnalyticsService = {
        if AppConfig.useMockAPI {
            return LocalAnalyticsService()  // MOCK данные
        } else {
            return RemoteAnalyticsService()  // Реальные данные
        }
    }()
    return AnalyticsViewModel(service: service)
}
```

**Шаг 3:** Проверить данные в сервисах
```swift
// Проверить LocalAnalyticsService - какие данные возвращает?
// Проверить RemoteAnalyticsService - какой API endpoint используется?
```

### ✅ Чек-лист:
- [ ] Проверить `viewModel.threatsBlocked` - откуда берутся данные?
- [ ] Проверить `viewModel.itemsScanned` - откуда берутся данные?
- [ ] Проверить `viewModel.threatsDetected` - откуда берутся данные?
- [ ] Проверить `viewModel.protectionLevel` - откуда берутся данные?
- [ ] Проверить `viewModel.threatCategories` - откуда берутся данные?
- [ ] Проверить все цифры в компонентах защиты - откуда берутся данные?
- [ ] Убедиться, что данные реальные или MOCK (задокументировать)

### 📋 TODO для проверки цифр:
- [ ] **TODO NUMBERS.1:** Открыть файл `ViewModels/AnalyticsViewModel.swift`
- [ ] **TODO NUMBERS.2:** Проверить все свойства:
  - `threatsBlocked`
  - `threatsDetected`
  - `itemsScanned`
  - `protectionLevel`
  - `threatCategories`
- [ ] **TODO NUMBERS.3:** Найти метод `load()` и проследить, откуда приходят данные
- [ ] **TODO NUMBERS.4:** Открыть файл `Screens/04_AnalyticsScreen.swift`
- [ ] **TODO NUMBERS.5:** Найти функцию `makeViewModel()` и проверить, какой сервис используется
- [ ] **TODO NUMBERS.6:** Проверить `LocalAnalyticsService` - какие данные возвращает?
- [ ] **TODO NUMBERS.7:** Проверить `RemoteAnalyticsService` - какой API endpoint используется?
- [ ] **TODO NUMBERS.8:** Проверить все цифры в компонентах защиты:
  - Отчеты о вождении: "12", "8.5/10"
  - Мониторинг Дарк Веб: "3", "0"
  - Защита кражи личности: "0", "47"
  - Пузырь местоположения: "47", "Средняя"
  - Очистка данных: "2.3 ГБ", "2ч назад"
  - Блокировка трекеров: "1,247", "+234"
  - AI категоризация: "342", "12"
- [ ] **TODO NUMBERS.9:** Убедиться, что данные реальные или MOCK (задокументировать)
- [ ] **TODO NUMBERS.10:** Если данные MOCK, проверить, нужно ли переключить на реальные

---

## 📊 ИТОГОВАЯ СТАТИСТИКА

| Пункт | Задача | Файлы | Время | Приоритет | Статус |
|-------|--------|-------|-------|-----------|--------|
| 1 | Исправить "Заблокировано" | AnalyticsScreen.swift | 10 мин | Критический | 🔴 Ожидает |
| 2 | Исправить "Эффективность" | AnalyticsScreen.swift | 10 мин | Критический | 🔴 Ожидает |
| 3 | Проверить данные угроз | AnalyticsViewModel.swift | 15 мин | Критический | 🔴 Ожидает |
| 4 | Исправить "Отчет о вождении" | ComponentReportCard.swift | 10 мин | Высокий | 🔴 Ожидает |
| 5 | Исправить "Мониторинг Дарк Веб" | ComponentReportCard.swift | 5 мин | Высокий | 🔴 Ожидает |
| 6 | Исправить "Защита кражи личности" | ComponentReportCard.swift | 5 мин | Высокий | 🔴 Ожидает |
| 7 | Исправить "Пузырь местоположения" | ComponentReportCard.swift | 5 мин | Высокий | 🔴 Ожидает |
| 8 | Исправить "Блокировка трекеров" | ComponentReportCard.swift | 5 мин | Высокий | 🔴 Ожидает |
| 9 | Исправить "АИ категоризация" | ComponentReportCard.swift | 5 мин | Высокий | 🔴 Ожидает |
| EN | Исправить английскую локализацию | ComponentReportCard.swift | 10 мин | Высокий | 🔴 Ожидает |
| PROTECTION | Изучить страницу "Защита АЛАДДИН" | MainScreen.swift | 15 мин | Высокий | 🔴 Ожидает |
| NUMBERS | Проверить все цифры | AnalyticsViewModel.swift | 20 мин | Проверка | 🔴 Ожидает |
| **ИТОГО** | **12 задач** | **4 файла** | **120 минут** | - | **0/12 выполнено** |

---

## ✅ ФИНАЛЬНЫЙ ПРОВЕРОЧНЫЙ ЧЕК-ЛИСТ

### После выполнения всех задач:

- [ ] Пункт 1: "Заблокировано" полностью видно на 1 строке
- [ ] Пункт 2: "Эффективность" полностью видно на 1 строке и выровнено
- [ ] Пункт 3: Данные угроз проверены (реальные или MOCK)
- [ ] Пункт 4: "Отчет о вождении" полностью видно на 1 строке
- [ ] Пункт 5: "Мониторинг Дарк Веб" полностью видно на 1 строке
- [ ] Пункт 6: "Защита кражи личности" полностью видно на 1 строке
- [ ] Пункт 7: "Пузырь местоположения" полностью видно на 1 строке
- [ ] Пункт 8: "Блокировка трекеров" полностью видно на 1 строке
- [ ] Пункт 9: "АИ категоризация" полностью видно на 1 строке
- [ ] Английская локализация: Все тексты полностью видимы
- [ ] Страница "Защита АЛАДДИН": Изучена и применен аналогичный подход
- [ ] Все цифры: Проверены и задокументированы

---

## 🎯 РЕЗУЛЬТАТ

### После выполнения всех задач:

- ✅ Все тексты полностью видимы на 1 строке без переносов
- ✅ Шрифт автоматически уменьшается при необходимости
- ✅ Нет обрезания текста
- ✅ Данные угроз проверены и подтверждены
- ✅ Все цифры проверены и задокументированы
- ✅ Английская локализация исправлена
- ✅ Применен подход с страницы "Защита АЛАДДИН"

---

**Статус:** 🔴 **ГОТОВ К РЕАЛИЗАЦИИ**  
**Рекомендация:** Выполнить все задачи по порядку, проверить компиляцию после каждой задачи
