# 📋 ОБЩИЙ СВОДНЫЙ ПЛАН РЕАЛИЗАЦИИ: 42 КОМПОНЕНТА + 138 ФУНКЦИЙ

**Дата создания:** 2026-03-14  
**Статус:** ✅ План готов к реализации  
**Версия:** 1.0 FINAL

---

## 🎯 КЛЮЧЕВЫЕ ПРИНЦИПЫ АРХИТЕКТУРЫ

### ✅ **ПРАВИЛЬНАЯ ЛОГИКА (ПОДТВЕРЖДЕНА):**

1. **Общая система для статусов и конфигураций:**
   - ✅ Общий роутер `/api/components/*` для всех 42 компонентов
   - ✅ Общая таблица `component_status` для статусов всех компонентов
   - ✅ Общая таблица `component_configuration` для конфигураций всех компонентов

2. **Специфичные роутеры и таблицы для бизнес-данных:**
   - Специфичные роутеры для работы с бизнес-данными (утечки, отчеты, запросы)
   - Специфичные таблицы для хранения бизнес-данных

3. **SFM (Safe Function Manager) - центральная система:**
   - Все компоненты регистрируются в SFM
   - Все функции выполняются через SFM
   - Function Registry содержит 1,074+ функций

---

## ✅ ЧТО УЖЕ РЕАЛИЗОВАНО (ПРОВЕРЕНО)

### **1. Общая архитектура компонентов:**
- ✅ Таблицы `component_status` и `component_configuration` созданы (13 января 2026)
- ✅ Общий роутер `/api/components/*` работает (`app/routers/components.py`)
- ✅ 6 endpoints реализованы и протестированы:
  - `GET /api/components/status/{componentId}` ✅
  - `POST /api/components/enable/{componentId}` ✅
  - `POST /api/components/disable/{componentId}` ✅
  - `GET /api/components/configuration/{componentId}` ✅
  - `POST /api/components/configuration/{componentId}` ✅
  - `POST /api/components/batch/status` ✅
- ✅ Авторизация настроена (JWT токены)
- ✅ AsyncSession работает с PostgreSQL

### **2. Компоненты с подключением к БД (7 компонентов):**
- ✅ `dark_web_monitoring_agent` - роутер `/api/reports/dark-web/*`, таблицы `dark_web_leaks`, `dark_web_scans`
- ✅ `russian_identity_theft_protection_agent` - роутер `/api/reports/identity/*`, таблица `identity_theft_attempts`
- ✅ `location_bubble_agent` - роутер `/api/location-bubble/*`, таблица `location_requests`
- ✅ `personal_data_cleanup_agent` - роутер `/api/data-cleanup/*`, таблица `data_cleanup_records`
- ✅ `anti_tracker_agent` - роутер `/api/anti-tracker/*`, таблица `tracker_blocks`
- ✅ `ai_categories_agent` - роутер `/api/ai-categories/*`, таблица `ai_category_reports`
- ✅ `driving_reports_agent` - роутер `/api/driving-reports/*`, таблица `driving_reports`

**Проблема:** Эти компоненты НЕ используют общую систему `component_status` для статусов.

### **3. SFM система:**
- ✅ SFM работает (`safe_function_manager.py` - 172 функции)
- ✅ Function Registry существует (`function_registry.json` - 1,074+ функций)
- ✅ 104+ компонента на сервере работают через SFM
- ✅ 8,944+ функций реализованы на сервере

### **4. API endpoints:**
- ✅ 193 endpoints работают
- ✅ 138 endpoints в зеленой зоне (200 OK)
- ✅ 51 endpoint в желтой зоне (401/422 - защищено)
- ✅ 0 endpoints в красной зоне (500/503 - исправлено)

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ (БЕЗ ДУБЛЕЙ)

### **ЭТАП 1: ИНТЕГРАЦИЯ 7 КОМПОНЕНТОВ С ОБЩЕЙ СИСТЕМОЙ**

**Цель:** Интегрировать 7 уже подключенных компонентов с общей системой `component_status`, сохранив их специфичные роутеры для бизнес-данных.

#### **Задача 1.1: Проверить работу общего роутера для 7 компонентов**

**Компоненты:**
1. `dark_web_monitoring_agent`
2. `russian_identity_theft_protection_agent`
3. `location_bubble_agent`
4. `personal_data_cleanup_agent`
5. `anti_tracker_agent`
6. `ai_categories_agent`
7. `driving_reports_agent`

**Действия:**
- [ ] Протестировать `GET /api/components/status/{componentId}` для каждого компонента
- [ ] Протестировать `POST /api/components/enable/{componentId}` для каждого компонента
- [ ] Протестировать `POST /api/components/disable/{componentId}` для каждого компонента
- [ ] Убедиться, что статусы сохраняются в таблицу `component_status`

**Время:** 1-2 часа

---

#### **Задача 1.2: Обновить ViewModels для использования общего роутера**

**ViewModels для обновления:**
1. `DarkWebMonitoringViewModel.swift`
2. `IdentityTheftViewModel.swift`
3. `LocationBubbleViewModel.swift`
4. `DataCleanupViewModel.swift`
5. `AntiTrackerViewModel.swift`
6. `AICategoriesViewModel.swift`
7. `DrivingReportsViewModel.swift`

**Действия:**
- [ ] Заменить вызовы специфичных endpoints на `/api/components/status/{componentId}`
- [ ] Сохранить вызовы специфичных роутеров для бизнес-данных (утечки, отчеты)
- [ ] Добавить обработку `unauthorized` (если еще нет)
- [ ] Протестировать работу ViewModels

**Время:** 2-3 часа

---

### **ЭТАП 2: ПОДКЛЮЧЕНИЕ 5 КОМПОНЕНТОВ К БД**

**Цель:** Подключить 5 компонентов с роутерами, но без БД, к общей системе и создать специфичные таблицы для бизнес-данных.

#### **Задача 2.1: crash_detection_agent**

**Текущее состояние:**
- ✅ Роутер существует: `crash_detection_router.py`
- ❌ Использует in-memory хранилище для статусов
- ❌ Нет подключения к `component_status`
- ❌ Нет таблицы для алертов

**Действия:**
- [ ] Удалить in-memory хранилище для статусов
- [ ] Использовать `/api/components/status/crash_detection_agent` для статусов
- [ ] Создать таблицу `crash_detection_alerts` (SQL миграция)
- [ ] Добавить `Depends(get_db)` в endpoints для алертов
- [ ] Сохранять алерты в таблицу `crash_detection_alerts`
- [ ] Обновить `NetworkProtectionViewModel.swift` для использования общего роутера
- [ ] Добавить обработку `unauthorized` в ViewModel

**SQL для таблицы:**
```sql
CREATE TABLE IF NOT EXISTS crash_detection_alerts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    session_id VARCHAR(255) NOT NULL,
    latitude DECIMAL(10, 8) NOT NULL,
    longitude DECIMAL(11, 8) NOT NULL,
    severity VARCHAR(20) NOT NULL,
    accelerometer_data JSONB,
    gyroscope_data JSONB,
    speed DECIMAL(10, 2),
    timestamp TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

**Время:** 3-4 часа

---

#### **Задача 2.2: roadside_assistance_agent**

**Текущее состояние:**
- ✅ Роутер существует: `roadside_assistance_router.py`
- ❌ Использует агент напрямую, нет БД
- ❌ Нет подключения к `component_status`
- ❌ Нет таблицы для запросов

**Действия:**
- [ ] Использовать `/api/components/status/roadside_assistance_agent` для статусов
- [ ] Создать таблицу `roadside_assistance_requests` (SQL миграция)
- [ ] Добавить `Depends(get_db)` в endpoints для запросов
- [ ] Сохранять запросы в таблицу `roadside_assistance_requests`
- [ ] Обновить `NetworkProtectionViewModel.swift` для использования общего роутера
- [ ] Добавить обработку `unauthorized` в ViewModel

**SQL для таблицы:**
```sql
CREATE TABLE IF NOT EXISTS roadside_assistance_requests (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    request_id VARCHAR(255) NOT NULL UNIQUE,
    problem_type VARCHAR(50) NOT NULL,
    latitude DECIMAL(10, 8) NOT NULL,
    longitude DECIMAL(11, 8) NOT NULL,
    address TEXT,
    description TEXT,
    vehicle_make VARCHAR(100),
    vehicle_model VARCHAR(100),
    vehicle_year INTEGER,
    vehicle_color VARCHAR(50),
    license_plate VARCHAR(50),
    vin VARCHAR(50),
    partner VARCHAR(100),
    status VARCHAR(50) NOT NULL DEFAULT 'pending',
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

**Время:** 3-4 часа

---

#### **Задача 2.3: parental_control_bot**

**Текущее состояние:**
- ✅ Роутер существует: `parental_control_router.py`
- ❌ Использует mock данные
- ❌ Нет подключения к `component_status`
- ❌ Нет таблиц для статистики

**Действия:**
- [ ] Использовать `/api/components/status/parental_control_bot` для статусов
- [ ] Создать таблицы `parental_control_stats` и `parental_bypass_stats` (SQL миграция)
- [ ] Заменить mock данные на SQL запросы
- [ ] Добавить `Depends(get_db)` в endpoints
- [ ] Обновить `ParentalControlViewModel.swift` для использования общего роутера
- [ ] Добавить обработку `unauthorized` в ViewModel

**SQL для таблиц:**
```sql
CREATE TABLE IF NOT EXISTS parental_control_stats (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    child_id UUID NOT NULL,
    child_name VARCHAR(255),
    websites_blocked INTEGER NOT NULL DEFAULT 0,
    apps_blocked INTEGER NOT NULL DEFAULT 0,
    search_queries_blocked INTEGER NOT NULL DEFAULT 0,
    active_filters INTEGER NOT NULL DEFAULT 0,
    today_usage VARCHAR(50),
    today_limit VARCHAR(50),
    remaining VARCHAR(50),
    schedules_count INTEGER NOT NULL DEFAULT 0,
    current_location VARCHAR(255),
    geofences_count INTEGER NOT NULL DEFAULT 0,
    events_today INTEGER NOT NULL DEFAULT 0,
    sites_tracked INTEGER NOT NULL DEFAULT 0,
    apps_tracked INTEGER NOT NULL DEFAULT 0,
    contacts_tracked INTEGER NOT NULL DEFAULT 0,
    messages_monitored BOOLEAN NOT NULL DEFAULT FALSE,
    last_update TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, child_id)
);

CREATE TABLE IF NOT EXISTS parental_bypass_stats (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    child_id UUID NOT NULL,
    today INTEGER NOT NULL DEFAULT 0,
    week INTEGER NOT NULL DEFAULT 0,
    blocked INTEGER NOT NULL DEFAULT 0,
    incognito INTEGER NOT NULL DEFAULT 0,
    tor INTEGER NOT NULL DEFAULT 0,
    proxy INTEGER NOT NULL DEFAULT 0,
    message TEXT,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, child_id)
);
```

**Время:** 4-5 часов

---

#### **Задача 2.4: subscription_manager**

**Текущее состояние:**
- ✅ Роутер существует: `subscription_sync_router.py`
- ✅ Таблица `subscriptions` существует
- ❌ Использует SFM adapter, не PostgreSQL напрямую
- ❌ Нет подключения к `component_status`

**Действия:**
- [ ] Использовать `/api/components/status/subscription_manager` для статусов
- [ ] Добавить `Depends(get_db)` как fallback к SFM
- [ ] Использовать PostgreSQL для fallback, если SFM недоступен
- [ ] Обновить `TariffsViewModel.swift` для использования общего роутера
- [ ] Добавить обработку `unauthorized` в ViewModel

**Время:** 2-3 часа

---

#### **Задача 2.5: analytics_manager**

**Текущее состояние:**
- ✅ Роутер существует: `metrics_router.py`
- ❌ Нет таблицы для метрик
- ❌ Нет подключения к `component_status`

**Действия:**
- [ ] Использовать `/api/components/status/analytics_manager` для статусов
- [ ] Создать таблицу `analytics_metrics` (SQL миграция)
- [ ] Добавить `Depends(get_db)` в endpoints
- [ ] Сохранять метрики в таблицу `analytics_metrics`
- [ ] Обновить `AnalyticsViewModel.swift` для использования общего роутера
- [ ] Добавить `guard AppConfig.authToken != nil`
- [ ] Добавить обработку `unauthorized` в ViewModel

**SQL для таблицы:**
```sql
CREATE TABLE IF NOT EXISTS analytics_metrics (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID,
    device_id VARCHAR(255) NOT NULL,
    app_version VARCHAR(50),
    platform VARCHAR(50),
    metric_type VARCHAR(50) NOT NULL,
    timestamp TIMESTAMP NOT NULL,
    action VARCHAR(255),
    parameters JSONB,
    endpoint VARCHAR(255),
    method VARCHAR(10),
    response_time DECIMAL(10, 3),
    status_code INTEGER,
    success BOOLEAN,
    error_type VARCHAR(100),
    error_message TEXT,
    context JSONB,
    alert_id VARCHAR(255),
    alert_type VARCHAR(100),
    severity VARCHAR(50),
    message TEXT,
    status VARCHAR(50),
    uptime DECIMAL(10, 2),
    active_components INTEGER,
    total_components INTEGER,
    issues TEXT[],
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

**Время:** 3-4 часа

**ИТОГО ЭТАП 2:** 15-20 часов

---

### **ЭТАП 3: ОБРАБОТКА UNAUTHORIZED ВО ВСЕХ VIEWMODELS**

**Цель:** Добавить обработку `NetworkError.unauthorized` во все ViewModels для корректной обработки истечения токена.

#### **Задача 3.1: NetworkProtectionScreen ViewModels**

**ViewModels для обновления:**
1. `NetworkProtectionViewModel.swift` - для 10 компонентов
2. `PhishingSettingsViewModel.swift`
3. `MalwareSettingsViewModel.swift`
4. `MobileSecuritySettingsViewModel.swift`
5. `NetworkSecuritySettingsViewModel.swift`
6. `IncidentResponseSettingsViewModel.swift`
7. `PasswordGeneratorSettingsViewModel.swift`

**Действия для каждого ViewModel:**
- [ ] Добавить `guard AppConfig.authToken != nil` перед запросами
- [ ] Добавить обработку `case .unauthorized(let message)` в catch блоке
- [ ] Показать экран авторизации при unauthorized
- [ ] Добавить локализацию ошибок через `LocalizationManager.shared.localizedString(forKey: "error.unauthorized")`
- [ ] Добавить ключи локализации в `LocalizedStrings/ru.lproj/Localizable.strings` и `LocalizedStrings/en.lproj/Localizable.strings`

**Время:** 3-4 часа

---

#### **Задача 3.2: ParentalControlScreen ViewModels**

**ViewModels для обновления:**
1. `ParentalControlViewModel.swift` - для 5 компонентов

**Действия:**
- [ ] Добавить `guard AppConfig.authToken != nil` перед запросами
- [ ] Добавить обработку `case .unauthorized(let message)` в catch блоке
- [ ] Показать экран авторизации при unauthorized
- [ ] Добавить локализацию ошибок

**Время:** 1-2 часа

---

#### **Задача 3.3: AdvancedProtectionSettingsScreen ViewModels**

**ViewModels для обновления:**
1. Проверить существование ViewModels для мессенджеров
2. `LocationBubbleViewModel.swift` (если есть)
3. `DataCleanupViewModel.swift` (если есть)
4. `AntiTrackerViewModel.swift` (если есть)
5. `DarkWebMonitoringViewModel.swift` (уже есть обработка)
6. `IdentityTheftViewModel.swift` (уже есть обработка)
7. `AICategoriesViewModel.swift` (уже есть обработка)
8. `DrivingReportsViewModel.swift` (уже есть обработка)

**Действия:**
- [ ] Проверить существование ViewModels
- [ ] Добавить обработку `unauthorized` в существующие ViewModels

**Время:** 2-3 часа

---

#### **Задача 3.4: SettingsScreen ViewModels**

**ViewModels для обновления:**
1. `EmergencyNotificationsViewModel.swift`
2. Проверить другие ViewModels в SettingsScreen

**Действия:**
- [ ] Добавить обработку `unauthorized` в существующие ViewModels

**Время:** 1-2 часа

---

#### **Задача 3.5: ViewModels для менеджеров**

**ViewModels для обновления:**
1. `AnalyticsViewModel.swift` (уже есть обработка, но нет проверки токена)
2. Проверить другие ViewModels для менеджеров

**Действия:**
- [ ] Добавить `guard AppConfig.authToken != nil` в `AnalyticsViewModel.swift`
- [ ] Добавить обработку `unauthorized` в другие ViewModels

**Время:** 1-2 часа

**ИТОГО ЭТАП 3:** 8-13 часов

---

### **ЭТАП 3.6: Добавить локализацию ошибок**

**Цель:** Убедиться, что все ошибки локализованы на русский и английский языки.

**Действия:**
- [ ] Проверить наличие ключа `error.unauthorized` в локализации
- [ ] Добавить ключ `error.unauthorized` в `LocalizedStrings/ru.lproj/Localizable.strings`: `"error.unauthorized" = "Сессия истекла. Пожалуйста, войдите снова.";`
- [ ] Добавить ключ `error.unauthorized` в `LocalizedStrings/en.lproj/Localizable.strings`: `"error.unauthorized" = "Session expired. Please login again.";`
- [ ] Добавить другие ключи ошибок (если нужно):
  - `error.network_unavailable`
  - `error.server_error`
  - `error.component_not_found`
- [ ] Проверить использование локализации во всех ViewModels

**Время:** 1-2 часа

**ИТОГО ЭТАП 3 (с локализацией):** 9-15 часов

---

### **ЭТАП 4: СОЗДАНИЕ СПЕЦИФИЧНЫХ ТАБЛИЦ ДЛЯ БИЗНЕС-ДАННЫХ**

**Цель:** Создать SQL миграцию со всеми специфичными таблицами для бизнес-данных.

#### **Задача 4.1: Создать SQL миграцию**

**Таблицы для создания:**
1. `crash_detection_alerts` - для crash_detection_agent
2. `roadside_assistance_requests` - для roadside_assistance_agent
3. `parental_control_stats` - для parental_control_bot
4. `parental_bypass_stats` - для parental_control_bot
5. `analytics_metrics` - для analytics_manager
6. ⚠️ `driving_reports` - проверить существование (возможно уже создана отдельно)

**Действия:**
- [ ] Проверить существование таблицы `driving_reports` в БД
- [ ] Создать файл `migrations/create_component_data_tables.sql`
- [ ] Добавить CREATE TABLE для всех таблиц (с IF NOT EXISTS)
- [ ] Добавить индексы для всех таблиц (с IF NOT EXISTS):
  - Индексы по `user_id` для всех таблиц
  - Индексы по `created_at` для временных запросов
  - Индексы по `status` для таблиц со статусами
  - Уникальные индексы где нужно (UNIQUE constraints)
- [ ] Добавить комментарии к таблицам
- [ ] Добавить FOREIGN KEY constraints (если нужно)

**Время:** 1-2 часа

---

#### **Задача 4.2: Применить миграцию на сервере**

**Действия:**
- [ ] Подключиться к серверу
- [ ] Создать backup БД
- [ ] Применить миграцию: `psql -U postgres -d aladdin_db -f migrations/create_component_data_tables.sql`
- [ ] Проверить создание таблиц: `\dt`
- [ ] Проверить индексы: `\di`

**Время:** 0.5-1 час

---

#### **Задача 4.3: Протестировать работу таблиц**

**Действия:**
- [ ] Тест: INSERT в каждую таблицу
- [ ] Тест: SELECT из каждой таблицы
- [ ] Тест: UPDATE в каждой таблице
- [ ] Тест: DELETE в каждой таблице (если нужно)
- [ ] Проверить производительность запросов

**Время:** 1-2 часа

**ИТОГО ЭТАП 4:** 2.5-5 часов

---

### **ЭТАП 5: ПРОВЕРКА РЕГИСТРАЦИИ В SFM**

**Цель:** Убедиться, что все 42 компонента зарегистрированы в SFM и работают корректно.

#### **Задача 5.1: Проверить регистрацию компонентов в function_registry.json**

**Действия:**
- [ ] Проверить наличие всех 42 компонентов в `function_registry.json`
- [ ] Убедиться, что каждый компонент имеет правильную запись
- [ ] Проверить зависимости между компонентами
- [ ] Проверить политики выполнения для каждого компонента

**Время:** 2-3 часа

---

#### **Задача 5.2: Проверить работу SFM для всех компонентов**

**Действия:**
- [ ] Протестировать `SFM.execute_function` для каждого компонента
- [ ] Проверить обработку ошибок
- [ ] Проверить fallback механизмы
- [ ] Проверить производительность

**Время:** 2-3 часа

**ИТОГО ЭТАП 5:** 4-6 часов

---

### **ЭТАП 6: ПРОВЕРКА РАБОТЫ ВСЕХ 138 ФУНКЦИЙ**

**Цель:** Убедиться, что все 138 функций безопасности работают через правильные API endpoints.

#### **Задача 6.1: Проверить работу функций защиты от угроз (100 функций)**

**Действия:**
- [ ] Проверить работу всех 10 функций киберугроз
- [ ] Проверить работу всех 6 функций интернет-угроз
- [ ] Проверить работу всех 12 функций мошенничества
- [ ] Проверить работу всех 12 функций утечек данных
- [ ] Проверить работу всех 10 функций мобильных угроз
- [ ] Проверить работу всех 17 функций угроз для детей
- [ ] Проверить работу всех 15 функций семейных угроз
- [ ] Проверить работу всех 10 функций IoT угроз
- [ ] Проверить работу всех 8 функций Deepfake

**Время:** 4-6 часов

---

#### **Задача 6.2: Проверить работу функций родительского контроля (32 функции)**

**Действия:**
- [ ] Проверить работу всех 5 функций блокировки контента
- [ ] Проверить работу всех 4 функций управления временем
- [ ] Проверить работу всех 5 функций мониторинга
- [ ] Проверить работу всех 5 функций геолокации
- [ ] Проверить работу всех 3 функций отчётов
- [ ] Проверить работу всех 4 функций дополнительно
- [ ] Проверить работу всех 3 функций защиты от обхода
- [ ] Проверить работу всех 3 функций вознаграждений

**Время:** 2-3 часа

---

#### **Задача 6.3: Проверить работу дополнительных функций (6 функций)**

**Действия:**
- [ ] Проверить работу VPN (3 уровня)
- [ ] Проверить работу AI помощника
- [ ] Проверить работу защиты пожилых
- [ ] Проверить работу голосового управления
- [ ] Проверить работу геймификации
- [ ] Проверить работу анонимных профилей

**Время:** 1-2 часа

---

#### **Задача 6.4: Проверить соответствие функций тарифам**

**Действия:**
- [ ] Проверить, что FREE тариф имеет доступ к 26 функциям
- [ ] Проверить, что PERSONAL тариф имеет доступ к 69 функциям
- [ ] Проверить, что FAMILY тариф имеет доступ к 124 функциям
- [ ] Проверить, что PREMIUM тариф имеет доступ к 138 функциям
- [ ] Проверить блокировку функций для неподходящих тарифов

**Время:** 2-3 часа

**ИТОГО ЭТАП 6:** 9-14 часов

---

### **ЭТАП 7: ПРОВЕРКА 30 КОМПОНЕНТОВ БЕЗ РОУТЕРОВ**

**Цель:** Убедиться, что все 30 компонентов без специфичных роутеров используют общий роутер и работают корректно.

#### **Задача 7.1: Проверить использование общего роутера в ViewModels**

**Компоненты для проверки (30 компонентов):**

**NetworkProtectionScreen (8 компонентов):**
1. `emergency_response_bot`
2. `emergency_event_manager`
3. `phishing_protection_agent`
4. `malware_detection_agent`
5. `mobile_security_agent`
6. `network_security_agent`
7. `incident_response_agent`
8. `password_security_agent`

**ParentalControlScreen (4 компонента):**
9. `self_harm_detection_agent`
10. `grooming_detection_agent`
11. `online_predators_agent`
12. `psychological_support_agent`

**AdvancedProtectionSettingsScreen (6 компонентов):**
13. `telegram_security_bot`
14. `whatsapp_security_bot`
15. `instagram_security_bot`
16. `max_messenger_security_bot`
17. `gaming_security_bot`
18. `browser_security_bot`

**SettingsScreen (5 менеджеров):**
19. `emergency_contacts_manager`
20. `emergency_notifications_manager`
21. `voice_control_manager`
22. `russian_child_protection_compliance_manager`
23. `russian_data_protection_compliance_manager`

**Улучшение существующих (7 менеджеров):**
24. `family_notification_manager`
25. `smart_notification_manager`
26. `child_interface_manager`
27. `elderly_interface_manager`
28. `referral_manager`
29. `qr_payment_manager`
30. `report_manager`

**Действия:**
- [ ] Проверить, что все ViewModels используют `/api/components/status/{componentId}` для получения статусов
- [ ] Проверить, что все ViewModels используют `/api/components/enable/{componentId}` для включения
- [ ] Проверить, что все ViewModels используют `/api/components/disable/{componentId}` для выключения
- [ ] Убедиться, что статусы сохраняются в таблицу `component_status`
- [ ] Протестировать работу каждого компонента через общий роутер

**Время:** 4-6 часов

---

#### **Задача 7.2: Проверить отсутствие необходимости в специфичных таблицах**

**Действия:**
- [ ] Для каждого из 30 компонентов проверить, нужны ли специфичные таблицы для бизнес-данных
- [ ] Если компонент не хранит бизнес-данные (только статус/конфигурация), подтвердить, что общих таблиц достаточно
- [ ] Если компонент хранит бизнес-данные, добавить в Этап 4 создание таблицы

**Время:** 2-3 часа

**ИТОГО ЭТАП 7:** 6-9 часов

---

## 📊 ИТОГОВАЯ ОЦЕНКА ВРЕМЕНИ

| Этап | Задач | Время (часы) | Время (дни) | Приоритет |
|------|-------|--------------|-------------|-----------|
| **Этап 1: Интеграция 7 компонентов** | 2 | 3-5 | 0.5 | 🔴 Высокий |
| **Этап 2: Подключение 5 компонентов к БД** | 5 | 15-20 | 2-2.5 | 🔴 Высокий |
| **Этап 3: Обработка unauthorized + локализация** | 6 | 9-15 | 1-2 | 🟡 Средний |
| **Этап 4: Создание специфичных таблиц** | 3 | 2.5-5 | 0.5 | 🟡 Средний |
| **Этап 5: Проверка регистрации в SFM** | 2 | 4-6 | 0.5-1 | 🟢 Низкий |
| **Этап 6: Проверка работы функций** | 4 | 9-14 | 1-2 | 🟢 Низкий |
| **Этап 7: Проверка 30 компонентов без роутеров** | 2 | 6-9 | 1-1.5 | 🟡 Средний |
| **ИТОГО** | **24** | **48.5-74** | **6.5-10** | |

---

## ✅ КРИТЕРИИ УСПЕХА

### **Для каждого компонента:**

1. ✅ Статус компонента хранится в общей таблице `component_status`
2. ✅ Конфигурация компонента хранится в общей таблице `component_configuration`
3. ✅ ViewModel использует общий роутер `/api/components/status/{component_id}` для статусов
4. ✅ ViewModel обрабатывает `NetworkError.unauthorized`
5. ✅ Специфичные данные (если есть) хранятся в специфичных таблицах
6. ✅ Все endpoints протестированы

### **Для всех функций:**

1. ✅ Все 138 функций работают через правильные API endpoints
2. ✅ Функции соответствуют тарифам (FREE/PERSONAL/FAMILY/PREMIUM)
3. ✅ Функции зарегистрированы в SFM
4. ✅ Функции выполняются через SFM корректно

---

## 🎯 ПРИОРИТЕТЫ ВЫПОЛНЕНИЯ

### **🔴 Высокий приоритет (начать сразу):**
1. Этап 1: Интеграция 7 компонентов (быстро даст результат)
2. Этап 2: Подключение 5 компонентов к БД (критично для функциональности)

### **🟡 Средний приоритет (после высокого):**
3. Этап 3: Обработка unauthorized + локализация (важно для UX)
4. Этап 4: Создание специфичных таблиц (можно делать параллельно)
5. Этап 7: Проверка 30 компонентов без роутеров (убедиться, что все работает)

### **🟢 Низкий приоритет (можно делать параллельно):**
5. Этап 5: Проверка регистрации в SFM (проверка качества)
6. Этап 6: Проверка работы функций (проверка качества)

---

## 📝 ЗАМЕТКИ

1. **Общая архитектура правильная:** Не нужно создавать отдельные роутеры для каждого компонента
2. **Специфичные таблицы нужны только для бизнес-данных:** Статусы и конфигурации хранятся в общих таблицах
3. **SFM остается центральной системой:** Все компоненты работают через SFM для выполнения функций
4. **Общий роутер масштабируемый:** Легко добавить новые компоненты без создания новых роутеров
5. **30 компонентов без роутеров:** Они УЖЕ работают через общий роутер `/api/components/*` - нужно только проверить и добавить обработку unauthorized

---

## ✅ ПРОВЕРКА ПОЛНОТЫ ПЛАНА

### **Все ли учтено из наших обсуждений:**

- [x] ✅ 42 компонента - все учтены
- [x] ✅ 138 функций - все учтены
- [x] ✅ 7 компонентов с БД - интегрировать с component_status
- [x] ✅ 5 компонентов без БД - подключить к БД
- [x] ✅ 30 компонентов без роутеров - проверить использование общего роутера
- [x] ✅ Обработка unauthorized - во всех ViewModels
- [x] ✅ Локализация ошибок - добавлена в план
- [x] ✅ Специфичные таблицы - для бизнес-данных
- [x] ✅ Индексы для таблиц - добавлены в план
- [x] ✅ Проверка таблицы driving_reports - добавлена в план
- [x] ✅ Проверка регистрации в SFM - учтена
- [x] ✅ Проверка работы функций - учтена
- [x] ✅ Проверка соответствия тарифам - учтена

**СТАТУС:** ✅ **ВСЕ УЧТЕНО!**

---

**Статус плана:** ✅ ГОТОВ К РЕАЛИЗАЦИИ  
**Следующий шаг:** Начать с Этапа 1 - интеграция 7 уже подключенных компонентов
