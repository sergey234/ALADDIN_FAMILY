# Лимиты для публичного GET /sub/ (aladdin-shop-vpn-api через nginx)
# Подключается из /etc/nginx/nginx.conf через include conf.d/*.conf

limit_req_zone $binary_remote_addr zone=vpn_sub_limit:10m rate=4r/s;
