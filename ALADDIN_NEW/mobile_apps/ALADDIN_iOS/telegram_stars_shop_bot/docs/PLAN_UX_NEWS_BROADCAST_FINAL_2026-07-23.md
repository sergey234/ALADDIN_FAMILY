# Итоговый план: UX + Новости + Рассылки + Админы — 2026-07-23

## Canon

Единственный живой план. Старые `PLAN_UX_COMMANDS_*` / `UX_COMMANDS_MINIMAL_*` — архив, не править.

## Этап 0 — Админы (сначала, Contabo)

### Что нашли (прод Contabo)

| Кто | Telegram ID | В `ADMIN_IDS`? | `/admin` | Алерты заказов |
|-----|-------------|----------------|----------|----------------|
| Sergey | `493897224` | ✅ | должен работать | ✅ (чат = его личка) |
| Миша | `744254201` | ✅ | должен работать | ❌ не видит (чат = личка Сергея) |
| Дима | **нет в списке** | ❌ | тишина (код просто `return`) | ❌ |

`ADMIN_IDS=493897224,744254201` · `SUPER_ADMIN_IDS` пуст (оба админа = супер) · `ALERT_TELEGRAM_CHAT_ID=493897224` (**личка**, не группа).

Кандидаты Димы в БД (нужно подтвердить ID): `@dmitriy777vne` `854726070` · `@dmitriy7070` `5530292533`.

### Причины «не работает»

1. **Дима** — его ID нет в `ADMIN_IDS` → `/admin` молчит.  
2. **Второй админ (Миша)** — права есть, но **все алерты/кнопки заказов** уходят только в личку Сергея → кажется, что «админка мёртвая».  
3. **UX** — отказ без текста; без ID Димы нельзя починить доступом.

### Что делаем

1. `adm-01` Дима `854726070` (@dmitriy777vne) → `ADMIN_IDS` ✅  
2. `adm-02` Алерты fan-out всем `ADMIN_IDS` в боте ✅  
3. `adm-03` Чужой `/admin` → короткий отказ ✅  
4. `adm-help` Каталог `/admin_help` (разделы + что прописать) + кнопка «📚 Команды» в `/admin` ✅  
5. `adm-04` Смоук: трое открывают `/admin` и `/admin_help`  

## Уже есть / не трогать

- short-канал, inline-хаб, `/menu` `/vpn` `/orders`
- профиль + VPN copy-link (частично → в `ux-04` только дырки)
- новости в Помощи; дубль в VPN — убрать
- рефка 15% + дни после выдачи
- **VPN trial/expiry loops — не менять; opt-out акций на них не действует**

## Этап A+B — один деплой Contabo

**A UX**
1. `ux-01` Меню ☰: default = user; scope админов = полный список  
2. `ux-02` / `adm-03` Не-админ `/admin*` → короткий отказ  
3. `ux-03` ReplyKeyboard после канала: Меню · Профиль · Друзья · Помощь  
4. `ux-04` Профиль: «Мои заказы»; VPN-кнопки — только если нет  

**B Новости**
1. `news-01` Хаб: «📢 Новости» → канал  
2. `news-02` Убрать новости с экрана VPN  

## Этап C — отдельный релиз

1. `bc-01` `/admin_broadcast` + dry-run / throttle  
2. `bc-02` Opt-out акций; VPN-напоминания не режет  
3. `bc-03` Стата: sent / fail / unsubscribed  

## Не делаем

`is_admin` в БД · вырезать `/menu`/`/vpn` · рефка «за connect» · конструктор воронок · title_only · рассылка алертов по всем личкам вместо группы

## Одна фраза

Сначала починить доступ админов (Дима в `ADMIN_IDS` + общий ops-чат); потом UX+новости; рассылки — отдельно от VPN-автонапоминаний.
