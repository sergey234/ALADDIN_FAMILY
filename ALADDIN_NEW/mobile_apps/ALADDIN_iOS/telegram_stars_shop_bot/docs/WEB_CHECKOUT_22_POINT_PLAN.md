# AiMonkeyVPN Web — план из 22 пунктов

**Сайт:** https://get.aladdin-ai.ru/  
**SSOT:** Contabo (bot + Partner API :8090 + VPN API)  
**Без ПД:** нет обязательного email; доступ = `/o/{token}` + session браузера; Telegram опционально  

**Правило работ:** сначала закрываем пункты кодом → **локальные тесты** → только потом **деплой пакетом** по правилам → смоук на проде.  
**Не деплоить** пункт, пока не пройдены проверки из колонки «До деплоя».

---

## Уже на проде (фон, не входят в 22 как «сделать с нуля»)

- get. + TLS + Contabo :8090  
- Lava СБП/карта, `successUrl` → `/o/{token}`  
- Accounts без email, `link_`, реф `/r/`  
- VPN: `DEVICE_LIMIT=1`, HWID gate  
- Часть SPA: оплата на `/o`, A/B/C link/bind  

Локально уже начато (ещё **не выкатано**): `drawQr`, блок Happ текст+QR+предупреждение 1 устройство.

---

## Прогресс

- **D1 (1–14):** ✅ Contabo `20260723-173727`  
- **D2 (15–20):** ✅ Contabo `20260723-174507` · SEC grace 48h + TTL 14d · Happ кнопки · invited  
- **w21 полный смоук оплаты:** ⏳ ручной (СБП → Happ)  
- **w22 F5:** позже  

## 22 пункта (порядок выполнения)

| # | ID | Задача | До деплоя (тест) | Пакет деплоя |
|---|-----|--------|------------------|--------------|
| **1** | w01 | **Главная:** текстовая `/sub` + кнопка «Скопировать» + QR (qrserver) | `node --check` SPA; глазами: оба элемента | **D1** |
| **2** | w02 | **`/o` после оплаты:** тот же блок Happ (текст + QR + шаги) | то же + mock order HTML | **D1** |
| **3** | w03 | Предупреждение: «личная · **1 устройство** · не пересылайте» | текст на Главной и `/o` | **D1** |
| **4** | w04 | Кнопка «Сохранить / скопировать ссылку на заказ» (`/o/{token}`) | clipboard / видимость URL | **D1** |
| **5** | w05 | **Профиль → Мои заказы:** last `/o` + история API | session + empty/full states | **D1** |
| **6** | w06 | **Подписки:** «активна до …» + `/sub` + QR если есть | с `me` и без | **D1** |
| **7** | w07 | Единый `drawQr()` на всех экранах (без мёртвого `QRCode`) | grep: нет `window.QRCode` | **D1** |
| **8** | w08 | Env Contabo: `SUPPORT_URL=https://t.me/AiMonkeyStars_bot` | grep на сервере после merge `.env` | **D1** (env) |
| **9** | w09 | API отдаёт `support_url` (`/v1/web/products` и/или `/me`) | pytest + curl | **D1** |
| **10** | w10 | Кнопка **Поддержка** на Главной, Профиле, `/o` | клик → t.me | **D1** |
| **11** | w11 | Текст: «Оплатили, ключа нет → поддержка + № заказа» | на `/o` pending/failed | **D1** |
| **12** | w12 | **Локальный прогон D1:** pytest web + `py_compile` + SPA check | все зелёные | — |
| **13** | w13 | **Деплой D1** на Contabo по правилам (rsync exclude `.env`/`data`, restart 3 unit) | health :8090; get. SPA markers | **D1** |
| **14** | w14 | **Смоук прод D1:** health/products; Главная QR+текст; поддержка; `/o` UI | чеклист ниже | — |
| **15** | w15 | **Бонусы:** QR реф-ссылки + счётчики приглашено / с покупкой / дни | API stats + UI | **D2** |
| **16** | w16 | Deep-link / кнопка «Открыть в Happ» где возможно | iOS/Android smoke | **D2** |
| **17** | w17 | Профиль: «устройства / сброс — через поддержку» | текст + support | **D2** |
| **18** | w18 | **SEC:** `/o` после paid — `/sub` только со session того же account | unit + ручной негатив | **D2** |
| **19** | w19 | **SEC:** TTL / revoke `order_access_tokens` | unit expire | **D2** |
| **20** | w20 | **Локальный прогон D2** + **деплой D2** Contabo | как w12–w13 | **D2** |
| **21** | w21 | **Полный смоук оплаты:** СБП → `/o` → `/sub` → Happ; link TG Start; `/r/` | ручной чеклист | — |
| **22** | w22 | **F5 бэклог (не блокер):** Secure cookie; HWID reset ops; Lava H2H QR; restore order; домен/апка | только после зелёного w21 | позже |

---

## Пакеты деплоя

### D1 = пункты 1–11 (после w12)

1. Снаружи health MAIN `:8002` (по правилу).  
2. Shop SSOT = **Contabo** `185.225.233.150`: rsync `telegram_stars_shop_bot/` → `releases/<TS>/`, exclude `.git`, `__pycache__`, `.venv`, **`data`**, **`.env`**.  
3. Symlink `current_app` → restart `aladdin-telegram-bot`, `aladdin-partner-api`, `aladdin-webhook-worker`.  
4. Merge вручную только `SUPPORT_URL` в `shared/.env` (пункт 8).  
5. `curl` `:8090/health` + `get.aladdin-ai.ru` markers.

### D2 = пункты 15–19 (после локальных тестов w20)

Тот же порядок rsync/restart/smoke API.

---

## Смоук (кратко)

**После D1:**  
`/v1/web/health` · products · Главная показывает текст+QR при наличии sub · Поддержка открывает Telegram · `/o` pending показывает оплату+QR  

**После D2 / w21:**  
Реальная СБП → `/o` с `/sub` → Happ Connect · второй «друг» с той же `/sub` не заводится · link_ Start · `/r/code` cookie  

---

## DoD всего плана (22)

Пользователь без Telegram: оплатил → видит **текст + QR** личной `/sub` → Happ работает на **1** устройстве → есть **Поддержка** → может сохранить `/o` → нельзя обобрать чужие ключи через сайт.

---

## Cursor TODO

Ids: `w01` … `w22` (этот файл — SSOT плана).
