# 🚀 ИНСТРУКЦИЯ ДЛЯ ИСПРАВЛЕНИЯ НА СЕРВЕРЕ

**Дата:** 2026-02-13  
**Цель:** Проверить и исправить все проблемы для продакшн

---

## 📋 БЫСТРЫЙ СТАРТ:

### **Вариант 1: Автоматическое исправление (РЕКОМЕНДУЕТСЯ)** ✅

1. **Подключиться к серверу:**
```bash
ssh root@149.154.65.180
```

2. **Скопировать скрипт на сервер:**
```bash
# На вашем компьютере:
scp EXECUTE_ON_SERVER.sh root@149.154.65.180:/tmp/

# Или создать на сервере вручную
```

3. **Выполнить скрипт:**
```bash
cd /opt/aladdin-backend
bash /tmp/EXECUTE_ON_SERVER.sh
```

**Скрипт автоматически:**
- ✅ Проверит файл metrics_router.py
- ✅ Проверит и исправит префикс роутера
- ✅ Проверит подключение в main.py
- ✅ Исправит независимость подключения
- ✅ Перезапустит сервис
- ✅ Протестирует endpoint

---

### **Вариант 2: Ручное исправление**

Следуйте инструкциям в файле: `SSH_COMMANDS_FOR_SERVER.md`

---

## 🔍 ЧТО ПРОВЕРЯЕТСЯ:

### **1. Файл metrics_router.py:**
- ✅ Существует ли файл
- ✅ Правильный ли префикс (`/metrics`)

### **2. Подключение в main.py:**
- ✅ Есть ли импорт
- ✅ Есть ли подключение роутера
- ✅ Независим ли роутер (не зависит от system_router)

### **3. Сервис:**
- ✅ Активен ли сервис
- ✅ Есть ли записи в логах

### **4. Endpoint:**
- ✅ Возвращает ли HTTP 200

---

## ✅ ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

После выполнения скрипта:

```
✅ Файл metrics_router.py существует
✅ Префикс правильный (/metrics)
✅ metrics_router найден в main.py
✅ Роутер подключен независимо
✅ Сервис активен
✅ Endpoint работает! HTTP 200
```

---

## 📋 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ:

1. **Проверьте логи:**
```bash
journalctl -u aladdin-production-api -n 100 --no-pager
```

2. **Проверьте синтаксис main.py:**
```bash
python3 -m py_compile /opt/aladdin-backend/main.py
```

3. **Проверьте вручную:**
```bash
grep -A 10 "metrics_router_available" /opt/aladdin-backend/main.py
```

---

**Последнее обновление:** 2026-02-13
