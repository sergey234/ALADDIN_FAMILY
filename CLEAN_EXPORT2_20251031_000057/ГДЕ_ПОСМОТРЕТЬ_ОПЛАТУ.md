# 📍 ГДЕ В ПРИЛОЖЕНИИ ОСУЩЕСТВЛЯЕТСЯ ОПЛАТА И ГДЕ ПОСМОТРЕТЬ НАСТРОЙКИ

---

## 🎯 ТОЧКА ВХОДА В ОПЛАТУ

### 1️⃣ Главный экран → Тарифы

**Файл:** `Screens/01_MainScreen.swift`  
**Строка:** 243-244

```swift
// Тарифы карточка
NavigationLink(destination: TariffsScreen()) {
    VStack(spacing: 8) {
        Text("💎")
        Text("Тарифы")
        Text("Выбор плана")
    }
}
```

**Где посмотреть:**
- Откройте `01_MainScreen.swift`
- Найдите строку 244: `NavigationLink(destination: TariffsScreen())`
- Это карточка "Тарифы" на главном экране

---

## 💳 ЭКРАН ВЫБОРА ТАРИФОВ

### 2️⃣ Экран тарифов — выбор и оплата

**Файл:** `Screens/10_TariffsScreen.swift`  
**Строка:** 204-233 (кнопка оплаты)

```swift
Button(action: {
    selectedTariff = tariff
    
    // Проверяем регион
    if AppConfig.useAlternativePayments {
        // Россия → QR оплата
        selectedTariffForPayment = tariffObj
        showPaymentQRScreen = true  // ← Открывает QR-экран
    } else {
        // За границей → IAP
        print("IAP purchase for: \(tariff.title)")
    }
})
```

**Где посмотреть:**
- Откройте `10_TariffsScreen.swift`
- Строка 226: `if AppConfig.useAlternativePayments` — здесь проверяется регион
- Строка 229: `showPaymentQRScreen = true` — открывает QR-экран для России

---

### Текст кнопки зависит от региона

**Файл:** `Screens/10_TariffsScreen.swift`  
**Строка:** 127-140

```swift
private func getButtonText(for tariff: TariffType) -> String {
    if tariff == .free {
        return "БЕСПЛАТНО"
    } else if selectedTariff == tariff {
        return "✓ ВЫБРАН"
    } else {
        // Проверяем регион
        if AppConfig.useAlternativePayments {
            return "ОПЛАТИТЬ ЧЕРЕЗ QR"  // ← Для России
        } else {
            return "ПОДКЛЮЧИТЬ"  // ← Для других регионов
        }
    }
}
```

**Где посмотреть:**
- Откройте `10_TariffsScreen.swift`
- Строка 134-137 — здесь определяется текст кнопки:
  - `"ОПЛАТИТЬ ЧЕРЕЗ QR"` — если Россия
  - `"ПОДКЛЮЧИТЬ"` — если другие регионы

---

## ⚙️ ОПРЕДЕЛЕНИЕ СПОСОБА ОПЛАТЫ

### 3️⃣ Настройки оплаты в конфиге

**Файл:** `Core/Config/AppConfig.swift`  
**Строка:** 113-130

```swift
/**
 * Проверка региона пользователя
 * Если Россия → используем QR оплату
 * Если не Россия → используем IAP (App Store)
 */
static var isRussianRegion: Bool {
    return Locale.current.regionCode == "RU"
}

/**
 * Включить альтернативные способы оплаты (QR-коды)
 * В России IAP ограничен, поэтому используем СБП/SberPay
 */
static var useAlternativePayments: Bool {
    return isRussianRegion  // ← Здесь определяется способ!
}
```

**Где посмотреть:**
1. Откройте `Core/Config/AppConfig.swift`
2. Строка 113-115: определение российского региона
3. Строка 121-123: выбор способа оплаты
   - Если регион `RU` → `useAlternativePayments = true` → QR
   - Если другой регион → `useAlternativePayments = false` → IAP

---

## 🎬 ПОТОК ОПЛАТЫ

### 4️⃣ Где происходит фактическая оплата

#### Для России (QR-оплата):

**Экран оплаты:** `Screens/25_PaymentQRScreen.swift`  
**Создание платежа:** `ViewModels/PaymentQRViewModel.swift`, строка 149

```swift
func createPayment() {
    // Парсим сумму из строки (например, "590 ₽" → 590)
    let amountString = tariff.price.replacingOccurrences(of: "[^0-9]", with: "", options: .regularExpression)
    guard let amount = Double(amountString) else { return }
    
    // Создаем запрос
    let request = CreateQRPaymentRequest(
        amount: amount,
        currency: "RUB",
        description: tariff.title,
        tariffId: tariff.id
    )
    
    // Отправляем запрос на backend
    APIService(networkManager: NetworkManager()).createQRPayment(request: request) { ... }
}
```

**Где посмотреть:**
- `25_PaymentQRScreen.swift` — экран с QR-кодами
- `ViewModels/PaymentQRViewModel.swift`, строка 149 — функция создания платежа

#### Для других регионов (IAP):

**Покупка:** `Core/Store/StoreManager.swift`  
**Функция:** `purchase(_ product: Product)`, строка 95

```swift
func purchase(_ product: Product) async throws -> Transaction? {
    let result = try await product.purchase()
    // ... обработка покупки через App Store
}
```

**Где посмотреть:**
- `Core/Store/StoreManager.swift`, строка 95 — функция покупки через App Store

---

## 🔍 КАК ПРОВЕРИТЬ ТЕКУЩИЙ СПОСОБ ОПЛАТЫ

### Вариант 1: Через Xcode (Debug)

Добавьте breakpoint или print в `10_TariffsScreen.swift`, строка 226:

```swift
// Проверяем регион
print("🔍 Регион: \(Locale.current.regionCode ?? "unknown")")
print("🔍 useAlternativePayments: \(AppConfig.useAlternativePayments)")

if AppConfig.useAlternativePayments {
    print("✅ Используется QR-оплата (Россия)")
    // ...
} else {
    print("✅ Используется IAP (App Store)")
    // ...
}
```

### Вариант 2: Посмотреть в коде

1. Откройте `Core/Config/AppConfig.swift`
2. Найдите строку 113:
   ```swift
   static var isRussianRegion: Bool {
       return Locale.current.regionCode == "RU"
   }
   ```
3. Проверьте, что возвращает `Locale.current.regionCode`:
   - `"RU"` → QR-оплата
   - Другое значение → IAP

### Вариант 3: В UI приложения

На экране `10_TariffsScreen.swift` посмотрите на текст кнопки:
- **"ОПЛАТИТЬ ЧЕРЕЗ QR"** → QR-оплата (Россия)
- **"ПОДКЛЮЧИТЬ"** → IAP (другие регионы)

---

## 📋 СВОДНАЯ ТАБЛИЦА МЕСТ

| Что проверить | Файл | Строка | Что смотреть |
|---------------|------|--------|--------------|
| **Вход в оплату** | `01_MainScreen.swift` | 244 | NavigationLink к TariffsScreen |
| **Кнопка оплаты** | `10_TariffsScreen.swift` | 226 | `if AppConfig.useAlternativePayments` |
| **Текст кнопки** | `10_TariffsScreen.swift` | 134 | `"ОПЛАТИТЬ ЧЕРЕЗ QR"` или `"ПОДКЛЮЧИТЬ"` |
| **Определение региона** | `Core/Config/AppConfig.swift` | 113 | `isRussianRegion` |
| **Выбор способа оплаты** | `Core/Config/AppConfig.swift` | 121 | `useAlternativePayments` |
| **QR-платеж** | `25_PaymentQRScreen.swift` | - | Экран с QR-кодами |
| **Создание QR-платежа** | `ViewModels/PaymentQRViewModel.swift` | 149 | `createPayment()` |
| **IAP покупка** | `Core/Store/StoreManager.swift` | 95 | `purchase()` |

---

## 🎯 БЫСТРЫЙ ПУТЬ К ПРОВЕРКЕ

1. **Откройте:** `Core/Config/AppConfig.swift`
2. **Найдите строку 121-123:**
   ```swift
   static var useAlternativePayments: Bool {
       return isRussianRegion
   }
   ```
3. **Если нужно изменить способ оплаты**, временно замените на:
   ```swift
   static var useAlternativePayments: Bool {
       return true   // ← Для принудительного QR
       // return false  // ← Для принудительного IAP
   }
   ```

---

## ✅ ИТОГ

- **Где осуществляется оплата:** `10_TariffsScreen.swift` → кнопка (строка 204)
- **Какой способ выбран:** Определяется в `AppConfig.useAlternativePayments` (строка 121)
- **Где посмотреть:** 
  - Код: `Core/Config/AppConfig.swift`, строка 113-123
  - UI: текст кнопки на экране тарифов ("ОПЛАТИТЬ ЧЕРЕЗ QR" или "ПОДКЛЮЧИТЬ")
