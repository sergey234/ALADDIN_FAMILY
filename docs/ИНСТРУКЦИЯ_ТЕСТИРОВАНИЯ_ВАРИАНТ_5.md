# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ ВАРИАНТА 5

**Дата:** 2026-03-14  
**Цель:** Убедиться, что все endpoints работают правильно после реализации Варианта 5

---

## 📋 БЫСТРЫЙ СТАРТ

### **1. Запустить тестовый скрипт:**

```bash
cd docs/server
python3 test_variant5_endpoints.py
```

**С токеном (для защищенных endpoints):**
```bash
python3 test_variant5_endpoints.py YOUR_JWT_TOKEN
```

---

## ✅ ЧТО ПРОВЕРЯЕТСЯ

### **ТЕСТ 1: Reports Router endpoints (7 endpoints)**
- ✅ `/api/reports/driving/stats`
- ✅ `/api/reports/dark-web/stats`
- ✅ `/api/reports/identity-theft/stats`
- ✅ `/api/reports/privacy/location/stats`
- ✅ `/api/reports/privacy/cleanup/stats`
- ✅ `/api/reports/privacy/tracker/stats`
- ✅ `/api/reports/ai-categories/stats`

**Ожидаемый результат:**
- Статус: `200 OK`
- Источник: `sfm_real`
- НЕ должен попадать в Wildcard Proxy

---

### **ТЕСТ 2: Analytics Router endpoints (3 endpoints)**
- ✅ `/api/analytics` (существующий)
- ✅ `/api/analytics/threats` (НОВЫЙ)
- ✅ `/api/analytics/top-threats` (НОВЫЙ)

**Ожидаемый результат:**
- Статус: `200 OK`
- Источник: `sfm_real` или `fallback`
- НЕ должен попадать в Wildcard Proxy

---

### **ТЕСТ 3: Endpoints с роутерами (НЕ должны попадать в Wildcard Proxy)**
- ✅ `/api/auth/login`
- ✅ `/api/components/list`
- ✅ `/api/family/stats`
- ✅ `/api/payments/create`
- ✅ `/api/referral/code`
- ✅ `/api/protection/status`

**Ожидаемый результат:**
- Статус: `200`, `401`, или `422` (зависит от авторизации)
- НЕ должен попадать в Wildcard Proxy
- НЕ должно быть сообщения "SFM_PROXIED"

---

### **ТЕСТ 4: Неизвестные endpoints (должны обрабатываться через Wildcard Proxy + SFM)**
- ✅ `/api/unknown/endpoint`
- ✅ `/api/test/function`
- ✅ `/api/custom/stats`

**Ожидаемый результат:**
- Статус: `200` (если SFM доступен) или `404` (если SFM недоступен)
- Может попадать в Wildcard Proxy
- Должен вызывать SFM функцию

---

## 🔍 ПРОВЕРКА ЛОГОВ СЕРВЕРА

### **После запуска тестов проверьте логи:**

**✅ Правильные логи (Reports Router):**
```
✅ Роутер Reports API подключен: /api/reports/*/stats доступны
🔄 Вызов SFM функции: get_driving_reports_stats → ...
✅ SFM функция выполнена успешно
```

**✅ Правильные логи (Analytics Router):**
```
✅ Роутер Analytics API подключен: /api/analytics доступен
```

**✅ Правильные логи (Wildcard Proxy - только для неизвестных):**
```
📡 [WILDCARD] Обработка пути: /api/unknown/endpoint [GET]
🔍 [WILDCARD] Имя функции: get_unknown_endpoint
✅ [WILDCARD] SFM функция выполнена успешно
```

**❌ НЕПРАВИЛЬНЫЕ логи (не должно быть):**
```
📡 [WILDCARD] Обработка пути: /api/reports/driving/stats [GET]  ← НЕ ДОЛЖНО БЫТЬ!
📡 [WILDCARD] Обработка пути: /api/analytics [GET]  ← НЕ ДОЛЖНО БЫТЬ!
```

---

## ✅ КРИТЕРИИ УСПЕХА

### **Все тесты должны пройти:**

1. ✅ **Reports Router endpoints** - возвращают данные через SFM
2. ✅ **Analytics Router endpoints** - возвращают данные через SFM
3. ✅ **Endpoints с роутерами** - НЕ попадают в Wildcard Proxy
4. ✅ **Неизвестные endpoints** - обрабатываются через Wildcard Proxy + SFM
5. ✅ **Нет ошибок 404** для endpoints с роутерами
6. ✅ **Нет сообщений "SFM_PROXIED"** для endpoints с роутерами

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

### **После тестирования:**

✅ **Все endpoints работают правильно:**
- Reports Router → SFM → Реальные данные ✅
- Analytics Router → SFM → Реальные данные ✅
- Wildcard Proxy → SFM → Реальные данные (для неизвестных) ✅

✅ **Нет конфликтов:**
- Endpoints с роутерами обрабатываются роутерами ✅
- Endpoints без роутеров обрабатываются Wildcard Proxy ✅

✅ **Все работает на 100%:**
- Нет ошибок 404 для известных endpoints ✅
- Нет сообщений "SFM_PROXIED" для endpoints с роутерами ✅
- Все endpoints возвращают реальные данные через SFM ✅

---

## 📝 ЧЕКЛИСТ ПЕРЕД ТЕСТИРОВАНИЕМ

- [ ] Убедиться, что сервер запущен
- [ ] Убедиться, что SFM доступен (порт 8003)
- [ ] Убедиться, что все роутеры подключены правильно
- [ ] Запустить тестовый скрипт
- [ ] Проверить логи сервера
- [ ] Проверить ответы API
- [ ] Убедиться, что нет ошибок

---

## 🚀 ЗАПУСК ТЕСТОВ

```bash
# 1. Перейти в директорию с тестами
cd docs/server

# 2. Запустить тесты (без токена)
python3 test_variant5_endpoints.py

# 3. Запустить тесты (с токеном)
python3 test_variant5_endpoints.py YOUR_JWT_TOKEN

# 4. Проверить результаты
# Все тесты должны пройти успешно
```

---

## ✅ ВЫВОД

**После выполнения всех проверок:**
- ✅ Все endpoints работают правильно
- ✅ Нет конфликтов между роутерами и Wildcard Proxy
- ✅ Все данные возвращаются через SFM
- ✅ Система работает на 100%

**Статус:** ✅ ГОТОВО К ТЕСТИРОВАНИЮ НА СЕРВЕРЕ
