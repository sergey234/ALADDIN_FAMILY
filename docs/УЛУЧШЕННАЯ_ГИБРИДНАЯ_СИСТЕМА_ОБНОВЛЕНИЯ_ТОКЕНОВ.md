# 🚀 УЛУЧШЕННАЯ ГИБРИДНАЯ СИСТЕМА ОБНОВЛЕНИЯ ТОКЕНОВ

**Дата:** 9 марта 2026  
**Сборка:** BUILD 120  
**Статус:** ✅ Полная архитектура готова

---

## ✅ ДА, ЭТО УЛУЧШЕННАЯ ГИБРИДНАЯ СИСТЕМА!

**Ваша схема правильная!** Но нужно добавить **согласование с тарифами** в `saveNewToken()`.

---

## 🎯 ПОЛНАЯ УЛУЧШЕННАЯ АРХИТЕКТУРА

```
TokenHealthMonitor.performProactiveRefresh()
    ↓
ШАГ 1: refreshToken() с retry (2 попытки)
    ├─ Попытка 1 → Успех → saveNewToken() → ✅
    └─ Попытка 1 → Ошибка → Попытка 2 (через 2 сек) → Успех → saveNewToken() → ✅
    └─ Обе попытки → Ошибка → Fallback
    ↓
ШАГ 2: Fallback - registerDeviceAnonymously()
    └─ Всегда работает → saveNewToken() → ✅
    ↓
saveNewToken() [УЛУЧШЕННАЯ ВЕРСИЯ]
    ├─ 1. AppConfig.authToken = новый_токен (автоматически для всех запросов)
    ├─ 2. KeychainManager.save(новый_токен)
    ├─ 3. KeychainManager.save(refresh_token) [если получен]
    ├─ 4. СОГЛАСОВАНИЕ С ТАРИФАМИ:
    │   ├─ 4.1. Декодировать JWT → извлечь subscriptionLevel, trialInfo, limits, components
    │   ├─ 4.2. Если тариф найден в JWT → сохранить в SubscriptionManager
    │   └─ 4.3. Если тариф НЕ найден → fetchSubscriptionFromServer() → сохранить
    ├─ 5. SubscriptionManager.storeToken(jwtToken) [с тарифом]
    └─ 6. TokenHealthMonitor.startMonitoring() [перезапуск мониторинга]
```

---

## 📋 ДЕТАЛЬНАЯ ЛОГИКА РАБОТЫ

### **ШАГ 1: refreshToken() с retry**

```swift
private func performProactiveRefresh() async {
    logger.business("🔄 DEFENSIVE JWT: Executing smart hybrid token refresh")
    
    // Проверяем refresh_token
    if let refreshToken = KeychainManager.shared.loadString(forKey: .refreshToken),
       !refreshToken.isEmpty {
        
        // Попытка 1
        do {
            let response = try await refreshTokenWithRetry(refreshToken: refreshToken, attempt: 1)
            await saveNewToken(response) // ✅ СОГЛАСОВАНИЕ С ТАРИФАМИ ВНУТРИ
            logger.business("✅ DEFENSIVE JWT: Token refreshed via refresh_token (attempt 1)")
            return
        } catch {
            logger.business("⚠️ DEFENSIVE JWT: Attempt 1 failed, trying retry")
            
            // Попытка 2 (retry с задержкой 2 секунды)
            do {
                try await Task.sleep(nanoseconds: 2_000_000_000)
                let response = try await refreshTokenWithRetry(refreshToken: refreshToken, attempt: 2)
                await saveNewToken(response) // ✅ СОГЛАСОВАНИЕ С ТАРИФАМИ ВНУТРИ
                logger.business("✅ DEFENSIVE JWT: Token refreshed via refresh_token (attempt 2)")
                return
            } catch {
                logger.business("⚠️ DEFENSIVE JWT: Both refresh attempts failed, using fallback")
            }
        }
    }
    
    // ШАГ 2: Fallback
    logger.business("🔄 DEFENSIVE JWT: Using device re-registration as fallback")
    await performEmergencyReRegistration() // ✅ ТОЖЕ СОГЛАСОВАНИЕ С ТАРИФАМИ
}
```

---

### **ШАГ 2: saveNewToken() [УЛУЧШЕННАЯ ВЕРСИЯ]**

```swift
/// Сохранить новый токен с СОГЛАСОВАНИЕМ С ТАРИФАМИ
private func saveNewToken(_ response: RefreshTokenResponse) async {
    logger.business("💾 DEFENSIVE JWT: Сохранение нового токена с согласованием тарифов")
    
    // 1. Сохраняем access_token в AppConfig (для всех API запросов)
    AppConfig.authToken = response.access_token
    logger.business("✅ DEFENSIVE JWT: Access token сохранен в AppConfig.authToken")
    
    // 2. Сохраняем refresh_token в Keychain (если получен)
    if let newRefreshToken = response.refresh_token, !newRefreshToken.isEmpty {
        KeychainManager.shared.save(newRefreshToken, forKey: .refreshToken)
        logger.business("✅ DEFENSIVE JWT: Refresh token сохранен в Keychain")
    }
    
    // 3. СОГЛАСОВАНИЕ С ТАРИФАМИ: Декодируем JWT и извлекаем тариф
    if let (subscriptionLevel, trialInfo, limits, components) = extractSubscriptionFromJWT(response.access_token) {
        // ✅ ТАРИФ НАЙДЕН В JWT - сохраняем
        let expiresAt = Date().addingTimeInterval(response.expires_in ?? 86400)
        let currentToken = await SubscriptionManager.shared.currentToken
        
        let jwtToken = JWTToken(
            token: response.access_token,
            deviceId: currentToken?.deviceId ?? UIDevice.current.identifierForVendor?.uuidString ?? "",
            subscriptionLevel: subscriptionLevel,  // ✅ СОХРАНЯЕМ ТАРИФ
            trialInfo: trialInfo,                  // ✅ СОХРАНЯЕМ TRIAL
            expiresAt: expiresAt,
            issuedAt: Date(),
            issuer: currentToken?.issuer ?? "aladdin_server",
            limits: limits,                        // ✅ СОХРАНЯЕМ ЛИМИТЫ
            components: components                 // ✅ СОХРАНЯЕМ КОМПОНЕНТЫ
        )
        
        await SubscriptionManager.shared.storeToken(jwtToken)
        logger.business("✅ DEFENSIVE JWT: Токен сохранен с тарифом: \(subscriptionLevel)")
    } else {
        // ⚠️ ТАРИФ НЕ НАЙДЕН В JWT - запрашиваем с сервера
        logger.business("⚠️ DEFENSIVE JWT: Тариф не найден в JWT, запрашиваем с сервера")
        await fetchSubscriptionFromServer()
    }
    
    // 4. Перезапускаем мониторинг для нового токена
    TokenHealthMonitor.shared.startMonitoring()
    logger.business("✅ DEFENSIVE JWT: Мониторинг перезапущен")
}
```

---

### **ШАГ 3: performEmergencyReRegistration() [УЛУЧШЕННАЯ ВЕРСИЯ]**

```swift
/// Fallback: registerDeviceAnonymously с СОХРАНЕНИЕМ ТАРИФА
private func performEmergencyReRegistration() async {
    logger.business("🚑 DEFENSIVE JWT: Экстренная перерегистрация с сохранением тарифа")
    
    // 1. Сохраняем текущий тариф ПЕРЕД перерегистрацией
    let currentLevel = await SubscriptionManager.shared.getCurrentLevel()
    let currentTrial = await SubscriptionManager.shared.trialStatus
    logger.business("💾 DEFENSIVE JWT: Текущий тариф сохранен: \(currentLevel)")
    
    // 2. Выполняем перерегистрацию
    do {
        let newToken = try await SubscriptionManager.shared.registerDeviceAnonymously()
        
        // 3. Проверяем, сохранился ли тариф
        let newLevel = await SubscriptionManager.shared.getCurrentLevel()
        
        if newLevel != currentLevel && currentLevel != .free {
            // ⚠️ ТАРИФ ИЗМЕНИЛСЯ - запрашиваем с сервера
            logger.business("⚠️ DEFENSIVE JWT: Тариф изменился после перерегистрации (\(currentLevel) → \(newLevel))")
            logger.business("🔄 DEFENSIVE JWT: Запрашиваем тариф с сервера для восстановления")
            await fetchSubscriptionFromServer()
        } else {
            logger.business("✅ DEFENSIVE JWT: Тариф сохранен: \(currentLevel)")
        }
        
        // 4. Сохраняем токен в AppConfig
        AppConfig.authToken = newToken.token
        logger.business("✅ DEFENSIVE JWT: Токен сохранен в AppConfig.authToken")
        
        // 5. Перезапускаем мониторинг
        TokenHealthMonitor.shared.startMonitoring()
        logger.business("✅ DEFENSIVE JWT: Мониторинг перезапущен")
        
    } catch {
        logger.error("❌ DEFENSIVE JWT: Ошибка перерегистрации: \(error)")
        // Входим в offline режим
        logger.business("📴 DEFENSIVE JWT: Offline режим из-за ошибки перерегистрации")
    }
}
```

---

## 🔧 ВСПОМОГАТЕЛЬНЫЕ МЕТОДЫ

### **1. extractSubscriptionFromJWT()**

```swift
/// Декодировать JWT и извлечь информацию о тарифе
private func extractSubscriptionFromJWT(_ token: String) -> (SubscriptionLevel, TrialInfo?, SubscriptionLimits, [String])? {
    // Декодируем JWT payload
    let parts = token.split(separator: ".")
    guard parts.count == 3 else {
        logger.business("⚠️ DEFENSIVE JWT: Неверный формат JWT (не 3 части)")
        return nil
    }
    
    // Декодируем payload (base64)
    guard let payloadData = Data(base64Encoded: String(parts[1]), options: .ignoreUnknownCharacters),
          let payload = try? JSONDecoder().decode(JWTPayload.self, from: payloadData) else {
        logger.business("⚠️ DEFENSIVE JWT: Не удалось декодировать JWT payload")
        return nil
    }
    
    // Извлекаем информацию о тарифе
    let subscriptionLevel = SubscriptionLevel(rawValue: payload.subscription.level) ?? .free
    let trialInfo = payload.subscription.trialInfo
    let limits = payload.subscription.limits
    let components = payload.subscription.components
    
    logger.business("✅ DEFENSIVE JWT: Тариф извлечен из JWT: \(subscriptionLevel)")
    
    return (subscriptionLevel, trialInfo, limits, components)
}
```

---

### **2. fetchSubscriptionFromServer()**

```swift
/// Запросить информацию о тарифе с сервера (fallback)
private func fetchSubscriptionFromServer() async {
    logger.business("🔄 DEFENSIVE JWT: Запрос тарифа с сервера")
    
    // Запрашиваем статус подписки
    return await withCheckedContinuation { (continuation: CheckedContinuation<Void, Never>) in
        APIService.shared.getSubscriptionStatus { result in
            switch result {
            case .success(let status):
                // Обновляем тариф в SubscriptionManager
                Task { @MainActor in
                    await SubscriptionManager.shared.updateSubscriptionStatus(status)
                    self.logger.business("✅ DEFENSIVE JWT: Тариф обновлен с сервера: \(status.level)")
                    continuation.resume()
                }
            case .failure(let error):
                self.logger.error("❌ DEFENSIVE JWT: Ошибка запроса тарифа: \(error)")
                // Используем текущий тариф из SubscriptionManager
                self.logger.business("⚠️ DEFENSIVE JWT: Используем текущий тариф из локального хранилища")
                continuation.resume()
            }
        }
    }
}
```

---

## ✅ ПРЕИМУЩЕСТВА УЛУЧШЕННОЙ СИСТЕМЫ

### **1. Надежность:**
- ✅ Retry логика (2 попытки)
- ✅ Fallback на перерегистрацию
- ✅ Согласование с тарифами

### **2. Сохранение данных:**
- ✅ Тариф сохраняется
- ✅ Trial информация сохраняется
- ✅ Лимиты сохраняются
- ✅ Компоненты сохраняются

### **3. UX:**
- ✅ Бесшовное обновление
- ✅ Пользователь не замечает проблем
- ✅ Автоматическое применение ко всем запросам

### **4. Безопасность:**
- ✅ Токены в Keychain
- ✅ Refresh token в Keychain
- ✅ Валидация JWT

---

## 📊 СРАВНЕНИЕ: БЫЛО → СТАЛО

| Критерий | Было | Стало (Улучшенная система) |
|----------|------|----------------------------|
| **Обновление токена** | Только перерегистрация | refreshToken + fallback |
| **Retry логика** | ❌ Нет | ✅ 2 попытки |
| **Согласование тарифов** | ❌ Нет | ✅ Декодирование JWT + fallback |
| **Сохранение trial** | ❌ Терялось | ✅ Сохраняется |
| **Сохранение лимитов** | ❌ Терялось | ✅ Сохраняется |
| **Надежность** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **UX** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

## 🎯 ИТОГОВАЯ СХЕМА (ПОЛНАЯ)

```
TokenHealthMonitor.performProactiveRefresh()
    ↓
ШАГ 1: Проверка refresh_token
    ├─ Если есть → refreshToken() с retry
    │  ├─ Попытка 1 → Успех → saveNewToken() → ✅
    │  └─ Попытка 1 → Ошибка → Попытка 2 → Успех → saveNewToken() → ✅
    │  └─ Обе попытки → Ошибка → Fallback
    └─ Если нет → Fallback
    ↓
ШАГ 2: Fallback - registerDeviceAnonymously()
    └─ Всегда работает → saveNewToken() → ✅
    ↓
saveNewToken() [УЛУЧШЕННАЯ]
    ├─ 1. AppConfig.authToken = новый_токен
    ├─ 2. KeychainManager.save(access_token)
    ├─ 3. KeychainManager.save(refresh_token)
    ├─ 4. СОГЛАСОВАНИЕ С ТАРИФАМИ:
    │   ├─ 4.1. extractSubscriptionFromJWT() → извлечь тариф
    │   ├─ 4.2. Если найден → сохранить в SubscriptionManager
    │   └─ 4.3. Если НЕ найден → fetchSubscriptionFromServer()
    ├─ 5. SubscriptionManager.storeToken(jwtToken)
    └─ 6. TokenHealthMonitor.startMonitoring()
    ↓
РЕЗУЛЬТАТ:
    ├─ ✅ Токен обновлен
    ├─ ✅ Тариф сохранен
    ├─ ✅ Trial сохранен
    ├─ ✅ Лимиты сохранены
    └─ ✅ Все API запросы используют новый токен автоматически
```

---

## ✅ ЗАКЛЮЧЕНИЕ

**ДА, ЭТО УЛУЧШЕННАЯ ГИБРИДНАЯ СИСТЕМА!**

**Ваша схема правильная**, но нужно добавить:
1. ✅ Согласование с тарифами в `saveNewToken()`
2. ✅ Декодирование JWT для извлечения тарифа
3. ✅ Fallback на запрос тарифа с сервера
4. ✅ Сохранение тарифа при fallback

**Результат:**
- ✅ Максимальная надежность
- ✅ Сохранение всех данных
- ✅ Бесшовное обновление
- ✅ Комфортно для пользователей

---

**Дата создания:** 9 марта 2026  
**Версия:** 1.0  
**Статус:** ✅ Архитектура готова к реализации
