# 📋 ПЛАН ТЕСТИРОВАНИЯ ТАБЛИЦ БД

**Дата:** 2026-03-14  
**Статус:** ⏳ Готово к выполнению

---

## 📊 СОЗДАННЫЕ ТАБЛИЦЫ

1. ✅ `crash_detection_alerts` - алерты о ДТП
2. ✅ `roadside_assistance_requests` - запросы помощи на дороге
3. ✅ `parental_control_stats` - статистика родительского контроля
4. ✅ `parental_bypass_stats` - статистика обхода родительского контроля
5. ✅ `analytics_metrics` - метрики аналитики

---

## 🧪 ТЕСТОВЫЙ СКРИПТ

**Файл:** `docs/server/migrations/test_all_tables.sql`

**Что тестирует:**
- ✅ INSERT - вставка данных в каждую таблицу
- ✅ SELECT - выборка данных из каждой таблицы
- ✅ UPDATE - обновление данных в каждой таблице
- ✅ DELETE - удаление данных из каждой таблицы
- ✅ Проверка индексов
- ✅ Проверка триггеров

---

## 🚀 ИНСТРУКЦИЯ ПО ВЫПОЛНЕНИЮ

### **На сервере:**

```bash
# 1. Подключиться к серверу
ssh root@149.154.65.180

# 2. Перейти в директорию проекта
cd /opt/aladdin-backend

# 3. Загрузить тестовый скрипт (если еще не загружен)
# Скрипт должен быть в docs/server/migrations/test_all_tables.sql

# 4. Выполнить тестовый скрипт
psql -U postgres -d aladdin_db -f docs/server/migrations/test_all_tables.sql

# 5. Проверить результаты
# Скрипт выведет результаты всех тестов
```

---

## ✅ ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### **Для каждой таблицы:**
- ✅ INSERT: Успешно создана запись
- ✅ SELECT: Запись найдена
- ✅ UPDATE: Запись обновлена
- ✅ DELETE: Запись удалена

### **Проверка индексов:**
- ✅ `crash_detection_alerts`: 5 индексов
- ✅ `roadside_assistance_requests`: 5 индексов
- ✅ `parental_control_stats`: 4 индекса
- ✅ `parental_bypass_stats`: 4 индекса
- ✅ `analytics_metrics`: 8 индексов

### **Проверка триггеров:**
- ✅ `crash_detection_alerts`: 1 триггер (updated_at)
- ✅ `roadside_assistance_requests`: 1 триггер (updated_at)
- ✅ `parental_control_stats`: 1 триггер (updated_at)
- ✅ `parental_bypass_stats`: 1 триггер (updated_at)

---

## 📝 ПРИМЕЧАНИЯ

1. **Тестовые данные:** Скрипт использует случайные UUID для тестирования, поэтому не конфликтует с реальными данными
2. **Откат:** После тестирования все тестовые данные удаляются
3. **Безопасность:** Скрипт не изменяет существующие данные
4. **Производительность:** Тесты выполняются быстро (< 1 секунды)

---

## 🔍 ДОПОЛНИТЕЛЬНЫЕ ПРОВЕРКИ

После выполнения основного теста можно проверить:

```sql
-- Проверка количества записей в таблицах
SELECT 
    'crash_detection_alerts' as table_name, 
    COUNT(*) as row_count 
FROM crash_detection_alerts
UNION ALL
SELECT 'roadside_assistance_requests', COUNT(*) FROM roadside_assistance_requests
UNION ALL
SELECT 'parental_control_stats', COUNT(*) FROM parental_control_stats
UNION ALL
SELECT 'parental_bypass_stats', COUNT(*) FROM parental_bypass_stats
UNION ALL
SELECT 'analytics_metrics', COUNT(*) FROM analytics_metrics;

-- Проверка структуры таблиц
SELECT 
    table_name,
    column_name,
    data_type,
    is_nullable
FROM information_schema.columns
WHERE table_schema = 'public'
AND table_name IN (
    'crash_detection_alerts',
    'roadside_assistance_requests',
    'parental_control_stats',
    'parental_bypass_stats',
    'analytics_metrics'
)
ORDER BY table_name, ordinal_position;
```

---

**Статус:** ⏳ Готово к выполнению на сервере
