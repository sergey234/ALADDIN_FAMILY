# 💾 ОТЧЕТ: БЭКАП САЙТА НА СЕРВЕРЕ

**Дата:** 3 декабря 2025, 22:25  
**Статус:** ✅ Успешно завершено

---

## 📊 ИНФОРМАЦИЯ О БЭКАПЕ

### Основные данные:
- **Директория сайта:** `/var/www/aladdin-ai.ru`
- **Файл архива:** `/var/www/backups/site_backup_20251203_222517.tar.gz`
- **Размер архива:** 177K
- **Количество файлов:** 24
- **Формат:** tar.gz (сжатый архив)

### Дополнительные файлы:
- **Директория:** `/var/www/backups/files_20251203_222517/`
- Содержит копии ключевых файлов:
  - `index.html.backup`
  - Все файлы из `cms/*.json`

---

## 🔄 КАК ВОССТАНОВИТЬ ИЗ БЭКАПА

### Вариант 1: Полное восстановление (весь сайт)

```bash
# Подключиться к серверу
ssh root@149.154.65.180

# Создать резервную копию текущего состояния (на всякий случай)
cd /var/www
tar -czf /var/www/backups/before_restore_$(date +%Y%m%d_%H%M%S).tar.gz aladdin-ai.ru

# Восстановить из бэкапа
tar -xzf /var/www/backups/site_backup_20251203_222517.tar.gz -C /var/www

# Установить права доступа
chown -R www-data:www-data /var/www/aladdin-ai.ru
chmod -R 755 /var/www/aladdin-ai.ru
chmod 644 /var/www/aladdin-ai.ru/*.html
chmod 644 /var/www/aladdin-ai.ru/cms/*.json
```

### Вариант 2: Восстановление отдельных файлов

```bash
# Подключиться к серверу
ssh root@149.154.65.180

# Восстановить index.html
cp /var/www/backups/files_20251203_222517/index.html.backup /var/www/aladdin-ai.ru/index.html

# Восстановить файлы CMS
cp /var/www/backups/files_20251203_222517/*.json /var/www/aladdin-ai.ru/cms/

# Установить права
chmod 644 /var/www/aladdin-ai.ru/index.html
chmod 644 /var/www/aladdin-ai.ru/cms/*.json
chown www-data:www-data /var/www/aladdin-ai.ru/index.html
chown www-data:www-data /var/www/aladdin-ai.ru/cms/*.json
```

---

## 📋 СПИСОК ФАЙЛОВ В БЭКАПЕ

Бэкап содержит все файлы из директории `/var/www/aladdin-ai.ru`:
- `index.html` - главная страница
- `styles.css` - стили
- `cms/` - директория с JSON файлами:
  - `methods.json` - методы оплаты
  - `tariffs.json` - тарифы
  - `banks.json` - банки
  - `faq.json` - FAQ
  - `testimonials.json` - отзывы
  - `hero.json` - контент главного блока
- Другие файлы сайта

---

## 🛠️ СКРИПТ ДЛЯ БЭКАПА

**Файл:** `landing/backup_site.sh`

**Использование:**
```bash
cd landing
chmod +x backup_site.sh
./backup_site.sh
```

**Что делает скрипт:**
1. Находит директорию сайта на сервере
2. Создает директорию для бэкапов (`/var/www/backups`)
3. Создает полный архив сайта (tar.gz)
4. Создает дополнительные копии ключевых файлов
5. Показывает информацию о бэкапе

---

## ✅ ПРОВЕРКА БЭКАПА

### Проверить наличие бэкапа:
```bash
expect -c "
set timeout 30
set password \"Sergio675\"
spawn ssh root@149.154.65.180 \"ls -lh /var/www/backups/site_backup_20251203_222517.tar.gz\"
expect \"password:\" { send \"\$password\\r\" }
expect eof
"
```

### Проверить содержимое архива:
```bash
expect -c "
set timeout 30
set password \"Sergio675\"
spawn ssh root@149.154.65.180 \"tar -tzf /var/www/backups/site_backup_20251203_222517.tar.gz | head -10\"
expect \"password:\" { send \"\$password\\r\" }
expect eof
"
```

---

## 📝 ЗАМЕТКИ

- ✅ Бэкап создан перед внесением изменений на сайте
- ✅ Все файлы сохранены в сжатом архиве
- ✅ Дополнительные копии ключевых файлов созданы
- ✅ Бэкап находится на сервере в `/var/www/backups/`

---

## 🔒 БЕЗОПАСНОСТЬ

**Рекомендации:**
1. Регулярно создавать бэкапы перед изменениями
2. Хранить бэкапы в отдельной директории
3. Проверять целостность бэкапов перед восстановлением
4. Не удалять старые бэкапы сразу (хранить хотя бы 3-5 последних)

---

**Документ создан:** 3 декабря 2025  
**Статус:** ✅ Бэкап успешно создан

