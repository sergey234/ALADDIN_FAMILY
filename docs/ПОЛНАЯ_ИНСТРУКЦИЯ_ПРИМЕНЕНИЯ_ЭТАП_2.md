# 📋 ПОЛНАЯ ИНСТРУКЦИЯ: ПРИМЕНЕНИЕ ЭТАП 2 НА СЕРВЕРЕ

**Дата:** 2026-03-14  
**Статус:** ✅ **ГОТОВО К ПРИМЕНЕНИЮ**

---

## 🎯 ОБЗОР

Эта инструкция описывает полный процесс применения ЭТАП 2 на сервере:
1. Применение миграций
2. Загрузка обновленных роутеров
3. Перезапуск сервера
4. Тестирование endpoints

---

## 📝 ШАГ 1: ПРИМЕНЕНИЕ МИГРАЦИЙ

**Файл:** `docs/ШАГ_1_ПРИМЕНЕНИЕ_МИГРАЦИЙ.md`

**Краткая инструкция:**
```bash
ssh root@149.154.65.180
cd /opt/aladdin-backend
pg_dump -U postgres -d aladdin_db > backup_etap2_$(date +%Y%m%d_%H%M%S).sql

psql -U postgres -d aladdin_db -f docs/server/migrations/create_crash_detection_alerts_table.sql
psql -U postgres -d aladdin_db -f docs/server/migrations/create_roadside_assistance_requests_table.sql
psql -U postgres -d aladdin_db -f docs/server/migrations/create_parental_control_tables.sql
psql -U postgres -d aladdin_db -f docs/server/migrations/create_analytics_metrics_table.sql

# Проверить создание таблиц
psql -U postgres -d aladdin_db -c "\dt"
```

---

## 📝 ШАГ 2: ЗАГРУЗКА РОУТЕРОВ

**Файл:** `docs/ШАГ_2_ЗАГРУЗКА_РОУТЕРОВ.md`

**Краткая инструкция:**
```bash
# На сервере: создать backups
mkdir -p backups/routers_$(date +%Y%m%d_%H%M%S)
cp app/security/api/routers/*.py backups/routers_*/

# С локальной машины: загрузить обновленные роутеры
scp app/security/api/routers/crash_detection_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/crash_detection_router.py
scp app/security/api/routers/roadside_assistance_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/roadside_assistance_router.py
scp app/security/api/routers/parental_control_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/parental_control_router.py
scp app/security/api/routers/subscription_sync_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/subscription_sync_router.py
scp app/security/api/routers/metrics_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/metrics_router.py

# На сервере: проверить синтаксис
python3 -m py_compile app/security/api/routers/*.py
```

---

## 📝 ШАГ 3: ПЕРЕЗАПУСК СЕРВЕРА

**Файл:** `docs/ШАГ_3_ПЕРЕЗАПУСК_СЕРВЕРА.md`

**Краткая инструкция:**
```bash
# Перезапустить сервер
systemctl restart aladdin-backend

# Проверить статус
systemctl status aladdin-backend

# Проверить логи
tail -n 100 /opt/aladdin-backend/logs/app.log

# Проверить health check
curl http://localhost:8002/health
```

---

## 📝 ШАГ 4: ТЕСТИРОВАНИЕ ENDPOINTS

**Файл:** `docs/ШАГ_4_ТЕСТИРОВАНИЕ_ENDPOINTS.md`

**Краткая инструкция:**
```bash
# Получить токен
export TOKEN="Bearer $(curl -X POST http://localhost:8002/api/auth/login -H "Content-Type: application/json" -d '{"email": "...", "password": "..."}' | jq -r '.access_token')"

# Тестировать endpoints
curl -X GET http://localhost:8002/api/crash-detection/status -H "Authorization: $TOKEN"
curl -X GET http://localhost:8002/api/roadside-assistance/history -H "Authorization: $TOKEN"
curl -X GET http://localhost:8002/api/v1/parental-control/stats -H "Authorization: $TOKEN"
curl -X GET http://localhost:8002/api/subscription/status -H "Authorization: $TOKEN"
curl -X POST http://localhost:8002/api/metrics/upload -H "Authorization: $TOKEN" -H "Content-Type: application/json" -d '{...}'
```

---

## ✅ ЧЕКЛИСТ ПРОВЕРКИ

### **После ШАГ 1 (Миграции):**
- [ ] Backup создан
- [ ] 5 таблиц созданы
- [ ] Индексы созданы
- [ ] Нет ошибок

### **После ШАГ 2 (Роутеры):**
- [ ] Backups созданы
- [ ] 5 роутеров загружены
- [ ] Синтаксис проверен
- [ ] Импорты доступны

### **После ШАГ 3 (Перезапуск):**
- [ ] Сервер перезапущен
- [ ] Нет ошибок в логах
- [ ] Роутеры загружены
- [ ] Health check работает

### **После ШАГ 4 (Тестирование):**
- [ ] Все endpoints работают
- [ ] Данные сохраняются в БД
- [ ] Нет ошибок
- [ ] Система готова

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Всегда создавайте backup перед изменениями**
2. **Проверяйте логи после каждого шага**
3. **Тестируйте endpoints после перезапуска**
4. **При ошибках - проверяйте детальные логи**

---

## 📞 ПОДДЕРЖКА

При возникновении проблем:
1. Проверьте детальные инструкции в соответствующих файлах
2. Проверьте логи сервера
3. Проверьте подключение к БД
4. Проверьте синтаксис файлов

---

**Статус:** ✅ **ГОТОВО К ПРИМЕНЕНИЮ**
