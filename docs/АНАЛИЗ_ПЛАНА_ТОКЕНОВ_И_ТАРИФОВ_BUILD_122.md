# 🔍 АНАЛИЗ ПЛАНА ИСПРАВЛЕНИЯ ТОКЕНОВ: ВЛИЯНИЕ НА ТАРИФЫ И ПОДПИСКИ

**Дата:** 16 марта 2026  
**Build:** 122  
**Цель:** Проверить влияние плана исправления токенов на тарифы, подписки и триал период

---

## 🎯 КРАТКОЕ РЕЗЮМЕ

### ✅ БЕЗОПАСНЫЕ ИЗМЕНЕНИЯ (не влияют на тарифы):
1. **Восстановление токена перед запросами** - только техническое исправление
2. **Refresh token для device tokens** - только обновление токена, подписка сохраняется

### ⚠️ КРИТИЧЕСКОЕ ИЗМЕНЕНИЕ (может влиять на тарифы):
3. **Автоматическая перерегистрация при 403** - **РИСК ПОТЕРИ ПОДПИСКИ!**

---

## 📊 АНАЛИЗ ТЕКУЩЕЙ ЛОГИКИ ТАРИФОВ

### 1. Регистрация устройства

#### `/api/auth/register-device` (обычная регистрация):
```python
# Создает FREE подписку
subscription_data = {
    "level": "free",  # ❌ ВСЕГДА FREE!
    "limits": SubscriptionLimits.free_limits(),
    "expiresAt": None,
    "trialDaysRemaining": None
}
```

#### `/api/auth/register-device-trial` (регистрация с триалом):
```python
# Создает TRIAL подписку (14 дней)
subscription_data = {
    "level": "trial",  # ✅ TRIAL
    "limits": SubscriptionLimits.trial_limits(),  # 80% Premium
    "expiresAt": trial_expires_at,  # 14 дней
    "trialDaysRemaining": 14
}
```

### 2. Триал период (14 дней)

**Логика:**
- ✅ Триал активируется через `/register-device-trial`
- ✅ Длительность: 14 дней (hardcoded)
- ✅ После истечения → автоматически переходит на FREE
- ✅ Проверка: `trialStatus.isActive` в `SubscriptionManager`

**Код проверки:**
```swift
func getCurrentLevel() -> SubscriptionLevel {
    if let trial = trialStatus, trial.isActive {
        return .trial  // ✅ Приоритет триалу
    }
    return currentSubscription?.level ?? .free
}
```

### 3. Переход с триала на платную подписку

**Логика:**
- ✅ Пользователь может купить Premium/Family/Personal
- ✅ Подписка обновляется через StoreKit
- ✅ Новый токен содержит обновленную подписку
- ✅ Триал автоматически отменяется

### 4. Переход с триала на FREE

**Логика:**
- ✅ После истечения триала (14 дней)
- ✅ Автоматически переходит на FREE
- ✅ Сохраняются только бесплатные функции
- ✅ Лимиты уменьшаются до FREE уровня

---

## ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА: АВТОМАТИЧЕСКАЯ ПЕРЕРЕГИСТРАЦИЯ

### Проблема в плане:

**ЭТАП 2: Автоматическая перерегистрация при 403**

```swift
// ❌ ПРОБЛЕМА: При 403 ошибке происходит перерегистрация
case 403:
    Task { @MainActor in
        await SubscriptionManager.shared.performDeviceRegistration()
    }
```

**Что происходит:**
1. Пользователь получает 403 ошибку (токен истек)
2. Вызывается `performDeviceRegistration()`
3. Вызывается `registerDeviceAnonymously()` → `/api/auth/register-device`
4. **Создается НОВЫЙ токен с FREE подпиской!** ❌
5. **Триал или платная подписка ТЕРЯЕТСЯ!** ❌

### Сценарии потери подписки:

#### Сценарий 1: Пользователь на триале (день 5 из 14)
```
1. Токен истекает → 403 ошибка
2. Автоматическая перерегистрация
3. Создается новый токен с FREE подпиской
4. ❌ Триал потерян! Пользователь переведен на FREE
```

#### Сценарий 2: Пользователь с Premium подпиской
```
1. Токен истекает → 403 ошибка
2. Автоматическая перерегистрация
3. Создается новый токен с FREE подпиской
4. ❌ Premium подписка потеряна! Пользователь переведен на FREE
```

#### Сценарий 3: Пользователь с Family подпиской
```
1. Токен истекает → 403 ошибка
2. Автоматическая перерегистрация
3. Создается новый токен с FREE подпиской
4. ❌ Family подписка потеряна! Пользователь переведен на FREE
```

---

## 🔍 АНАЛИЗ: МОЖНО ЛИ ОБОЙТИ ТАРИФЫ?

### ❌ ОБХОД ТАРИФОВ НЕВОЗМОЖЕН (текущая логика):

1. **Проверка подписки на сервере:**
   - ✅ JWT токен содержит информацию о подписке
   - ✅ Сервер проверяет `subscription.level` в токене
   - ✅ Лимиты проверяются на сервере

2. **Проверка подписки на клиенте:**
   - ✅ `SubscriptionManager` проверяет уровень подписки
   - ✅ `checkResourceAccess()` проверяет лимиты
   - ✅ UI блокирует функции для FREE пользователей

3. **Защита от обхода:**
   - ✅ Токен подписан секретным ключом (JWT_SECRET)
   - ✅ Невозможно подделать токен без ключа
   - ✅ Сервер проверяет подпись токена

### ⚠️ НО: ПЕРЕРЕГИСТРАЦИЯ МОЖЕТ ОБОЙТИ ТАРИФЫ!

**Проблема:**
- Если пользователь с Premium подпиской получает 403
- Автоматическая перерегистрация создает FREE токен
- Пользователь **временно** получает FREE доступ
- Но это **не обход**, а **потеря подписки** (баг)

---

## ✅ РЕШЕНИЕ: ИСПРАВЛЕННЫЙ ПЛАН

### Изменение ЭТАП 2: Умная перерегистрация

**Вместо простой перерегистрации:**

```swift
// ❌ БЫЛО (в плане):
case 403:
    Task { @MainActor in
        await SubscriptionManager.shared.performDeviceRegistration()
    }

// ✅ ДОЛЖНО БЫТЬ:
case 403:
    // ✅ КРИТИЧНО: Проверяем, есть ли активная подписка
    if let subscription = SubscriptionManager.shared.currentSubscription,
       subscription.level != .free {
        // ✅ У пользователя есть платная подписка или триал
        // ✅ Используем refresh token вместо перерегистрации
        Task { @MainActor in
            let refreshed = await JWTTokenManager.shared.refreshTokenIfNeeded()
            if !refreshed {
                // ✅ Если refresh не удался, пытаемся восстановить подписку
                await SubscriptionManager.shared.restoreSubscriptionFromServer()
            }
        }
    } else {
        // ✅ Только для FREE пользователей - перерегистрация безопасна
        Task { @MainActor in
            await SubscriptionManager.shared.performDeviceRegistration()
        }
    }
```

### Новый метод: Восстановление подписки с сервера

```swift
// ✅ НОВЫЙ МЕТОД: Восстановление подписки с сервера
func restoreSubscriptionFromServer() async {
    guard let deviceId = currentToken?.deviceId else {
        // Нет deviceId → перерегистрация
        await performDeviceRegistration()
        return
    }
    
    // ✅ Запрашиваем текущую подписку с сервера
    APIService.shared.getSubscriptionStatus { result in
        switch result {
        case .success(let status):
            // ✅ Восстанавливаем подписку из ответа сервера
            await updateSubscriptionStatus(status)
            // ✅ Обновляем токен с правильной подпиской
            await refreshTokenWithSubscription(status)
        case .failure:
            // ✅ Если не удалось → перерегистрация (только для FREE)
            await performDeviceRegistration()
        }
    }
}
```

---

## 📋 ПРОВЕРКА ЛОГИКИ ТАРИФОВ

### ✅ Триал период (14 дней):

**Текущая логика:**
1. ✅ Триал активируется через `/register-device-trial`
2. ✅ Длительность: 14 дней (hardcoded в `TrialInfo.durationDays = 14`)
3. ✅ Проверка: `trialStatus.isActive` в `getCurrentLevel()`
4. ✅ После истечения → автоматически переходит на FREE

**Влияние плана:**
- ✅ **НЕ влияет** - триал проверяется на клиенте и сервере
- ⚠️ **МОЖЕТ влиять** - если перерегистрация создает новый FREE токен

**Рекомендация:**
- ✅ Использовать восстановление подписки вместо перерегистрации
- ✅ Сохранять информацию о триале при обновлении токена

### ✅ Продление платных подписок:

**Текущая логика:**
1. ✅ Пользователь покупает подписку через StoreKit
2. ✅ Подписка обновляется через `/api/subscription/update`
3. ✅ Новый токен содержит обновленную подписку
4. ✅ Токен обновляется через refresh token

**Влияние плана:**
- ✅ **НЕ влияет** - refresh token сохраняет подписку
- ⚠️ **МОЖЕТ влиять** - если перерегистрация создает новый FREE токен

**Рекомендация:**
- ✅ Использовать refresh token для обновления токена
- ✅ Восстанавливать подписку с сервера при необходимости

### ✅ Остаться на бесплатном тарифе:

**Текущая логика:**
1. ✅ После истечения триала → автоматически FREE
2. ✅ FREE подписка не имеет срока действия
3. ✅ Лимиты ограничены (меньше, чем в триале)

**Влияние плана:**
- ✅ **НЕ влияет** - FREE подписка работает как раньше
- ✅ Перерегистрация для FREE пользователей безопасна

---

## 🛡️ ЗАЩИТА ОТ ОБХОДА ТАРИФОВ

### ✅ Текущая защита:

1. **JWT токен подписан:**
   - ✅ Невозможно подделать без `JWT_SECRET`
   - ✅ Сервер проверяет подпись при каждом запросе

2. **Проверка на сервере:**
   - ✅ `get_current_user()` проверяет токен
   - ✅ Endpoints проверяют `subscription.level` в токене
   - ✅ Лимиты проверяются на сервере

3. **Проверка на клиенте:**
   - ✅ `SubscriptionManager` проверяет уровень подписки
   - ✅ UI блокирует функции для FREE пользователей

### ⚠️ Потенциальные уязвимости:

1. **Перерегистрация при 403:**
   - ⚠️ Может создать новый FREE токен для платного пользователя
   - ✅ **РЕШЕНИЕ:** Восстановление подписки с сервера

2. **Refresh token:**
   - ✅ Сохраняет подписку при обновлении токена
   - ✅ Не создает новую подписку

---

## 📝 ИСПРАВЛЕННЫЙ ПЛАН

### ЭТАП 2 (ИСПРАВЛЕН): Умная обработка 403

**Вместо простой перерегистрации:**

```swift
// ✅ ИСПРАВЛЕННЫЙ КОД:
case 403:
    logger.business("🔄 DEFENSIVE JWT: 403 Forbidden - проверяем подписку")
    
    // ✅ КРИТИЧНО: Проверяем, есть ли активная подписка
    let currentLevel = SubscriptionManager.shared.getCurrentLevel()
    
    if currentLevel != .free {
        // ✅ У пользователя есть платная подписка или триал
        // ✅ Используем refresh token или восстанавливаем с сервера
        Task { @MainActor in
            // 1. Пытаемся обновить через refresh token
            let refreshed = await JWTTokenManager.shared.refreshTokenIfNeeded()
            
            if !refreshed {
                // 2. Если refresh не удался, восстанавливаем с сервера
                await SubscriptionManager.shared.restoreSubscriptionFromServer()
            }
        }
        return .silentRetry
    } else {
        // ✅ Только для FREE пользователей - перерегистрация безопасна
        Task { @MainActor in
            await SubscriptionManager.shared.performDeviceRegistration()
        }
        return .silentRetry
    }
```

### Новый метод: Восстановление подписки

```swift
// ✅ НОВЫЙ МЕТОД в SubscriptionManager:
func restoreSubscriptionFromServer() async {
    guard let deviceId = currentToken?.deviceId else {
        // Нет deviceId → перерегистрация (только для FREE)
        logger.business("⚠️ Нет deviceId - перерегистрация")
        await performDeviceRegistration()
        return
    }
    
    logger.business("🔄 Восстановление подписки с сервера для deviceId: \(deviceId)")
    
    // ✅ Запрашиваем текущую подписку с сервера
    APIService.shared.getSubscriptionStatus { [weak self] result in
        Task { @MainActor [weak self] in
            guard let self = self else { return }
            
            switch result {
            case .success(let status):
                // ✅ Восстанавливаем подписку из ответа сервера
                logger.business("✅ Подписка восстановлена: \(status.level)")
                await self.updateSubscriptionStatus(status)
                
                // ✅ Обновляем токен с правильной подпиской
                if let newToken = await self.refreshTokenWithSubscription(status) {
                    await self.storeToken(newToken)
                    logger.business("✅ Токен обновлен с правильной подпиской")
                }
                
            case .failure(let error):
                // ✅ Если не удалось → перерегистрация (только для FREE)
                logger.error("❌ Не удалось восстановить подписку: \(error.localizedDescription)")
                logger.business("⚠️ Перерегистрация устройства")
                await self.performDeviceRegistration()
            }
        }
    }
}
```

---

## ✅ ФИНАЛЬНЫЕ РЕКОМЕНДАЦИИ

### 1. Исправить ЭТАП 2 плана:
- ❌ **УБРАТЬ:** Простую перерегистрацию при 403
- ✅ **ДОБАВИТЬ:** Проверку уровня подписки
- ✅ **ДОБАВИТЬ:** Восстановление подписки с сервера
- ✅ **ДОБАВИТЬ:** Refresh token для платных пользователей

### 2. Добавить новый метод:
- ✅ `restoreSubscriptionFromServer()` - восстановление подписки
- ✅ `refreshTokenWithSubscription()` - обновление токена с подпиской

### 3. Защита от потери подписки:
- ✅ Проверять уровень подписки перед перерегистрацией
- ✅ Восстанавливать подписку с сервера
- ✅ Использовать refresh token для платных пользователей

### 4. Тестирование:
- ✅ Тест: Триал пользователь получает 403 → подписка сохраняется
- ✅ Тест: Premium пользователь получает 403 → подписка сохраняется
- ✅ Тест: FREE пользователь получает 403 → перерегистрация работает

---

## 📊 ИТОГОВАЯ ОЦЕНКА

### ✅ Безопасные изменения:
1. **Восстановление токена** - ✅ НЕ влияет на тарифы
2. **Refresh token для device tokens** - ✅ НЕ влияет на тарифы

### ⚠️ Требует исправления:
3. **Автоматическая перерегистрация** - ❌ МОЖЕТ ПОТЕРЯТЬ ПОДПИСКУ!

### 🛡️ Защита от обхода:
- ✅ JWT токен подписан - обход невозможен
- ✅ Проверка на сервере - обход невозможен
- ⚠️ Перерегистрация - может потерять подписку (баг)

---

**Дата:** 16 марта 2026  
**Статус:** Требуется исправление ЭТАП 2 плана
