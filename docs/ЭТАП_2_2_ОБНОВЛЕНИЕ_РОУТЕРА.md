# 📋 ЭТАП 2.2: ОБНОВЛЕНИЕ РОУТЕРА - roadside_assistance_router.py

**Дата:** 2026-03-14  
**Статус:** ✅ **РОУТЕР ОБНОВЛЕН**

---

## 🎯 ЧТО СДЕЛАНО

### ✅ **1. СОЗДАН ОБНОВЛЕННЫЙ РОУТЕР**

**Файл:** `app/security/api/routers/roadside_assistance_router_updated.py`

**Изменения:**
- ✅ Добавлены импорты для PostgreSQL (`Session`, `get_db`, `get_current_user`)
- ✅ Добавлена функция `save_request_to_db()` для сохранения запросов в БД
- ✅ Добавлена функция `update_request_status_in_db()` для обновления статуса в БД
- ✅ Добавлена функция `get_request_from_db()` для получения запроса из БД
- ✅ Добавлена функция `get_user_requests_from_db()` для получения истории из БД
- ✅ Обновлены все endpoints для использования PostgreSQL

---

## 📝 ОБНОВЛЕННЫЕ ENDPOINTS

### **1. POST /api/roadside-assistance/call**
- ✅ Сохраняет запрос в таблицу `roadside_assistance_requests`
- ✅ Использует `save_request_to_db()` для сохранения в PostgreSQL
- ✅ Использует `get_current_user` для получения user_id из токена

### **2. GET /api/roadside-assistance/status/{request_id}**
- ✅ Использует агента как основной источник
- ✅ Использует БД как fallback, если агент не вернул данные

### **3. POST /api/roadside-assistance/cancel/{request_id}**
- ✅ Обновляет статус в БД при отмене запроса
- ✅ Использует `update_request_status_in_db()`

### **4. GET /api/roadside-assistance/history**
- ✅ Получает историю запросов из БД
- ✅ Использует `get_user_requests_from_db()`
- ✅ Использует `get_current_user` для получения user_id из токена

---

## 🔄 КАК ПРИМЕНИТЬ ОБНОВЛЕНИЕ

### **ШАГ 1: Создать backup текущего роутера**

```bash
# На сервере
ssh root@149.154.65.180
cd /opt/aladdin-backend

# Создать backup
cp app/security/api/routers/roadside_assistance_router.py app/security/api/routers/roadside_assistance_router.py.backup
```

---

### **ШАГ 2: Загрузить обновленный роутер на сервер**

**Через SFTP:**
- **Локальный файл:** `app/security/api/routers/roadside_assistance_router_updated.py`
- **Серверный путь:** `/opt/aladdin-backend/app/security/api/routers/roadside_assistance_router.py`

**Или через scp:**
```bash
scp app/security/api/routers/roadside_assistance_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/roadside_assistance_router.py
```

---

### **ШАГ 3: Применить миграцию (если еще не применена)**

```bash
# На сервере
psql -U postgres -d aladdin_db -f docs/server/migrations/create_roadside_assistance_requests_table.sql
```

---

### **ШАГ 4: Перезапустить сервер**

```bash
# На сервере
systemctl restart aladdin-backend
```

---

### **ШАГ 5: Проверить работу**

```bash
# Проверить логи
tail -f /opt/aladdin-backend/logs/app.log

# Проверить endpoint
curl -X GET http://localhost:8002/api/roadside-assistance/health \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## ✅ КРИТЕРИИ УСПЕХА

- [ ] Роутер обновлен на сервере
- [ ] Миграция применена (таблица `roadside_assistance_requests` создана)
- [ ] Сервер перезапущен
- [ ] Endpoint `/api/roadside-assistance/call` сохраняет данные в БД
- [ ] Endpoint `/api/roadside-assistance/status/{request_id}` работает
- [ ] Endpoint `/api/roadside-assistance/history` возвращает данные из БД

---

**Статус:** ✅ **РОУТЕР ОБНОВЛЕН - ГОТОВ К ПРИМЕНЕНИЮ НА СЕРВЕРЕ**
