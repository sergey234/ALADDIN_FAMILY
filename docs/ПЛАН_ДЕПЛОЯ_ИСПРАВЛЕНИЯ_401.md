# 🚀 ПЛАН ДЕПЛОЯ: Исправление 401 ошибки для `/api/family/stats`

## ✅ Исправление применено локально

Исправление применено в следующих файлах:
- ✅ `app/auth/auth.py` - исправлено
- ✅ `docs/server/auth.py` - исправлено

## 📋 Что было исправлено

### Проблема
Серверный код `get_current_user` проверял только поля `user_id` или `id` в JWT payload, но device tokens используют поле `sub` (subject).

### Решение
Добавлена поддержка поля `sub` с приоритетом: `user_id` > `id` > `sub`

```python
# ✅ BUILD 121: Проверяем что в payload есть user_id, id или sub (для device tokens)
if "user_id" not in payload and "id" not in payload and "sub" not in payload:
    raise HTTPException(...)

# ✅ BUILD 121: Нормализуем user_id (приоритет: user_id > id > sub)
user_id = payload.get("user_id") or payload.get("id") or payload.get("sub")
```

## 🚀 Деплой на сервер

### Вариант 1: Через SSH (рекомендуется)

```bash
# 1. Подключиться к серверу
ssh root@149.154.65.180

# 2. Создать backup текущего файла
cp /opt/aladdin-backend/app/auth/auth.py /opt/aladdin-backend/app/auth/auth.py.backup_$(date +%Y%m%d_%H%M%S)

# 3. Скопировать исправленный файл
# (скопировать содержимое из app/auth/auth.py локально)

# 4. Перезапустить сервер
systemctl restart aladdin-backend
# ИЛИ
pm2 restart aladdin-backend
# ИЛИ (если запущен через uvicorn напрямую)
pkill -f "uvicorn.*aladdin"
cd /opt/aladdin-backend && nohup uvicorn main:app --host 0.0.0.0 --port 8000 > /var/log/aladdin-backend.log 2>&1 &
```

### Вариант 2: Через SCP

```bash
# 1. Скопировать файл на сервер
scp app/auth/auth.py root@149.154.65.180:/opt/aladdin-backend/app/auth/auth.py

# 2. Подключиться и перезапустить
ssh root@149.154.65.180 "systemctl restart aladdin-backend"
```

### Вариант 3: Через Git (если используется)

```bash
# 1. Закоммитить изменения
git add app/auth/auth.py
git commit -m "BUILD 121: Fix 401 error for /api/family/stats - support 'sub' field in device tokens"

# 2. Запушить на сервер
git push origin main

# 3. На сервере: pull и перезапуск
ssh root@149.154.65.180
cd /opt/aladdin-backend
git pull origin main
systemctl restart aladdin-backend
```

## 🧪 Тестирование после деплоя

### 1. Проверить что сервер запущен
```bash
curl https://aladdin-ai.ru/api/health
# Ожидается: {"status":"ok"}
```

### 2. Проверить `/api/family/stats` с device token
```bash
# Получить токен (из логов приложения или после регистрации устройства)
TOKEN="your_device_token_here"

# Проверить endpoint
curl -H "Authorization: Bearer $TOKEN" https://aladdin-ai.ru/api/family/stats

# Ожидается: 200 OK с данными статистики семьи
```

### 3. Проверить обратную совместимость
```bash
# Проверить с user token (если есть)
TOKEN="your_user_token_here"
curl -H "Authorization: Bearer $TOKEN" https://aladdin-ai.ru/api/family/stats

# Ожидается: 200 OK (должно работать как раньше)
```

## ✅ Критерии успешного деплоя

- ✅ `/api/family/stats` возвращает `200 OK` вместо `401`
- ✅ Данные статистики семьи загружаются корректно
- ✅ Другие endpoints продолжают работать
- ✅ Обратная совместимость сохранена (user tokens работают)

## 📝 Откат (если что-то пошло не так)

```bash
# 1. Восстановить backup
cp /opt/aladdin-backend/app/auth/auth.py.backup_* /opt/aladdin-backend/app/auth/auth.py

# 2. Перезапустить сервер
systemctl restart aladdin-backend
```

## 🔍 Мониторинг после деплоя

Проверить логи сервера:
```bash
# Если используется systemd
journalctl -u aladdin-backend -f

# Если используется pm2
pm2 logs aladdin-backend

# Если используется uvicorn напрямую
tail -f /var/log/aladdin-backend.log
```

## 📊 Ожидаемый результат

После деплоя:
- ✅ `/api/family/stats` работает с device tokens (с полем `sub`)
- ✅ Обратная совместимость сохранена (user tokens с `user_id` или `id` продолжают работать)
- ✅ Приоритет: `user_id` > `id` > `sub`
