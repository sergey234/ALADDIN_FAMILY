# 📋 ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ ОТЧЕТОВ КОМПОНЕНТОВ

**Дата:** 2025-01-08

---

## 🎯 ОБЩАЯ КОНЦЕПЦИЯ

### Структура отчетов:
1. **Краткая статистика в Аналитике** - карточки с основными метриками
2. **Детальные экраны** - полная информация по каждому компоненту
3. **Выбор пользователя** - для компонентов, которые работают на уровне пользователя/ребенка

---

## 📊 АНАЛИЗ КОМПОНЕНТОВ ПО ТИПАМ

### 🔴 Компоненты с выбором пользователя (нужен селектор):

1. **🚗 Отчеты о вождении** - работает для конкретного пользователя/ребенка
2. **🤖 AI категоризация** - работает для конкретного ребенка (родительский контроль)

### 🟡 Компоненты без выбора пользователя (только для текущего пользователя):

3. **🌑 Мониторинг Дарк вэб** - только для текущего пользователя
4. **🛡️ Защита кражи личности** - только для текущего пользователя
5. **📍 Пузырь местоположения** - только для текущего пользователя
6. **🧹 Очистка данных** - только для текущего пользователя
7. **🚫 Блокировка трекеров** - только для текущего пользователя

---

## 1. 🚗 ОТЧЕТЫ О ВОЖДЕНИИ (Driving Reports Agent)

### 📱 Структура экрана:

#### **Экран:** `27_DrivingReportsScreen.swift`

#### **Выбор пользователя:**
```
┌─────────────────────────────────────┐
│ 🚗 Отчеты о вождении                │
│                                     │
│ 👤 Выбрать пользователя:            │
│ [Я] [Маша] [Сергей] [Бабушка]      │ ← Селектор
└─────────────────────────────────────┘
```

**Логика выбора:**
- По умолчанию: текущий пользователь ("Я")
- Если есть дети в семье → показывать их в селекторе
- Если родитель → может выбрать любого члена семьи
- Если ребенок → видит только свои отчеты

#### **Основная статистика (карточки):**
```
┌──────────────┬──────────────┬──────────────┐
│ 📊 12        │ ⭐ 8.5/10    │ ⚠️ 3         │
│ Поездок     │ Безопасность │ Нарушения    │
└──────────────┴──────────────┴──────────────┘
```

#### **График поездок:**
```
┌─────────────────────────────────────┐
│ 📈 Поездки за неделю                │
│                                     │
│  [График: поездки по дням]         │
│  Пн Вт Ср Чт Пт Сб Вс              │
│  ▁▃▅▇▆▄▂                            │
└─────────────────────────────────────┘
```

#### **Детальная статистика:**
```
┌─────────────────────────────────────┐
│ 📊 Детальная статистика             │
│                                     │
│ • Средняя скорость: 45 км/ч        │
│ • Максимальная скорость: 78 км/ч   │
│ • Резких торможений: 12             │
│ • Резких ускорений: 8               │
│ • Резких поворотов: 5               │
│ • Время в пути: 8ч 32мин           │
└─────────────────────────────────────┘
```

#### **История поездок:**
```
┌─────────────────────────────────────┐
│ 📅 История поездок                   │
│                                     │
│ ▶ Сегодня, 14:30                    │
│   Дом → Работа                      │
│   ⭐ 9.2/10 | 25 мин | 12 км        │
│                                     │
│ ▶ Вчера, 18:15                      │
│   Работа → Дом                      │
│   ⭐ 7.8/10 | 28 мин | 12 км        │
│   ⚠️ 1 резкое торможение            │
│                                     │
│ ▶ Вчера, 08:20                      │
│   Дом → Школа                       │
│   ⭐ 8.5/10 | 15 мин | 8 км         │
└─────────────────────────────────────┘
```

#### **Фильтры:**
- Период: Сегодня / Неделя / Месяц / Год
- Тип: Все / С нарушениями / Без нарушений
- Сортировка: По дате / По оценке / По расстоянию

#### **Экспорт:**
- Кнопка "Экспорт отчета" → PDF / CSV

---

### 📊 Модель данных:

```swift
struct DrivingReport: Identifiable, Codable {
    let id: String
    let userId: String  // ID пользователя/ребенка
    let userName: String
    let startTime: Date
    let endTime: Date
    let startLocation: String
    let endLocation: String
    let distance: Double  // км
    let duration: TimeInterval  // секунды
    let averageSpeed: Double  // км/ч
    let maxSpeed: Double  // км/ч
    let safetyScore: Double  // 0-10
    let events: [DrivingEvent]  // торможения, ускорения, повороты
    let violations: [DrivingViolation]  // нарушения
}

struct DrivingEvent: Codable {
    let type: DrivingEventType  // braking, acceleration, turn
    let timestamp: Date
    let severity: Double  // 0-1 (0 = мягкое, 1 = резкое)
    let location: String?
}

enum DrivingEventType: String, Codable {
    case braking = "braking"
    case acceleration = "acceleration"
    case turn = "turn"
}

struct DrivingViolation: Codable {
    let type: ViolationType  // speed, harsh_braking, etc.
    let timestamp: Date
    let description: String
    let severity: ViolationSeverity
}

enum ViolationType: String, Codable {
    case speedExceeded = "speed_exceeded"
    case harshBraking = "harsh_braking"
    case harshAcceleration = "harsh_acceleration"
    case sharpTurn = "sharp_turn"
}

enum ViolationSeverity: String, Codable {
    case low = "low"
    case medium = "medium"
    case high = "high"
}

struct DrivingStats: Codable {
    let totalTrips: Int
    let totalDistance: Double  // км
    let totalDuration: TimeInterval  // секунды
    let averageSafetyScore: Double  // 0-10
    let violationsCount: Int
    let period: String  // "week", "month", "year"
}
```

---

### 🔌 API методы:

```swift
// Получить отчеты о вождении для пользователя
GET /components/driving_reports_agent/reports?userId={userId}&period={period}

// Получить статистику
GET /components/driving_reports_agent/stats?userId={userId}&period={period}

// Получить детальный отчет
GET /components/driving_reports_agent/report/{reportId}

// Экспорт отчета
GET /components/driving_reports_agent/export/{reportId}?format={pdf|csv}
```

---

## 2. 🌑 МОНИТОРИНГ ДАРК ВЭБ (Dark Web Monitoring Agent)

### 📱 Структура экрана:

#### **Экран:** `28_DarkWebMonitoringScreen.swift`

#### **Выбор пользователя:** НЕТ (только для текущего пользователя)

#### **Основная статистика:**
```
┌──────────────┬──────────────┬──────────────┐
│ 🔍 3         │ ⚠️ 0         │ ✅ 2         │
│ Утечек      │ Новых        │ Решено       │
│ найдено     │ за неделю    │              │
└──────────────┴──────────────┴──────────────┘
```

#### **Список утечек:**
```
┌─────────────────────────────────────┐
│ 🌑 Найденные утечки                 │
│                                     │
│ 🔴 КРИТИЧНО - Email                 │
│   Найдено: user@example.com         │
│   Дата утечки: 15.12.2024          │
│   Источник: BreachDB                │
│   [Действия] → Сменить пароль       │
│                                     │
│ 🟡 СРЕДНЕ - Пароль                  │
│   Найдено: пароль123                │
│   Дата утечки: 10.12.2024          │
│   Источник: LeakBase                │
│   [Действия] → Сменить пароль       │
│                                     │
│ ✅ РЕШЕНО - Номер телефона          │
│   Найдено: +7 900 123-45-67        │
│   Дата утечки: 05.12.2024          │
│   Источник: DarkNetDB               │
│   Решено: 08.12.2024               │
└─────────────────────────────────────┘
```

#### **Детали утечки:**
```
┌─────────────────────────────────────┐
│ 📋 Детали утечки                    │
│                                     │
│ Тип данных: Email                   │
│ Значение: user@example.com          │
│ Дата обнаружения: 15.12.2024       │
│ Дата утечки: 10.12.2024            │
│ Источник: BreachDB                  │
│ Статус: Критично                    │
│                                     │
│ Рекомендации:                      │
│ • Немедленно сменить пароль        │
│ • Включить 2FA                      │
│ • Проверить другие аккаунты        │
│                                     │
│ [Сменить пароль] [Отметить решено] │
└─────────────────────────────────────┘
```

#### **История сканирований:**
```
┌─────────────────────────────────────┐
│ 🔍 История сканирований              │
│                                     │
│ ✅ Сегодня, 10:30                    │
│   Сканировано: 12 баз данных        │
│   Найдено: 0 новых утечек            │
│                                     │
│ ✅ Вчера, 10:30                      │
│   Сканировано: 12 баз данных        │
│   Найдено: 1 новая утечка           │
│                                     │
│ ✅ 3 дня назад, 10:30                │
│   Сканировано: 12 баз данных        │
│   Найдено: 0 новых утечек           │
└─────────────────────────────────────┘
```

#### **Настройки:**
- Автоматическое сканирование: Вкл/Выкл
- Частота сканирования: Ежедневно / Еженедельно
- Уведомления: Вкл/Выкл
- Мониторить: Email / Пароли / Телефоны / Банковские данные

---

### 📊 Модель данных:

```swift
struct DarkWebLeak: Identifiable, Codable {
    let id: String
    let dataType: LeakDataType  // email, password, phone, bank
    let value: String  // замаскированное значение (user@***.com)
    let fullValue: String?  // полное значение (только для отображения)
    let leakDate: Date
    let discoveryDate: Date
    let source: String  // название базы данных
    let severity: LeakSeverity
    let status: LeakStatus
    let recommendations: [String]
}

enum LeakDataType: String, Codable {
    case email = "email"
    case password = "password"
    case phone = "phone"
    case bank = "bank"
    case passport = "passport"
    case snils = "snils"
}

enum LeakSeverity: String, Codable {
    case critical = "critical"
    case high = "high"
    case medium = "medium"
    case low = "low"
}

enum LeakStatus: String, Codable {
    case new = "new"
    case inProgress = "in_progress"
    case resolved = "resolved"
    case ignored = "ignored"
}

struct DarkWebScan: Codable {
    let id: String
    let scanDate: Date
    let databasesScanned: Int
    let newLeaksFound: Int
    let status: ScanStatus
}

enum ScanStatus: String, Codable {
    case completed = "completed"
    case inProgress = "in_progress"
    case failed = "failed"
}

struct DarkWebStats: Codable {
    let totalLeaks: Int
    let newLeaks: Int
    let resolvedLeaks: Int
    let criticalLeaks: Int
    let lastScanDate: Date?
}
```

---

### 🔌 API методы:

```swift
// Получить список утечек
GET /components/dark_web_monitoring_agent/leaks?status={new|resolved|all}

// Получить детали утечки
GET /components/dark_web_monitoring_agent/leak/{leakId}

// Отметить утечку как решенную
POST /components/dark_web_monitoring_agent/leak/{leakId}/resolve

// Запустить сканирование
POST /components/dark_web_monitoring_agent/scan

// Получить историю сканирований
GET /components/dark_web_monitoring_agent/scans?limit={limit}

// Получить статистику
GET /components/dark_web_monitoring_agent/stats
```

---

## 3. 🛡️ ЗАЩИТА КРАЖИ ЛИЧНОСТИ (Identity Theft Protection Agent)

### 📱 Структура экрана:

#### **Экран:** `29_IdentityTheftScreen.swift`

#### **Выбор пользователя:** НЕТ (только для текущего пользователя)

#### **Основная статистика:**
```
┌──────────────┬──────────────┬──────────────┐
│ 🛡️ 0         │ ✅ 47        │ ⚠️ 0         │
│ Попыток     │ Заблокировано│ Подозрительных
│ кражи       │ запросов     │ активностей  │
└──────────────┴──────────────┴──────────────┘
```

#### **История попыток:**
```
┌─────────────────────────────────────┐
│ 📋 История попыток использования    │
│                                     │
│ ✅ Заблокировано - Паспорт         │
│   Запрос от: bank.ru               │
│   Время: 15.12.2024, 14:30         │
│   Действие: Автоматически заблокирован
│                                     │
│ ✅ Заблокировано - СНИЛС           │
│   Запрос от: gosuslugi.ru          │
│   Время: 14.12.2024, 10:15         │
│   Действие: Автоматически заблокирован
│                                     │
│ ⚠️ Подозрительно - Банковские данные
│   Запрос от: unknown-site.com      │
│   Время: 13.12.2024, 18:45         │
│   Действие: Требуется проверка     │
└─────────────────────────────────────┘
```

#### **Заблокированные запросы:**
```
┌─────────────────────────────────────┐
│ 🚫 Заблокированные запросы         │
│                                     │
│ 📊 За неделю: 47                    │
│                                     │
│ • Паспорт: 12 запросов              │
│ • СНИЛС: 8 запросов                 │
│ • Банковские данные: 15 запросов    │
│ • Другие: 12 запросов               │
└─────────────────────────────────────┘
```

#### **Настройки защиты:**
- Мониторинг паспортных данных: Вкл/Выкл
- Мониторинг СНИЛС: Вкл/Выкл
- Мониторинг банковских данных: Вкл/Выкл
- Уровень защиты: Низкий / Средний / Высокий

---

### 📊 Модель данных:

```swift
struct IdentityTheftAttempt: Identifiable, Codable {
    let id: String
    let dataType: IdentityDataType
    let requestSource: String  // сайт/приложение
    let timestamp: Date
    let action: AttemptAction
    let severity: AttemptSeverity
    let details: String?
}

enum IdentityDataType: String, Codable {
    case passport = "passport"
    case snils = "snils"
    case bank = "bank"
    case other = "other"
}

enum AttemptAction: String, Codable {
    case blocked = "blocked"
    case allowed = "allowed"
    case suspicious = "suspicious"
    case requiresReview = "requires_review"
}

enum AttemptSeverity: String, Codable {
    case critical = "critical"
    case high = "high"
    case medium = "medium"
    case low = "low"
}

struct IdentityTheftStats: Codable {
    let totalAttempts: Int
    let blockedAttempts: Int
    let suspiciousActivities: Int
    let byDataType: [String: Int]  // "passport": 12, "snils": 8
}
```

---

## 4. 📍 ПУЗЫРЬ МЕСТОПОЛОЖЕНИЯ (Location Bubble Agent)

### 📱 Структура экрана:

#### **Экран:** `30_PrivacyReportsScreen.swift` (вкладка "Местоположение")

#### **Выбор пользователя:** НЕТ (только для текущего пользователя)

#### **Основная статистика:**
```
┌──────────────┬──────────────┬──────────────┐
│ 🚫 47        │ 📍 Средняя   │ ✅ 12        │
│ Заблокировано│ Точность    │ Разрешено    │
│ запросов     │ (300м)      │ запросов     │
└──────────────┴──────────────┴──────────────┘
```

#### **История запросов:**
```
┌─────────────────────────────────────┐
│ 📋 История запросов местоположения │
│                                     │
│ 🚫 Заблокировано - Instagram        │
│   Время: 15.12.2024, 14:30         │
│   Запрос: Точное местоположение    │
│   Действие: Показано приблизительное
│                                     │
│ ✅ Разрешено - Карты               │
│   Время: 15.12.2024, 13:15         │
│   Запрос: Приблизительное          │
│   Действие: Разрешено               │
└─────────────────────────────────────┘
```

#### **График активности:**
```
┌─────────────────────────────────────┐
│ 📈 Активность запросов              │
│                                     │
│  [График: запросы по часам]        │
│  00 06 12 18 24                     │
│  ▁▂▃▅▆▇▆▅▃▂▁                      │
└─────────────────────────────────────┘
```

#### **Настройки:**
- Уровень точности: Высокая (100м) / Средняя (300м) / Низкая (500м)
- Автоматическая блокировка: Вкл/Выкл
- Белый список приложений: [Список]

---

## 5. 🧹 ОЧИСТКА ДАННЫХ (Personal Data Cleanup Agent)

### 📱 Структура экрана:

#### **Экран:** `30_PrivacyReportsScreen.swift` (вкладка "Очистка")

#### **Выбор пользователя:** НЕТ (только для текущего пользователя)

#### **Основная статистика:**
```
┌──────────────┬──────────────┬──────────────┐
│ 🧹 2.3 ГБ    │ ⏰ 2ч назад  │ 📊 12        │
│ Освобождено  │ Последняя    │ Очисток      │
│              │ очистка      │ за месяц     │
└──────────────┴──────────────┴──────────────┘
```

#### **История очисток:**
```
┌─────────────────────────────────────┐
│ 📋 История очисток                  │
│                                     │
│ ✅ Сегодня, 10:30                   │
│   Освобождено: 450 МБ               │
│   • Кэш приложений: 320 МБ         │
│   • Временные файлы: 100 МБ         │
│   • Логи: 30 МБ                    │
│                                     │
│ ✅ Вчера, 10:30                     │
│   Освобождено: 1.2 ГБ               │
│   • Кэш приложений: 800 МБ         │
│   • Временные файлы: 350 МБ         │
│   • Логи: 50 МБ                    │
└─────────────────────────────────────┘
```

#### **Разбивка по категориям:**
```
┌─────────────────────────────────────┐
│ 📊 Освобожденное место              │
│                                     │
│ • Кэш приложений: 1.2 ГБ (52%)     │
│ • Временные файлы: 800 МБ (35%)     │
│ • Логи: 200 МБ (9%)                │
│ • Другое: 100 МБ (4%)              │
│                                     │
│  [Круговая диаграмма]               │
└─────────────────────────────────────┘
```

#### **Настройки:**
- Автоматическая очистка: Вкл/Выкл
- Частота: Ежедневно / Еженедельно / Ежемесячно
- Что очищать: Кэш / Временные файлы / Логи / Все

---

## 6. 🚫 БЛОКИРОВКА ТРЕКЕРОВ (Anti Tracker Agent)

### 📱 Структура экрана:

#### **Экран:** `30_PrivacyReportsScreen.swift` (вкладка "Трекеры")

#### **Выбор пользователя:** НЕТ (только для текущего пользователя)

#### **Основная статистика:**
```
┌──────────────┬──────────────┬──────────────┐
│ 🚫 1,247      │ 📈 +234      │ ✅ 100%      │
│ Заблокировано│ За неделю    │ Эффективность│
│ трекеров     │              │              │
└──────────────┴──────────────┴──────────────┘
```

#### **Топ заблокированных трекеров:**
```
┌─────────────────────────────────────┐
│ 🏆 Топ трекеров                     │
│                                     │
│ 1. Google Analytics - 342 раза     │
│ 2. Facebook Pixel - 234 раза      │
│ 3. Яндекс.Метрика - 189 раз        │
│ 4. DoubleClick - 156 раз           │
│ 5. AdSense - 98 раз                │
└─────────────────────────────────────┘
```

#### **График блокировок:**
```
┌─────────────────────────────────────┐
│ 📈 Блокировки за неделю             │
│                                     │
│  [График: блокировки по дням]      │
│  Пн Вт Ср Чт Пт Сб Вс              │
│  ▁▃▅▇▆▄▂                            │
└─────────────────────────────────────┘
```

#### **Белый список:**
```
┌─────────────────────────────────────┐
│ ✅ Белый список сайтов              │
│                                     │
│ • example.com                       │
│ • trusted-site.ru                   │
│                                     │
│ [+ Добавить сайт]                   │
└─────────────────────────────────────┘
```

---

## 7. 🤖 AI КАТЕГОРИЗАЦИЯ (AI Categories Agent)

### 📱 Структура экрана:

#### **Экран:** `31_AICategoriesScreen.swift`

#### **Выбор пользователя:** ДА (для детей)

#### **Основная статистика:**
```
┌──────────────┬──────────────┬──────────────┐
│ 🤖 342       │ 🚫 12        │ ✅ 95%        │
│ Категоризиро-│ Заблокировано│ Точность     │
│ вано сайтов  │ сайтов       │              │
└──────────────┴──────────────┴──────────────┘
```

#### **Разбивка по категориям:**
```
┌─────────────────────────────────────┐
│ 📊 Категории контента               │
│                                     │
│ • Образование: 145 сайтов (42%)   │
│ • Игры: 98 сайтов (29%)            │
│ • Развлечения: 67 сайтов (20%)    │
│ • Взрослый контент: 12 (4%)        │
│ • Другое: 20 сайтов (6%)          │
│                                     │
│  [Круговая диаграмма]               │
└─────────────────────────────────────┘
```

#### **Заблокированный контент:**
```
┌─────────────────────────────────────┐
│ 🚫 Заблокированный контент           │
│                                     │
│ ⚠️ Взрослый контент - 12 сайтов     │
│   • site1.com                       │
│   • site2.com                       │
│                                     │
│ ⚠️ Насилие - 3 сайта                 │
│   • violent-site.com                │
└─────────────────────────────────────┘
```

---

## 🎨 ДИЗАЙН КАРТОЧЕК В АНАЛИТИКЕ

### Компонент карточки:

```swift
struct ComponentReportCard: View {
    let componentId: String
    let icon: String
    let title: String
    let metrics: [(String, String)]  // [("Метрика 1", "Значение 1"), ("Метрика 2", "Значение 2")]
    let color: Color
    let onTap: () -> Void
    
    var body: some View {
        Button(action: onTap) {
            HStack(spacing: Spacing.m) {
                // Иконка
                Text(icon)
                    .font(.system(size: 32))
                    .frame(width: 50, height: 50)
                    .background(
                        Circle()
                            .fill(color.opacity(0.2))
                    )
                
                // Информация
                VStack(alignment: .leading, spacing: Spacing.xs) {
                    Text(title)
                        .font(.bodyBold)
                        .foregroundColor(.textPrimary)
                    
                    // Метрики (максимум 2)
                    ForEach(Array(metrics.prefix(2)), id: \.0) { key, value in
                        HStack(spacing: Spacing.xs) {
                            Text(key)
                                .font(.caption)
                                .foregroundColor(.textSecondary)
                            Text(value)
                                .font(.captionBold)
                                .foregroundColor(.textPrimary)
                        }
                    }
                }
                
                Spacer()
                
                // Стрелка
                Image(systemName: "chevron.right")
                    .foregroundColor(.textSecondary)
            }
            .padding(Spacing.m)
            .background(cardBackground)
            .cornerRadius(CornerRadius.medium)
        }
        .buttonStyle(PlainButtonStyle())
    }
}
```

---

## 🔄 ВЫБОР ПОЛЬЗОВАТЕЛЯ

### Компонент селектора:

```swift
struct UserSelectorView: View {
    @Binding var selectedUserId: String?
    let users: [UserOption]
    let currentUserId: String
    
    struct UserOption: Identifiable {
        let id: String
        let name: String
        let role: String
        let avatar: String?
    }
    
    var body: some View {
        ScrollView(.horizontal, showsIndicators: false) {
            HStack(spacing: Spacing.s) {
                // "Я" (текущий пользователь)
                UserChip(
                    id: currentUserId,
                    name: "Я",
                    isSelected: selectedUserId == currentUserId || selectedUserId == nil,
                    onTap: { selectedUserId = currentUserId }
                )
                
                // Другие пользователи
                ForEach(users) { user in
                    UserChip(
                        id: user.id,
                        name: user.name,
                        isSelected: selectedUserId == user.id,
                        onTap: { selectedUserId = user.id }
                    )
                }
            }
            .padding(.horizontal, Spacing.screenPadding)
        }
    }
}

struct UserChip: View {
    let id: String
    let name: String
    let isSelected: Bool
    let onTap: () -> Void
    
    var body: some View {
        Button(action: onTap) {
            Text(name)
                .font(.body)
                .foregroundColor(isSelected ? .white : .textPrimary)
                .padding(.horizontal, Spacing.m)
                .padding(.vertical, Spacing.s)
                .background(
                    Capsule()
                        .fill(isSelected ? Color.primaryBlue : Color.backgroundMedium)
                )
        }
    }
}
```

---

## 📋 ПЛАН РЕАЛИЗАЦИИ

### Этап 1: Расширить Аналитику

**Файл:** `Screens/04_AnalyticsScreen.swift`

**Добавить:**
1. Раздел `componentsReportsSection` после `protectionBlock`
2. Компонент `ComponentReportCard`
3. ViewModel для загрузки статистики компонентов

**Структура:**
```swift
private var componentsReportsSection: some View {
    VStack(spacing: Spacing.m) {
        Text("Компоненты защиты")
            .font(.h3)
            .foregroundColor(.textPrimary)
        
        VStack(spacing: Spacing.s) {
            // Карточки для каждого компонента
            ComponentReportCard(...)
            ComponentReportCard(...)
            // ...
        }
    }
    .padding(Spacing.cardPadding)
    .background(cardBackground)
    .cardShadow()
}
```

---

### Этап 2: Создать детальные экраны

**Новые файлы:**
1. `Screens/27_DrivingReportsScreen.swift`
2. `Screens/28_DarkWebMonitoringScreen.swift`
3. `Screens/29_IdentityTheftScreen.swift`
4. `Screens/30_PrivacyReportsScreen.swift` (с вкладками)
5. `Screens/31_AICategoriesScreen.swift`

**ViewModels:**
1. `ViewModels/DrivingReportsViewModel.swift`
2. `ViewModels/DarkWebMonitoringViewModel.swift`
3. `ViewModels/IdentityTheftViewModel.swift`
4. `ViewModels/PrivacyReportsViewModel.swift`
5. `ViewModels/AICategoriesViewModel.swift`

---

### Этап 3: Добавить модели данных

**Файл:** `Core/Models/ComponentReportsModels.swift`

**Содержимое:**
- Все модели данных для отчетов (см. выше)

---

### Этап 4: Добавить API методы

**Файл:** `Core/Network/APIService.swift`

**Добавить методы:**
- Для каждого компонента (см. выше)

---

### Этап 5: Добавить навигацию

**Файл:** `Core/Navigation/NavigationManager.swift`

**Добавить экраны:**
```swift
enum ALADDINScreen {
    // ...
    case drivingReports
    case darkWebMonitoring
    case identityTheft
    case privacyReports
    case aiCategories
}
```

---

## 🎯 ПРИОРИТЕТЫ РЕАЛИЗАЦИИ

### 🔴 Высокий приоритет:
1. **Отчеты о вождении** - нужен селектор пользователя
2. **Мониторинг Дарк вэб** - критичные уведомления
3. **Защита кражи личности** - важная безопасность

### 🟡 Средний приоритет:
4. **Пузырь местоположения** - приватность
5. **Очистка данных** - полезная функция
6. **Блокировка трекеров** - приватность

### 🟢 Низкий приоритет:
7. **AI категоризация** - для родительского контроля

---

## ✅ ИТОГОВАЯ СТРУКТУРА

### Аналитика (расширенная):
```
Аналитика
├── Общая статистика (уже есть)
├── Разбивка по типам угроз (уже есть)
├── Уровень защиты (уже есть)
└── Компоненты защиты (НОВОЕ)
    ├── 🚗 Отчеты о вождении → DrivingReportsScreen
    ├── 🌑 Мониторинг Дарк вэб → DarkWebMonitoringScreen
    ├── 🛡️ Защита кражи личности → IdentityTheftScreen
    ├── 📍 Пузырь местоположения → PrivacyReportsScreen (вкладка)
    ├── 🧹 Очистка данных → PrivacyReportsScreen (вкладка)
    ├── 🚫 Блокировка трекеров → PrivacyReportsScreen (вкладка)
    └── 🤖 AI категоризация → AICategoriesScreen
```

---

**Статус:** ✅ ДЕТАЛЬНЫЙ ПЛАН СОЗДАН  
**Дата:** 2025-01-08

