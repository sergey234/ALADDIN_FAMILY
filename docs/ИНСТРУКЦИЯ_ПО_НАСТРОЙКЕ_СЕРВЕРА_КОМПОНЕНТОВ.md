# 🔧 ИНСТРУКЦИЯ ПО НАСТРОЙКЕ СЕРВЕРА ДЛЯ 42 КОМПОНЕНТОВ

**Дата:** 11 января 2026  
**Сервер:** 149.154.65.180  
**Пользователь:** root  
**Пароль:** Sergio675

---

## 📋 ПРОБЛЕМА

Все API endpoints для 42 компонентов возвращают **404 Not Found**:
- `GET /api/components/status/{componentId}` → 404
- `POST /api/components/enable/{componentId}` → 404
- `POST /api/components/disable/{componentId}` → 404
- `GET /api/components/configuration/{componentId}` → 404

---

## ✅ РЕШЕНИЕ

### ШАГ 1: Подключиться к серверу

```bash
ssh root@149.154.65.180
# Пароль: Sergio675
```

### ШАГ 2: Найти главный файл FastAPI приложения

Обычно это:
- `main.py`
- `app.py`
- `server.py`
- Или в директории `security/api/`

### ШАГ 3: Скопировать файл с endpoints

Скопировать файл `docs/server/COMPONENTS_API_ENDPOINTS.py` на сервер в директорию с API роутерами.

### ШАГ 4: Зарегистрировать router в главном app

В главном файле FastAPI (например, `main.py` или `app.py`) добавить:

```python
from security.api.routers.components_router import router as components_router

# Или если файл в docs/server/:
from docs.server.COMPONENTS_API_ENDPOINTS import router as components_router

app.include_router(components_router)
```

### ШАГ 5: Создать таблицы в базе данных

Выполнить SQL скрипт для создания таблиц:

```sql
-- Таблица статусов компонентов
CREATE TABLE IF NOT EXISTS component_status (
    id SERIAL PRIMARY KEY,
    component_id VARCHAR(255) NOT NULL,
    user_id INTEGER NOT NULL,
    is_enabled BOOLEAN DEFAULT FALSE,
    last_updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(component_id, user_id)
);

-- Таблица конфигураций компонентов
CREATE TABLE IF NOT EXISTS component_configuration (
    id SERIAL PRIMARY KEY,
    component_id VARCHAR(255) NOT NULL,
    user_id INTEGER NOT NULL,
    settings JSONB DEFAULT '{}',
    version VARCHAR(50) DEFAULT '1.0.0',
    last_updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(component_id, user_id)
);

-- Индексы для быстрого поиска
CREATE INDEX IF NOT EXISTS idx_component_status_user ON component_status(user_id);
CREATE INDEX IF NOT EXISTS idx_component_config_user ON component_configuration(user_id);
```

### ШАГ 6: Реализовать функции работы с БД

В файле `COMPONENTS_API_ENDPOINTS.py` раскомментировать и реализовать:

1. **`get_component_status_from_db`** - получить статус из БД
2. **`update_component_status_in_db`** - обновить статус в БД
3. **`get_component_configuration_from_db`** - получить конфигурацию из БД
4. **`update_component_configuration_in_db`** - обновить конфигурацию в БД

### ШАГ 7: Настроить авторизацию

Раскомментировать и настроить:
- `get_current_user` - функция проверки токена
- `get_db` - функция подключения к БД

### ШАГ 8: Перезапустить сервер

```bash
# Если используется systemd:
sudo systemctl restart aladdin-api

# Или если используется supervisor:
sudo supervisorctl restart aladdin-api

# Или если запущен вручную:
# Остановить процесс и запустить снова
```

### ШАГ 9: Протестировать endpoints

```bash
# Тест 1: Получить статус компонента
curl -X GET "https://aladdin-ai.ru/api/components/status/crash_detection_agent" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Тест 2: Включить компонент
curl -X POST "https://aladdin-ai.ru/api/components/enable/crash_detection_agent" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Тест 3: Выключить компонент
curl -X POST "https://aladdin-ai.ru/api/components/disable/crash_detection_agent" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Тест 4: Получить конфигурацию
curl -X GET "https://aladdin-ai.ru/api/components/configuration/crash_detection_agent" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 📁 СТРУКТУРА ФАЙЛОВ

```
/root/
├── security/
│   └── api/
│       └── routers/
│           └── components_router.py  # Скопировать сюда COMPONENTS_API_ENDPOINTS.py
└── main.py  # Или app.py - главный файл FastAPI
```

---

## 🔍 ПРОВЕРКА

После настройки все endpoints должны возвращать:
- **200 OK** - для успешных запросов
- **401 Unauthorized** - если нет токена
- **404 Not Found** - только если компонент не существует

---

## ⚠️ ВАЖНО

1. **Авторизация:** Все endpoints требуют токен в заголовке `Authorization: Bearer {token}`
2. **База данных:** Нужно создать таблицы `component_status` и `component_configuration`
3. **42 компонента:** Все компоненты из списка `ALL_COMPONENTS` должны быть доступны
4. **Логирование:** Рекомендуется добавить логирование всех операций

---

## 📞 ПОДДЕРЖКА

Если возникли проблемы:
1. Проверить логи сервера: `tail -f /var/log/aladdin-api.log`
2. Проверить доступность endpoints: `curl -v https://aladdin-ai.ru/api/components/status/crash_detection_agent`
3. Проверить подключение к БД
4. Проверить авторизацию

---

## ✅ КРИТЕРИИ УСПЕХА

- [ ] Все 6 endpoints возвращают 200 OK (с токеном)
- [ ] Все 42 компонента доступны через API
- [ ] Статусы сохраняются в БД
- [ ] Конфигурации сохраняются в БД
- [ ] Batch запросы работают корректно
- [ ] Авторизация работает корректно

