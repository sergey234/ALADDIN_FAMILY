# 📋 ДЕТАЛЬНЫЙ ПЛАН РАБОТЫ ПО ЭКРАНАМ НАВИГАЦИИ

**Дата:** 12 января 2026  
**Статус:** План составлен, ожидает выполнения

---

## 📊 ОБЩАЯ СТАТИСТИКА

- **Всего экранов:** 45
- **Экранов с задачами:** 11
- **Экранов без задач:** 34

---

## ✅ ОСНОВНЫЕ ЭКРАНЫ (25)

### 1. **MainScreen** - Главная
**Статус:** ✅ Нет задач

---

### 2. **FamilyScreen** - Семья
**Статус:** ✅ Нет задач

---

### 3. **NetworkProtectionScreen** - Защита сети
**Проблема:** Название на русском при английском языке в навигации

**План действий:**
1. Проверить использование ключа локализации `nav_screen_network_protection` в `NavigationManager.swift`
2. Убедиться, что ключ `nav_screen_network_protection` присутствует в английской секции `LocalizationManager.swift`
3. Проверить метод `localizedTitle` в `NavigationManager.ALADDINScreen`
4. Исправить локализацию названия экрана

**Файлы для изменения:**
- `Core/Navigation/NavigationManager.swift` (метод `localizedTitle`)
- `Core/Localization/LocalizationManager.swift` (проверка ключа `nav_screen_network_protection`)

**TODO:**
- [ ] Проверить наличие ключа `nav_screen_network_protection` в английской секции
- [ ] Исправить метод `localizedTitle` для использования правильного ключа
- [ ] Протестировать переключение языка

---

### 4. **AnalyticsScreen** - Аналитика
**Статус:** ✅ Нет задач

---

### 5. **SettingsScreen** - Настройки
**Статус:** ✅ Нет задач

---

### 6. **AIAssistantScreen** - AI помощник
**Статус:** ✅ Нет задач

---

### 7. **ParentalControlScreen** - Родительский контроль
**Статус:** ✅ Нет задач

---

### 8. **ChildInterfaceScreen** - Детский интерфейс
**Проблема:** Нет иконки в навигации

**Анализ:**
- В `NavigationManager.swift` строка 134: `case .childInterface: return "child"`
- Иконка уже определена, но возможно не отображается в меню навигации

**План действий:**
1. Проверить, используется ли `screen.icon` в `ALADDINNavigationBar.swift`
2. Убедиться, что `ChildInterfaceScreen` отображается в списке навигации
3. Проверить фильтрацию экранов в `ALADDINNavigationBar`

**Файлы для проверки:**
- `Shared/Components/Navigation/ALADDINNavigationBar.swift` (строка 203-213)
- `Core/Navigation/NavigationManager.swift` (строка 134)

**TODO:**
- [ ] Проверить отображение иконки в `ALADDINNavigationBar`
- [ ] Убедиться, что экран не фильтруется из списка
- [ ] Протестировать отображение в меню навигации

---

### 9. **ChildContentScreen** - Контент для детей
**Статус:** ✅ Нет задач

---

### 10. **ElderlyInterfaceScreen** - Интерфейс 60+
**Статус:** ✅ Нет задач

---

### 11. **TariffsScreen** - Тарифы
**Статус:** ✅ Нет задач

---

### 12. **ProfileScreen** - Профиль
**Статус:** ✅ Нет задач

---

### 13. **NotificationsScreen** - Уведомления
**Статус:** ✅ Нет задач

---

### 14. **SupportScreen** - Поддержка
**Статус:** ✅ Нет задач

---

### 15. **AddMemberOptionsScreen** - Добавить участника
**Статус:** ✅ Нет задач

---

### 16. **OnboardingScreen** - Онбординг
**Статус:** ✅ Нет задач

---

### 17. **PrivacyPolicyScreen** - Политика конфиденциальности
**Статус:** ✅ Нет задач

---

### 18. **TermsOfServiceScreen** - Условия использования
**Статус:** ✅ Нет задач

---

### 19. **DevicesScreen** - Устройства
**Статус:** ✅ Нет задач

---

### 20. **ReferralScreen** - Реферальная программа
**Статус:** ✅ Нет задач

---

### 21. **DeviceDetailScreen** - Детали устройства
**Статус:** ✅ Нет задач

---

### 22. **FamilyChatScreen** - Семейный чат
**Проблема:** Не работает, ошибка "ресурс не найден" при переходе

**Анализ текущей реализации:**
- Файл: `Screens/23_FamilyChatScreen.swift`
- Использует `APIService.getFamilyChatMessages` и `APIService.sendFamilyChatMessage`
- Endpoints: `/family/chat/messages` и `/family/chat/send`
- Ошибка возникает при загрузке сообщений

**Детальный анализ логики:**

1. **Загрузка сообщений:**
   - Метод `loadMessages()` вызывает `apiService.getFamilyChatMessages`
   - Ожидает массив `FamilyChatMessageResponse`
   - Ошибка: "ресурс не найден" (404 или сетевой сбой)

2. **Отправка сообщений:**
   - Метод `sendMessage()` вызывает `apiService.sendFamilyChatMessage`
   - Требует `message` и опциональный `familyId`
   - После успешной отправки перезагружает сообщения

3. **Взаимодействие между членами семьи:**
   - Каждое сообщение имеет `sender`, `text`, `timestamp`, `isCurrentUser`
   - Сообщения отображаются в `ChatBubbleView` с разными стилями для текущего пользователя и других
   - Время форматируется из timestamp в формат "HH:mm"

**План действий:**

1. **Проверить API endpoints:**
   - Убедиться, что endpoints `/family/chat/messages` и `/family/chat/send` существуют в `AppConfig.Endpoint`
   - Проверить правильность URL и параметров запроса

2. **Проверить модели данных:**
   - Убедиться, что `FamilyChatMessageResponse` и `SendFamilyChatMessageResponse` определены в `APIModels.swift`
   - Проверить соответствие структуры ответа API

3. **Добавить обработку ошибок:**
   - Улучшить обработку ошибок 404 (ресурс не найден)
   - Добавить fallback на mock данные для тестирования
   - Показать понятное сообщение пользователю

4. **Проверить авторизацию:**
   - Убедиться, что токен авторизации передается в запросах
   - Проверить, что `familyId` правильно определяется

5. **Добавить логирование:**
   - Добавить подробное логирование запросов и ответов
   - Логировать ошибки для отладки

**Файлы для изменения:**
- `Screens/23_FamilyChatScreen.swift` (обработка ошибок, логирование)
- `Core/Network/APIService.swift` (проверка endpoints)
- `Core/Models/APIModels.swift` (проверка моделей)
- `Core/Config/AppConfig.swift` (проверка endpoints)

**TODO:**
- [ ] Проверить наличие endpoints в `AppConfig.Endpoint`
- [ ] Проверить модели `FamilyChatMessageResponse` и `SendFamilyChatMessageResponse`
- [ ] Добавить обработку ошибки 404 с понятным сообщением
- [ ] Добавить fallback на mock данные для тестирования
- [ ] Проверить передачу токена авторизации
- [ ] Добавить подробное логирование
- [ ] Протестировать загрузку и отправку сообщений

---

### 23. **NetworkProtectionEnergyStatsScreen** - Статистика энергопотребления
**Проблема:** Удалить страницу из навигации и всех связанных мест

**Анализ связей:**
- Определен в `NavigationManager.swift` (строка 43): `case networkProtectionEnergyStats = "24_NetworkProtectionEnergyStatsScreen"`
- Имеет `displayName` (строка 99): "Статистика защиты сети"
- Имеет `icon` (строка 149): "battery.100percent"
- Файл экрана: `Screens/24_NetworkProtectionEnergyStatsScreen.swift`
- Используется в `ALADDINApp.swift` в switch для навигации

**План действий:**

1. **Удалить из NavigationManager:**
   - Удалить case `networkProtectionEnergyStats` из enum `ALADDINScreen`
   - Удалить из `displayName` switch
   - Удалить из `icon` switch
   - Удалить из `localizedTitle` (если есть)

2. **Удалить из ALADDINApp.swift:**
   - Найти case `.networkProtectionEnergyStats` в switch navigationManager.currentScreen
   - Удалить соответствующий case

3. **Проверить другие использования:**
   - Найти все упоминания `networkProtectionEnergyStats` в проекте
   - Проверить ссылки из других экранов
   - Удалить все навигации к этому экрану

4. **Удалить файл экрана (опционально):**
   - Файл `Screens/24_NetworkProtectionEnergyStatsScreen.swift` можно оставить для истории или удалить

**Файлы для изменения:**
- `Core/Navigation/NavigationManager.swift` (удалить case, displayName, icon)
- `ALADDINApp.swift` (удалить case из switch)
- Проверить все файлы на упоминания `networkProtectionEnergyStats`

**TODO:**
- [ ] Найти все упоминания `networkProtectionEnergyStats` в проекте
- [ ] Удалить case из `NavigationManager.ALADDINScreen`
- [ ] Удалить из `displayName` switch
- [ ] Удалить из `icon` switch
- [ ] Удалить из `ALADDINApp.swift` switch
- [ ] Проверить и удалить навигации из других экранов
- [ ] Протестировать, что экран больше не доступен

---

### 24. **PaymentQRScreen** - QR-код оплаты
**Статус:** ✅ Нет задач

---

### 25. **ActivationCodeScreen** - Код активации
**Статус:** ✅ Нет задач

---

## 🎮 ИГРОВЫЕ ЭКРАНЫ (9)

### 26. **ChildRewardsScreen** - Детские награды
**Статус:** ✅ Нет задач

---

### 27. **FamilyTournamentView** - Семейный турнир
**Статус:** ✅ Нет задач

---

### 28. **SecurityEducationScreen** - Обучение безопасности
**Задача:** Вывести все слова и предложения с этой страницы

**Анализ файла:**
- Файл: `Screens/SecurityEducationScreen.swift`
- Приветствие: "Привет, Алексей!" и "Ты молодец! Продолжай изучать безопасность!"
- Заголовок: "Безопасность" и "Обучение защите"
- Уровень безопасности: "Уровень 5", "Защитник семьи", "XP", "уроков пройдено"
- Уроки: "Киберзащита", "Фишинг", "Соц. инженерия", "Пароли"
- Правила: "Простые правила", "Не разговаривай с незнакомцами", и т.д.
- Советы: "Продвинутые советы", "Используй двухфакторную аутентификацию", и т.д.

**Все слова и предложения (hardcoded):**

1. **Заголовок:**
   - "Безопасность"
   - "Обучение защите"
   - "Назад"

2. **Приветствие:**
   - "Привет, Алексей!"
   - "Ты молодец! Продолжай изучать безопасность!"

3. **Уровень безопасности:**
   - "Уровень \(securityLevel)"
   - "Защитник семьи"
   - "\(securityXP) XP"
   - "\(totalLessonsCompleted) уроков пройдено"

4. **Уроки:**
   - "Уроки безопасности"
   - "Киберзащита" - "Стань экспертом по безопасности"
   - "Фишинг" - "Распознай опасные письма"
   - "Соц. инженерия" - "Защитись от манипуляций"
   - "Пароли" - "Создай надёжный пароль"
   - "+\(lesson.xpReward) XP"

5. **Простые правила:**
   - "✅ Простые правила"
   - "Не разговаривай с незнакомцами"
   - "Не открывай странные письма"
   - "Позови маму или папу если страшно"

6. **Продвинутые советы:**
   - "💡 Продвинутые советы"
   - "Используй двухфакторную аутентификацию (2FA)"
   - "Проверяй URL перед переходом по ссылкам"
   - "Не публикуй личную информацию в соц. сетях"
   - "Используй защиту сети в общественных Wi-Fi"
   - "Обновляй приложения регулярно"

7. **LessonDetailView:**
   - "🎓 Изучить урок"
   - "+\(lesson.xpReward) XP +10 🦄"
   - "✅ Урок изучен!"
   - "Продолжить"

**TODO:**
- [x] Вывести все слова и предложения (выполнено выше)

---

### 29. **GamesParentalControlView** - Родительский контроль игр
**Статус:** ✅ Нет задач

---

### 30. **UnicornPetView** - Единорог-питомец
**Статус:** ✅ Нет задач

---

### 31. **UnicornUniverseView** - Вселенная единорогов
**Проблема:** Удалить страницу из навигации

**План действий:**
1. Удалить case `unicornUniverse` из `NavigationManager.ALADDINScreen`
2. Удалить из `displayName` switch
3. Удалить из `icon` switch
4. Удалить из `ALADDINApp.swift` switch (если есть)
5. Проверить и удалить навигации из других экранов

**Файлы для изменения:**
- `Core/Navigation/NavigationManager.swift`
- `ALADDINApp.swift` (если используется)

**TODO:**
- [ ] Найти все упоминания `unicornUniverse` в проекте
- [ ] Удалить case из `NavigationManager.ALADDINScreen`
- [ ] Удалить из `displayName` switch
- [ ] Удалить из `icon` switch
- [ ] Удалить из `ALADDINApp.swift` switch
- [ ] Проверить и удалить навигации из других экранов

---

### 32. **WheelOfFortuneView** - Колесо фортуны
**Проблема:** Удалить страницу из навигации

**План действий:**
1. Удалить case `wheelOfFortune` из `NavigationManager.ALADDINScreen`
2. Удалить из `displayName` switch
3. Удалить из `icon` switch
4. Удалить из `ALADDINApp.swift` switch (если есть)
5. Проверить и удалить навигации из других экранов

**Файлы для изменения:**
- `Core/Navigation/NavigationManager.swift`
- `ALADDINApp.swift` (если используется)

**TODO:**
- [ ] Найти все упоминания `wheelOfFortune` в проекте
- [ ] Удалить case из `NavigationManager.ALADDINScreen`
- [ ] Удалить из `displayName` switch
- [ ] Удалить из `icon` switch
- [ ] Удалить из `ALADDINApp.swift` switch
- [ ] Проверить и удалить навигации из других экранов

---

### 33. **YoungDefenderView** - Юный защитник
**Статус:** ✅ Нет задач

---

### 34. **FamilyProtectorView** - Я защитник
**Статус:** ✅ Нет задач

---

### 35. **ChildGoalEditorView** - Моя цель
**Проблема:** Проверить локализацию, страница не переведена вообще

**Анализ файла:**
- Файл: `Screens/ChildGoalEditorView.swift`
- Все тексты hardcoded на русском языке

**Все слова и предложения (hardcoded):**

1. **Заголовок:**
   - "🎯 Моя цель"

2. **Информационная карточка:**
   - "💡"
   - "Установить новую цель"
   - "Опиши, на что ты копишь единорогов, и родители одобрят твою цель!"

3. **Форма цели:**
   - "На что копишь? 🎁"
   - "Опиши, какой подарок ты хочешь"
   - "Сколько единорогов нужно? 🦄"
   - "Примерная стоимость подарка в единорогах"
   - "100" (placeholder)

4. **Кнопка отправки:**
   - "📨 Отправить родителям"

5. **Alert:**
   - "Цель отправлена! 📨"
   - "Родители получили твою цель и скоро ответят!"
   - "ОК"

**План действий:**
1. Добавить все ключи локализации в `LocalizationManager.swift` для русского и английского
2. Заменить все hardcoded строки на `localizationManager.localized()` вызовы
3. Протестировать переключение языка

**Ключи локализации для добавления:**
- `child_goal_editor_title` - "Моя цель" / "My Goal"
- `child_goal_editor_info_title` - "Установить новую цель" / "Set a new goal"
- `child_goal_editor_info_description` - "Опиши, на что ты копишь единорогов, и родители одобрят твою цель!" / "Describe what you're saving unicorns for, and parents will approve your goal!"
- `child_goal_editor_goal_title` - "На что копишь? 🎁" / "What are you saving for? 🎁"
- `child_goal_editor_goal_hint` - "Опиши, какой подарок ты хочешь" / "Describe the gift you want"
- `child_goal_editor_cost_title` - "Сколько единорогов нужно? 🦄" / "How many unicorns do you need? 🦄"
- `child_goal_editor_cost_hint` - "Примерная стоимость подарка в единорогах" / "Approximate cost of the gift in unicorns"
- `child_goal_editor_submit_button` - "📨 Отправить родителям" / "📨 Send to parents"
- `child_goal_editor_success_title` - "Цель отправлена! 📨" / "Goal sent! 📨"
- `child_goal_editor_success_message` - "Родители получили твою цель и скоро ответят!" / "Parents received your goal and will respond soon!"
- `child_goal_editor_ok` - "ОК" / "OK"
- `child_goal_editor_placeholder` - используется существующий ключ `child_goal_editor_placeholder`

**Файлы для изменения:**
- `Screens/ChildGoalEditorView.swift` (заменить все hardcoded строки)
- `Core/Localization/LocalizationManager.swift` (добавить ключи)

**TODO:**
- [ ] Добавить все ключи локализации в русскую секцию
- [ ] Добавить все ключи локализации в английскую секцию
- [ ] Заменить hardcoded строки в `ChildGoalEditorView.swift`
- [ ] Протестировать переключение языка

---

## 🛡️ ЭКРАНЫ ЗАЩИТЫ (4)

### 36. **ThreatProtectionScreen** - Защита от угроз
**Статус:** ✅ Нет задач

---

### 37. **ThreatProtectionSettingsScreen** - Настройки защиты
**Статус:** ✅ Нет задач

---

### 38. **IoTSecurityScreen** - Безопасность IoT
**Статус:** ✅ Нет задач

---

### 39. **AdvancedProtectionSettingsScreen** - Расширенные настройки
**Статус:** ✅ Нет задач

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ ЭКРАНЫ (7)

### 40. **MainScreenWithRegistration** - Главная с регистрацией
**Статус:** ✅ Нет задач

---

### 41. **LanguageSettingsScreen** - Настройки языка
**Статус:** ✅ Нет задач

---

### 42. **NotificationSettingsScreen** - Настройки уведомлений
**Проблема:** Скрыть из навигации

**План действий:**
1. Добавить фильтр в `ALADDINNavigationBar.swift` для исключения этого экрана
2. Или добавить свойство `isHiddenInNavigation` в `ALADDINScreen` enum
3. Обновить `ForEach` в `ALADDINNavigationBar` для фильтрации скрытых экранов

**Файлы для изменения:**
- `Shared/Components/Navigation/ALADDINNavigationBar.swift` (добавить фильтр)
- `Core/Navigation/NavigationManager.swift` (опционально: добавить свойство `isHiddenInNavigation`)

**TODO:**
- [ ] Добавить фильтр для скрытия `notificationSettings` из навигации
- [ ] Протестировать, что экран не отображается в меню

---

### 43. **WidgetConfigurationScreen** - Настройка виджетов
**Статус:** ✅ Нет задач

---

### 44. **RewardsModalView** - Модальное окно наград
**Проблема:** Скрыть из навигации

**План действий:**
1. Добавить фильтр в `ALADDINNavigationBar.swift` для исключения этого экрана
2. Или добавить свойство `isHiddenInNavigation` в `ALADDINScreen` enum
3. Обновить `ForEach` в `ALADDINNavigationBar` для фильтрации скрытых экранов

**Файлы для изменения:**
- `Shared/Components/Navigation/ALADDINNavigationBar.swift` (добавить фильтр)
- `Core/Navigation/NavigationManager.swift` (опционально: добавить свойство `isHiddenInNavigation`)

**TODO:**
- [ ] Добавить фильтр для скрытия `rewardsModal` из навигации
- [ ] Протестировать, что экран не отображается в меню

---

### 45. **RewardsQuickModal** - Быстрое модальное окно наград
**Проблема:** Скрыть из навигации

**План действий:**
1. Добавить фильтр в `ALADDINNavigationBar.swift` для исключения этого экрана
2. Или добавить свойство `isHiddenInNavigation` в `ALADDINScreen` enum
3. Обновить `ForEach` в `ALADDINNavigationBar` для фильтрации скрытых экранов

**Файлы для изменения:**
- `Shared/Components/Navigation/ALADDINNavigationBar.swift` (добавить фильтр)
- `Core/Navigation/NavigationManager.swift` (опционально: добавить свойство `isHiddenInNavigation`)

**TODO:**
- [ ] Добавить фильтр для скрытия `rewardsQuickModal` из навигации
- [ ] Протестировать, что экран не отображается в меню

---

## 📝 ОБЩИЙ TODO ЛИСТ

### Приоритет 1 (Критично):
- [ ] **Задача 3:** Исправить локализацию названия NetworkProtectionScreen
- [ ] **Задача 8:** Проверить отображение иконки ChildInterfaceScreen в навигации
- [ ] **Задача 22:** Исправить работу FamilyChatScreen (ресурс не найден)
- [ ] **Задача 23:** Удалить NetworkProtectionEnergyStatsScreen из навигации
- [ ] **Задача 35:** Локализовать ChildGoalEditorView

### Приоритет 2 (Важно):
- [ ] **Задача 31:** Удалить UnicornUniverseView из навигации
- [ ] **Задача 32:** Удалить WheelOfFortuneView из навигации
- [ ] **Задача 42:** Скрыть NotificationSettingsScreen из навигации
- [ ] **Задача 44:** Скрыть RewardsModalView из навигации
- [ ] **Задача 45:** Скрыть RewardsQuickModal из навигации

### Приоритет 3 (Информация):
- [x] **Задача 28:** Вывести все слова SecurityEducationScreen (выполнено)

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После выполнения всех задач:
1. ✅ Все экраны правильно локализованы
2. ✅ Все неработающие экраны исправлены или удалены
3. ✅ Скрытые экраны не отображаются в навигации
4. ✅ Все иконки отображаются корректно
5. ✅ FamilyChatScreen работает корректно

---

**Дата создания плана:** 12 января 2026  
**Последнее обновление:** 12 января 2026

