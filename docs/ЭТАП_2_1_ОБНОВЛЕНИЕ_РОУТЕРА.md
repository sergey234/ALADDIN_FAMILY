# 📋 ЭТАП 2.1: ОБНОВЛЕНИЕ РОУТЕРА - crash_detection_router.py

**Дата:** 2026-03-14  
**Статус:** ✅ **РОУТЕР ОБНОВЛЕН**

---

## 🎯 ЧТО СДЕЛАНО

### ✅ **1. СОЗДАН ОБНОВЛЕННЫЙ РОУТЕР**

**Файл:** `app/security/api/routers/crash_detection_router_updated.py`

**Изменения:**
- ✅ Удалено in-memory хранилище (`crash_detection_sessions`, `_active_session_ids`, `_inactive_session_ids`)
- ✅ Добавлены импорты для PostgreSQL (`Session`, `get_db`, `get_current_user`)
- ✅ Добавлена функция `save_crash_alert_to_db()` для сохранения алертов в БД
- ✅ Добавлена функция `get_active_sessions_count()` для получения количества активных сессий из БД
- ✅ Обновлены все endpoints для использования PostgreSQL

---

## 📝 ОБНОВЛЕННЫЕ ENDPOINTS

### **1. POST /api/crash-detection/setup**
- ✅ Удалена работа с in-memory хранилищем
- ✅ Использует общий роутер `/api/components/enable/crash_detection_agent` для управления статусом

### **2. POST /api/crash-detection/alert**
- ✅ Сохраняет алерт в таблицу `crash_detection_alerts`
- ✅ Использует `save_crash_alert_to_db()` для сохранения в PostgreSQL

### **3. POST /api/crash-detection/start**
- ✅ Использует общий роутер для управления статусом
- ✅ Получает количество активных сессий из БД

### **4. POST /api/crash-detection/stop**
- ✅ Использует общий роутер для управления статусом

### **5. POST /api/crash-detection/data**
- ✅ Сохраняет алерт в БД при обнаружении ДТП (`crash_detected == True`)
- ✅ Использует `save_crash_alert_to_db()` для сохранения

### **6. GET /api/crash-detection/status**
- ✅ Получает количество активных сессий из БД
- ✅ Получает общее количество алертов за последний час из БД

---

## 🔄 КАК ПРИМЕНИТЬ ОБНОВЛЕНИЕ

### **ШАГ 1: Создать backup текущего роутера**

```bash
# На сервере
ssh root@149.154.65.180
cd /opt/aladdin-backend

# Создать backup
cp app/security/api/routers/crash_detection_router.py app/security/api/routers/crash_detection_router.py.backup
```

---

### **ШАГ 2: Загрузить обновленный роутер на сервер**

**Через SFTP:**
- **Локальный файл:** `app/security/api/routers/crash_detection_router_updated.py`
- **Серверный путь:** `/opt/aladdin-backend/app/security/api/routers/crash_detection_router.py`

**Или через scp:**
```bash
scp app/security/api/routers/crash_detection_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/crash_detection_router.py
```

---

### **ШАГ 3: Применить миграцию (если еще не применена)**

```bash
# На сервере
psql -U postgres -d aladdin_db -f docs/server/migrations/create_crash_detection_alerts_table.sql
```

---

### **ШАГ 4: Перезапустить сервер**

```bash
# На сервере
systemctl restart aladdin-backend
# Или
pm2 restart aladdin-backend
# Или вручную перезапустить процесс
```

---

### **ШАГ 5: Проверить работу**

```bash
# Проверить логи
tail -f /opt/aladdin-backend/logs/app.log

# Проверить endpoint
curl -X GET http://localhost:8002/api/crash-detection/status \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## ✅ КРИТЕРИИ УСПЕХА

- [ ] Роутер обновлен на сервере
- [ ] Миграция применена (таблица `crash_detection_alerts` создана)
- [ ] Сервер перезапущен
- [ ] Endpoint `/api/crash-detection/status` работает
- [ ] Endpoint `/api/crash-detection/alert` сохраняет данные в БД
- [ ] Endpoint `/api/crash-detection/data` сохраняет алерты при обнаружении ДТП

---

## 📝 ЗАМЕТКИ

1. **Важно:** Создать backup текущего роутера перед заменой
2. **Важно:** Применить миграцию перед использованием обновленного роутера
3. **Важно:** Проверить работу endpoints после обновления

---

**Статус:** ✅ **РОУТЕР ОБНОВЛЕН - ГОТОВ К ПРИМЕНЕНИЮ НА СЕРВЕРЕ**
