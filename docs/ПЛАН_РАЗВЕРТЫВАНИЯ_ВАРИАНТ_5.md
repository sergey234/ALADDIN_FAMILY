# 🚀 ПЛАН РАЗВЕРТЫВАНИЯ ВАРИАНТА 5 НА СЕРВЕР

**Дата:** 2026-03-14  
**Статус:** ⚠️ ТРЕБУЕТСЯ РАЗВЕРТЫВАНИЕ НА СЕРВЕР

---

## 🔍 ПРОБЛЕМА

**Текущая ситуация:**
- ❌ Все endpoints попадают в Wildcard Proxy
- ❌ Reports Router не подключен на сервере
- ❌ Analytics Router не обновлен на сервере
- ❌ Main.py не обновлен на сервере

**Причина:**
- Файлы созданы локально, но не загружены на сервер
- Сервер использует старую версию кода

---

## 📋 ПЛАН ДЕЙСТВИЙ

### **ШАГ 1: Подготовка файлов**

**Проверить наличие файлов:**
```bash
ls -la security/api/routers/reports_router.py
ls -la app/routers/analytics_router.py
ls -la main.py
```

**Все файлы должны существовать!**

---

### **ШАГ 2: Загрузка файлов на сервер**

**Вариант A: Автоматическая загрузка (если установлен sshpass)**
```bash
cd /path/to/ALADDIN_iOS
./docs/server/deploy_variant5_to_server.sh
```

**Вариант B: Ручная загрузка**
```bash
# 1. Загрузка Reports Router
scp security/api/routers/reports_router.py root@149.154.65.180:/opt/aladdin-backend/security/api/routers/

# 2. Загрузка Analytics Router
scp app/routers/analytics_router.py root@149.154.65.180:/opt/aladdin-backend/app/routers/

# 3. Загрузка Main.py
scp main.py root@149.154.65.180:/opt/aladdin-backend/
```

**Пароль:** `Sergio675`

---

### **ШАГ 3: Проверка на сервере**

**Подключиться к серверу:**
```bash
ssh root@149.154.65.180
# Пароль: Sergio675
```

**Проверить синтаксис:**
```bash
cd /opt/aladdin-backend
python3 -m py_compile security/api/routers/reports_router.py
python3 -m py_compile app/routers/analytics_router.py
python3 -m py_compile main.py
```

**Если ошибок нет - продолжаем!**

---

### **ШАГ 4: Перезапуск сервиса**

**На сервере:**
```bash
# Перезапуск
systemctl restart aladdin-main-api-gateway

# Проверка статуса
systemctl status aladdin-main-api-gateway

# Ожидание запуска
sleep 10

# Проверка health
curl -s http://127.0.0.1:8002/api/health
```

---

### **ШАГ 5: Проверка логов**

**На сервере:**
```bash
# Проверить логи запуска
journalctl -u aladdin-main-api-gateway -n 100 | grep -E "(Reports|Analytics|WILDCARD|SFM Adapter)"

# Должны быть логи:
# ✅ Роутер Reports API подключен
# ✅ Роутер Analytics API подключен
# ✅ SFM Adapter для Wildcard Proxy инициализирован
```

---

### **ШАГ 6: Тестирование**

**Вариант A: Локальное тестирование (с вашего компьютера)**
```bash
cd docs/server
python3 test_variant5_endpoints.py
```

**Вариант B: Тестирование через SSH**
```bash
./docs/server/test_variant5_remote.sh
```

**Вариант C: Прямое тестирование**
```bash
# Проверка Reports Router
curl -s "https://aladdin-ai.ru/api/reports/driving/stats" | python3 -m json.tool

# Проверка Analytics Router
curl -s "https://aladdin-ai.ru/api/analytics?period=day" | python3 -m json.tool
```

---

## ✅ ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### **После развертывания:**

**Логи сервера должны показывать:**
```
✅ Роутер Reports API подключен: /api/reports/*/stats доступны
✅ Роутер Analytics API подключен: /api/analytics доступен
✅ SFM Adapter для Wildcard Proxy инициализирован
```

**Тесты должны показывать:**
```
✅ /api/reports/driving/stats: Статус 200, источник: sfm_real
✅ /api/reports/dark-web/stats: Статус 200, источник: sfm_real
✅ /api/analytics: Статус 200, НЕ Wildcard Proxy
✅ /api/analytics/threats: Статус 200, НЕ Wildcard Proxy
```

**НЕ должно быть:**
```
❌ "SFM_PROXIED" для /api/reports/*/stats
❌ "SFM_PROXIED" для /api/analytics
❌ Логов Wildcard Proxy для endpoints с роутерами
```

---

## 🚨 ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### **Проблема 1: "Permission denied"**
**Решение:**
- Проверьте пароль: `Sergio675` (первая буква заглавная)
- Проверьте IP: `149.154.65.180`

### **Проблема 2: "Syntax error"**
**Решение:**
- Проверьте синтаксис Python локально
- Убедитесь, что все импорты правильные

### **Проблема 3: "Service failed to start"**
**Решение:**
- Проверьте логи: `journalctl -u aladdin-main-api-gateway -n 50`
- Проверьте синтаксис всех файлов
- Убедитесь, что все зависимости установлены

### **Проблема 4: "Endpoints все еще попадают в Wildcard Proxy"**
**Решение:**
- Проверьте порядок подключения роутеров в main.py
- Убедитесь, что Wildcard Proxy подключен последним
- Проверьте логи запуска сервера

---

## 📝 ЧЕКЛИСТ РАЗВЕРТЫВАНИЯ

- [ ] Файлы подготовлены локально
- [ ] Файлы загружены на сервер
- [ ] Синтаксис проверен на сервере
- [ ] Сервис перезапущен
- [ ] Health endpoint работает
- [ ] Логи проверены
- [ ] Тесты запущены
- [ ] Все тесты пройдены

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. ⏳ **Загрузить файлы на сервер**
2. ⏳ **Перезапустить сервис**
3. ⏳ **Запустить тесты**
4. ⏳ **Проверить результаты**

---

**Статус:** ⚠️ ТРЕБУЕТСЯ РАЗВЕРТЫВАНИЕ НА СЕРВЕР
