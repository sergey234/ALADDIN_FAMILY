# БЭКАП ПЕРЕД ПЕРЕИМЕНОВАНИЕМ VPN → NETWORK PROTECTION

**Дата:** 15 декабря 2025  
**Цель:** Сохранить все файлы перед переименованием VPN компонентов

---

## 📋 ФАЙЛЫ ДЛЯ БЭКАПА

### 🔴 КРИТИЧНО - Основные файлы для переименования:

1. **Screens/03_VPNScreen.swift**
   - Будет переименован в: `Screens/03_NetworkProtectionScreen.swift`
   - Содержит: struct VPNScreen → struct NetworkProtectionScreen

2. **Core/VPN/VPNManager.swift**
   - Будет переименован в: `Core/VPN/NetworkProtectionManager.swift`
   - Содержит: class VPNManager → class NetworkProtectionManager

---

### 🟡 ВАЖНО - Файлы с зависимостями (будут обновлены):

3. **ALADDINApp.swift**
   - Использует: `VPNScreen()` → `NetworkProtectionScreen()`
   - Строка: ~41

4. **Core/Navigation/NavigationManager.swift**
   - Использует: `case vpn = "03_VPNScreen"` → `case networkProtection = "03_NetworkProtectionScreen"`
   - Строка: ~23

5. **Screens/01_MainScreen.swift**
   - Использует: `VPNScreen()` → `NetworkProtectionScreen()`
   - Строка: ~250
   - Использует: `VPNViewModel.shared` (остается без изменений)

6. **Screens/24_VPNEnergyStatsScreen.swift**
   - Использует: `VPNManager.shared` → `NetworkProtectionManager.shared`
   - Строка: ~14

7. **ViewModels/VPNViewModel.swift**
   - Может использовать: `VPNManager` → `NetworkProtectionManager`
   - Нужно проверить

---

### 🟢 ДОПОЛНИТЕЛЬНО - Файлы для проверки:

8. **Core/Localization/LocalizationManager.swift**
   - Уже обновлен (русская локализация)
   - Нужно проверить английскую локализацию

9. **ALADDIN.xcodeproj/project.pbxproj**
   - Будет автоматически обновлен Xcode при переименовании файлов
   - НО: нужно проверить вручную после изменений

---

## 📝 КОМАНДЫ ДЛЯ БЭКАПА

### Создать папку для бэкапа:
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
mkdir -p BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215
```

### Скопировать файлы:
```bash
# Основные файлы для переименования
cp Screens/03_VPNScreen.swift BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/
cp Core/VPN/VPNManager.swift BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/

# Файлы с зависимостями
cp ALADDINApp.swift BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/
cp Core/Navigation/NavigationManager.swift BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/
cp Screens/01_MainScreen.swift BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/
cp Screens/24_VPNEnergyStatsScreen.swift BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/
cp ViewModels/VPNViewModel.swift BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/

# project.pbxproj (важно!)
cp ALADDIN.xcodeproj/project.pbxproj BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/project.pbxproj.backup
```

---

## 📋 ЧЕКЛИСТ ПЕРЕД ИЗМЕНЕНИЯМИ

### ✅ Подготовка:
- [ ] Создать папку для бэкапа
- [ ] Скопировать все файлы из списка выше
- [ ] Проверить, что бэкап создан успешно
- [ ] Убедиться, что проект компилируется (текущее состояние)

### ✅ Перед переименованием:
- [ ] Закоммитить текущие изменения в git (если используется)
- [ ] Создать git branch для переименования (рекомендуется)
- [ ] Убедиться, что нет незакоммиченных изменений

---

## 🔄 ПЛАН ПЕРЕИМЕНОВАНИЯ

### ШАГ 1: Переименовать файлы
1. `Screens/03_VPNScreen.swift` → `Screens/03_NetworkProtectionScreen.swift`
2. `Core/VPN/VPNManager.swift` → `Core/VPN/NetworkProtectionManager.swift`

### ШАГ 2: Обновить содержимое файлов
1. В `03_NetworkProtectionScreen.swift`:
   - `struct VPNScreen` → `struct NetworkProtectionScreen`
   - `VPNScreen()` → `NetworkProtectionScreen()`

2. В `NetworkProtectionManager.swift`:
   - `class VPNManager` → `class NetworkProtectionManager`
   - `VPNManager.shared` → `NetworkProtectionManager.shared`
   - `VPNManager.VPNServer` → `NetworkProtectionManager.NetworkProtectionServer` (если нужно)

### ШАГ 3: Обновить зависимости
1. `ALADDINApp.swift`: `VPNScreen()` → `NetworkProtectionScreen()`
2. `NavigationManager.swift`: `case vpn` → `case networkProtection`
3. `MainScreen.swift`: `VPNScreen()` → `NetworkProtectionScreen()`
4. `VPNEnergyStatsScreen.swift`: `VPNManager.shared` → `NetworkProtectionManager.shared`
5. `VPNViewModel.swift`: проверить использование `VPNManager`

### ШАГ 4: Обновить project.pbxproj
- Xcode должен автоматически обновить ссылки
- НО: проверить вручную после изменений

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **VPNViewModel остается без изменений** - это только ViewModel, не связан с VPN функциональностью напрямую

2. **Локализация уже обновлена** - русская локализация уже использует "Защита сети", нужно проверить английскую

3. **project.pbxproj** - Xcode автоматически обновит при переименовании через Xcode, но при ручном переименовании нужно обновить вручную

4. **Тестирование** - после всех изменений нужно:
   - Проверить компиляцию
   - Проверить навигацию
   - Проверить работу экрана защиты сети

---

## 📊 СТАТИСТИКА ИЗМЕНЕНИЙ

**Файлов для переименования:** 2  
**Файлов для обновления:** 5-7  
**Строк кода для изменения:** ~20-30  

---

## 🔙 ВОССТАНОВЛЕНИЕ ИЗ БЭКАПА

Если что-то пошло не так:

```bash
# Восстановить файлы
cp BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/03_VPNScreen.swift Screens/
cp BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/VPNManager.swift Core/VPN/
cp BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/ALADDINApp.swift .
cp BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/NavigationManager.swift Core/Navigation/
cp BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/01_MainScreen.swift Screens/
cp BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/VPNEnergyStatsScreen.swift Screens/
cp BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/VPNViewModel.swift ViewModels/
cp BACKUPS/BACKUP_BEFORE_VPN_RENAME_20251215/project.pbxproj.backup ALADDIN.xcodeproj/project.pbxproj
```

---

**Дата создания:** 15 декабря 2025  
**Статус:** Готов к использованию
