# ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ БЕСПЛАТНОГО ТАРИФА

**Дата:** 17 декабря 2025  
**Задача:** Автоматическая активация бесплатного тарифа при первом запуске приложения

---

## 📋 ФАЙЛЫ ДЛЯ ИЗМЕНЕНИЯ

### 1. **Core/Store/StoreManager.swift**
**Что добавить:**
- Константа для бесплатного тарифа
- Методы для работы с бесплатным тарифом
- Обновление методов проверки подписки

### 2. **ALADDINApp.swift**
**Что добавить:**
- Автоматическая активация бесплатного тарифа при первом запуске
- Вызов в методе `initializeNavigation`

### 3. **Core/Config/AppConfig.swift** (опционально)
**Что добавить:**
- Константы для ключей UserDefaults

---

## 🔧 ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ

### ШАГ 1: Добавить логику бесплатного тарифа в StoreManager.swift

**Место:** После строки 19 (после `@Published var errorMessage`)

**Что добавить:**
```swift
// MARK: - Free Tariff
    
/// ID бесплатного тарифа
static let FREE_TARIFF_ID = "free"
    
/// Ключ для хранения статуса бесплатного тарифа в UserDefaults
private let FREE_TARIFF_KEY = "hasFreeTariffActivated"
    
/// Проверить активирован ли бесплатный тариф
var hasFreeTariff: Bool {
    UserDefaults.standard.bool(forKey: FREE_TARIFF_KEY)
}
    
/// Активировать бесплатный тариф
func activateFreeTariff() {
    UserDefaults.standard.set(true, forKey: FREE_TARIFF_KEY)
    print("✅ Free tariff activated")
}
```

**Место:** После метода `activeSubscription` (после строки 278)

**Что добавить:**
```swift
/// Проверить есть ли активная подписка (платная или бесплатная)
func hasActiveSubscription() -> Bool {
    // Сначала проверяем платную подписку
    if !purchasedProductIDs.isEmpty {
        return true
    }
    
    // Если платной нет → проверяем бесплатный тариф
    return hasFreeTariff
}
    
/// Получить тип текущего тарифа
func currentTariffType() -> String {
    // Если есть платная подписка → возвращаем её ID
    if let active = activeSubscription {
        return active.id
    }
    
    // Если есть бесплатный тариф → возвращаем "free"
    if hasFreeTariff {
        return StoreManager.FREE_TARIFF_ID
    }
    
    // По умолчанию → нет тарифа
    return ""
}
```

---

### ШАГ 2: Автоматическая активация при первом запуске в ALADDINApp.swift

**Место:** В методе `initializeNavigation` (после строки 332)

**Что добавить:**
```swift
// ✅ Активируем бесплатный тариф при первом запуске
let storeManager = StoreManager()
if !storeManager.hasFreeTariff && !storeManager.hasActiveSubscription() {
    storeManager.activateFreeTariff()
    print("✅ First launch: Free tariff activated automatically")
}
```

---

### ШАГ 3: Обновить проверки в других местах (если нужно)

**Файлы для проверки:**
- `ViewModels/TariffsViewModel.swift` - обновить проверки тарифа
- Другие места, где используется `isPurchased` или `activeSubscription`

---

## ✅ ПОСЛЕДОВАТЕЛЬНОСТЬ ВЫПОЛНЕНИЯ

1. ✅ Создать бэкап файлов
2. ✅ Добавить логику в StoreManager.swift
3. ✅ Добавить автоматическую активацию в ALADDINApp.swift
4. ✅ Проверить компиляцию
5. ✅ Проверить логику работы

---

## 🧪 ПРОВЕРКА

**Что проверить:**
1. ✅ Компиляция без ошибок
2. ✅ При первом запуске автоматически активируется бесплатный тариф
3. ✅ `hasActiveSubscription()` возвращает `true` для бесплатного тарифа
4. ✅ `currentTariffType()` возвращает `"free"` для бесплатного тарифа
5. ✅ При покупке платной подписки → платная имеет приоритет

---

**Статус:** Готово к реализации
