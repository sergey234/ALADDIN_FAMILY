# 🛡️ ПЛАН: ЗАМЕНА VPN НА АНТИВИРУС НА ГЛАВНОЙ СТРАНИЦЕ

**Дата:** 9 декабря 2025

---

## 📋 ЗАДАЧА

**Заменить VPN карточку на главной странице на карточку "Антивирус Аладин"**

**Текущая структура:**
```
┌─────────────┬─────────────┐
│ 🛡️ VPN      │ 💎 Тарифы   │
├─────────────┼─────────────┤
│ 📊 Аналитика│ ⚙️ Настройки│
└─────────────┴─────────────┘
```

**Новая структура:**
```
┌─────────────┬─────────────┐
│ 🛡️ Антивирус│ 💎 Тарифы   │
│   Аладин    │             │
├─────────────┼─────────────┤
│ 📊 Аналитика│ ⚙️ Настройки│
└─────────────┴─────────────┘
```

---

## 🔍 ЧТО НУЖНО СДЕЛАТЬ

### 1. Заменить VPN карточку на Антивирус карточку

**Файл:** `Screens/01_MainScreen.swift`  
**Строки:** 247-281

**Было:**
```swift
// VPN карточка
NavigationLink(destination: VPNScreen()) {
    VStack(spacing: 8) {
        HStack(spacing: 8) {
            Text("🛡️")
            Text(vpnConnected ? "🟢" : "🔴")
        }
        Text(localizationManager.localized("main_vpn_title"))
        Text(localizationManager.localized("main_vpn_subtitle"))
    }
    // ... стили
}
```

**Стало:**
```swift
// Антивирус карточка
NavigationLink(destination: VPNScreen()) {
    VStack(spacing: 8) {
        HStack(spacing: 8) {
            Text("🛡️")
            Text(antivirusEnabled ? "🟢" : "🔴")
        }
        Text(localizationManager.localized("main_antivirus_title"))
        Text(localizationManager.localized("main_antivirus_subtitle"))
    }
    // ... стили
}
```

---

### 2. Добавить локализацию для антивируса

**Файл:** `Core/Localization/LocalizationManager.swift`

**Русский:**
```swift
"main_antivirus_title": "Антивирус Аладин",
"main_antivirus_subtitle": "Защита устройств",
```

**Английский:**
```swift
"main_antivirus_title": "ALADDIN Antivirus",
"main_antivirus_subtitle": "Device protection",
```

---

### 3. Добавить состояние антивируса

**Файл:** `Screens/01_MainScreen.swift`

**Нужно добавить:**
```swift
@StateObject private var antivirusManager = AntivirusManager.shared
@AppStorage("antivirusEnabled") private var antivirusEnabled = true
```

---

## 🎯 КУДА ВЕДЕТ КАРТОЧКА

**Вариант 1: На VPNScreen (где есть антивирус секция)**
- ✅ Антивирус уже есть в VPNScreen
- ✅ Не нужно создавать новый экран
- ⚠️ Но там есть и VPN, и антивирус

**Вариант 2: Создать отдельный экран AntivirusScreen**
- ✅ Только антивирус
- ⚠️ Нужно создавать новый экран

**Рекомендация:** Вариант 1 - вести на VPNScreen, где есть антивирус секция

---

## 📝 ИЗМЕНЕНИЯ В КОДЕ

### Файл: `Screens/01_MainScreen.swift`

**1. Добавить импорты и состояние:**
```swift
@StateObject private var antivirusManager = AntivirusManager.shared
@AppStorage("antivirusEnabled") private var antivirusEnabled = true
```

**2. Заменить VPN карточку:**
```swift
// Антивирус карточка (вместо VPN)
NavigationLink(destination: VPNScreen()) {
    VStack(spacing: 8) {
        HStack(spacing: 8) {
            Text("🛡️")
                .font(.system(size: 20))
                .accessibilityLabel("Иконка антивируса")
            Text(antivirusEnabled ? "🟢" : "🔴")
                .font(.system(size: 24))
                .accessibilityLabel(antivirusEnabled ? "Статус: Активен" : "Статус: Отключен")
        }
        
        Text(localizationManager.localized("main_antivirus_title"))
            .font(.system(size: 12, weight: .semibold))
            .foregroundColor(.white)
            .accessibilityLabel("Название: Антивирус Аладин")
        
        Text(localizationManager.localized("main_antivirus_subtitle"))
            .font(.system(size: 10))
            .foregroundColor(.white.opacity(0.8))
            .accessibilityLabel("Описание: Защита устройств")
    }
    .frame(maxWidth: .infinity)
    .frame(height: 80)
    .background(
        RoundedRectangle(cornerRadius: 10)
            .fill(Color.white.opacity(0.1))
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.white.opacity(0.2), lineWidth: 1)
            )
    )
}
.accessibilityLabel("Антивирус Аладин - \(antivirusEnabled ? "Активен" : "Отключен")")
.accessibilityHint("Нажмите для открытия экрана защиты")
```

---

## ✅ ЧЕКЛИСТ

- [ ] Заменить VPN карточку на Антивирус карточку
- [ ] Добавить состояние `antivirusEnabled`
- [ ] Добавить локализацию `main_antivirus_title`
- [ ] Добавить локализацию `main_antivirus_subtitle`
- [ ] Проверить, что карточка ведет на VPNScreen
- [ ] Проверить, что статус антивируса отображается правильно

---

**Дата создания:** 09.12.2025  
**Статус:** План замены VPN на Антивирус

