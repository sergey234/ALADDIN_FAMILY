# 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ: ВАРИАНТ Б ДЛЯ КНОПКИ ОПЛАТЫ

**Дата:** 9 декабря 2025  
**Вариант:** Б - Всегда показывать "Subscribe"  
**Регионы:** Россия и все остальные

---

## 💳 ВАРИАНТ Б: ВСЕГДА "SUBSCRIBE"

### Как это работает:

1. **Все пользователи видят:** "Subscribe" / "Оформить подписку"
2. **При нажатии:**
   - Код проверяет регион через `AppConfig.useAlternativePayments`
   - Если Россия → открывается сайт (QR оплата)
   - Если не Россия → открывается IAP (App Store)

**Важно:** Логика уже есть в обработчике кнопки! Нужно только изменить текст кнопки.

---

## 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

### Шаг 1: Изменить функцию getButtonText

**Файл:** `Screens/10_TariffsScreen.swift`  
**Строки:** 189-198

**Текущий код:**
```swift
private func getButtonText(for tariff: TariffType) -> String {
    if tariff == .free {
        return localizationManager.localized("tariffs_free_button")
    } else if selectedTariff == tariff {
        return localizationManager.localized("tariffs_selected")
    } else {
        // ❌ Сейчас всегда возвращает "Go to website"
        return localizationManager.localized("tariffs_website_button")
    }
}
```

**Новый код:**
```swift
private func getButtonText(for tariff: TariffType) -> String {
    if tariff == .free {
        return localizationManager.localized("tariffs_free_button")
    } else if selectedTariff == tariff {
        return localizationManager.localized("tariffs_selected")
    } else {
        // ✅ ВАРИАНТ Б: Всегда показываем "Subscribe"
        return localizationManager.localized("tariffs_purchase_button")
    }
}
```

**Что изменилось:**
- Убрали проверку региона из `getButtonText`
- Всегда возвращаем `tariffs_purchase_button` вместо `tariffs_website_button`

---

### Шаг 2: Добавить локализацию

**Файл:** `Core/Localization/LocalizationManager.swift`

**Русский (строка ~1357):**
```swift
"tariffs_purchase_button": "Оформить подписку",
```

**Английский (строка ~3870):**
```swift
"tariffs_purchase_button": "Subscribe",
```

---

### Шаг 3: Проверить обработчик кнопки

**Файл:** `Screens/10_TariffsScreen.swift`  
**Строки:** 305-338

**Текущий код (уже правильный):**
```swift
if AppConfig.useAlternativePayments {
    // Россия → QR оплата на сайте
    guard !tariffObj.id.isEmpty,
          !tariffObj.title.isEmpty,
          !tariffObj.price.isEmpty else {
        viewModel.errorMessage = localizationManager.localized("tariffs_error_create_tariff")
        return
    }
    
    // ⚠️ Оплата по QR перенесена на лендинг: открываем сайт напрямую
    let referralCode = UserDefaults.standard.string(forKey: "referral_code")
    
    URLHelper.openWebsite(
        urlString: AppConfig.subscriptionWebsiteURL,
        tariffId: tariffObj.id,
        referralCode: referralCode
    )
} else {
    // Не Россия → IAP (App Store)
    guard !tariffObj.id.isEmpty,
          !tariffObj.title.isEmpty else {
        viewModel.errorMessage = localizationManager.localized("tariffs_error_purchase_tariff")
        return
    }
    
    #if targetEnvironment(simulator)
    viewModel.errorMessage = "In-App Purchase недоступен в симуляторе..."
    #else
    let localTariffObj = tariffObj
    
    Task { @MainActor in
        await viewModel.purchaseSelectedTariff(tariff: localTariffObj)
    }
    #endif
}
```

**Важно:** Этот код уже правильный! Он проверяет регион и:
- Для России → открывает сайт
- Для не-России → запускает IAP

**Ничего менять не нужно!**

---

## 📋 КАК ЭТО РАБОТАЕТ ДЛЯ РАЗНЫХ РЕГИОНОВ

### Для России:

1. **Пользователь видит:** "Оформить подписку"
2. **Нажимает кнопку**
3. **Код проверяет:** `AppConfig.useAlternativePayments` → `true` (Россия)
4. **Открывается:** Сайт aladdin-ai.ru с QR-оплатой
5. **Пользователь оплачивает** через QR-код
6. **Получает код активации**
7. **Вводит код** в приложении

### Для США/Европы/других стран:

1. **Пользователь видит:** "Subscribe"
2. **Нажимает кнопку**
3. **Код проверяет:** `AppConfig.useAlternativePayments` → `false` (не Россия)
4. **Открывается:** Стандартное окно App Store
5. **Пользователь видит:** "ALADDIN Premium - $9.99/month"
6. **Нажимает "Купить"** → оплачивает через Apple Pay
7. **Подписка активируется** автоматически

---

## ✅ ЧТО НУЖНО СДЕЛАТЬ

### Изменения в коде:

1. **Файл:** `Screens/10_TariffsScreen.swift`
   - Изменить функцию `getButtonText` (строки 189-198)
   - Заменить `tariffs_website_button` на `tariffs_purchase_button`

2. **Файл:** `Core/Localization/LocalizationManager.swift`
   - Добавить `tariffs_purchase_button` (русский и английский)

### Что НЕ нужно менять:

- ❌ Обработчик кнопки (строки 305-338) - уже правильный
- ❌ `AppConfig.useAlternativePayments` - уже правильный
- ❌ Логика проверки региона - уже правильная

---

## 🎯 ИТОГ

**Вариант Б технически очень простой:**
- Меняем только текст кнопки
- Логика проверки региона уже есть в обработчике
- Для России → сайт, для остальных → IAP

**Сложность:** ⭐ (очень просто)

---

**Дата создания:** 09.12.2025  
**Статус:** Техническая реализация Варианта Б

