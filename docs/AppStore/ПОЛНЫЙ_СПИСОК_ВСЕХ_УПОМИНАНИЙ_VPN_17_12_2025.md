# 🚨 ПОЛНЫЙ СПИСОК ВСЕХ УПОМИНАНИЙ VPN В КОДЕ

**Дата:** 17 декабря 2025  
**Статус:** 🔴 КРИТИЧНО - НУЖНО УДАЛИТЬ ВСЕ!

---

## 📊 СТАТИСТИКА

**Найдено упоминаний VPN:** 1879+ строк  
**Файлов с VPN:** 100+ файлов  
**Критичность:** 🔴 МАКСИМАЛЬНАЯ

---

## ❌ ФАЙЛЫ С VPN В НАЗВАНИИ

### 1. Screens/24_VPNEnergyStatsScreen.swift
- ❌ **Файл называется VPN**
- ❌ Структура `VPNEnergyStatsScreen`
- ❌ Использует `vpnManager`
- ❌ Ключи локализации `vpn_energy_*`

**Действие:** Переименовать в `24_NetworkProtectionEnergyStatsScreen.swift`

---

### 2. Core/VPN/NetworkProtectionManager.swift
- ❌ **Папка называется VPN**
- ❌ Путь содержит `VPN`
- ❌ Комментарии содержат упоминания VPN

**Действие:** Переместить в `Core/NetworkProtection/NetworkProtectionManager.swift`

---

## ❌ УПОМИНАНИЯ VPN В project.pbxproj

### Найдено 10+ упоминаний:

1. **Строка 80:** `24_VPNEnergyStatsScreen.swift in Sources`
2. **Строка 109:** `Core/VPN/NetworkProtectionManager.swift in Sources`
3. **Строка 275:** `24_VPNEnergyStatsScreen.swift` (fileRef)
4. **Строка 288:** `03_VPNScreen.swift` (fileRef) - **СТАРАЯ ССЫЛКА!**
5. **Строка 307:** `ViewModels/VPNViewModel.swift` (fileRef) - **УДАЛЕННЫЙ ФАЙЛ!**
6. **Строка 309:** `Core/VPN/NetworkProtectionManager.swift` (fileRef)
7. **Строка 454:** `24_VPNEnergyStatsScreen.swift` (в группе Screens)
8. **Строка 587:** `Core/VPN/NetworkProtectionManager.swift` (в группе Core)
9. **Строка 826:** `Core/VPN/NetworkProtectionManager.swift in Sources`
10. **Строка 831:** `03_VPNScreen.swift in Sources` - **СТАРАЯ ССЫЛКА!**
11. **Строка 924:** `24_VPNEnergyStatsScreen.swift in Sources`

**Действие:** Удалить все старые ссылки, обновить новые

---

## ❌ УПОМИНАНИЯ VPN В КОДЕ

### 1. Core/Config/AppConfig.swift

**Найдено:**
```swift
// VPN
static let vpnStatus = "/vpn/status"
static let vpnConnect = "/vpn/connect"
static let vpnDisconnect = "/vpn/disconnect"
static let vpnServers = "/vpn/servers"
static let isVPNEnabled = true
```

**Исправить на:**
```swift
// Network Protection
static let networkProtectionStatus = "/network-protection/status"
static let networkProtectionConnect = "/network-protection/connect"
static let networkProtectionDisconnect = "/network-protection/disconnect"
static let networkProtectionServers = "/network-protection/servers"
static let isNetworkProtectionEnabled = true
```

---

### 2. Core/Network/APIService.swift

**Найдено:**
```swift
// MARK: - VPN API

func getVPNStatus(...)
func connectVPN(...)
func disconnectVPN(...)
func getVPNServers(...)
func getVPNConfig(...)
func sendVPNStats(...)
```

**Исправить на:**
```swift
// MARK: - Network Protection API

func getNetworkProtectionStatus(...)
func connectNetworkProtection(...)
func disconnectNetworkProtection(...)
func getNetworkProtectionServers(...)
func getNetworkProtectionConfig(...)
func sendNetworkProtectionStats(...)
```

---

### 3. Core/Network/MockAPIService.swift

**Найдено:**
- Использует методы из APIService (getVPNStatus, connectVPN, disconnectVPN)

**Действие:** Обновить после исправления APIService

---

### 4. Core/Cache/CachedAPIService.swift

**Найдено:**
```swift
// MARK: - VPN API Methods

func getVPNStatus() async
func connectVPN() async
func disconnectVPN() async
```

**Исправить на:**
```swift
// MARK: - Network Protection API Methods

func getNetworkProtectionStatus() async
func connectNetworkProtection() async
func disconnectNetworkProtection() async
```

---

### 5. Core/VPN/NetworkProtectionManager.swift

**Найдено:**
- Комментарии: `// import NetworkExtension`, `// VPN`, `VPN отключен`
- Переменные: `VPNStatus`, `vpnManager`
- Методы: упоминания VPN в логах

**Действие:** 
- Переместить файл в `Core/NetworkProtection/`
- Удалить все упоминания VPN из комментариев
- Переименовать `VPNStatus` → `NetworkProtectionStatus`

---

### 6. Core/Navigation/NavigationManager.swift

**Найдено:**
```swift
case vpnEnergyStats = "24_VPNEnergyStatsScreen"
```

**Исправить на:**
```swift
case networkProtectionEnergyStats = "24_NetworkProtectionEnergyStatsScreen"
```

**Также:**
```swift
case .vpnEnergyStats: return "nav_screen_vpn_energy_stats"
```

**Исправить на:**
```swift
case .networkProtectionEnergyStats: return "nav_screen_network_protection_energy_stats"
```

---

### 7. ALADDINApp.swift

**Найдено:**
```swift
case .vpnEnergyStats:
    AnyView(VPNEnergyStatsScreen().id("vpnEnergyStats")...)
```

**Исправить на:**
```swift
case .networkProtectionEnergyStats:
    AnyView(NetworkProtectionEnergyStatsScreen().id("networkProtectionEnergyStats")...)
```

---

### 8. ViewModels/MainViewModel.swift

**Найдено:**
```swift
/// Управляет состоянием VPN, функций, статистикой
@Published var isVPNEnabled: Bool = true
func toggleVPN()
func openVPN()
private func connectVPN()
private func disconnectVPN()
```

**Исправить на:**
```swift
/// Управляет состоянием защиты сети, функций, статистикой
@Published var isNetworkProtectionEnabled: Bool = true
func toggleNetworkProtection()
func openNetworkProtection()
private func connectNetworkProtection()
private func disconnectNetworkProtection()
```

---

### 9. ViewModels/SettingsViewModel.swift

**Найдено:**
```swift
@Published var isVPNEnabled: Bool = true
func toggleVPN()
```

**Исправить на:**
```swift
@Published var isNetworkProtectionEnabled: Bool = true
func toggleNetworkProtection()
```

---

### 10. ViewModels/SupportViewModel.swift

**Найдено:**
```swift
FAQItem(icon: "🛡️", question: "Как работает VPN?", answer: "VPN шифрует весь ваш интернет-трафик и скрывает IP адрес.")
```

**Исправить на:**
```swift
FAQItem(icon: "🛡️", question: "Как работает защита сети?", answer: "Защита сети шифрует весь ваш интернет-трафик и скрывает IP адрес.")
```

---

### 11. Screens/01_MainScreen.swift

**Найдено:**
```swift
Text(localizationManager.localized("main_family_vpn_info", mainViewModel.threatsBlocked))
```

**Действие:** Обновить ключ локализации

---

### 12. Screens/03_NetworkProtectionScreen.swift

**Найдено множество ключей локализации:**
- `vpn_nav_panel`
- `vpn_cards_list`
- `vpn_screen_lang_*`
- `vpn_disconnect`
- `vpn_connect`
- `vpn_toggle_hint`
- `vpn_battery_icon`
- `vpn_battery_saving`
- `vpn_security_features`
- `vpn_ad_blocking`
- `vpn_anti_tracking`
- `vpn_threat_protection`
- `vpn_quick_actions`
- `vpn_settings`
- `vpn_statistics_title`
- `vpn_help`
- `vpn_antivirus`
- `vpn_active`
- `vpn_inactive`
- `vpn_files_scanned`
- `vpn_threats_found`
- `vpn_ago`
- `vpn_protection`
- `vpn_scanning`
- `vpn_start_scan`
- `vpn_just_now`
- `vpn_min`
- `vpn_hour`
- `vpn_never`
- `vpn_retry`
- `vpn_servers`
- `vpn_done`

**Также:**
```swift
apiService.getVPNServers { result in
    let vpnManager = NetworkProtectionManager.shared
    let vpnManagerServers = vpnManager.getAvailableServers()
    self.availableServers = vpnManagerServers.map { vpnServer in
```

**Действие:** Заменить все ключи локализации и переменные

---

### 13. Screens/24_VPNEnergyStatsScreen.swift

**Найдено:**
- Структура `VPNEnergyStatsScreen`
- `vpnManager`
- Ключи локализации `vpn_energy_*`

**Действие:** Переименовать все

---

### 14. Shared/Components/Navigation/ALADDINNavigationBar.swift

**Найдено:**
```swift
case .vpn: return "nav_screen_vpn"
case .vpnEnergyStats: return "nav_screen_vpn_energy_stats"
```

**Исправить на:**
```swift
case .vpn: return "nav_screen_network_protection"
case .networkProtectionEnergyStats: return "nav_screen_network_protection_energy_stats"
```

---

### 15. Shared/Models/AdditionalFeature.swift

**Найдено:**
```swift
id: "vpn_free",
id: "vpn_personal",
id: "vpn_family",
id: "vpn_premium",
titleKey: "tariff_additional_vpn_free",
titleKey: "tariff_additional_vpn_personal",
titleKey: "tariff_additional_vpn_family",
titleKey: "tariff_additional_vpn_premium",
// Исключаем VPN и рекламу из Free (VPN будет Premium версия, реклама не нужна)
allFeatures.append(contentsOf: freeFeatures.filter { $0.id != "vpn_free" && $0.id != "ads_free" })
// Исключаем VPN из Personal (будет Premium версия)
allFeatures.append(contentsOf: personalFeatures.filter { $0.id != "vpn_personal" })
// Исключаем VPN из Family (будет Premium версия)
allFeatures.append(contentsOf: familyFeatures.filter { $0.id != "vpn_family" })
```

**Исправить на:**
```swift
id: "network_protection_free",
id: "network_protection_personal",
id: "network_protection_family",
id: "network_protection_premium",
titleKey: "tariff_additional_network_protection_free",
titleKey: "tariff_additional_network_protection_personal",
titleKey: "tariff_additional_network_protection_family",
titleKey: "tariff_additional_network_protection_premium",
// Исключаем Network Protection и рекламу из Free
allFeatures.append(contentsOf: freeFeatures.filter { $0.id != "network_protection_free" && $0.id != "ads_free" })
// Исключаем Network Protection из Personal
allFeatures.append(contentsOf: personalFeatures.filter { $0.id != "network_protection_personal" })
// Исключаем Network Protection из Family
allFeatures.append(contentsOf: familyFeatures.filter { $0.id != "network_protection_family" })
```

---

### 16. Shared/Models/ParentalControlFeature.swift

**Найдено:**
```swift
id: "bypass_vpn_tor",
titleKey: "tariff_parental_bypass_vpn_tor_family",
```

**Исправить на:**
```swift
id: "bypass_network_protection_tor",
titleKey: "tariff_parental_bypass_network_protection_tor_family",
```

---

### 17. Shared/Models/ThreatProtectionCategory.swift

**Найдено:**
```swift
settingsScreen: NavigationManager.ALADDINScreen.vpn,
```

**Исправить на:**
```swift
settingsScreen: NavigationManager.ALADDINScreen.vpn, // Уже переименован в .vpn = "03_NetworkProtectionScreen"
```

---

### 18. Shared/Components/ALADDINToggle.swift

**Найдено:**
```swift
Text("VPN защита")
```

**Исправить на:**
```swift
Text("Защита сети")
```

---

### 19. Shared/Components/Inputs/ALADDINToggle.swift

**Найдено:**
```swift
Text("VPN защита")
```

**Исправить на:**
```swift
Text("Защита сети")
```

---

### 20. Shared/Components/Cards/StatusCard.swift

**Найдено:**
```swift
title: "VPN Статус",
print("Открыть VPN")
```

**Исправить на:**
```swift
title: "Статус защиты сети",
print("Открыть защиту сети")
```

---

### 21. Shared/Components/Buttons/PrimaryButton.swift

**Найдено:**
```swift
PrimaryButton("Подключить VPN", icon: "shield.fill") {
    print("VPN включается...")
}
```

**Исправить на:**
```swift
PrimaryButton("Подключить защиту сети", icon: "shield.fill") {
    print("Защита сети включается...")
}
```

---

### 22. Shared/Extensions/Accessibility+Extensions.swift

**Найдено:**
```swift
// VPN
static let vpnToggle = "VPN переключатель"
static let vpnStatusProtected = "VPN подключён, соединение защищено"
static let vpnStatusNotProtected = "VPN отключён, соединение не защищено"
```

**Исправить на:**
```swift
// Network Protection
static let networkProtectionToggle = "Переключатель защиты сети"
static let networkProtectionStatusProtected = "Защита сети подключена, соединение защищено"
static let networkProtectionStatusNotProtected = "Защита сети отключена, соединение не защищено"
```

---

## ❌ УПОМИНАНИЯ VPN В ЛОКАЛИЗАЦИИ

### Resources/Localization/ru.lproj/Localizable.strings

**Найдено 20+ ключей:**
- `"notification.vpn"`
- `"notification.vpn.subtitle"`
- `"notification.test.vpn"`
- `"widget.vpn.title"`
- `"widget.vpn.description"`
- `"main.vpn.title"`
- `"vpn.title"`
- `"vpn.connected"`
- `"vpn.disconnected"`
- `"vpn.protected"`
- `"vpn.not.protected"`
- `"vpn.connection.encrypted"`
- `"vpn.connection.not.encrypted"`
- `"vpn.server"`
- `"vpn.statistics"`
- `"vpn.downloaded"`
- `"vpn.uploaded"`
- `"vpn.session.time"`
- `"vpn.threats.blocked"`
- `"vpn.connect"`
- `"vpn.disconnect"`
- `"onboarding.description.2"` (содержит "VPN")
- `"energy.title"` (содержит "VPN")
- `"energy.efficient"` (содержит "VPN")
- `"energy.vpn.time"`
- `"energy.comparison"` (содержит "VPN")
- `"energy.tip.2"` (содержит "VPN")

**Действие:** Заменить все ключи на `network_protection_*`

---

### Resources/Localization/en.lproj/Localizable.strings

**Найдено 20+ ключей:**
- `"main.vpn.title"`
- `"vpn.title"`
- `"vpn.connected"`
- `"vpn.disconnected"`
- `"vpn.protected"`
- `"vpn.not.protected"`
- `"vpn.connection.encrypted"`
- `"vpn.connection.not.encrypted"`
- `"vpn.server"`
- `"vpn.statistics"`
- `"vpn.downloaded"`
- `"vpn.uploaded"`
- `"vpn.session.time"`
- `"vpn.threats.blocked"`
- `"vpn.connect"`
- `"vpn.disconnect"`
- `"onboarding.description.2"` (содержит "VPN")
- `"energy.title"` (содержит "VPN")
- `"energy.efficient"` (содержит "VPN")
- `"energy.vpn.time"`
- `"energy.comparison"` (содержит "VPN")
- `"energy.tip.2"` (содержит "VPN")

**Действие:** Заменить все ключи на `network_protection_*`

---

### Resources/Localization/zh-Hans.lproj/Localizable.strings

**Найдено 10+ ключей:**
- `"main.vpn.title"`
- `"vpn.title"`
- `"vpn.status"`
- `"vpn.connected"`
- `"vpn.disconnected"`
- `"vpn.connect"`
- `"vpn.disconnect"`
- `"vpn.server"`
- `"vpn.location"`
- `"vpn.speed"`

**Действие:** Заменить все ключи

---

### Resources/Localization/ar.lproj/Localizable.strings

**Найдено 10+ ключей:**
- `"main.vpn.title"`
- `"vpn.title"`
- `"vpn.status"`
- `"vpn.connected"`
- `"vpn.disconnected"`
- `"vpn.connect"`
- `"vpn.disconnect"`
- `"vpn.server"`
- `"vpn.location"`
- `"vpn.speed"`

**Действие:** Заменить все ключи

---

## ❌ УПОМИНАНИЯ VPN В WORKFLOW ФАЙЛАХ

### .github/workflows/check-secrets.yml

**Найдено:**
- `ALADDINPacketTunnel`
- `family.aladdin.ios.packetTunnel`
- `Personal VPN`
- `com.apple.developer.networking.vpn.api`
- `com.apple.developer.networking.networkextension`

**Действие:** Удалить или закомментировать все упоминания VPN

---

## 📋 ПОЛНЫЙ ПЛАН ИСПРАВЛЕНИЯ

### ✅ ШАГ 1: Переименовать файлы

1. **Переименовать файл:**
```bash
mv Screens/24_VPNEnergyStatsScreen.swift Screens/24_NetworkProtectionEnergyStatsScreen.swift
```

2. **Внутри файла:**
   - `struct VPNEnergyStatsScreen` → `struct NetworkProtectionEnergyStatsScreen`
   - `vpnManager` → `networkProtectionManager`
   - Все ключи локализации `vpn_energy_*` → `network_protection_energy_*`

---

### ✅ ШАГ 2: Переместить папку

```bash
mkdir -p Core/NetworkProtection
mv Core/VPN/NetworkProtectionManager.swift Core/NetworkProtection/NetworkProtectionManager.swift
rmdir Core/VPN
```

---

### ✅ ШАГ 3: Обновить project.pbxproj

**Удалить:**
- Строка 288: `03_VPNScreen.swift` (fileRef)
- Строка 307: `ViewModels/VPNViewModel.swift` (fileRef)
- Строка 831: `03_VPNScreen.swift in Sources`

**Обновить:**
- Все `24_VPNEnergyStatsScreen.swift` → `24_NetworkProtectionEnergyStatsScreen.swift`
- Все `Core/VPN/` → `Core/NetworkProtection/`

---

### ✅ ШАГ 4: Обновить код

**Файлы для обновления:**
1. `Core/Config/AppConfig.swift` - endpoints
2. `Core/Network/APIService.swift` - методы
3. `Core/Network/MockAPIService.swift` - методы
4. `Core/Cache/CachedAPIService.swift` - методы
5. `Core/NetworkProtection/NetworkProtectionManager.swift` - комментарии
6. `Core/Navigation/NavigationManager.swift` - case
7. `ALADDINApp.swift` - использование
8. `ViewModels/MainViewModel.swift` - переменные и методы
9. `ViewModels/SettingsViewModel.swift` - переменные
10. `ViewModels/SupportViewModel.swift` - FAQ
11. `Screens/01_MainScreen.swift` - локализация
12. `Screens/03_NetworkProtectionScreen.swift` - все ключи локализации
13. `Screens/24_NetworkProtectionEnergyStatsScreen.swift` - все упоминания
14. `Shared/Components/Navigation/ALADDINNavigationBar.swift` - case
15. `Shared/Models/AdditionalFeature.swift` - id и titleKey
16. `Shared/Models/ParentalControlFeature.swift` - id
17. `Shared/Components/ALADDINToggle.swift` - текст
18. `Shared/Components/Inputs/ALADDINToggle.swift` - текст
19. `Shared/Components/Cards/StatusCard.swift` - текст
20. `Shared/Components/Buttons/PrimaryButton.swift` - текст
21. `Shared/Extensions/Accessibility+Extensions.swift` - константы

---

### ✅ ШАГ 5: Обновить локализацию

**Файлы:**
- `Resources/Localization/ru.lproj/Localizable.strings`
- `Resources/Localization/en.lproj/Localizable.strings`
- `Resources/Localization/zh-Hans.lproj/Localizable.strings`
- `Resources/Localization/ar.lproj/Localizable.strings`

**Действие:** Заменить все ключи `vpn_*` на `network_protection_*`

---

### ✅ ШАГ 6: Обновить workflow

**Файл:** `.github/workflows/check-secrets.yml`

**Действие:** Удалить или закомментировать все упоминания:
- `ALADDINPacketTunnel`
- `family.aladdin.ios.packetTunnel`
- `Personal VPN`
- `com.apple.developer.networking.vpn.api`

---

## 📋 ИТОГОВЫЙ ЧЕКЛИСТ

### ✅ ФАЙЛЫ (2):
- [ ] Переименовать `24_VPNEnergyStatsScreen.swift`
- [ ] Переместить `Core/VPN/` → `Core/NetworkProtection/`

### ✅ project.pbxproj (11 упоминаний):
- [ ] Удалить ссылки на `03_VPNScreen.swift` (2 места)
- [ ] Удалить ссылки на `ViewModels/VPNViewModel.swift` (1 место)
- [ ] Обновить ссылки на `24_VPNEnergyStatsScreen.swift` (4 места)
- [ ] Обновить ссылки на `Core/VPN/` (4 места)

### ✅ КОД (21 файл):
- [ ] AppConfig.swift
- [ ] APIService.swift
- [ ] MockAPIService.swift
- [ ] CachedAPIService.swift
- [ ] NetworkProtectionManager.swift
- [ ] NavigationManager.swift
- [ ] ALADDINApp.swift
- [ ] MainViewModel.swift
- [ ] SettingsViewModel.swift
- [ ] SupportViewModel.swift
- [ ] 01_MainScreen.swift
- [ ] 03_NetworkProtectionScreen.swift
- [ ] 24_NetworkProtectionEnergyStatsScreen.swift
- [ ] ALADDINNavigationBar.swift
- [ ] AdditionalFeature.swift
- [ ] ParentalControlFeature.swift
- [ ] ThreatProtectionCategory.swift
- [ ] ALADDINToggle.swift
- [ ] Inputs/ALADDINToggle.swift
- [ ] StatusCard.swift
- [ ] PrimaryButton.swift
- [ ] Accessibility+Extensions.swift

### ✅ ЛОКАЛИЗАЦИЯ (4 файла):
- [ ] ru.lproj/Localizable.strings (20+ ключей)
- [ ] en.lproj/Localizable.strings (20+ ключей)
- [ ] zh-Hans.lproj/Localizable.strings (10+ ключей)
- [ ] ar.lproj/Localizable.strings (10+ ключей)

### ✅ WORKFLOW (1 файл):
- [ ] check-secrets.yml (удалить упоминания VPN)

---

**ИТОГО:** 39+ файлов для исправления!

---

**Дата создания:** 17 декабря 2025  
**Статус:** ✅ ПОЛНЫЙ СПИСОК СОСТАВЛЕН
