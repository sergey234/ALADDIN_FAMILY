# 📋 ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ ОТСУТСТВУЮЩИХ ФУНКЦИЙ

**Дата создания:** 9 декабря 2025  
**Статус:** План реализации и интеграции

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

### ✅ ЧТО УЖЕ ЕСТЬ

1. **IoT Security Agent (сервер)**
   - Файл: `/opt/aladdin-backend/security/ai_agents/iot_security_agent.py`
   - Функции: Защита камер умного дома (Compromised cameras)
   - Статус: ✅ Реализован на сервере

2. **IoT Security Module (iOS)**
   - Файл: `Core/IoT/IoTSecurityModule.swift`
   - Файл: `Screens/IoTSecurityScreen.swift`
   - Статус: ✅ Есть в iOS приложении

3. **Менеджер паролей (сервер)**
   - Файл: `/opt/aladdin-backend/security/ai_agents/password_security_agent.py`
   - Статус: ✅ Реализован на сервере

4. **Российские компоненты**
   - `russian_data_protection_manager.py` (152-ФЗ)
   - `russian_threat_intelligence.py`
   - База мошенников (147,000 записей)

### ❌ ЧТО ОТСУТСТВУЕТ

1. **Dark Web мониторинг** - нет на сервере
2. **Identity Theft Protection для России** - нет на сервере
3. **Интеграция IoT Security Agent с iOS** - требует проверки
4. **Интеграция менеджера паролей с iOS** - нет в приложении

---

## 🎯 ПЛАН РЕАЛИЗАЦИИ

### 1. 📹 SAFECAM (IoT ЗАЩИТА КАМЕР)

#### Текущее состояние

**Сервер:**
- ✅ `iot_security_agent.py` - защищает камеры умного дома
- ✅ Покрывает: "Compromised cameras" - защита от компрометации камер

**iOS:**
- ✅ `Core/IoT/IoTSecurityModule.swift` - есть модуль
- ✅ `Screens/IoTSecurityScreen.swift` - есть экран
- ❓ Требуется проверка интеграции с API

#### План реализации

**Этап 1: Проверка текущей интеграции** (1 день)

1. Проверить `IoTSecurityModule.swift`:
   - Есть ли API endpoints для IoT?
   - Подключен ли к серверу?
   - Работает ли запрос данных?

2. Проверить `IoTSecurityScreen.swift`:
   - Отображает ли данные с сервера?
   - Есть ли UI для защиты камер?

3. Проверить API endpoints:
   - Есть ли `/iot/status` в `AppConfig.swift`?
   - Есть ли `/iot/devices` в `AppConfig.swift`?
   - Есть ли `/iot/cameras` в `AppConfig.swift`?

**Этап 2: Добавление API endpoints** (1 день)

Если endpoints отсутствуют, добавить в `AppConfig.swift`:

```swift
enum Endpoint {
    // ... существующие endpoints
    
    // IoT Security
    static let iotStatus = "/iot/status"
    static let iotDevices = "/iot/devices"
    static let iotCameras = "/iot/cameras"
    static let iotCameraStatus = "/iot/cameras/{cameraId}/status"
    static let iotBlockCamera = "/iot/cameras/{cameraId}/block"
    static let iotUnblockCamera = "/iot/cameras/{cameraId}/unblock"
    static let iotCameraLogs = "/iot/cameras/{cameraId}/logs"
}
```

**Этап 3: Интеграция с сервером** (2 дня)

1. Добавить методы в `APIService.swift`:
   ```swift
   func getIoTStatus(completion: @escaping (Result<IoTStatusResponse, Error>) -> Void)
   func getIoTDevices(completion: @escaping (Result<[IoTDeviceResponse], Error>) -> Void)
   func getIoTCameras(completion: @escaping (Result<[IoTCameraResponse], Error>) -> Void)
   func blockIoTCamera(cameraId: String, completion: @escaping (Result<APIResponse<Bool>, Error>) -> Void)
   func unblockIoTCamera(cameraId: String, completion: @escaping (Result<APIResponse<Bool>, Error>) -> Void)
   ```

2. Создать модели в `APIModels.swift`:
   ```swift
   struct IoTStatusResponse: Codable {
       let totalDevices: Int
       let totalCameras: Int
       let compromisedCameras: Int
       let lastScan: String
   }
   
   struct IoTDeviceResponse: Codable {
       let id: String
       let name: String
       let type: String
       let status: String
       let cameras: [IoTCameraResponse]
   }
   
   struct IoTCameraResponse: Codable {
       let id: String
       let name: String
       let isCompromised: Bool
       let lastAccess: String?
       let isBlocked: Bool
   }
   ```

**Этап 4: Обновление UI** (2 дня)

1. Обновить `IoTSecurityScreen.swift`:
   - Добавить список камер
   - Добавить статус защиты каждой камеры
   - Добавить кнопки блокировки/разблокировки
   - Добавить логи доступа к камерам

2. Добавить уведомления:
   - Push-уведомление при обнаружении компрометации камеры
   - In-App уведомление при попытке доступа к камере

**Этап 5: Тестирование** (1 день)

1. Тестирование интеграции с сервером
2. Тестирование UI
3. Тестирование уведомлений

**Итого:** 7 дней

---

### 2. 🌐 DARK WEB МОНИТОРИНГ

#### Текущее состояние

**Сервер:**
- ❌ Нет прямого Dark Web мониторинга
- ✅ `threat_intelligence_agent.py` - может быть расширен
- ✅ `fraud_detection_api.py` - может быть расширен

#### План реализации

**Этап 1: Создание Dark Web мониторинг агента** (3 дня)

1. Создать файл `/opt/aladdin-backend/security/ai_agents/dark_web_monitoring_agent.py`:

```python
"""
Dark Web Monitoring Agent - Мониторинг Dark Web для ALADDIN
Проверяет утечки данных на Dark Web и отправляет алерты
"""

import asyncio
import hashlib
import requests
from typing import List, Dict, Optional
from datetime import datetime

class DarkWebMonitoringAgent:
    """
    Агент мониторинга Dark Web
    """
    
    def __init__(self):
        self.haveibeenpwned_api = "https://haveibeenpwned.com/api/v3"
        self.breachdirectory_api = "https://breachdirectory.tk/api"
        self.monitored_emails = []
        self.monitored_phones = []
        self.monitored_credit_cards = []
    
    async def check_email_breach(self, email: str) -> List[Dict]:
        """
        Проверка email на утечки через Have I Been Pwned API
        """
        try:
            # Используем k-анонимность для безопасности
            email_hash = hashlib.sha1(email.encode()).hexdigest()[:5]
            url = f"{self.haveibeenpwned_api}/range/{email_hash}"
            
            response = requests.get(url, timeout=10)
            if response.status_code == 200:
                breaches = self._parse_breaches(response.text, email)
                return breaches
        except Exception as e:
            logger.error(f"Ошибка проверки email: {e}")
        return []
    
    async def check_phone_breach(self, phone: str) -> List[Dict]:
        """
        Проверка телефона на утечки
        """
        # Интеграция с российскими базами утечек
        pass
    
    async def monitor_user_data(self, user_id: str, email: str, phone: Optional[str] = None) -> Dict:
        """
        Мониторинг данных пользователя
        """
        breaches = []
        
        # Проверка email
        email_breaches = await self.check_email_breach(email)
        breaches.extend(email_breaches)
        
        # Проверка телефона (если есть)
        if phone:
            phone_breaches = await self.check_phone_breach(phone)
            breaches.extend(phone_breaches)
        
        return {
            "user_id": user_id,
            "breaches_found": len(breaches),
            "breaches": breaches,
            "checked_at": datetime.now().isoformat()
        }
    
    async def start_monitoring(self, user_id: str, email: str, interval_hours: int = 24):
        """
        Запуск постоянного мониторинга
        """
        while True:
            result = await self.monitor_user_data(user_id, email)
            
            if result["breaches_found"] > 0:
                # Отправка алерта
                await self.send_breach_alert(result)
            
            await asyncio.sleep(interval_hours * 3600)
```

2. Интеграция с SFM:
   - Зарегистрировать агент в SFM
   - Добавить в `safe_function_manager.py`

**Этап 2: Создание API endpoints** (2 дня)

1. Добавить в `main.py` (сервер):
```python
@app.route('/api/darkweb/check', methods=['POST'])
def check_darkweb():
    """Проверка данных на Dark Web"""
    data = request.json
    email = data.get('email')
    phone = data.get('phone')
    
    agent = DarkWebMonitoringAgent()
    result = asyncio.run(agent.monitor_user_data(
        user_id=data.get('user_id'),
        email=email,
        phone=phone
    ))
    
    return jsonify(result)

@app.route('/api/darkweb/start-monitoring', methods=['POST'])
def start_darkweb_monitoring():
    """Запуск постоянного мониторинга"""
    data = request.json
    # Запуск фонового мониторинга
    return jsonify({"status": "started"})

@app.route('/api/darkweb/breaches', methods=['GET'])
def get_breaches():
    """Получить список утечек для пользователя"""
    user_id = request.args.get('user_id')
    # Получить утечки из БД
    return jsonify({"breaches": []})
```

**Этап 3: Интеграция в iOS** (3 дня)

1. Добавить endpoints в `AppConfig.swift`:
```swift
enum Endpoint {
    // Dark Web Monitoring
    static let darkWebCheck = "/darkweb/check"
    static let darkWebStartMonitoring = "/darkweb/start-monitoring"
    static let darkWebBreaches = "/darkweb/breaches"
    static let darkWebStatus = "/darkweb/status"
}
```

2. Добавить методы в `APIService.swift`:
```swift
func checkDarkWeb(email: String, phone: String?, completion: @escaping (Result<DarkWebCheckResponse, Error>) -> Void)
func startDarkWebMonitoring(email: String, phone: String?, completion: @escaping (Result<APIResponse<Bool>, Error>) -> Void)
func getDarkWebBreaches(completion: @escaping (Result<[DarkWebBreachResponse], Error>) -> Void)
```

3. Создать модели в `APIModels.swift`:
```swift
struct DarkWebCheckResponse: Codable {
    let breachesFound: Int
    let breaches: [DarkWebBreachResponse]
    let checkedAt: String
}

struct DarkWebBreachResponse: Codable {
    let breachId: String
    let breachName: String
    let breachDate: String
    let description: String
    let affectedData: [String]
    let severity: String
}
```

4. Создать экран `DarkWebMonitoringScreen.swift`:
   - Статус мониторинга
   - Список найденных утечек
   - Кнопка запуска проверки
   - Настройки мониторинга

**Этап 4: Интеграция с уведомлениями** (1 день)

1. Добавить тип уведомления "Dark Web Breach"
2. Настроить отправку push-уведомлений при обнаружении утечки

**Этап 5: Тестирование** (1 день)

1. Тестирование API
2. Тестирование iOS интеграции
3. Тестирование уведомлений

**Итого:** 10 дней

---

### 3. 🆔 IDENTITY THEFT PROTECTION ДЛЯ РОССИИ

#### Текущее состояние

**Сервер:**
- ✅ `russian_data_protection_manager.py` (152-ФЗ)
- ✅ `russian_threat_intelligence.py`
- ✅ База мошенников (147,000 записей)
- ❌ Нет мониторинга кредитных бюро
- ❌ Нет мониторинга СНИЛС

#### План реализации

**Этап 1: Исследование российских API** (2 дня)

1. Изучить API кредитных бюро:
   - НБКИ (Национальное бюро кредитных историй)
   - ОКБ (Объединенное кредитное бюро)
   - Эквифакс (российское)

2. Изучить возможности мониторинга СНИЛС:
   - ПФР (Пенсионный фонд России)
   - Госуслуги API

3. Изучить юридические требования:
   - 152-ФЗ о защите персональных данных
   - Требования к хранению данных
   - Требования к согласию пользователя

**Этап 2: Создание Identity Theft Protection агента** (5 дней)

1. Создать файл `/opt/aladdin-backend/security/ai_agents/russian_identity_theft_protection_agent.py`:

```python
"""
Russian Identity Theft Protection Agent
Защита от кражи личности для российского рынка
"""

import asyncio
from typing import Dict, List, Optional
from datetime import datetime

class RussianIdentityTheftProtectionAgent:
    """
    Агент защиты от кражи личности для России
    """
    
    def __init__(self):
        self.nbki_api = None  # НБКИ API
        self.okb_api = None   # ОКБ API
        self.equifax_api = None  # Эквифакс API
        self.monitored_snils = []
        self.monitored_credit_reports = []
    
    async def monitor_snils(self, snils: str) -> Dict:
        """
        Мониторинг СНИЛС на подозрительную активность
        """
        # Проверка через ПФР API (если доступно)
        # Проверка через Госуслуги API (если доступно)
        # Проверка базы мошенников (147,000 записей)
        pass
    
    async def monitor_credit_report(self, user_id: str) -> Dict:
        """
        Мониторинг кредитного отчета через НБКИ/ОКБ
        """
        # Запрос кредитного отчета через API
        # Проверка изменений
        # Обнаружение подозрительных кредитов
        pass
    
    async def check_fraud_database(self, snils: str, passport: str) -> List[Dict]:
        """
        Проверка в базе мошенников (147,000 записей)
        """
        # Использовать существующую базу мошенников
        # Проверка СНИЛС
        # Проверка паспортных данных
        pass
    
    async def detect_identity_theft(self, user_id: str, snils: str) -> Dict:
        """
        Обнаружение кражи личности
        """
        results = {
            "snils_check": await self.monitor_snils(snils),
            "credit_check": await self.monitor_credit_report(user_id),
            "fraud_check": await self.check_fraud_database(snils, None),
            "risk_score": 0.0,
            "alerts": []
        }
        
        # Расчет риска
        if results["snils_check"].get("suspicious_activity"):
            results["risk_score"] += 0.3
            results["alerts"].append("Обнаружена подозрительная активность СНИЛС")
        
        if results["credit_check"].get("new_credits"):
            results["risk_score"] += 0.5
            results["alerts"].append("Обнаружены новые кредиты")
        
        if results["fraud_check"]:
            results["risk_score"] += 0.7
            results["alerts"].append("СНИЛС найден в базе мошенников")
        
        return results
```

2. Интеграция с существующими компонентами:
   - Использовать `russian_data_protection_manager.py` для соответствия 152-ФЗ
   - Использовать базу мошенников (147,000 записей)
   - Использовать `russian_threat_intelligence.py`

**Этап 3: Создание API endpoints** (3 дня)

1. Добавить в `main.py` (сервер):
```python
@app.route('/api/identity-theft/monitor-snils', methods=['POST'])
def monitor_snils():
    """Мониторинг СНИЛС"""
    data = request.json
    snils = data.get('snils')
    # Проверка согласия пользователя (152-ФЗ)
    # Мониторинг СНИЛС
    return jsonify({"status": "monitoring_started"})

@app.route('/api/identity-theft/monitor-credit', methods=['POST'])
def monitor_credit():
    """Мониторинг кредитного отчета"""
    data = request.json
    user_id = data.get('user_id')
    # Интеграция с НБКИ/ОКБ
    return jsonify({"status": "monitoring_started"})

@app.route('/api/identity-theft/check', methods=['POST'])
def check_identity_theft():
    """Проверка на кражу личности"""
    data = request.json
    # Полная проверка
    return jsonify({"risk_score": 0.0, "alerts": []})

@app.route('/api/identity-theft/alerts', methods=['GET'])
def get_identity_theft_alerts():
    """Получить алерты о краже личности"""
    user_id = request.args.get('user_id')
    return jsonify({"alerts": []})
```

**Этап 4: Интеграция в iOS** (4 дня)

1. Добавить endpoints в `AppConfig.swift`:
```swift
enum Endpoint {
    // Identity Theft Protection
    static let identityTheftMonitorSNILS = "/identity-theft/monitor-snils"
    static let identityTheftMonitorCredit = "/identity-theft/monitor-credit"
    static let identityTheftCheck = "/identity-theft/check"
    static let identityTheftAlerts = "/identity-theft/alerts"
    static let identityTheftStatus = "/identity-theft/status"
}
```

2. Добавить методы в `APIService.swift`:
```swift
func monitorSNILS(snils: String, consent: Bool, completion: @escaping (Result<APIResponse<Bool>, Error>) -> Void)
func monitorCreditReport(completion: @escaping (Result<APIResponse<Bool>, Error>) -> Void)
func checkIdentityTheft(completion: @escaping (Result<IdentityTheftCheckResponse, Error>) -> Void)
func getIdentityTheftAlerts(completion: @escaping (Result<[IdentityTheftAlertResponse], Error>) -> Void)
```

3. Создать модели в `APIModels.swift`:
```swift
struct IdentityTheftCheckResponse: Codable {
    let riskScore: Double
    let alerts: [IdentityTheftAlertResponse]
    let snilsStatus: String
    let creditStatus: String
    let fraudCheckStatus: String
}

struct IdentityTheftAlertResponse: Codable {
    let alertId: String
    let type: String
    let severity: String
    let message: String
    let detectedAt: String
    let actionRequired: Bool
}
```

4. Создать экран `IdentityTheftProtectionScreen.swift`:
   - Статус мониторинга СНИЛС
   - Статус мониторинга кредитного отчета
   - Оценка риска
   - Список алертов
   - Настройки мониторинга
   - Согласие на обработку данных (152-ФЗ)

**Этап 5: Соответствие 152-ФЗ** (2 дня)

1. Добавить согласие на обработку данных:
   - СНИЛС
   - Паспортные данные
   - Кредитный отчет

2. Добавить возможность отзыва согласия

3. Добавить логирование всех операций

**Этап 6: Тестирование** (2 дня)

1. Тестирование API
2. Тестирование iOS интеграции
3. Тестирование соответствия 152-ФЗ

**Итого:** 18 дней

---

## 📊 ОБЩИЙ ПЛАН РЕАЛИЗАЦИИ

### Приоритеты

1. **SafeCam (IoT)** - 7 дней ⭐⭐⭐
   - Уже есть на сервере и в iOS
   - Требуется только интеграция

2. **Dark Web мониторинг** - 10 дней ⭐⭐⭐
   - Критично для безопасности
   - Нет на сервере, нужно создать

3. **Identity Theft Protection** - 18 дней ⭐⭐
   - Важно для российского рынка
   - Требует интеграции с российскими API

### Временная шкала

**Неделя 1-2: SafeCam (IoT)**
- Дни 1-2: Проверка и добавление API endpoints
- Дни 3-4: Интеграция с сервером
- Дни 5-6: Обновление UI
- День 7: Тестирование

**Неделя 3-4: Dark Web мониторинг**
- Дни 1-3: Создание агента на сервере
- Дни 4-5: Создание API endpoints
- Дни 6-8: Интеграция в iOS
- День 9: Интеграция с уведомлениями
- День 10: Тестирование

**Неделя 5-7: Identity Theft Protection**
- Дни 1-2: Исследование российских API
- Дни 3-7: Создание агента
- Дни 8-10: Создание API endpoints
- Дни 11-14: Интеграция в iOS
- Дни 15-16: Соответствие 152-ФЗ
- Дни 17-18: Тестирование

**Итого:** 35 дней (~5 недель)

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Архитектура интеграции

```
iOS Приложение
    ↓
APIService.swift
    ↓
NetworkManager.swift
    ↓
HTTPS → Сервер (aladdin-ai.ru/api)
    ↓
main.py (Flask)
    ↓
AI Agents (Python)
    ↓
SFM (Safe Function Manager)
```

### Безопасность

1. **Шифрование данных:**
   - СНИЛС - только в зашифрованном виде
   - Email - хеширование для Dark Web проверки
   - Кредитные данные - шифрование AES-256

2. **Соответствие 152-ФЗ:**
   - Согласие на обработку данных
   - Возможность отзыва согласия
   - Логирование всех операций
   - Минимизация данных

3. **API Security:**
   - JWT токены
   - Rate limiting
   - SSL Pinning (iOS)
   - Валидация данных

---

## 📝 ЧЕКЛИСТ РЕАЛИЗАЦИИ

### SafeCam (IoT)

- [ ] Проверить текущую интеграцию IoT
- [ ] Добавить API endpoints в AppConfig.swift
- [ ] Добавить методы в APIService.swift
- [ ] Создать модели в APIModels.swift
- [ ] Обновить IoTSecurityScreen.swift
- [ ] Добавить уведомления
- [ ] Тестирование

### Dark Web мониторинг

- [ ] Создать dark_web_monitoring_agent.py
- [ ] Интегрировать с Have I Been Pwned API
- [ ] Интегрировать с BreachDirectory API
- [ ] Добавить API endpoints на сервере
- [ ] Добавить endpoints в AppConfig.swift
- [ ] Добавить методы в APIService.swift
- [ ] Создать модели в APIModels.swift
- [ ] Создать DarkWebMonitoringScreen.swift
- [ ] Добавить уведомления
- [ ] Тестирование

### Identity Theft Protection

- [ ] Исследовать НБКИ/ОКБ API
- [ ] Создать russian_identity_theft_protection_agent.py
- [ ] Интегрировать с базой мошенников
- [ ] Добавить API endpoints на сервере
- [ ] Добавить endpoints в AppConfig.swift
- [ ] Добавить методы в APIService.swift
- [ ] Создать модели в APIModels.swift
- [ ] Создать IdentityTheftProtectionScreen.swift
- [ ] Добавить согласие на обработку данных (152-ФЗ)
- [ ] Тестирование

---

**Дата создания:** 9 декабря 2025  
**Статус:** ✅ План готов к реализации
