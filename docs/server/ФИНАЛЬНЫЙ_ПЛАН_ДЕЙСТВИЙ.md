# ✅ ФИНАЛЬНЫЙ ПЛАН ДЕЙСТВИЙ: Деплой исправлений JWT 401

**Дата:** 2026-03-17  
**Статус:** 🚀 **ГОТОВО К ВЫПОЛНЕНИЮ**

---

## 📋 TODO СПИСОК ВЫПОЛНЕНИЯ

### ✅ **ШАГ 1: ПРОВЕРКА ТЕКУЩЕГО СОСТОЯНИЯ НА СЕРВЕРЕ**

**Задача:** Проверить, есть ли уже исправления на сервере

**Команда:**
```bash
ssh root@149.154.65.180 "cd /opt/aladdin-backend && \
grep -q 'aladdin-super-secret-key-change-in-production' app/auth/auth.py && echo '✅ auth.py: JWT_SECRET найден' || echo '❌ auth.py: JWT_SECRET НЕ найден' && \
grep -q 'leeway=60' app/auth/auth.py && echo '✅ auth.py: leeway найден' || echo '❌ auth.py: leeway НЕ найден' && \
grep -q 'aladdin-super-secret-key-change-in-production' backend/app/services/jwt_service.py && echo '✅ jwt_service.py: SECRET_KEY найден' || echo '❌ jwt_service.py: SECRET_KEY НЕ найден' && \
grep -q 'aladdin-super-secret-key-change-in-production' app/auth/__init__.py && echo '✅ __init__.py: JWT_SECRET найден' || echo '❌ __init__.py: JWT_SECRET НЕ найден' && \
grep -q 'from app.auth.auth import get_current_user' app/routers/analytics_router.py && echo '✅ analytics_router.py: правильный импорт найден' || echo '❌ analytics_router.py: правильный импорт НЕ найден'"
```

**Результат:**
- Если все ✅ - исправления уже на сервере, переходим к тестированию
- Если есть ❌ - переходим к деплою

---

### ✅ **ШАГ 2: СОЗДАНИЕ BACKUP**

**Задача:** Создать backup текущих файлов на сервере

**Команда:**
```bash
ssh root@149.154.65.180 "cd /opt/aladdin-backend && \
BACKUP_DIR=backup_jwt_fix_\$(date +%Y%m%d_%H%M%S) && \
mkdir -p \$BACKUP_DIR && \
cp app/auth/auth.py \$BACKUP_DIR/auth.py.backup && \
cp backend/app/services/jwt_service.py \$BACKUP_DIR/jwt_service.py.backup && \
cp app/auth/__init__.py \$BACKUP_DIR/__init__.py.backup && \
cp app/routers/analytics_router.py \$BACKUP_DIR/analytics_router.py.backup && \
echo \"✅ Backup создан в: \$BACKUP_DIR\""
```

---

### ✅ **ШАГ 3: ДЕПЛОЙ 4 ФАЙЛОВ**

**Задача:** Задеплоить исправленные файлы на сервер

**Вариант А: Автоматический деплой (скрипт)**
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
./docs/server/deploy_jwt_fix.sh
```

**Вариант Б: Ручной деплой**
```bash
# Настройка
SERVER="root@149.154.65.180"
SERVER_PATH="/opt/aladdin-backend"
LOCAL_PATH="/Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS"

# Деплой файлов
scp ${LOCAL_PATH}/app/auth/auth.py ${SERVER}:${SERVER_PATH}/app/auth/auth.py
scp ${LOCAL_PATH}/backend/app/services/jwt_service.py ${SERVER}:${SERVER_PATH}/backend/app/services/jwt_service.py
scp ${LOCAL_PATH}/app/auth/__init__.py ${SERVER}:${SERVER_PATH}/app/auth/__init__.py
scp ${LOCAL_PATH}/app/routers/analytics_router.py ${SERVER}:${SERVER_PATH}/app/routers/analytics_router.py
```

**Файлы для деплоя:**
1. ✅ `app/auth/auth.py` - JWT_SECRET, leeway, логирование
2. ✅ `backend/app/services/jwt_service.py` - SECRET_KEY, import os, leeway, логирование
3. ✅ `app/auth/__init__.py` - JWT_SECRET
4. ✅ `app/routers/analytics_router.py` - удален fallback

---

### ✅ **ШАГ 4: ПЕРЕЗАПУСК СЕРВЕРА**

**Задача:** Перезапустить FastAPI приложение

**Команда (выбрать подходящий):**
```bash
# Вариант 1: systemd
ssh root@149.154.65.180 "sudo systemctl restart aladdin-api && sudo systemctl status aladdin-api --no-pager"

# Вариант 2: pm2
ssh root@149.154.65.180 "pm2 restart aladdin-api && pm2 status aladdin-api"

# Вариант 3: supervisor
ssh root@149.154.65.180 "supervisorctl restart aladdin-api && supervisorctl status aladdin-api"
```

---

### ✅ **ШАГ 5: ТЕСТИРОВАНИЕ ПОСЛЕ ДЕПЛОЯ**

**Задача:** Протестировать все 75 защищенных эндпоинтов

**Команда:**
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
python3 docs/server/test_protected_endpoints_jwt_fix.py
```

**Ожидаемый результат:**
- ✅ Успешно: ~73 эндпоинта (96%)
- ⚠️ Валидация (422): ~12 эндпоинтов (16%)
- 🔴 401 ошибка: ~0 эндпоинтов (0%)
- ❓ Другие ошибки: ~3 эндпоинта (4%)

---

### ✅ **ШАГ 6: ПРОВЕРКА ЛОГОВ СЕРВЕРА**

**Задача:** Проверить логи на наличие ошибок 401

**Команда:**
```bash
ssh root@149.154.65.180 "tail -100 /var/log/aladdin-api.log | grep -i 'jwt-003\|401\|unauthorized'"
```

**Что искать:**
- ✅ Успешное декодирование: `✅ [JWT-003] decode_token: Успешно декодирован токен`
- ❌ Ошибки: `❌ [JWT-003] decode_token: Invalid token`

---

### ✅ **ШАГ 7: СОЗДАНИЕ ФИНАЛЬНОГО ОТЧЕТА**

**Задача:** Создать отчет о результатах деплоя

**Содержание:**
- Статус деплоя каждого файла
- Результаты тестирования
- Количество успешных/неуспешных эндпоинтов
- Рекомендации

---

## 📊 ПРОГРЕСС ВЫПОЛНЕНИЯ

- [x] TODO список создан
- [x] Скрипт деплоя создан: `docs/server/deploy_jwt_fix.sh`
- [x] Инструкция создана: `docs/server/ДЕПЛОЙ_ИНСТРУКЦИЯ.md`
- [ ] Шаг 1: Проверка на сервере
- [ ] Шаг 2: Создание backup
- [ ] Шаг 3: Деплой 4 файлов
- [ ] Шаг 4: Перезапуск сервера
- [ ] Шаг 5: Тестирование
- [ ] Шаг 6: Проверка логов
- [ ] Шаг 7: Финальный отчет

---

## 🎯 ИТОГОВАЯ ИНФОРМАЦИЯ

### **4 файла для деплоя:**
1. `app/auth/auth.py`
2. `backend/app/services/jwt_service.py`
3. `app/auth/__init__.py`
4. `app/routers/analytics_router.py`

### **25 технических эндпоинтов:**
- Components (6)
- Analytics (3)
- Protection (8)
- Family (1)
- Referral (4)
- Referral Test (3)

### **Всего защищенных эндпоинтов: 76**
- Бизнес-функции: 51
- Технические: 25

---

## 🚀 ГОТОВО К ВЫПОЛНЕНИЮ!

**Следующий шаг:** Выполнить Шаг 1 - проверка на сервере

**Статус:** ✅ **ВСЕ ПОДГОТОВЛЕНО**
