# 📋 ДЕТАЛЬНЫЙ ПЛАН ДЕЙСТВИЙ: Что нужно задеплоить

**Дата:** 2026-03-17  
**Цель:** Точный список изменений и план деплоя

---

## 🔍 ТОЧНЫЙ СПИСОК 24 ТЕХНИЧЕСКИХ ЭНДПОИНТОВ

### **Найдено через поиск в коде: 25 эндпоинтов**

**Категории:**

#### **1. Components (6 эндпоинтов):**
1. `GET /api/components/status/{component_id}`
2. `POST /api/components/enable/{component_id}`
3. `POST /api/components/disable/{component_id}`
4. `GET /api/components/configuration/{component_id}`
5. `POST /api/components/configuration/{component_id}`
6. `POST /api/components/batch/status`

#### **2. Analytics (3 эндпоинта):**
7. `GET /api/analytics`
8. `GET /api/analytics/threats`
9. `GET /api/analytics/top-threats`

#### **3. Protection (8 эндпоинтов):**
10. `GET /api/protection/status`
11. `GET /api/protection/settings`
12. `GET /api/protection/stats`
13. `GET /api/protection/threat-scenarios`
14. `POST /api/protection/enable`
15. `POST /api/protection/disable`
16. `POST /api/protection/settings`
17. `POST /api/protection/sync`

#### **4. Family (1 эндпоинт):**
18. `GET /api/family/stats`

#### **5. Referral (4 эндпоинта):**
19. `GET /code`
20. `GET /stats`
21. `GET /history`
22. `GET /rewards`

#### **6. Referral Test (3 эндпоинта):**
23. `GET /api/referral/test/discount/apply`
24. `POST /api/referral/test/payment/create`
25. `POST /api/referral/test/payment/confirm`

**Итого: 6+3+8+1+4+3 = 25 эндпоинтов**

**Примечание:** В тестах показано 24, возможно один дубликат или не протестирован.

---

## ✅ ПРОВЕРКА ИЗМЕНЕНИЙ В ФАЙЛАХ

### **1. app/auth/auth.py**

**Что изменено:**
```python
# БЫЛО:
JWT_SECRET = os.getenv("JWT_SECRET", "your-secret-key-change-in-production")

# СТАЛО:
JWT_SECRET = os.getenv("JWT_SECRET", "aladdin-super-secret-key-change-in-production")
```

**Добавлено:**
- ✅ `leeway=60` в `jwt.decode()`
- ✅ Детальное логирование (JWT-003)

**Проверка:**
```bash
grep "aladdin-super-secret-key-change-in-production" app/auth/auth.py
# ✅ Должно быть: JWT_SECRET = os.getenv("JWT_SECRET", "aladdin-super-secret-key-change-in-production")

grep "leeway=60" app/auth/auth.py
# ✅ Должно быть: leeway=60
```

---

### **2. backend/app/services/jwt_service.py**

**Что изменено:**
```python
# БЫЛО:
SECRET_KEY = "aladdin-super-secret-key-change-in-production"  # (возможно был другой)

# СТАЛО:
SECRET_KEY = os.getenv("JWT_SECRET", "aladdin-super-secret-key-change-in-production")
```

**Добавлено:**
- ✅ `import os`
- ✅ `leeway=60` в `jwt.decode()`
- ✅ Детальное логирование (JWT-003)

**Проверка:**
```bash
grep "aladdin-super-secret-key-change-in-production" backend/app/services/jwt_service.py
# ✅ Должно быть: SECRET_KEY = os.getenv("JWT_SECRET", "aladdin-super-secret-key-change-in-production")

grep "leeway=60" backend/app/services/jwt_service.py
# ✅ Должно быть: leeway=60
```

---

### **3. app/auth/__init__.py**

**Что изменено:**
```python
# БЫЛО:
JWT_SECRET = os.getenv("JWT_SECRET", "your-secret-key-change-in-production")

# СТАЛО:
JWT_SECRET = os.getenv("JWT_SECRET", "aladdin-super-secret-key-change-in-production")
```

**Проверка:**
```bash
grep "aladdin-super-secret-key-change-in-production" app/auth/__init__.py
# ✅ Должно быть: JWT_SECRET = os.getenv("JWT_SECRET", "aladdin-super-secret-key-change-in-production")
```

---

### **4. app/routers/analytics_router.py**

**Что изменено:**
```python
# БЫЛО:
try:
    from app.auth.auth import get_current_user
except ImportError:
    # Fallback для случаев когда auth не найден
    from fastapi.security import HTTPBearer
    security = HTTPBearer()
    
    async def get_current_user(credentials = Depends(security)) -> dict:
        """Fallback функция для авторизации"""
        # В реальной реализации здесь должна быть проверка JWT
        return {"id": "default_user", "sub": "default_user"}

# СТАЛО:
from app.auth.auth import get_current_user
```

**Проверка:**
```bash
grep "from app.auth.auth import get_current_user" app/routers/analytics_router.py
# ✅ Должно быть: from app.auth.auth import get_current_user

grep "except ImportError:" app/routers/analytics_router.py
# ❌ НЕ должно быть fallback функции
```

---

## 🚀 ПЛАН ДЕПЛОЯ

### **ШАГ 1: Проверка текущего состояния на сервере**

**Проверить на сервере:**
```bash
# 1. Проверить app/auth/auth.py
grep "JWT_SECRET" /opt/aladdin-backend/app/auth/auth.py
# Должно быть: "aladdin-super-secret-key-change-in-production"

# 2. Проверить backend/app/services/jwt_service.py
grep "SECRET_KEY" /opt/aladdin-backend/backend/app/services/jwt_service.py
# Должно быть: "aladdin-super-secret-key-change-in-production"

# 3. Проверить app/auth/__init__.py
grep "JWT_SECRET" /opt/aladdin-backend/app/auth/__init__.py
# Должно быть: "aladdin-super-secret-key-change-in-production"

# 4. Проверить app/routers/analytics_router.py
grep "from app.auth.auth import get_current_user" /opt/aladdin-backend/app/routers/analytics_router.py
# Должно быть: from app.auth.auth import get_current_user

# 5. Проверить leeway
grep "leeway=60" /opt/aladdin-backend/app/auth/auth.py
# Должно быть: leeway=60
```

---

### **ШАГ 2: Если изменения НЕ на сервере - задеплоить**

**Файлы для деплоя (4 файла):**

1. **app/auth/auth.py**
   - Унифицированный JWT_SECRET
   - Leeway=60
   - Детальное логирование

2. **backend/app/services/jwt_service.py**
   - Унифицированный SECRET_KEY
   - import os
   - Leeway=60
   - Детальное логирование

3. **app/auth/__init__.py**
   - Унифицированный JWT_SECRET

4. **app/routers/analytics_router.py**
   - Удален fallback
   - Используется правильный get_current_user

---

### **ШАГ 3: После деплоя - перезапустить сервер**

```bash
# Перезапустить FastAPI приложение
sudo systemctl restart aladdin-api
# или
pm2 restart aladdin-api
# или
supervisorctl restart aladdin-api
```

---

### **ШАГ 4: Проверка после деплоя**

**Запустить тест:**
```bash
python3 docs/server/test_protected_endpoints_jwt_fix.py
```

**Ожидаемый результат:**
- ✅ Успешно: ~72 эндпоинта (96%)
- 🔴 401 ошибка: ~0 эндпоинтов (0%)

---

## 📊 ИТОГОВАЯ СТАТИСТИКА

### **24 технических эндпоинта (не в документации):**

**Разбивка:**
- Components: 6
- Analytics: 3
- Protection: 8
- Family: 1
- Referral: 4
- Referral Test: 3

**Итого: 6+3+8+1+4+3 = 25** (в поиске найдено 25, в тестах 24 - возможно один дубликат)

### **Всего защищенных эндпоинтов:**
- Бизнес-функции (документация): 51
- Технические (дополнительно): 24-25
- **ВСЕГО: 75-76**

---

## ✅ ЧЕКЛИСТ ДЕПЛОЯ

### **Перед деплоем:**
- [ ] Проверить текущее состояние на сервере
- [ ] Убедиться, что все 4 файла изменены локально
- [ ] Создать backup текущих файлов на сервере

### **Деплой:**
- [ ] Задеплоить `app/auth/auth.py`
- [ ] Задеплоить `backend/app/services/jwt_service.py`
- [ ] Задеплоить `app/auth/__init__.py`
- [ ] Задеплоить `app/routers/analytics_router.py`

### **После деплоя:**
- [ ] Перезапустить сервер
- [ ] Запустить тест всех эндпоинтов
- [ ] Проверить логи сервера
- [ ] Убедиться, что 401 ошибок нет

---

## ✅ ЗАКЛЮЧЕНИЕ

**24 технических эндпоинта:**
- Components (6)
- Analytics (3)
- Protection (8)
- Family (1)
- Referral (4)
- Referral Test (3)

**4 файла для деплоя:**
1. `app/auth/auth.py`
2. `backend/app/services/jwt_service.py`
3. `app/auth/__init__.py`
4. `app/routers/analytics_router.py`

**Статус:** ✅ Готово к деплою
