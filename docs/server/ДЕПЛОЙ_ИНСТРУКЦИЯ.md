# 🚀 ИНСТРУКЦИЯ ПО ДЕПЛОЮ: Исправление JWT 401

**Дата:** 2026-03-17  
**Статус:** ✅ **ГОТОВО К ВЫПОЛНЕНИЮ**

---

## 📋 TODO СПИСОК ВЫПОЛНЕНИЯ

### ✅ **ШАГ 1: ПРОВЕРКА ТЕКУЩЕГО СОСТОЯНИЯ НА СЕРВЕРЕ**

**Задача:** Проверить, есть ли уже исправления на сервере

**Команда для выполнения на сервере:**
```bash
ssh root@149.154.65.180 "cd /opt/aladdin-backend && \
echo '=== Проверка app/auth/auth.py ===' && \
grep 'aladdin-super-secret-key-change-in-production' app/auth/auth.py && echo '✅ JWT_SECRET найден' || echo '❌ JWT_SECRET НЕ найден' && \
grep 'leeway=60' app/auth/auth.py && echo '✅ leeway найден' || echo '❌ leeway НЕ найден' && \
echo && \
echo '=== Проверка backend/app/services/jwt_service.py ===' && \
grep 'aladdin-super-secret-key-change-in-production' backend/app/services/jwt_service.py && echo '✅ SECRET_KEY найден' || echo '❌ SECRET_KEY НЕ найден' && \
grep 'leeway=60' backend/app/services/jwt_service.py && echo '✅ leeway найден' || echo '❌ leeway НЕ найден' && \
echo && \
echo '=== Проверка app/auth/__init__.py ===' && \
grep 'aladdin-super-secret-key-change-in-production' app/auth/__init__.py && echo '✅ JWT_SECRET найден' || echo '❌ JWT_SECRET НЕ найден' && \
echo && \
echo '=== Проверка app/routers/analytics_router.py ===' && \
grep 'from app.auth.auth import get_current_user' app/routers/analytics_router.py && echo '✅ Правильный импорт найден' || echo '❌ Правильный импорт НЕ найден'"
```

**Результат:**
- Если все ✅ - исправления уже на сервере, переходим к тестированию
- Если есть ❌ - переходим к деплою

---

### ✅ **ШАГ 2: ДЕПЛОЙ ФАЙЛОВ**

**Вариант А: Автоматический деплой (скрипт)**

```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
./docs/server/deploy_jwt_fix.sh
```

**Вариант Б: Ручной деплой**

```bash
# Настройка
SERVER_USER="root"
SERVER_HOST="149.154.65.180"
SERVER_PATH="/opt/aladdin-backend"
LOCAL_PATH="/Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS"

# Создание backup
ssh ${SERVER_USER}@${SERVER_HOST} "mkdir -p /opt/aladdin-backend/backup_jwt_fix_\$(date +%Y%m%d_%H%M%S)"
BACKUP_DIR=$(ssh ${SERVER_USER}@${SERVER_HOST} "ls -td /opt/aladdin-backend/backup_jwt_fix_* | head -1")

# Деплой файлов
scp ${LOCAL_PATH}/app/auth/auth.py ${SERVER_USER}@${SERVER_HOST}:${SERVER_PATH}/app/auth/auth.py
scp ${LOCAL_PATH}/backend/app/services/jwt_service.py ${SERVER_USER}@${SERVER_HOST}:${SERVER_PATH}/backend/app/services/jwt_service.py
scp ${LOCAL_PATH}/app/auth/__init__.py ${SERVER_USER}@${SERVER_HOST}:${SERVER_PATH}/app/auth/__init__.py
scp ${LOCAL_PATH}/app/routers/analytics_router.py ${SERVER_USER}@${SERVER_HOST}:${SERVER_PATH}/app/routers/analytics_router.py
```

---

### ✅ **ШАГ 3: ПЕРЕЗАПУСК СЕРВЕРА**

**Выбрать метод перезапуска:**

```bash
# Вариант 1: systemd
ssh root@149.154.65.180 "sudo systemctl restart aladdin-api && sudo systemctl status aladdin-api"

# Вариант 2: pm2
ssh root@149.154.65.180 "pm2 restart aladdin-api && pm2 status aladdin-api"

# Вариант 3: supervisor
ssh root@149.154.65.180 "supervisorctl restart aladdin-api && supervisorctl status aladdin-api"
```

---

### ✅ **ШАГ 4: ТЕСТИРОВАНИЕ ПОСЛЕ ДЕПЛОЯ**

```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
python3 docs/server/test_protected_endpoints_jwt_fix.py
```

**Ожидаемый результат:**
- ✅ Успешно: ~73 эндпоинта (96%)
- 🔴 401 ошибка: ~0 эндпоинтов (0%)

---

### ✅ **ШАГ 5: ПРОВЕРКА ЛОГОВ**

```bash
ssh root@149.154.65.180 "tail -100 /var/log/aladdin-api.log | grep -i 'jwt-003\|401\|unauthorized'"
```

---

## 📊 ПРОГРЕСС

- [x] TODO список создан
- [ ] Шаг 1: Проверка на сервере
- [ ] Шаг 2: Деплой файлов
- [ ] Шаг 3: Перезапуск сервера
- [ ] Шаг 4: Тестирование
- [ ] Шаг 5: Проверка логов

---

**Статус:** 🚀 **ГОТОВО К ВЫПОЛНЕНИЮ**
