# 📋 ЭТАП 2.1: ПЛАН РЕАЛИЗАЦИИ - crash_detection_agent

**Дата:** 2026-03-14  
**Статус:** 🟡 В РАБОТЕ  
**Компонент:** `crash_detection_agent`

---

## 🎯 ЦЕЛИ

1. ✅ Удалить in-memory хранилище (`crash_detection_sessions: Dict`)
2. ✅ Использовать общий роутер `/api/components/status/crash_detection_agent` для статусов
3. ✅ Создать таблицу `crash_detection_alerts` в PostgreSQL
4. ✅ Обновить роутер для сохранения алертов в БД
5. ✅ Обновить ViewModel (если нужно)

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

### **Роутер:** `app/security/api/routers/crash_detection_router.py`

**Проблемы:**
- ❌ Использует in-memory хранилище: `crash_detection_sessions: Dict[str, Dict[str, Any]] = {}`
- ❌ Данные теряются при перезапуске сервера
- ❌ Нет истории алертов
- ❌ Нет подключения к PostgreSQL

**Endpoints:**
- `POST /api/crash-detection/setup` - настройка мониторинга
- `POST /api/crash-detection/start` - запуск мониторинга
- `POST /api/crash-detection/stop` - остановка мониторинга
- `POST /api/crash-detection/data` - отправка данных сенсоров
- `GET /api/crash-detection/status` - статус мониторинга
- `POST /api/crash-detection/alert` - отправка алерта (нужно добавить сохранение в БД)

---

## 📝 ПЛАН ДЕЙСТВИЙ

### **ШАГ 1: Создать SQL миграцию**

✅ **Создан файл:** `docs/server/migrations/create_crash_detection_alerts_table.sql`

**Что делает:**
- Создает таблицу `crash_detection_alerts`
- Добавляет индексы для оптимизации
- Добавляет триггер для `updated_at`
- Добавляет комментарии

---

### **ШАГ 2: Применить миграцию на сервере**

**Команды для выполнения на сервере:**

```bash
# 1. Подключиться к серверу
ssh root@149.154.65.180

# 2. Перейти в директорию проекта
cd /opt/aladdin-backend

# 3. Создать backup БД (ВАЖНО!)
pg_dump -U postgres -d aladdin_db > backup_$(date +%Y%m%d_%H%M%S).sql

# 4. Применить миграцию
psql -U postgres -d aladdin_db -f docs/server/migrations/create_crash_detection_alerts_table.sql

# 5. Проверить создание таблицы
psql -U postgres -d aladdin_db -c "\d crash_detection_alerts"

# 6. Проверить индексы
psql -U postgres -d aladdin_db -c "\di crash_detection_alerts*"
```

---

### **ШАГ 3: Обновить роутер для использования PostgreSQL**

**Файл:** `app/security/api/routers/crash_detection_router.py`

**Что нужно сделать:**

1. **Добавить импорты:**
```python
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, insert, update
from app.database import get_session
from app.models import CrashDetectionAlert  # Нужно создать модель
```

2. **Удалить in-memory хранилище:**
```python
# УДАЛИТЬ:
crash_detection_sessions: Dict[str, Dict[str, Any]] = {}
_active_session_ids: Set[str] = set()
_inactive_session_ids: Set[str] = set()
```

3. **Добавить endpoint для сохранения алертов в БД:**
```python
@router.post("/api/crash-detection/alert")
async def create_crash_alert(
    request: CrashAlertRequest,
    db: AsyncSession = Depends(get_session),
    current_user: dict = Depends(get_current_user)
):
    """Создать алерт о ДТП и сохранить в БД"""
    # Сохранить в таблицу crash_detection_alerts
    # ...
```

4. **Обновить endpoint `/api/crash-detection/data` для сохранения алертов:**
```python
@router.post("/api/crash-detection/data")
async def send_crash_detection_data(
    request: SensorDataRequest,
    db: AsyncSession = Depends(get_session),
    current_user: dict = Depends(get_current_user)
):
    """Отправить данные сенсоров и сохранить алерт в БД при обнаружении ДТП"""
    # Если crash_detected == True, сохранить в БД
    # ...
```

5. **Обновить endpoint `/api/crash-detection/status` для использования БД:**
```python
@router.get("/api/crash-detection/status")
async def get_crash_detection_status(
    db: AsyncSession = Depends(get_session),
    current_user: dict = Depends(get_current_user)
):
    """Получить статус из БД вместо in-memory"""
    # Загрузить активные сессии из БД
    # ...
```

---

### **ШАГ 4: Создать модель SQLAlchemy**

**Файл:** `app/models.py` (добавить)

```python
from sqlalchemy import Column, String, Numeric, Boolean, DateTime, JSON
from sqlalchemy.dialects.postgresql import UUID
import uuid
from datetime import datetime

class CrashDetectionAlert(Base):
    __tablename__ = "crash_detection_alerts"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    user_id = Column(UUID(as_uuid=True), nullable=False)
    session_id = Column(String(255), nullable=False)
    latitude = Column(Numeric(10, 8), nullable=False)
    longitude = Column(Numeric(11, 8), nullable=False)
    severity = Column(String(20), nullable=False)
    accelerometer_data = Column(JSON)
    gyroscope_data = Column(JSON)
    speed = Column(Numeric(10, 2))
    g_force = Column(Numeric(10, 2))
    crash_detected = Column(Boolean, nullable=False, default=False)
    timestamp = Column(DateTime, nullable=False, default=datetime.utcnow)
    created_at = Column(DateTime, nullable=False, default=datetime.utcnow)
    updated_at = Column(DateTime, nullable=False, default=datetime.utcnow, onupdate=datetime.utcnow)
```

---

### **ШАГ 5: Обновить ViewModel (если нужно)**

**Файл:** `ViewModels/NetworkProtectionViewModel.swift`

**Проверка:**
- ✅ Уже использует общий роутер через `ComponentStatusService`
- ✅ Уже имеет обработку ошибок

**Что может понадобиться:**
- Добавить метод для получения истории алертов (если нужно)

---

## ✅ КРИТЕРИИ УСПЕХА

- [ ] Таблица `crash_detection_alerts` создана в БД
- [ ] Индексы созданы
- [ ] Роутер обновлен для использования PostgreSQL
- [ ] In-memory хранилище удалено
- [ ] Алерты сохраняются в БД
- [ ] Endpoints работают корректно
- [ ] Тесты пройдены

---

## 📝 ЗАМЕТКИ

1. **Важно:** Создать backup БД перед применением миграции
2. **Важно:** Проверить работу endpoints после обновления
3. **Важно:** Убедиться, что старые данные не потеряны (если есть)

---

**Следующий шаг:** Применить миграцию на сервере и обновить роутер
