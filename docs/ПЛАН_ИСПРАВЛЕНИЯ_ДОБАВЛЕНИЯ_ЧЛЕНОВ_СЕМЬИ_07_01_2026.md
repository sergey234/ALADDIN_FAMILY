# ПЛАН ИСПРАВЛЕНИЯ ПРОБЛЕМ С ДОБАВЛЕНИЕМ/УДАЛЕНИЕМ ЧЛЕНОВ СЕМЬИ

**Дата создания:** 7 января 2026  
**Статус:** В работе

---

## АНАЛИЗ ПРОБЛЕМ

### ПРОБЛЕМА #1: Отсутствие локализации для "выберете возраст"
**Описание:**  
При выборе роли (Родитель/Ребенок/Подросток/60+) показывается текст "выберете возраст" без локализации, и внутри карточек отображается ключ локализации `age_group_adult_short` вместо переведенного текста "Выбор возраста".

**Файлы:**
- `Screens/MainScreenWithRegistration.swift` (строка 79)
- `Core/Localization/LocalizationManager.swift` (проверить наличие ключа `registration_select_age`)

**Решение:**
1. Проверить наличие ключа `registration_select_age` в LocalizationManager
2. Исправить отображение текста в модале выбора возраста - использовать локализованный ключ
3. Исправить отображение внутри карточек - показывать локализованный текст вместо ключа

---

### ПРОБЛЕМА #2: Отсутствие навигации после добавления члена семьи
**Описание:**  
После добавления ребенка/60+ на iPad (и возможно iPhone) не происходит переход на соответствующий экран (детский/взрослый интерфейс). Остается на том же экране с кнопкой "Отмена".

**Файлы:**
- `Screens/MainScreenWithRegistration.swift`
- `Shared/Components/Modals/AddMemberOptionsModal.swift` (строки 210-217)
- `Screens/02_FamilyScreen.swift` (функция `navigateToMemberScreen`)

**Решение:**
1. Проверить логику навигации в `AddMemberOptionsModal.onComplete`
2. Убедиться, что после создания семьи происходит правильная навигация по роли
3. Добавить навигацию для iPad и iPhone после успешного добавления члена семьи
4. Проверить работу функции `navigateToMemberScreen` в FamilyScreen

---

### ПРОБЛЕМА #3: Кнопка "Назад" не работает
**Описание:**  
На странице "Создать новую семью" (QR код и ввести код вручную) кнопка "Назад" не работает после добавления. Также нужно проверить работу кнопки "Назад" на всех этапах на iPhone и iPad.

**Файлы:**
- `Screens/MainScreenWithRegistration.swift` (кнопка "Отмена" в строке 36)
- `Shared/Components/Modals/AddMemberOptionsModal.swift`
- `Screens/AddMemberOptionsScreen.swift` (если существует)

**Решение:**
1. Проверить обработку кнопки "Назад" в `MainScreenWithRegistration`
2. Убедиться, что `onComplete` вызывается корректно
3. Проверить навигацию через `NavigationView` и `NavigationStack`
4. Добавить правильную обработку кнопки "Назад" на всех экранах добавления

---

### ПРОБЛЕМА #4: Кнопка удаления члена семьи не видна
**Описание:**  
Функция удаления члена семьи реализована (`removeFamilyMember` в `FamilyScreen.swift`), но кнопка удаления не отображается пользователю. Нужно найти логику удаления и вывести кнопку в понятном месте.

**Файлы:**
- `Screens/02_FamilyScreen.swift` (функция `removeFamilyMember` на строке 199)
- Компоненты карточек членов семьи

**Решение:**
1. Найти где используется `removeFamilyMember`
2. Добавить кнопку удаления в карточку члена семьи (для администратора/родителя)
3. Добавить подтверждение удаления
4. Убедиться, что удаление работает корректно

---

### ПРОБЛЕМА #5: Кнопки "Назад" и "Отмена" не работают на экране "Добавить участника"
**Описание:**  
На главном экране при нажатии "Добавить" -> "Добавить участника" кнопки "Назад" и "Отмена" внизу не работают на iPhone и iPad.

**Файлы:**
- `Shared/Components/Modals/AddMemberOptionsModal.swift` (кнопка "Отмена" на строке 127)
- `Screens/AddMemberOptionsScreen.swift` (если используется как отдельный экран)

**Решение:**
1. Проверить обработку кнопки "Отмена" в `AddMemberOptionsModal`
2. Убедиться, что `isPresented = false` работает корректно
3. Проверить навигацию при закрытии модала
4. Исправить работу кнопок на iPhone и iPad

---

### ПРОБЛЕМА #6: Добавление через "Управление" не работает
**Описание:**  
Через "Управление" -> "+" -> "Добавить участника" -> "Создать новую семью" или "Сканировать QR код" ничего не происходит, вылетает обратно на экран "Семья ALADDIN". Через желтую карточку на главном экране работает.

**Файлы:**
- `Screens/02_FamilyScreen.swift` (кнопка "+" в правом верхнем углу)
- `Shared/Components/Modals/AddMemberOptionsModal.swift`

**Решение:**
1. Проверить как открывается `AddMemberOptionsModal` из `FamilyScreen`
2. Сравнить логику открытия из главного экрана (желтая карточка) и из FamilyScreen
3. Исправить навигацию и закрытие модалов при добавлении через "Управление"
4. Убедиться, что после добавления происходит правильная навигация

---

## ДЕТАЛЬНЫЙ ПЛАН ИСПРАВЛЕНИЯ

### ЭТАП 1: Локализация (Проблема #1)
1. Проверить ключи локализации в `LocalizationManager.swift`
2. Исправить отображение текста "Выберите возраст" в `MainScreenWithRegistration.swift`
3. Исправить отображение внутри карточек возраста

### ЭТАП 2: Навигация после добавления (Проблема #2)
1. Проверить логику навигации в `AddMemberOptionsModal.onComplete`
2. Добавить правильную навигацию по роли после создания семьи
3. Протестировать на iPad и iPhone

### ЭТАП 3: Кнопка "Назад" (Проблема #3)
1. Исправить обработку кнопки "Назад" в `MainScreenWithRegistration`
2. Проверить все экраны добавления члена семьи
3. Убедиться, что навигация работает на всех этапах

### ЭТАП 4: Кнопка удаления (Проблема #4)
1. Найти функцию `removeFamilyMember` в `FamilyScreen.swift`
2. Добавить кнопку удаления в карточку члена семьи
3. Добавить подтверждение удаления
4. Протестировать удаление

### ЭТАП 5: Кнопки в модале (Проблема #5)
1. Исправить обработку кнопки "Отмена" в `AddMemberOptionsModal`
2. Проверить работу на iPhone и iPad
3. Убедиться, что модал закрывается корректно

### ЭТАП 6: Добавление через "Управление" (Проблема #6)
1. Проверить открытие `AddMemberOptionsModal` из `FamilyScreen`
2. Сравнить с логикой из главного экрана
3. Исправить навигацию и закрытие модалов
4. Протестировать полный флоу

---

## ПРИОРИТЕТЫ

1. **КРИТИЧНО:** Проблема #6 (добавление через "Управление" не работает)
2. **КРИТИЧНО:** Проблема #2 (навигация после добавления)
3. **ВЫСОКИЙ:** Проблема #3 (кнопка "Назад")
4. **ВЫСОКИЙ:** Проблема #5 (кнопки в модале)
5. **СРЕДНИЙ:** Проблема #1 (локализация)
6. **СРЕДНИЙ:** Проблема #4 (кнопка удаления)

---

## ТЕСТИРОВАНИЕ

После исправления каждой проблемы необходимо протестировать:
- ✅ На iPhone (симулятор и реальное устройство)
- ✅ На iPad (симулятор и реальное устройство)
- ✅ Все пути добавления члена семьи
- ✅ Навигацию после добавления
- ✅ Работу кнопок "Назад" и "Отмена"
- ✅ Удаление члена семьи

