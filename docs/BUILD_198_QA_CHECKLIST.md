# TestFlight Build 198 — QA (iPhone SE, iOS 26.x)

## Перед установкой

- [ ] Сборка `CFBundleVersion` = **198**
- [ ] В IPA есть `OnboardingHero_00` … `OnboardingHero_07` (Assets.xcassets)
- [ ] Чистая установка или сброс онбординга для проверки героев

## P0 — обязательно

### Онбординг

- [ ] Все 8 слайдов: иллюстрации видны (не пустой фон)
- [ ] Нет краша в первые ~30 с после запуска (spring/анимации)
- [ ] Только светлая тема на всех экранах

### Семейный чат

- [ ] История сообщений загружается (GET 200)
- [ ] Отправка текста **без** алерта «Не найден идентификатор семьи»
- [ ] При E2EE: входящие расшифровываются (если ключ есть)

### Навигация AI ↔ Settings

- [ ] AI → баннер/настройки → Назад → снова **AI чат** (не Main)
- [ ] Main → Settings (NavigationLink) → Назад → Main

### Settings

- [ ] Нет пункта «Тёмная тема»
- [ ] Скролл Settings ~2 мин без watchdog (0x8BADF00D)

## P1 — AI

- [ ] «Облачный AI-помощник» включён в Settings
- [ ] В логах Xcode: `AI source=faq_local` или `cloud_api` / `cloud_api_probable_mock`
- [ ] Ответ не повторяется только из FAQ при уникальном вопросе (опционально)

## Регрессия

- [ ] VPN / защита / главный экран открываются
- [ ] Push-токен (если включены уведомления)

## Логи для отчёта

Скопировать строки:

- `🤖 AI source=...`
- `sendMessage` / `familyId=` при ошибке чата
- Crash log если есть
