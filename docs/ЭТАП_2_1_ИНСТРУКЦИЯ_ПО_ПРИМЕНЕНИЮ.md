# 📋 ЭТАП 2.1: ИНСТРУКЦИЯ ПО ПРИМЕНЕНИЮ НА СЕРВЕРЕ

**Дата:** 2026-03-14  
**Компонент:** `crash_detection_agent`  
**Статус:** 🟡 ГОТОВО К ПРИМЕНЕНИЮ

---

## 🎯 ЧТО БУДЕТ СДЕЛАНО

1. ✅ Создана таблица `crash_detection_alerts` в PostgreSQL
2. ✅ Созданы индексы для оптимизации
3. ✅ Создан триггер для автоматического обновления `updated_at`
4. ✅ Подготовлен план обновления роутера

---

## 📝 ПОШАГОВАЯ ИНСТРУКЦИЯ

### **ШАГ 1: Подключиться к серверу**

```bash
ssh root@149.154.65.180
# Пароль: Sergio675
```

---

### **ШАГ 2: Создать backup БД (ОБЯЗАТЕЛЬНО!)**

```bash
# Перейти в директорию проекта
cd /opt/aladdin-backend

# Создать backup
pg_dump -U postgres -d aladdin_db > backup_crash_detection_$(date +%Y%m%d_%H%M%S).sql

# Проверить, что backup создан
ls -lh backup_crash_detection_*.sql
```

---

### **ШАГ 3: Загрузить SQL миграцию на сервер**

**Вариант 1: Через SFTP (FileZilla, Cyberduck, WinSCP)**

1. Подключиться к серверу:
   - **Host:** `149.154.65.180`
   - **Port:** `22`
   - **Protocol:** `SFTP`
   - **Username:** `root`
   - **Password:** `Sergio675`

2. Загрузить файл:
   - **Локальный путь:** `docs/server/migrations/create_crash_detection_alerts_table.sql`
   - **Серверный путь:** `/opt/aladdin-backend/docs/server/migrations/create_crash_detection_alerts_table.sql`

**Вариант 2: Через SSH (scp)**

```bash
# С локального компьютера
scp docs/server/migrations/create_crash_detection_alerts_table.sql root@149.154.65.180:/opt/aladdin-backend/docs/server/migrations/
```

---

### **ШАГ 4: Применить миграцию**

```bash
# На сервере
cd /opt/aladdin-backend

# Применить миграцию
psql -U postgres -d aladdin_db -f docs/server/migrations/create_crash_detection_alerts_table.sql
```

**Ожидаемый вывод:**
```
CREATE TABLE
CREATE INDEX
CREATE INDEX
CREATE INDEX
CREATE INDEX
CREATE INDEX
COMMENT
COMMENT
...
CREATE FUNCTION
CREATE TRIGGER
```

---

### **ШАГ 5: Проверить создание таблицы**

```bash
# Проверить структуру таблицы
psql -U postgres -d aladdin_db -c "\d crash_detection_alerts"

# Проверить индексы
psql -U postgres -d aladdin_db -c "\di crash_detection_alerts*"

# Проверить триггер
psql -U postgres -d aladdin_db -c "\dS crash_detection_alerts"
```

---

### **ШАГ 6: Тестовая вставка (опционально)**

```bash
# Тестовая вставка записи
psql -U postgres -d aladdin_db << EOF
INSERT INTO crash_detection_alerts (
    user_id,
    session_id,
    latitude,
    longitude,
    severity,
    crash_detected
) VALUES (
    gen_random_uuid(),
    'test_session_001',
    55.7558,
    37.6173,
    'high',
    true
);
EOF

# Проверить вставку
psql -U postgres -d aladdin_db -c "SELECT * FROM crash_detection_alerts LIMIT 1;"

# Удалить тестовую запись
psql -U postgres -d aladdin_db -c "DELETE FROM crash_detection_alerts WHERE session_id = 'test_session_001';"
```

---

## ✅ ПРОВЕРКА УСПЕШНОСТИ

После применения миграции проверьте:

- [ ] Таблица `crash_detection_alerts` существует
- [ ] Все индексы созданы (5 индексов)
- [ ] Триггер создан
- [ ] Тестовая вставка работает
- [ ] Backup создан

---

## 🚨 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### **Проблема: "relation already exists"**

```bash
# Проверить, существует ли таблица
psql -U postgres -d aladdin_db -c "\d crash_detection_alerts"

# Если таблица существует, но структура неправильная:
# 1. Создать backup
# 2. Удалить таблицу (ОСТОРОЖНО!)
# 3. Применить миграцию снова
```

### **Проблема: "permission denied"**

```bash
# Проверить права доступа
psql -U postgres -d aladdin_db -c "\du"

# Если нужно, дать права:
# GRANT ALL PRIVILEGES ON TABLE crash_detection_alerts TO aladdin_user;
```

### **Проблема: "database does not exist"**

```bash
# Проверить список баз данных
psql -U postgres -l

# Если база данных называется по-другому, изменить команду:
psql -U postgres -d <имя_базы> -f docs/server/migrations/create_crash_detection_alerts_table.sql
```

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

После успешного применения миграции:

1. ✅ Обновить роутер `crash_detection_router.py` для использования PostgreSQL
2. ✅ Создать модель SQLAlchemy в `app/models.py`
3. ✅ Обновить endpoints для сохранения алертов в БД
4. ✅ Протестировать работу endpoints

---

**Статус:** ✅ **МИГРАЦИЯ ГОТОВА К ПРИМЕНЕНИЮ**
