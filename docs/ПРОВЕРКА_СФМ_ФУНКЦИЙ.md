# 🔍 ПРОВЕРКА РЕАЛЬНОГО КОЛИЧЕСТВА ФУНКЦИЙ В SFM

**Дата:** 10 декабря 2025  
**Проблема:** Нужно подтвердить реальное количество функций в SFM

---

## ⚠️ МОЯ ОШИБКА

Я неправильно предположил, что в SFM только 23 функции (12 от Dark Web + 11 от Identity Theft).

**Реальность:**
- В SFM уже зарегистрировано множество других агентов
- Файл `function_registry.json` содержит около **33,431 строку**
- Общее количество функций: **около 1100** (по словам пользователя)

---

## 🔍 КАК ПРОВЕРИТЬ РЕАЛЬНОЕ КОЛИЧЕСТВО

### Вариант 1: Через скрипт (рекомендуется)

```bash
# Настройте сервер
export ALADDIN_SERVER=your-server.com
export ALADDIN_SERVER_USER=root

# Запустите проверку
./check_sfm_on_server.sh
```

Скрипт автоматически:
- ✅ Подключится к серверу
- ✅ Найдет `function_registry.json`
- ✅ Подсчитает все агенты и функции
- ✅ Покажет топ-20 агентов по количеству функций
- ✅ Выведет общее количество функций

### Вариант 2: Вручную на сервере

```bash
ssh user@server

cd /opt/aladdin-backend/data/sfm

# Проверка размера файла
wc -l function_registry.json
# Должно быть около 33,431 строки

# Подсчет функций
python3 << 'EOF'
import json
from pathlib import Path

registry_path = Path("/opt/aladdin-backend/data/sfm/function_registry.json")
with open(registry_path, 'r', encoding='utf-8') as f:
    registry = json.load(f)

if isinstance(registry, list):
    agents = registry
elif isinstance(registry, dict):
    agents = registry.get("agents", list(registry.values()) if "agents" not in registry else [])
else:
    agents = []

total_functions = sum(len(a.get("functions", [])) for a in agents if isinstance(a, dict))

print(f"Всего агентов: {len(agents)}")
print(f"Всего функций: {total_functions}")
EOF
```

### Вариант 3: Локальная проверка (если есть копия registry)

```bash
# Если у вас есть локальная копия registry
python3 count_sfm_functions.py
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После проверки вы должны увидеть:

```
📊 РЕАЛЬНАЯ СТАТИСТИКА SFM НА СЕРВЕРЕ:
================================================================================
Всего агентов: ~XXX
Всего функций: ~1100
```

**После добавления Identity Theft Protection:**
- Текущее количество: ~1100 функций
- Добавится: 11 функций (Identity Theft Protection)
- **Итого: ~1111 функций** ✅

---

## ✅ ИСПРАВЛЕНИЕ МОЕЙ ОШИБКИ

**Моя неправильная оценка:**
- Я считал только 2 агента (Dark Web + Identity Theft)
- Предположил только 23 функции
- Не учел, что в SFM уже зарегистрировано множество других агентов

**Реальность:**
- В SFM зарегистрировано много агентов
- Общее количество функций: **~1100**
- Файл registry: **33,431 строка**

**После добавления Identity Theft Protection:**
- Новое общее количество: **~1111 функций** ✅

---

## 🔧 СКРИПТЫ ДЛЯ ПРОВЕРКИ

1. **`check_sfm_on_server.sh`** - проверка на сервере через SSH
2. **`count_sfm_functions.py`** - подсчет локально (если есть копия registry)

---

**Извините за путаницу!** Нужно проверять реальный registry на сервере, а не только локальные entry файлы.
