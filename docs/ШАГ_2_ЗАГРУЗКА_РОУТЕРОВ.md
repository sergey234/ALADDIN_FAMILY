# 📋 ШАГ 2: ЗАГРУЗКА ОБНОВЛЕННЫХ РОУТЕРОВ НА СЕРВЕР

**Дата:** 2026-03-14  
**Статус:** ⏳ **В ПРОЦЕССЕ**

---

## 🎯 ЦЕЛЬ

Загрузить 5 обновленных роутеров на сервер, заменив существующие версии.

---

## 📝 ПОШАГОВАЯ ИНСТРУКЦИЯ

### **ШАГ 2.1: Подключиться к серверу**

```bash
ssh root@149.154.65.180
cd /opt/aladdin-backend
```

---

### **ШАГ 2.2: Создать backups существующих роутеров**

```bash
# Создать директорию для backups
mkdir -p backups/routers_$(date +%Y%m%d_%H%M%S)

# Создать backups всех роутеров
cp app/security/api/routers/crash_detection_router.py backups/routers_*/crash_detection_router.py.backup
cp app/security/api/routers/roadside_assistance_router.py backups/routers_*/roadside_assistance_router.py.backup
cp app/security/api/routers/parental_control_router.py backups/routers_*/parental_control_router.py.backup
cp app/security/api/routers/subscription_sync_router.py backups/routers_*/subscription_sync_router.py.backup
cp app/security/api/routers/metrics_router.py backups/routers_*/metrics_router.py.backup

# Проверить создание backups
ls -lh backups/routers_*/
```

---

### **ШАГ 2.3: Загрузить обновленные роутеры**

**ВАРИАНТ 1: Через SFTP (рекомендуется)**

1. Открыть SFTP клиент (FileZilla, WinSCP, или встроенный в IDE)
2. Подключиться к `149.154.65.180` с учетными данными root
3. Перейти в `/opt/aladdin-backend/app/security/api/routers/`
4. Загрузить файлы:
   - `crash_detection_router_updated.py` → `crash_detection_router.py`
   - `roadside_assistance_router_updated.py` → `roadside_assistance_router.py`
   - `parental_control_router_updated.py` → `parental_control_router.py`
   - `subscription_sync_router_updated.py` → `subscription_sync_router.py`
   - `metrics_router_updated.py` → `metrics_router.py`

---

**ВАРИАНТ 2: Через scp (из локальной машины)**

```bash
# Из локальной директории проекта
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS

# Загрузить каждый роутер
scp app/security/api/routers/crash_detection_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/crash_detection_router.py

scp app/security/api/routers/roadside_assistance_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/roadside_assistance_router.py

scp app/security/api/routers/parental_control_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/parental_control_router.py

scp app/security/api/routers/subscription_sync_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/subscription_sync_router.py

scp app/security/api/routers/metrics_router_updated.py root@149.154.65.180:/opt/aladdin-backend/app/security/api/routers/metrics_router.py
```

---

### **ШАГ 2.4: Проверить загрузку файлов**

```bash
# На сервере
cd /opt/aladdin-backend/app/security/api/routers/

# Проверить размеры файлов
ls -lh crash_detection_router.py
ls -lh roadside_assistance_router.py
ls -lh parental_control_router.py
ls -lh subscription_sync_router.py
ls -lh metrics_router.py

# Проверить первые строки файлов (должны содержать "ОБНОВЛЕННАЯ ВЕРСИЯ" или "PostgreSQL")
head -n 5 crash_detection_router.py
head -n 5 roadside_assistance_router.py
head -n 5 parental_control_router.py
head -n 5 subscription_sync_router.py
head -n 5 metrics_router.py
```

---

### **ШАГ 2.5: Проверить синтаксис Python**

```bash
# Проверить синтаксис каждого роутера
python3 -m py_compile app/security/api/routers/crash_detection_router.py
python3 -m py_compile app/security/api/routers/roadside_assistance_router.py
python3 -m py_compile app/security/api/routers/parental_control_router.py
python3 -m py_compile app/security/api/routers/subscription_sync_router.py
python3 -m py_compile app/security/api/routers/metrics_router.py
```

**Ожидаемый результат:** Нет ошибок (пустой вывод)

---

### **ШАГ 2.6: Проверить импорты**

```bash
# Проверить, что все импорты доступны
python3 << EOF
import sys
sys.path.insert(0, '/opt/aladdin-backend')

try:
    from app.database.database import get_db
    from app.auth.auth import get_current_user
    print("✅ Импорты доступны")
except ImportError as e:
    print(f"❌ Ошибка импорта: {e}")
EOF
```

---

## ✅ КРИТЕРИИ УСПЕХА

- [ ] Все 5 backups созданы
- [ ] Все 5 роутеров загружены
- [ ] Размеры файлов корректны
- [ ] Синтаксис Python проверен (нет ошибок)
- [ ] Импорты доступны

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### **Проблема 1: Ошибка доступа при загрузке**

**Решение:**
```bash
# Проверить права доступа
ls -la /opt/aladdin-backend/app/security/api/routers/

# Если нужно, изменить права
chmod 644 /opt/aladdin-backend/app/security/api/routers/*.py
chown root:root /opt/aladdin-backend/app/security/api/routers/*.py
```

---

### **Проблема 2: Ошибка импорта**

**Решение:**
```bash
# Проверить существование модулей
ls -la /opt/aladdin-backend/app/database/database.py
ls -la /opt/aladdin-backend/app/auth/auth.py

# Если файлы не найдены, проверить структуру проекта
find /opt/aladdin-backend -name "database.py" -type f
find /opt/aladdin-backend -name "auth.py" -type f
```

---

### **Проблема 3: Синтаксические ошибки**

**Решение:**
```bash
# Проверить детальные ошибки
python3 -m py_compile app/security/api/routers/crash_detection_router.py 2>&1

# Если есть ошибки, проверить файл вручную
python3 -c "import ast; ast.parse(open('app/security/api/routers/crash_detection_router.py').read())"
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После успешной загрузки:

- ✅ Все 5 роутеров обновлены
- ✅ Backups созданы
- ✅ Синтаксис проверен
- ✅ Готово к перезапуску сервера

---

**Статус:** ⏳ **ГОТОВО К ВЫПОЛНЕНИЮ**
