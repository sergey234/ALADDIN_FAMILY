# 🔑 КАК ПОЛУЧИТЬ ТОКЕН И ТЕСТИРОВАТЬ ENDPOINT'Ы

**Дата:** 2026-02-11  
**Цель:** Объяснить, как получить токен и протестировать endpoint'ы

---

## 🔑 ЧТО ТАКОЕ ТОКЕН?

**Токен** - это специальный ключ доступа, который сервер выдает после авторизации.  
Он нужен для доступа к защищенным endpoint'ам.

**Пример токена:**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjoxMjMsImV4cCI6MTYzODk2NzIwMH0.abc123...
```

---

## 🎯 КАК ПОЛУЧИТЬ ТОКЕН?

### **Способ 1: Через авторизацию (email/password)** ⚠️

**Для тестирования можно использовать:**

```bash
# Запрос на авторизацию
curl -X POST "http://149.154.65.180:8002/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email": "test@test.com", "password": "test"}'

# Ответ:
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "...",
  "expires_in": 3600,
  "token_type": "Bearer"
}
```

**Проблема:** Требует email/password (персональные данные)

---

### **Способ 2: Через Recovery Code** ✅ (правильный, но endpoint не реализован)

**Должен работать так:**

```bash
# 1. Создать семью (получить family_id и recovery_code)
curl -X POST "http://149.154.65.180:8002/api/family/create" \
  -H "Content-Type: application/json" \
  -d '{
    "role": "parent",
    "age_group": "Adult (18-64)",
    "personal_letter": "V",
    "device_type": "iOS"
  }'

# 2. Использовать recovery code для авторизации
curl -X POST "http://149.154.65.180:8002/api/auth/login-by-recovery-code" \
  -H "Content-Type: application/json" \
  -d '{
    "family_id": "FAM_59316C46-3F9",
    "recovery_code": "FAM-835E-78F4-E5B7"
  }'
```

**Проблема:** Endpoint `/api/auth/login-by-recovery-code` не реализован на сервере

---

## 🧪 ЧТО ЗНАЧИТ "ТЕСТИРОВАТЬ НА СЕРВЕРЕ (ЧЕРЕЗ CURL)"?

### **Curl - это команда для отправки HTTP запросов**

**Пример:**
```bash
# GET запрос
curl -X GET "http://149.154.65.180:8002/api/health"

# POST запрос
curl -X POST "http://149.154.65.180:8002/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email": "test@test.com", "password": "test"}'
```

**Что это значит:**
- Отправляем запрос напрямую на сервер
- Получаем ответ
- Проверяем, работает ли endpoint

---

## 🎯 КАКОЙ СПОСОБ НАИЛУЧШИЙ ДЛЯ НАС?

### **ВАРИАНТ 1: Тестировать прямо в iOS приложении** ✅ **РЕКОМЕНДУЕТСЯ**

**Преимущества:**
- ✅ Видим реальное поведение приложения
- ✅ Проверяем UI и логику
- ✅ Не нужны дополнительные инструменты
- ✅ Видим ошибки сразу

**Как делать:**
1. Открыть Xcode
2. Запустить приложение
3. Выполнить действие (например, создать семью)
4. Проверить, работает ли

**Недостатки:**
- ⚠️ Нужно запускать приложение каждый раз
- ⚠️ Медленнее, чем curl

---

### **ВАРИАНТ 2: Использовать Postman** ✅ **УДОБНО**

**Postman** - это программа для тестирования API (как curl, но с графическим интерфейсом)

**Преимущества:**
- ✅ Удобный интерфейс
- ✅ Сохранение запросов
- ✅ История запросов
- ✅ Легко тестировать разные endpoint'ы

**Как использовать:**
1. Установить Postman (бесплатно)
2. Создать новый запрос
3. Ввести URL и параметры
4. Отправить запрос
5. Посмотреть ответ

**Недостатки:**
- ⚠️ Нужно устанавливать программу
- ⚠️ Не проверяет iOS приложение напрямую

---

### **ВАРИАНТ 3: Использовать curl (командная строка)** ⚠️

**Преимущества:**
- ✅ Быстро
- ✅ Не нужны дополнительные программы
- ✅ Можно автоматизировать

**Недостатки:**
- ❌ Нужно знать команды
- ❌ Не проверяет iOS приложение
- ❌ Менее удобно

---

## ✅ РЕКОМЕНДУЕМЫЙ ПОДХОД

### **Комбинированный подход:**

**ШАГ 1: Тестируем на сервере (curl или Postman)**
- Проверяем, работает ли endpoint
- Получаем токен
- Проверяем ответы

**ШАГ 2: Тестируем в iOS приложении**
- Проверяем, работает ли в реальном приложении
- Проверяем UI
- Проверяем обработку ошибок

---

## 🎯 ПЛАН ДЕЙСТВИЙ

### **ШАГ 1: Получить токен**

**Вариант A: Через email/password (для тестирования)** ⚠️
```bash
curl -X POST "http://149.154.65.180:8002/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email": "test@test.com", "password": "test"}'
```

**Вариант B: Через iOS приложение** ✅
1. Открыть приложение
2. Выполнить авторизацию
3. Токен сохранится в Keychain
4. Использовать для запросов

---

### **ШАГ 2: Использовать токен**

**В curl:**
```bash
TOKEN="ваш_токен_здесь"
curl -X GET "http://149.154.65.180:8002/api/family/stats" \
  -H "Authorization: Bearer $TOKEN"
```

**В iOS приложении:**
- Токен автоматически добавляется в заголовки
- Через `NetworkManager` или `APIService`

---

## 📋 ЧТО ДЕЛАТЬ СЕЙЧАС?

### **Рекомендую:**

1. **Сначала:** Протестировать в iOS приложении
   - Открыть Xcode
   - Запустить приложение
   - Попробовать создать семью
   - Посмотреть, что происходит

2. **Если нужно:** Использовать Postman для проверки сервера
   - Установить Postman
   - Отправить запрос
   - Проверить ответ

3. **Для автоматизации:** Использовать curl
   - Создать скрипт
   - Запустить тесты

---

**Последнее обновление:** 2026-02-11  
**Статус:** ✅ **ГОТОВО К ИСПОЛЬЗОВАНИЮ**
