name: iOS APP Generator - Get .app File
on:
  push:
    branches: [ main ]
    paths: [ 'mobile_apps/ALADDIN_iOS/get_app.txt' ]
  workflow_dispatch:

jobs:
  generate-app:
    runs-on: macos-latest
    timeout-minutes: 15
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.0.0'
    
    - name: iOS APP Generator - Get .app File
      run: |
        cd mobile_apps/ALADDIN_iOS
        echo "🎯 ЦЕЛЬ: ПОЛУЧИТЬ .app ФАЙЛ ДЛЯ APP STORE"
        echo "=========================================="
        echo ""
        echo "📱 Диагностика окружения:"
        xcodebuild -version
        xcodebuild -showsdks | grep iphonesimulator
        xcrun simctl list devices available | grep iPhone
        echo ""
        
        echo "🔧 ТЕСТ 1: Minimal Build (без подписи и assets)"
        if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN \
          -sdk iphonesimulator \
          -destination "platform=iOS Simulator" \
          CODE_SIGNING_ALLOWED=NO \
          ASSETCATALOG_COMPILER_APPICON_NAME="" \
          ASSETCATALOG_COMPILER_ACCENT_COLOR_NAME="" \
          COMPILER_INDEX_STORE_ENABLE=NO \
          build; then
          
          echo "✅ ТЕСТ 1 УСПЕХЕН!"
          APP_PATH=$(find build/ -name "*.app" -type d | head -1)
          if [ -n "$APP_PATH" ]; then
            echo "🎉 НАЙДЕН .app ФАЙЛ: $APP_PATH"
            ls -la "$APP_PATH"
            echo "📱 Размер: $(du -sh "$APP_PATH")"
          fi
          
        else
          echo "❌ ТЕСТ 1 НЕ РАБОТАЕТ"
          echo "🔧 ТЕСТ 2: Generic Simulator"
          if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN \
            -destination "generic/platform=iOS Simulator" \
            CODE_SIGNING_ALLOWED=NO \
            build; then
            
            echo "✅ ТЕСТ 2 УСПЕХЕН!"
            APP_PATH=$(find build/ -name "*.app" -type d | head -1)
            if [ -n "$APP_PATH" ]; then
              echo "🎉 НАЙДЕН .app ФАЙЛ: $APP_PATH"
            fi
            
          else
            echo "❌ ТЕСТ 2 НЕ РАБОТАЕТ"
            echo "🔧 ТЕСТ 3: Без destination"
            if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN \
              CODE_SIGNING_ALLOWED=NO \
              build; then
              
              echo "✅ ТЕСТ 3 УСПЕХЕН!"
              APP_PATH=$(find build/ -name "*.app" -type d | head -1)
              if [ -n "$APP_PATH" ]; then
                echo "🎉 НАЙДЕН .app ФАЙЛ: $APP_PATH"
              fi
              
            else
              echo "❌ ВСЕ ТЕСТЫ ПРОВАЛИЛИСЬ"
              echo "Проверяем доступные runtime:"
              xcrun simctl list runtimes | grep iOS
              exit 1
            fi
          fi
        fi
        
        echo ""
        echo "📱 ФИНАЛЬНЫЙ РЕЗУЛЬТАТ:"
        find build/ -name "*.app" -type d || echo "❌ .app файлы не найдены"
        ls -la build/ || echo "❌ build папка не найдена"
    
    - name: Upload .app file
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: aladdin-ios-app
        path: mobile_apps/ALADDIN_iOS/build/*.app
        if-no-files-found: warn
        retention-days: 7
    
    - name: Upload build directory
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ios-build-directory
        path: mobile_apps/ALADDIN_iOS/build/
        if-no-files-found: warn
        retention-days: 7
