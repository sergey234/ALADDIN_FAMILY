name: iOS Test 3 - Deployment Target Fix
on:
  push:
    branches: [ main ]
    paths: [ 'mobile_apps/ALADDIN_iOS/test3.txt' ]
  workflow_dispatch:

jobs:
  ios-test-3:
    runs-on: macos-latest
    timeout-minutes: 5
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.0.0'
    
    - name: iOS Test 3 - Deployment Target Fix
      run: |
        cd mobile_apps/ALADDIN_iOS
        echo "🧪 ТЕСТ 3: DEPLOYMENT TARGET - iOS 12.0+"
        echo "========================================"
        echo ""
        echo "🔍 ПРОБЛЕМА:"
        echo "❌ iOS Simulator deployment target 'IPHONEOS_DEPLOYMENT_TARGET' is set to 11.0"
        echo "❌ Supported range: 12.0 to 18.0.99"
        echo ""
        echo "✅ РЕШЕНИЕ:"
        echo "Используем IPHONEOS_DEPLOYMENT_TARGET=12.0"
        echo ""
        echo "🧪 ТЕСТ: С правильным deployment target"
        if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN -destination "platform=iOS Simulator,name=iPhone 16 Pro" IPHONEOS_DEPLOYMENT_TARGET=12.0 build; then
          echo "✅ УСПЕХ! Deployment target 12.0 работает!"
          
          echo ""
          echo "📱 Результат сборки:"
          ls -la build/ || echo "build папка не найдена"
          echo ""
          echo "📱 Ищем .app файлы:"
          find build/ -name "*.app" -type d || echo ".app файлы не найдены"
          
          echo ""
          echo "🎉 РЕШЕНИЕ НАЙДЕНО!"
          echo "Используйте: IPHONEOS_DEPLOYMENT_TARGET=12.0"
        else
          echo "❌ Deployment target 12.0 не работает"
          echo "Пробуем с 13.0:"
          if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN -destination "platform=iOS Simulator,name=iPhone 16 Pro" IPHONEOS_DEPLOYMENT_TARGET=13.0 build; then
            echo "✅ УСПЕХ! Deployment target 13.0 работает!"
            echo "Используйте: IPHONEOS_DEPLOYMENT_TARGET=13.0"
          else
            echo "❌ Deployment target 13.0 тоже не работает"
          fi
        fi
