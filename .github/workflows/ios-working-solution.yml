name: iOS Working Solution - Universal Fix

on:
  push:
    branches: [ main ]
    paths: [ 'mobile_apps/ALADDIN_iOS/working_solution.txt' ]

jobs:
  working-solution:
    runs-on: macos-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.0.0'
      
    - name: iOS Working Solution
      run: |
        echo "🎯 РАБОЧЕЕ РЕШЕНИЕ ДЛЯ iOS СБОРКИ"
        echo "================================="
        
        cd mobile_apps/ALADDIN_iOS
        
        echo "🔍 Анализ доступных симуляторов:"
        xcrun simctl list devices available | grep iPhone | head -5
        
        echo ""
        echo "🧪 ТЕСТ 1: Без указания конкретного устройства"
        echo "Команда: xcodebuild -destination 'platform=iOS Simulator' build"
        
        if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN -destination "platform=iOS Simulator" build; then
          echo "✅ УСПЕХ! Решение найдено: platform=iOS Simulator"
          echo ""
          echo "📱 Результат сборки:"
          ls -la build/
          find build/ -name "*.app" -type d
          echo ""
          echo "🎉 РЕШЕНИЕ: Используйте 'platform=iOS Simulator' без указания конкретного устройства!"
          exit 0
        fi
        
        echo ""
        echo "🧪 ТЕСТ 2: Generic platform"
        echo "Команда: xcodebuild -destination 'generic/platform=iOS Simulator' build"
        
        if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN -destination "generic/platform=iOS Simulator" build; then
          echo "✅ УСПЕХ! Решение найдено: generic/platform=iOS Simulator"
          echo ""
          echo "📱 Результат сборки:"
          ls -la build/
          find build/ -name "*.app" -type d
          echo ""
          echo "🎉 РЕШЕНИЕ: Используйте 'generic/platform=iOS Simulator'!"
          exit 0
        fi
        
        echo ""
        echo "🧪 ТЕСТ 3: Без destination"
        echo "Команда: xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN build"
        
        if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN build; then
          echo "✅ УСПЕХ! Решение найдено: Без destination"
          echo ""
          echo "📱 Результат сборки:"
          ls -la build/
          find build/ -name "*.app" -type d
          echo ""
          echo "🎉 РЕШЕНИЕ: Используйте команду без destination!"
          exit 0
        fi
        
        echo ""
        echo "❌ Все тесты провалились. Проверяем доступные симуляторы:"
        xcrun simctl list devices available | grep iPhone
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ios-working-solution-artifacts
        path: mobile_apps/ALADDIN_iOS/build/
        retention-days: 7
