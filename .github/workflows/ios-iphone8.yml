name: iOS iPhone SE Build

on:
  push:
    branches: [ main ]
    paths: [ 'mobile_apps/ALADDIN_iOS/**' ]

jobs:
  build-ios:
    runs-on: macos-latest
    timeout-minutes: 15
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.0.0'
        
    - name: List available simulators
      run: |
        echo "=== ДОСТУПНЫЕ СИМУЛЯТОРЫ ==="
        xcrun simctl list devices available | grep iPhone || echo "iPhone симуляторы не найдены"
        echo ""
        echo "=== ПОИСК IPHONE SE (3RD GENERATION) ==="
        xcrun simctl list devices available | grep "F43B6F94-C64F-46BA-8469-0A1C69118839" || echo "iPhone SE 3rd generation не найден"
        
    - name: Find iPhone SE simulator
      id: simulator
      run: |
        echo "=== ПОИСК IPHONE SE (3RD GENERATION) СИМУЛЯТОРА ==="
        SIMULATOR=""
        if xcrun simctl list devices available | grep -q "F43B6F94-C64F-46BA-8469-0A1C69118839"; then
          SIMULATOR="F43B6F94-C64F-46BA-8469-0A1C69118839"
        elif xcrun simctl list devices available | grep -q "iPhone 16"; then
          SIMULATOR="iPhone 16"
        else
          echo "❌ iPhone SE 3rd generation симулятор не найден!"
          echo "Доступные iPhone симуляторы:"
          xcrun simctl list devices available | grep iPhone
          exit 1
        fi
        
        echo "✅ Найден симулятор: $SIMULATOR"
        echo "simulator=$SIMULATOR" >> $GITHUB_OUTPUT
        
    - name: Build iOS app for iPhone SE
      run: |
        cd mobile_apps/ALADDIN_iOS
        echo "=== КОМПИЛИРУЕМ iOS ПРИЛОЖЕНИЕ ДЛЯ IPHONE SE (3RD GENERATION) ==="
        echo "Используем симулятор: ${{ steps.simulator.outputs.simulator }}"
        echo "Deployment target: iOS 11.0+ (поддержка iPhone SE (3rd generation))"
        
        xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN -destination "platform=iOS Simulator" build
        
        echo "=== ПРОВЕРЯЕМ BUILD ПАПКУ ==="
        ls -la build/ || echo "build папка не найдена"
        echo ""
        echo "=== ИЩЕМ .app ФАЙЛЫ ==="
        find build/ -name "*.app" -type d || echo ".app файлы не найдены"
        echo ""
        echo "=== ПРОВЕРЯЕМ СОВМЕСТИМОСТЬ С IPHONE 8+ ==="
        echo "✅ Deployment target: iOS 11.0+" && echo "✅ Поддержка iPhone SE (3rd generation)" && echo "✅ iPhone SE 3rd generation - 4.7\" экран" && echo "✅ A15 Bionic процессор"
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ios-build-iphone-se
        path: mobile_apps/ALADDIN_iOS/build/
        retention-days: 7
