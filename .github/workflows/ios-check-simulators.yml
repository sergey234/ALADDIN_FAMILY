name: iOS Check Simulators

on:
  push:
    branches: [ main ]
    paths: [ 'mobile_apps/ALADDIN_iOS/check_simulators.txt' ]

jobs:
  check-simulators:
    runs-on: macos-latest
    timeout-minutes: 5
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.0.0'
        
    - name: Check All Simulators
      run: |
        echo "🔍 ПРОВЕРКА ВСЕХ СИМУЛЯТОРОВ"
        echo "============================="
        
        echo ""
        echo "📱 1. Все доступные устройства:"
        xcrun simctl list devices available
        
        echo ""
        echo "📱 2. Только iPhone симуляторы:"
        xcrun simctl list devices available | grep iPhone
        
        echo ""
        echo "📱 3. Device Types:"
        xcrun simctl list devicetypes | grep iPhone
        
        echo ""
        echo "📱 4. Runtimes:"
        xcrun simctl list runtimes
        
        echo ""
        echo "📱 5. Попытка найти любой iPhone симулятор:"
        FIRST_IPHONE=$(xcrun simctl list devices available | grep iPhone | head -1)
        if [ -n "$FIRST_IPHONE" ]; then
          echo "✅ Найден iPhone симулятор: $FIRST_IPHONE"
        else
          echo "❌ iPhone симуляторы не найдены!"
        fi
        
        echo ""
        echo "📱 6. Попытка найти любой симулятор:"
        FIRST_SIMULATOR=$(xcrun simctl list devices available | grep -v "unavailable" | head -1)
        if [ -n "$FIRST_SIMULATOR" ]; then
          echo "✅ Найден симулятор: $FIRST_SIMULATOR"
        else
          echo "❌ Симуляторы не найдены!"
        fi
