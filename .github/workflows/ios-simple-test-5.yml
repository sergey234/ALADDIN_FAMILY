name: iOS Simple Test 5 - Basic Build

on:
  push:
    branches: [ main ]
    paths: [ 'mobile_apps/ALADDIN_iOS/simple_test_5.txt' ]

jobs:
  basic-build-test:
    runs-on: macos-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.0.0'
        
    - name: Test 5 - Basic Build
      run: |
        echo "🧪 ТЕСТ 5: БАЗОВАЯ СБОРКА"
        echo "================================"
        
        cd mobile_apps/ALADDIN_iOS
        
        echo "Попытка базовой сборки без destination:"
        echo "Команда: xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN build"
        
        # Очищаем предыдущие сборки
        rm -rf build/
        
        # Базовая сборка без destination
        if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN build; then
          echo "✅ УСПЕХ! Базовая сборка работает"
          echo "Build папка:"
          ls -la build/
          echo "Ищем .app файлы:"
          find build/ -name "*.app" -type d || echo ".app файлы не найдены"
        else
          echo "❌ ОШИБКА: Базовая сборка не работает"
          echo "Пробуем с destination..."
          
          # Попытка с destination
          if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN -destination "platform=iOS Simulator" build; then
            echo "✅ УСПЕХ! Сборка с destination работает"
            echo "Build папка:"
            ls -la build/
            echo "Ищем .app файлы:"
            find build/ -name "*.app" -type d || echo ".app файлы не найдены"
          else
            echo "❌ ОШИБКА: Сборка с destination не работает"
          fi
        fi
        
        echo "✅ Тест 5 завершен"
        
    - name: Upload Test 5 Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ios-simple-test-5
        path: mobile_apps/ALADDIN_iOS/build/
        retention-days: 1
