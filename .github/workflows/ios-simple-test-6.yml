name: iOS Simple Test 6 - Simulator Build

on:
  push:
    branches: [ main ]
    paths: [ 'mobile_apps/ALADDIN_iOS/simple_test_6.txt' ]

jobs:
  simulator-build-test:
    runs-on: macos-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.0.0'
        
    - name: Test 6 - Simulator Build
      run: |
        echo "🧪 ТЕСТ 6: СБОРКА НА СИМУЛЯТОРЕ"
        echo "================================"
        
        cd mobile_apps/ALADDIN_iOS
        
        echo "Получаем список доступных симуляторов:"
        xcrun simctl list devices available | grep "iPhone" | head -5
        
        echo ""
        echo "Тестируем разные симуляторы:"
        
        # Очищаем предыдущие сборки
        rm -rf build/
        
        # Тестируем разные симуляторы
        simulators=("iPhone 15" "iPhone 14" "iPhone 13" "iPhone 12")
        
        for simulator in "${simulators[@]}"; do
          echo ""
          echo "Тестируем симулятор: $simulator"
          
          if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN -destination "platform=iOS Simulator,name=$simulator" build; then
            echo "✅ УСПЕХ с $simulator"
            echo "Build папка:"
            ls -la build/
            echo "Ищем .app файлы:"
            find build/ -name "*.app" -type d
            break
          else
            echo "❌ Не удалось с $simulator"
          fi
        done
        
        echo "✅ Тест 6 завершен"
        
    - name: Upload Test 6 Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ios-simple-test-6
        path: mobile_apps/ALADDIN_iOS/build/
        retention-days: 1
