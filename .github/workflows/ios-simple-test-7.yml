name: iOS Simple Test 7 - Release Build

on:
  push:
    branches: [ main ]
    paths: [ 'mobile_apps/ALADDIN_iOS/simple_test_7.txt' ]

jobs:
  release-build-test:
    runs-on: macos-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.0.0'
        
    - name: Test 7 - Release Build
      run: |
        echo "🧪 ТЕСТ 7: РЕЛИЗНАЯ СБОРКА"
        echo "================================"
        
        cd mobile_apps/ALADDIN_iOS
        
        echo "Тестируем релизную сборку:"
        echo "Команда: xcodebuild -configuration Release -destination 'platform=iOS Simulator,name=iPhone 15' build"
        
        # Очищаем предыдущие сборки
        rm -rf build/
        
        # Релизная сборка
        if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN -configuration Release -destination "platform=iOS Simulator,name=iPhone 15" build; then
          echo "✅ УСПЕХ! Релизная сборка работает"
          echo "Build папка:"
          ls -la build/
          echo "Ищем .app файлы:"
          find build/ -name "*.app" -type d
          
          # Проверяем размер .app файла
          APP_FILE=$(find build/ -name "*.app" -type d | head -1)
          if [ -n "$APP_FILE" ]; then
            echo "📱 Информация о .app файле:"
            echo "Путь: $APP_FILE"
            echo "Размер: $(du -sh "$APP_FILE" | cut -f1)"
          fi
        else
          echo "❌ ОШИБКА: Релизная сборка не работает"
          
          echo ""
          echo "Пробуем Debug конфигурацию:"
          if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN -configuration Debug -destination "platform=iOS Simulator,name=iPhone 15" build; then
            echo "✅ УСПЕХ! Debug сборка работает"
            echo "Build папка:"
            ls -la build/
            echo "Ищем .app файлы:"
            find build/ -name "*.app" -type d
          else
            echo "❌ ОШИБКА: Debug сборка тоже не работает"
          fi
        fi
        
        echo "✅ Тест 7 завершен"
        
    - name: Upload Test 7 Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ios-simple-test-7
        path: mobile_apps/ALADDIN_iOS/build/
        retention-days: 1
