name: iOS Final Test 3 - Runtime Compatibility
on:
  push:
    branches: [ main ]
    paths: [ 'mobile_apps/ALADDIN_iOS/final_test3.txt' ]
  workflow_dispatch:

jobs:
  runtime-compatibility:
    runs-on: macos-latest
    timeout-minutes: 5
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.0.0'
    
    - name: Run Runtime Compatibility Build
      run: |
        cd mobile_apps/ALADDIN_iOS
        echo "🎯 ТЕСТ 3: RUNTIME СОВМЕСТИМОСТЬ"
        echo "==============================="
        echo ""
        echo "🔍 ПРОБЛЕМА:"
        echo "❌ Runtime [22E238, 22F77, 22G86, 23A343] не совместим с SDK 22A3362"
        echo ""
        echo "✅ РЕШЕНИЕ:"
        echo "1. Используем совместимый SDK"
        echo "2. Отключаем asset catalog"
        echo "3. Используем generic destination"
        echo ""
        
        echo "🧪 ТЕСТ: Runtime совместимость"
        if xcodebuild -project ALADDIN.xcodeproj -scheme ALADDIN \
          -sdk iphonesimulator \
          -destination "generic/platform=iOS Simulator" \
          IPHONEOS_DEPLOYMENT_TARGET=13.0 \
          CODE_SIGNING_ALLOWED=NO \
          ASSETCATALOG_COMPILER_APPICON_NAME="" \
          build; then
          
          echo "✅ УСПЕХ! Runtime совместимость работает!"
          
          echo ""
          echo "📱 Результат сборки:"
          ls -la build/ || echo "build папка не найдена"
          echo ""
          echo "📱 Ищем .app файлы:"
          find build/ -name "*.app" -type d || echo ".app файлы не найдены"
          
          echo ""
          echo "🎉 РЕШЕНИЕ НАЙДЕНО!"
          echo "Команда: -destination 'generic/platform=iOS Simulator'"
          
        else
          echo "❌ Runtime совместимость не работает"
          echo "Проверяем доступные runtime:"
          xcrun simctl list runtimes | grep iOS | head -3
        fi
    
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: ios-runtime-compatibility
        path: mobile_apps/ALADDIN_iOS/build/
        if-no-files-found: warn
