# ✅ КОНКРЕТНЫЙ ПЛАН ДЕЙСТВИЙ: ПРОВЕРКИ ПЕРЕД ПРОДАКШЕНОМ

**Дата:** 04.11.2025  
**Статус:** ✅ ГОТОВ К ВЫПОЛНЕНИЮ  
**Время:** 4-6 часов (можно разбить на 2 дня)

---

## 🎯 КОГДА ДЕЛАТЬ

### ✅ **ВРЕМЯ ВЫПОЛНЕНИЯ:**

**Когда:**
- ✅ **Перед каждым релизом** (TestFlight, App Store)
- ✅ **После больших изменений** в коде
- ✅ **За 1-2 недели до публикации**

**Распределение времени:**
- **День 1:** Задачи 1 и 2 (2 часа)
- **День 2:** Задача 3 (2 часа)

---

## 📋 ЗАДАЧА 1: ПРОВЕРИТЬ ПОСТОЯННЫЕ ЗАПРОСЫ К API (1-2 часа)

### ✅ **НАЙДЕНО:**

#### **1. VPNManager.swift - 3 таймера:**

**Строка 185:** `Timer.scheduledTimer(withTimeInterval: 0.5)` - каждые 0.5 секунды
```swift
// Что делает: Проверяет статус подключения VPN во время подключения
// ✅ НОРМАЛЬНО - таймер останавливается после подключения (строка 206: timer.invalidate())
// ⚠️ ПРОВЕРИТЬ: Убедиться, что таймер останавливается во всех случаях
```

**Рекомендация:** ✅ **ОСТАВИТЬ** - это нормально для проверки подключения, таймер останавливается после подключения.

---

**Строка 247:** `Timer.scheduledTimer(withTimeInterval: 1.0)` - каждую секунду
```swift
// Что делает: Обновляет время подключения (connectionTime) - только UI
// ✅ НОРМАЛЬНО - это только обновление UI, не запросы к API
// ⚠️ ПРОВЕРИТЬ: Убедиться, что таймер останавливается при отключении VPN
```

**Рекомендация:** ✅ **ОСТАВИТЬ** - это только обновление UI (отображение времени подключения), не запросы к API.

---

**Строка 415:** `Timer.scheduledTimer(withTimeInterval: currentPollingInterval)` - адаптивный polling
```swift
// Что делает: Отправляет статистику на сервер (sendStatsToServer)
// ⚠️ ПРОВЕРИТЬ: Какой интервал currentPollingInterval?
// ⚠️ ПРОВЕРИТЬ: Не слишком ли часто отправляются запросы?
```

**Рекомендация:** ⚠️ **ПРОВЕРИТЬ** - нужно убедиться, что интервал не слишком маленький (минимум 15 минут).

---

#### **2. PaymentQRViewModel.swift - 1 таймер:**

**Строка 558:** `Timer.scheduledTimer(withTimeInterval: 30.0)` - каждые 30 секунд
```swift
// Что делает: Проверяет статус оплаты (checkPaymentStatus)
// ⚠️ ПРОВЕРИТЬ: Это запрос к API?
// ⚠️ ПРОВЕРИТЬ: Останавливается ли таймер после завершения оплаты?
```

**Рекомендация:** ⚠️ **ПРОВЕРИТЬ** - нужно убедиться, что таймер останавливается после завершения оплаты.

---

#### **3. CacheManager.swift - 1 таймер:**

**Строка 236:** `Timer.scheduledTimer(withTimeInterval: cleanupInterval)` - очистка кэша
```swift
// Что делает: Очищает старые данные из кэша
// ✅ НОРМАЛЬНО - это локальная операция, не запросы к API
```

**Рекомендация:** ✅ **ОСТАВИТЬ** - это локальная операция, не запросы к API.

---

### ✅ **ЧТО СДЕЛАТЬ:**

#### **ШАГ 1.1: Проверить VPNManager (30 минут)**

**Файл:** `Core/VPN/VPNManager.swift`

**Действия:**
1. Открыть файл
2. Проверить строку 415 - найти значение `currentPollingInterval`
3. Проверить строки 440-464 - найти `minPollingInterval` и `maxPollingInterval`
4. Убедиться, что минимальный интервал не меньше 15 минут (900 секунд)

**Если интервал слишком маленький:**
```swift
// ❌ ПЛОХО - если интервал < 15 минут:
private let minPollingInterval: TimeInterval = 60.0 // 1 минута

// ✅ ХОРОШО - интервал >= 15 минут:
private let minPollingInterval: TimeInterval = 900.0 // 15 минут
```

---

#### **ШАГ 1.2: Проверить PaymentQRViewModel (20 минут)**

**Файл:** `ViewModels/PaymentQRViewModel.swift`

**Действия:**
1. Открыть файл
2. Найти функцию `checkPaymentStatus()` (строка 559)
3. Проверить, делает ли она запрос к API
4. Проверить, останавливается ли таймер после завершения оплаты

**Если таймер не останавливается:**
```swift
// ✅ ДОБАВИТЬ: Остановка таймера после завершения оплаты
func checkPaymentStatus() {
    // Проверка статуса
    if paymentStatus == .completed || paymentStatus == .failed {
        stopAutoCheck() // Остановить таймер
    }
}
```

---

#### **ШАГ 1.3: Проверить ViewModels на постоянные запросы (30 минут)**

**Файлы для проверки:**
- `ViewModels/AnalyticsViewModel.swift`
- `ViewModels/MainViewModel.swift`
- `ViewModels/FamilyViewModel.swift`

**Что проверить:**
```bash
# Найти все while true циклы
grep -r "while.*true" ViewModels/ --include="*.swift"
```

**Если найдены:**
```swift
// ❌ ПЛОХО - постоянный цикл:
.onAppear {
    Task {
        while true {
            await viewModel.load()
            try? await Task.sleep(nanoseconds: 1_000_000_000)
        }
    }
}

// ✅ ХОРОШО - загрузка один раз:
.onAppear {
    Task {
        await viewModel.load()
    }
}
```

---

## 📋 ЗАДАЧА 2: ПРОВЕРИТЬ БОЛЬШИЕ JSON ФАЙЛЫ (1 час)

### ✅ **НАЙДЕНО:**

**JSON файлы:**
- ✅ `DerivedData/.../ALADDIN.abi.json` - 8.7 MB - ✅ **НЕ КРИТИЧНО** (временный файл сборки, не входит в приложение)
- ✅ Остальные JSON файлы - < 4 KB - ✅ **НОРМАЛЬНО**

**Локализация:**
- ✅ Найдены файлы локализации в `Resources/Localization/`
- ⚠️ Нужно проверить размер

---

### ✅ **ЧТО СДЕЛАТЬ:**

#### **ШАГ 2.1: Проверить размер JSON файлов (20 минут)**

**Команда:**
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
find . -name "*.json" -type f ! -path "*/DerivedData/*" ! -path "*/BACKUP*" ! -path "*/.git/*" -exec du -h {} + | sort -rh | head -20
```

**Действия:**
1. Запустить команду
2. Проверить размер каждого JSON файла
3. Если файл > 1 MB:
   - ✅ Перенести на сервер
   - ✅ Получать через API
   - ✅ Кэшировать локально

---

#### **ШАГ 2.2: Проверить размер файлов локализации (20 минут)**

**Команда:**
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
find Resources/Localization -name "*.strings" -type f -exec du -h {} + | sort -rh
```

**Действия:**
1. Запустить команду
2. Проверить размер каждого файла
3. Если файл > 5 MB:
   - ✅ Оптимизировать (удалить неиспользуемые строки)
   - ✅ Или получать с сервера (опционально)

---

#### **ШАГ 2.3: Оптимизировать найденные файлы (20 минут)**

**Если найден большой JSON файл (> 1 MB):**

**Пример оптимизации:**
```swift
// ❌ ПЛОХО - большой JSON файл в приложении:
let config = try JSONDecoder().decode(Config.self, from: Bundle.main.data(forResource: "config")!)

// ✅ ХОРОШО - получать с сервера:
let config = try await apiService.getConfig()
cacheManager.store(config, key: "config", ttl: 3600) // Кэш на 1 час
```

---

## 📋 ЗАДАЧА 3: ПРОФИЛИРОВАНИЕ ПРОИЗВОДИТЕЛЬНОСТИ (1-2 часа)

### ✅ **ЧТО СДЕЛАТЬ:**

#### **ШАГ 3.1: Создать PerformanceProfiler (30 минут)**

**Создать файл:** `Core/Performance/PerformanceProfiler.swift`

**Код:**
```swift
import Foundation
import os.signpost

/// Профилирование производительности
class PerformanceProfiler {
    static let shared = PerformanceProfiler()
    
    private let log = OSLog(subsystem: "com.aladdin.performance", category: "performance")
    
    func measureAsync<T>(_ name: String, block: () async throws -> T) async rethrows -> T {
        let signpostID = OSSignpostID(log: log)
        os_signpost(.begin, log: log, name: name, signpostID: signpostID)
        defer {
            os_signpost(.end, log: log, name: name, signpostID: signpostID)
        }
        return try await block()
    }
    
    func measure<T>(_ name: String, block: () throws -> T) rethrows -> T {
        let signpostID = OSSignpostID(log: log)
        os_signpost(.begin, log: log, name: name, signpostID: signpostID)
        defer {
            os_signpost(.end, log: log, name: name, signpostID: signpostID)
        }
        return try block()
    }
}
```

**Добавить в проект:**
1. Создать папку `Core/Performance/`
2. Создать файл `PerformanceProfiler.swift`
3. Добавить в Xcode проект

---

#### **ШАГ 3.2: Добавить профилирование в ключевые места (30 минут)**

**Добавить в:**

**1. AnalyticsViewModel.swift:**
```swift
func load() async {
    await PerformanceProfiler.shared.measureAsync("LoadAnalytics") {
        // Существующий код
    }
}
```

**2. NetworkManager.swift:**
```swift
func request(_ endpoint: String) async throws -> Data {
    let startTime = Date()
    let data = try await performRequest(endpoint)
    let duration = Date().timeIntervalSince(startTime)
    
    if duration > 1.0 {
        print("⚠️ Медленный запрос: \(endpoint) - \(String(format: "%.2f", duration))s")
    }
    
    return data
}
```

---

#### **ШАГ 3.3: Проверить производительность в Instruments (30 минут)**

**Действия:**
1. Открыть проект в Xcode
2. Product → Profile (⌘I)
3. Выбрать **Time Profiler**
4. Запустить приложение
5. Проверить:
   - Время запуска (< 2 секунды)
   - Время переключения экранов (< 0.5 секунды)
   - Использование памяти (< 150 MB)

---

#### **ШАГ 3.4: Проверить батарею и CPU (30 минут)**

**Действия:**
1. Запустить приложение на реальном устройстве
2. Settings → Battery → Посмотреть использование батареи
3. Проверить:
   - Расход батареи (< 20%/час)
   - CPU нагрузка (< 10% в фоне)

---

## 📊 ИТОГОВАЯ ТАБЛИЦА ПРОВЕРОК

| Задача | Время | Статус | Приоритет |
|--------|-------|--------|-----------|
| **1.1 Проверить VPNManager** | 30 мин | ⚠️ Проверить | 🔴 Высокий |
| **1.2 Проверить PaymentQRViewModel** | 20 мин | ⚠️ Проверить | 🔴 Высокий |
| **1.3 Проверить ViewModels** | 30 мин | ⚠️ Проверить | 🟡 Средний |
| **2.1 Проверить JSON файлы** | 20 мин | ✅ Проверить | 🟡 Средний |
| **2.2 Проверить локализацию** | 20 мин | ✅ Проверить | 🟢 Низкий |
| **2.3 Оптимизировать файлы** | 20 мин | ⚠️ Если нужно | 🟡 Средний |
| **3.1 Создать PerformanceProfiler** | 30 мин | ✅ Создать | 🟡 Средний |
| **3.2 Добавить профилирование** | 30 мин | ✅ Добавить | 🟡 Средний |
| **3.3 Проверить в Instruments** | 30 мин | ✅ Проверить | 🟢 Низкий |
| **3.4 Проверить батарею** | 30 мин | ✅ Проверить | 🟢 Низкий |

**ИТОГО:** 4-6 часов

---

## 🎯 КОНКРЕТНЫЕ ДЕЙСТВИЯ

### **ДЕНЬ 1 (2 часа):**

#### **Утро (1 час):**
1. ✅ **Задача 1.1** - Проверить VPNManager (30 минут)
2. ✅ **Задача 1.2** - Проверить PaymentQRViewModel (20 минут)
3. ✅ **Задача 1.3** - Проверить ViewModels (10 минут)

#### **День (1 час):**
1. ✅ **Задача 2.1** - Проверить JSON файлы (20 минут)
2. ✅ **Задача 2.2** - Проверить локализацию (20 минут)
3. ✅ **Задача 2.3** - Оптимизировать файлы (20 минут)

---

### **ДЕНЬ 2 (2 часа):**

#### **Утро (1 час):**
1. ✅ **Задача 3.1** - Создать PerformanceProfiler (30 минут)
2. ✅ **Задача 3.2** - Добавить профилирование (30 минут)

#### **День (1 час):**
1. ✅ **Задача 3.3** - Проверить в Instruments (30 минут)
2. ✅ **Задача 3.4** - Проверить батарею (30 минут)

---

## ✅ КРИТЕРИИ УСПЕХА

### **После проверки должно быть:**

1. ✅ **Нет постоянных запросов к API** чаще чем каждые 15 минут
2. ✅ **Нет больших JSON файлов** (> 1 MB) в приложении
3. ✅ **Время запуска** < 2 секунды
4. ✅ **Время переключения экранов** < 0.5 секунды
5. ✅ **Использование памяти** < 150 MB
6. ✅ **Расход батареи** < 20%/час
7. ✅ **CPU нагрузка** < 10% (в фоне)

---

## 🎯 ГОТОВО К ВЫПОЛНЕНИЮ!

**Все задачи можно выполнить в течение 2 дней (по 2 часа в день)**

**Начните с задачи 1 - это самое важное!** ✅

