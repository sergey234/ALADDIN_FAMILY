# 📋 ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ ДЛЯ 35 КОМПОНЕНТОВ

**Дата:** 2026-03-14  
**Статус:** ✅ Проверка завершена, план готов

---

## ✅ РЕЗУЛЬТАТЫ ПРОВЕРКИ

### **Что УЖЕ реализовано:**

1. **Роутеры с подключением к PostgreSQL (7):**
   - ✅ `dark_web_monitoring_router.py`
   - ✅ `identity_theft_protection_router.py`
   - ✅ `location_bubble_router.py`
   - ✅ `data_cleanup_router.py`
   - ✅ `anti_tracker_router.py`
   - ✅ `ai_categories_router.py`
   - ✅ `driving_reports_router.py`

2. **Таблицы в БД (7 компонентов):**
   - ✅ `dark_web_leaks`, `dark_web_scans`
   - ✅ `identity_theft_attempts`
   - ✅ `location_requests`
   - ✅ `data_cleanup_records`
   - ✅ `tracker_blocks`
   - ✅ `ai_category_reports`
   - ✅ `driving_reports` (предположительно)

3. **ViewModels с обработкой unauthorized (6):**
   - ✅ `DarkWebMonitoringViewModel.swift`
   - ✅ `IdentityTheftViewModel.swift`
   - ✅ `AICategoriesViewModel.swift`
   - ✅ `DrivingReportsViewModel.swift`
   - ✅ `PrivacyReportsViewModel.swift`
   - ✅ `AnalyticsViewModel.swift` (частично - есть обработка, но нет проверки токена)

---

## ❌ ЧТО НЕ РЕАЛИЗОВАНО

### **Роутеры БЕЗ подключения к БД (20):**

1. ❌ `crash_detection_router.py` - использует in-memory хранилище
2. ❌ `roadside_assistance_router.py` - использует агент, нет БД
3. ❌ `parental_control_router.py` - использует mock данные
4. ❌ `metrics_router.py` - нет БД
5. ❌ `subscription_sync_router.py` - использует SFM adapter, не PostgreSQL напрямую
6. ❌ `ai_assistant_router.py`
7. ❌ `app_settings_sync_router.py`
8. ❌ `crash_detection_sync_router.py`
9. ❌ `elderly_interface_sync_router.py`
10. ❌ `gamification_router.py`
11. ❌ `iot_router.py`
12. ❌ `notifications_router.py`
13. ❌ `offline_storage_sync_router.py`
14. ❌ `other_functions_sync_router.py`
15. ❌ `parental_control_sync_router.py`
16. ❌ `system_router.py`
17. ❌ `user_profile_sync_router.py`
18. ❌ `components_router.py` (общий роутер для всех компонентов)
19. ❌ `crash_detection_router_optimized.py`
20. ❌ И другие...

### **Таблицы в БД:**
- ❌ НЕТ таблиц для crash_detection
- ❌ НЕТ таблиц для roadside_assistance
- ❌ НЕТ таблиц для parental_control
- ❌ НЕТ таблиц для analytics (кроме общих)
- ❌ НЕТ таблиц для остальных 30 компонентов

### **ViewModels:**
- ❌ Большинство ViewModels НЕ имеют проверки токена `guard AppConfig.authToken != nil`
- ❌ Большинство ViewModels НЕ имеют обработки `NetworkError.unauthorized`

---

## 🎯 ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ

### **ЭТАП 1: ВЫСОКИЙ ПРИОРИТЕТ (5 компонентов с существующими роутерами)**

#### **1.1. crash_detection_agent**

**Текущее состояние:**
- ✅ Роутер существует: `crash_detection_router.py`
- ❌ Использует in-memory хранилище (`crash_detection_sessions: Dict`)
- ❌ Нет таблицы в БД
- ❌ Нет подключения к PostgreSQL

**Что нужно сделать:**

1. **Создать таблицу в БД:**
```sql
CREATE TABLE IF NOT EXISTS crash_detection_sessions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    session_id VARCHAR(255) NOT NULL,
    latitude DECIMAL(10, 8) NOT NULL,
    longitude DECIMAL(11, 8) NOT NULL,
    radius DECIMAL(10, 2) NOT NULL DEFAULT 500.0,
    active BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    last_activity TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, session_id)
);

CREATE TABLE IF NOT EXISTS crash_detection_alerts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    session_id VARCHAR(255) NOT NULL,
    latitude DECIMAL(10, 8) NOT NULL,
    longitude DECIMAL(11, 8) NOT NULL,
    severity VARCHAR(20) NOT NULL, -- critical, high, medium, low
    accelerometer_data JSONB,
    gyroscope_data JSONB,
    speed DECIMAL(10, 2),
    timestamp TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_crash_sessions_user_id ON crash_detection_sessions(user_id);
CREATE INDEX IF NOT EXISTS idx_crash_sessions_active ON crash_detection_sessions(active);
CREATE INDEX IF NOT EXISTS idx_crash_alerts_user_id ON crash_detection_alerts(user_id);
CREATE INDEX IF NOT EXISTS idx_crash_alerts_timestamp ON crash_detection_alerts(timestamp);
```

2. **Подключить роутер к PostgreSQL:**
   - Добавить `from app.database.database import get_db`
   - Добавить `from sqlalchemy.orm import Session`
   - Добавить `from sqlalchemy import text`
   - Заменить in-memory хранилище на SQL запросы
   - Добавить `db: Session = Depends(get_db)` в каждый endpoint

3. **Создать/обновить ViewModel:**
   - Проверить наличие `CrashDetectionViewModel.swift`
   - Добавить `guard AppConfig.authToken != nil`
   - Добавить обработку `NetworkError.unauthorized`

4. **Добавить локализацию:**
   - Добавить ключи ошибок в `LocalizationManager.swift`

---

#### **1.2. roadside_assistance_agent**

**Текущее состояние:**
- ✅ Роутер существует: `roadside_assistance_router.py`
- ❌ Использует агент напрямую, нет БД
- ❌ Нет таблицы в БД
- ❌ Нет подключения к PostgreSQL

**Что нужно сделать:**

1. **Создать таблицу в БД:**
```sql
CREATE TABLE IF NOT EXISTS roadside_assistance_requests (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    request_id VARCHAR(255) NOT NULL UNIQUE,
    problem_type VARCHAR(50) NOT NULL, -- towing, jump_start, tire_change, lockout, fuel_delivery
    latitude DECIMAL(10, 8) NOT NULL,
    longitude DECIMAL(11, 8) NOT NULL,
    address TEXT,
    description TEXT,
    vehicle_make VARCHAR(100),
    vehicle_model VARCHAR(100),
    vehicle_year INTEGER,
    vehicle_color VARCHAR(50),
    license_plate VARCHAR(50),
    vin VARCHAR(50),
    partner VARCHAR(100), -- rosgosstrah, alfastrahovanie, ingosstrah, reso
    status VARCHAR(50) NOT NULL DEFAULT 'pending', -- pending, in_progress, completed, cancelled
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_roadside_requests_user_id ON roadside_assistance_requests(user_id);
CREATE INDEX IF NOT EXISTS idx_roadside_requests_status ON roadside_assistance_requests(status);
CREATE INDEX IF NOT EXISTS idx_roadside_requests_created_at ON roadside_assistance_requests(created_at);
```

2. **Подключить роутер к PostgreSQL:**
   - Добавить `Depends(get_db)` в endpoints
   - Сохранять запросы в БД перед вызовом агента
   - Сохранять статусы обновлений

3. **Создать/обновить ViewModel:**
   - Проверить наличие ViewModel
   - Добавить обработку ошибок

---

#### **1.3. parental_control_bot**

**Текущее состояние:**
- ✅ Роутер существует: `parental_control_router.py`
- ❌ Использует mock данные (`_CHILDREN_STATS`, `_BYPASS_STATS`)
- ❌ Нет таблицы в БД
- ❌ Нет подключения к PostgreSQL

**Что нужно сделать:**

1. **Создать таблицы в БД:**
```sql
CREATE TABLE IF NOT EXISTS parental_control_stats (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    child_id UUID NOT NULL,
    child_name VARCHAR(255),
    websites_blocked INTEGER NOT NULL DEFAULT 0,
    apps_blocked INTEGER NOT NULL DEFAULT 0,
    search_queries_blocked INTEGER NOT NULL DEFAULT 0,
    active_filters INTEGER NOT NULL DEFAULT 0,
    today_usage VARCHAR(50), -- "1ч 22мин"
    today_limit VARCHAR(50), -- "2ч"
    remaining VARCHAR(50), -- "38мин"
    schedules_count INTEGER NOT NULL DEFAULT 0,
    current_location VARCHAR(255),
    geofences_count INTEGER NOT NULL DEFAULT 0,
    events_today INTEGER NOT NULL DEFAULT 0,
    sites_tracked INTEGER NOT NULL DEFAULT 0,
    apps_tracked INTEGER NOT NULL DEFAULT 0,
    contacts_tracked INTEGER NOT NULL DEFAULT 0,
    messages_monitored BOOLEAN NOT NULL DEFAULT FALSE,
    last_update TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, child_id)
);

CREATE TABLE IF NOT EXISTS parental_bypass_stats (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    child_id UUID NOT NULL,
    today INTEGER NOT NULL DEFAULT 0,
    week INTEGER NOT NULL DEFAULT 0,
    blocked INTEGER NOT NULL DEFAULT 0,
    incognito INTEGER NOT NULL DEFAULT 0,
    tor INTEGER NOT NULL DEFAULT 0,
    proxy INTEGER NOT NULL DEFAULT 0,
    message TEXT,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, child_id)
);

CREATE INDEX IF NOT EXISTS idx_parental_stats_user_id ON parental_control_stats(user_id);
CREATE INDEX IF NOT EXISTS idx_parental_stats_child_id ON parental_control_stats(child_id);
CREATE INDEX IF NOT EXISTS idx_bypass_stats_user_id ON parental_bypass_stats(user_id);
```

2. **Подключить роутер к PostgreSQL:**
   - Заменить mock данные на SQL запросы
   - Добавить `Depends(get_db)`

3. **Обновить ViewModel:**
   - `ParentalControlViewModel.swift` - добавить обработку ошибок

---

#### **1.4. subscription_manager**

**Текущее состояние:**
- ✅ Роутер существует: `subscription_sync_router.py`
- ✅ Таблица существует: `subscriptions` (в `setup_subscription_tables.sql`)
- ❌ Использует SFM adapter, не PostgreSQL напрямую
- ❌ Нет прямого подключения к БД через `Depends(get_db)`

**Что нужно сделать:**

1. **Проверить таблицы:**
   - ✅ `subscriptions` - уже существует
   - ✅ `usage_tracking` - уже существует
   - ✅ `audit_log` - уже существует

2. **Подключить роутер к PostgreSQL:**
   - Добавить `Depends(get_db)` как альтернативу SFM adapter
   - Использовать PostgreSQL для fallback, если SFM недоступен

3. **Обновить ViewModel:**
   - `TariffsViewModel.swift` - добавить обработку ошибок

---

#### **1.5. analytics_manager**

**Текущее состояние:**
- ✅ Роутер существует: `metrics_router.py`
- ❌ Нет таблицы в БД
- ❌ Нет подключения к PostgreSQL
- ✅ ViewModel существует: `AnalyticsViewModel.swift` (есть обработка unauthorized, но нет проверки токена)

**Что нужно сделать:**

1. **Создать таблицу в БД:**
```sql
CREATE TABLE IF NOT EXISTS analytics_metrics (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID,
    device_id VARCHAR(255) NOT NULL,
    app_version VARCHAR(50),
    platform VARCHAR(50), -- ios, android
    metric_type VARCHAR(50) NOT NULL, -- user_action, api_call, error, alert, health
    timestamp TIMESTAMP NOT NULL,
    action VARCHAR(255),
    parameters JSONB,
    endpoint VARCHAR(255),
    method VARCHAR(10),
    response_time DECIMAL(10, 3),
    status_code INTEGER,
    success BOOLEAN,
    error_type VARCHAR(100),
    error_message TEXT,
    context JSONB,
    alert_id VARCHAR(255),
    alert_type VARCHAR(100),
    severity VARCHAR(50),
    message TEXT,
    status VARCHAR(50),
    uptime DECIMAL(10, 2),
    active_components INTEGER,
    total_components INTEGER,
    issues TEXT[],
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_analytics_user_id ON analytics_metrics(user_id);
CREATE INDEX IF NOT EXISTS idx_analytics_device_id ON analytics_metrics(device_id);
CREATE INDEX IF NOT EXISTS idx_analytics_metric_type ON analytics_metrics(metric_type);
CREATE INDEX IF NOT EXISTS idx_analytics_timestamp ON analytics_metrics(timestamp);
```

2. **Подключить роутер к PostgreSQL:**
   - Добавить `Depends(get_db)`
   - Сохранять метрики в БД

3. **Обновить ViewModel:**
   - `AnalyticsViewModel.swift` - добавить `guard AppConfig.authToken != nil`

---

### **ЭТАП 2: СРЕДНИЙ ПРИОРИТЕТ (30 компонентов)**

#### **2.1. NetworkProtectionScreen (10 компонентов)**

**Компоненты:**
1. `crash_detection_agent` - ✅ В Этапе 1
2. `roadside_assistance_agent` - ✅ В Этапе 1
3. `emergency_response_bot` - ❌ Нет роутера
4. `emergency_event_manager` - ❌ Нет роутера
5. `phishing_protection_agent` - ❌ Нет роутера
6. `malware_detection_agent` - ❌ Нет роутера
7. `mobile_security_agent` - ❌ Нет роутера
8. `network_security_agent` - ❌ Нет роутера
9. `incident_response_agent` - ❌ Нет роутера
10. `password_security_agent` - ❌ Нет роутера

**План для каждого:**
1. Создать роутер (если нет)
2. Создать таблицу в БД
3. Подключить к PostgreSQL
4. Создать/обновить ViewModel
5. Добавить локализацию

---

#### **2.2. ParentalControlScreen (5 компонентов)**

**Компоненты:**
1. `self_harm_detection_agent` - ❌ Нет роутера
2. `grooming_detection_agent` - ❌ Нет роутера
3. `online_predators_agent` - ❌ Нет роутера
4. `psychological_support_agent` - ❌ Нет роутера
5. `parental_control_bot` - ✅ В Этапе 1

**План:** Аналогично Этапу 1

---

#### **2.3. AdvancedProtectionSettingsScreen (13 компонентов)**

**Компоненты:**
1. `telegram_security_bot` - ❌ Нет роутера
2. `whatsapp_security_bot` - ❌ Нет роутера
3. `instagram_security_bot` - ❌ Нет роутера
4. `max_messenger_security_bot` - ❌ Нет роутера
5. `gaming_security_bot` - ❌ Нет роутера
6. `browser_security_bot` - ❌ Нет роутера
7. `location_bubble_agent` - ✅ УЖЕ РЕАЛИЗОВАН
8. `personal_data_cleanup_agent` - ✅ УЖЕ РЕАЛИЗОВАН
9. `anti_tracker_agent` - ✅ УЖЕ РЕАЛИЗОВАН
10. `dark_web_monitoring_agent` - ✅ УЖЕ РЕАЛИЗОВАН
11. `russian_identity_theft_protection_agent` - ✅ УЖЕ РЕАЛИЗОВАН
12. `ai_categories_agent` - ✅ УЖЕ РЕАЛИЗОВАН
13. `driving_reports_agent` - ✅ УЖЕ РЕАЛИЗОВАН

**План:** Для 6 новых компонентов - аналогично Этапу 1

---

#### **2.4. SettingsScreen (5 менеджеров)**

**Компоненты:**
1. `emergency_contacts_manager` - ❌ Нет роутера
2. `emergency_notifications_manager` - ❌ Нет роутера
3. `voice_control_manager` - ❌ Нет роутера
4. `russian_child_protection_compliance_manager` - ❌ Нет роутера
5. `russian_data_protection_compliance_manager` - ❌ Нет роутера

**План:** Аналогично Этапу 1

---

#### **2.5. Улучшение существующих (9 менеджеров)**

**Компоненты:**
1. `family_notification_manager` - ❌ Нет роутера
2. `smart_notification_manager` - ❌ Нет роутера
3. `child_interface_manager` - ❌ Нет роутера
4. `elderly_interface_manager` - ❌ Нет роутера
5. `subscription_manager` - ✅ В Этапе 1
6. `referral_manager` - ❌ Нет роутера
7. `qr_payment_manager` - ❌ Нет роутера
8. `analytics_manager` - ✅ В Этапе 1
9. `report_manager` - ❌ Нет роутера

**План:** Для 7 новых компонентов - аналогично Этапу 1

---

## 📊 ИТОГОВАЯ СТАТИСТИКА

| Категория | Количество | Статус |
|-----------|------------|--------|
| **Всего компонентов** | 42 | - |
| **Уже реализовано** | 7 | ✅ |
| **Этап 1 (высокий приоритет)** | 5 | ⏳ |
| **Этап 2 (средний приоритет)** | 30 | ⏳ |
| **Всего нужно сделать** | 35 | ❌ |

---

## 🚀 ПОРЯДОК ВЫПОЛНЕНИЯ

1. **Начать с Этапа 1** (5 компонентов с существующими роутерами)
2. **Создать миграцию** для всех таблиц Этапа 1
3. **Применить миграцию** на сервере
4. **Подключить роутеры** к PostgreSQL
5. **Обновить ViewModels**
6. **Протестировать** endpoints
7. **Повторить для Этапа 2**

---

**Следующий шаг:** Начать реализацию Этапа 1 с `crash_detection_agent`
