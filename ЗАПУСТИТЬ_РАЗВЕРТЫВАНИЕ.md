# 🚀 ЗАПУСК РАЗВЕРТЫВАНИЯ ALADDIN API GATEWAY

## ✅ Все готово! Выберите один из способов:

### Способ 1: Двойной клик (macOS)
**Просто дважды кликните на файл:**
```
Deploy_ALADDIN.command
```
Этот файл автоматически откроет терминал и выполнит развертывание.

### Способ 2: Inline скрипт (рекомендуется)
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
chmod +x deploy_inline.sh
./deploy_inline.sh
```

### Способ 3: Expect скрипт
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
chmod +x deploy_api_gateway_final.exp
./deploy_api_gateway_final.exp
```

### Способ 4: Bash скрипт
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
chmod +x deploy_now.sh
./deploy_now.sh
```

### Способ 5: Python скрипт
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
python3 deploy_python.py
```

### Способ 6: Автоматический выбор
```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS
chmod +x deploy_auto.sh
./deploy_auto.sh
```

### Способ 7: Ручное выполнение
Откройте файл `COMMANDS_TO_RUN.txt` и выполните команды по порядку.

---

## 📋 Что будет сделано:

1. ✅ Создан backup текущей версии
2. ✅ Загружены файлы на сервер:
   - `api_gateway_complete.py` (101 endpoints)
   - `sfm_adapter.py` (SFM адаптер)
3. ✅ Проверен синтаксис Python
4. ✅ Заменен `api_gateway.py` на новую версию
5. ✅ Перезапущен сервис API Gateway
6. ✅ Протестирован health endpoint

## 🔍 После развертывания проверьте:

```bash
curl http://149.154.65.180/api/health
curl https://aladdin-ai.ru/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "sfm_adapter": "available",
  "endpoints": 101,
  "groups": ["components", "security", "monitoring", "protection", "system"]
}
```

---

## ⚡ БЫСТРЫЙ СТАРТ:

**Самый простой способ - выполните одну команду:**

```bash
cd /Users/sergejhlystov/ALADDIN_NEW/ALADDIN_NEW/mobile_apps/ALADDIN_iOS && chmod +x deploy_inline.sh && ./deploy_inline.sh
```

Или просто дважды кликните на `Deploy_ALADDIN.command`

---

**Готово к запуску! 🚀**



