# 🛠️ ПЛАН ИСПРАВЛЕНИЯ API ЭНДПОИНТОВ (ОСТАВШИЕСЯ 13.5%)

## 📊 ОБЗОР ПРОБЛЕМ
*   **Всего проверено:** 260 эндпоинтов
*   **❌ Не найдены (404):** 30 (11.5%)
*   **🔥 Ошибки сервера (500):** 5 (2.0%)
*   **✅ Живые (200/422):** 225 (86.5%)

---

## 🔴 ЭТАП 1: ИСПРАВЛЕНИЕ СЕРВЕРНЫХ ОШИБОК (500 INTERNAL SERVER ERROR)
*Эти ошибки критичны, так как эндпоинт есть, но он не работает.*

| # | Эндпоинт | Проблема | Рекомендация для Server Dev |
|---|----------|----------|-----------------------------|
| 1 | `/api/components/disable/{id}` | Ошибка логики | Проверить доступность SFM (8003) и права на процесс |
| 2 | `/api/components/enable/{id}` | Ошибка логики | Проверить инициализацию компонента в registry |
| 3 | `/api/components/restart/{id}` | Ошибка логики | Проверить наличие функции restart в SFM Core |
| 4 | `/api/components/update/{id}` | Ошибка логики | Проверить валидацию версии компонента |
| 5 | `/api/system/backup` | Ошибка прав | Проверить права на запись в `/opt/aladdin-backend/backups/` |

---

## 🟡 ЭТАП 2: СИНХРОНИЗАЦИЯ ПУТЕЙ (404 NOT FOUND)
*Эндпоинты отсутствуют в роутинге сервера или имеют другие имена.*

### **1. Блок: Подписки и Платежи (Критично для бизнеса)**
*   **Проблема:** В iOS `AppConfig` используются пути `/api/subscription/...`, но сервер их не находит.
*   **Задачи:**
    - [ ] Добавить роут `/api/subscription/sync` (POST)
    - [ ] Добавить роут `/api/subscription/purchase-history` (POST)
    - [ ] Синхронизировать `/api/subscription/status/update` (POST)
    - [ ] Исправить `/api/payments/status/{id}` (GET)

### **2. Блок: Родительский контроль**
*   **Проблема:** Несоответствие путей `/api/parental-control/` (iOS) и серверных роутов.
*   **Задачи:**
    - [ ] Реализовать `/api/parental-control/settings` (POST)
    - [ ] Реализовать `/api/parental-control/time-limits` (POST)
    - [ ] Реализовать `/api/parental-control/app-blocks` (POST)
    - [ ] Реализовать `/api/parental-control/geofences` (POST)

### **3. Блок: Геймификация**
*   **Проблема:** Геймификация почти полностью отсутствует на стороне сервера.
*   **Задачи:**
    - [ ] Добавить роут `/api/gamification/balance` (POST)
    - [ ] Добавить `/api/gamification/tournaments/history` (GET)
    - [ ] Добавить `/api/gamification/tournaments/leaderboard` (GET)

### **4. Блок: Crash Detection**
*   **Проблема:** Отсутствуют методы настройки и оповещения.
*   **Задачи:**
    - [ ] Добавить `/api/crash-detection/setup` (POST)
    - [ ] Добавить `/api/crash-detection/alert` (POST)

---

## 🟢 ЭТАП 3: ОПТИМИЗАЦИЯ ПРОВЕРКИ (SMART TESTER)
*   **Задачи:**
    - [ ] Обновить плейсхолдеры в `smart_api_tester.py` для более точной проверки 422 ошибок.
    - [ ] Добавить проверку заголовков `X-Request-ID` для отладки 500 ошибок на сервере.

---

## 📈 ЦЕЛЕВЫЕ ПОКАЗАТЕЛИ (ПОСЛЕ ВЫПОЛНЕНИЯ ПЛАНА)
*   **Success Coverage:** > 95%
*   **404 Errors:** < 2% (только зарезервированные на будущее)
*   **500 Errors:** 0%

**Дата составления плана:** 4 марта 2026 г.
**Ответственный:** AI Coding Assistant
