# 📋 ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ ВСЕХ 42 КОМПОНЕНТОВ

**Дата:** 2026-03-14  
**Статус:** ✅ План готов к реализации  
**Методология:** Общая архитектура + специфичные таблицы для бизнес-данных

---

## 🎯 КЛЮЧЕВОЕ ПОНИМАНИЕ АРХИТЕКТУРЫ

### ✅ **ПРАВИЛЬНАЯ ЛОГИКА:**

1. **Общая система для статусов и конфигураций:**
   - Общий роутер `/api/components/*` для всех 42 компонентов
   - Общая таблица `component_status` для статусов всех компонентов
   - Общая таблица `component_configuration` для конфигураций всех компонентов

2. **Специфичные роутеры и таблицы для бизнес-данных:**
   - Специфичные роутеры для работы с бизнес-данными (утечки, отчеты, запросы)
   - Специфичные таблицы для хранения бизнес-данных

3. **Интеграция:**
   - Все компоненты используют общий роутер для статусов
   - Компоненты с бизнес-данными имеют дополнительные роутеры для данных

---

## 📊 АНАЛИЗ ТЕКУЩЕГО СОСТОЯНИЯ

### ✅ **7 УЖЕ ПОДКЛЮЧЕННЫХ КОМПОНЕНТОВ:**

| Компонент | Роутер для данных | Таблица для данных | Использует component_status? |
|-----------|-------------------|-------------------|------------------------------|
| `dark_web_monitoring_agent` | ✅ `/api/reports/dark-web/*` | ✅ `dark_web_leaks`, `dark_web_scans` | ❌ НЕТ |
| `russian_identity_theft_protection_agent` | ✅ `/api/reports/identity/*` | ✅ `identity_theft_attempts` | ❌ НЕТ |
| `location_bubble_agent` | ✅ `/api/location-bubble/*` | ✅ `location_requests` | ❌ НЕТ |
| `personal_data_cleanup_agent` | ✅ `/api/data-cleanup/*` | ✅ `data_cleanup_records` | ❌ НЕТ |
| `anti_tracker_agent` | ✅ `/api/anti-tracker/*` | ✅ `tracker_blocks` | ❌ НЕТ |
| `ai_categories_agent` | ✅ `/api/ai-categories/*` | ✅ `ai_category_reports` | ❌ НЕТ |
| `driving_reports_agent` | ✅ `/api/driving-reports/*` | ✅ `driving_reports` | ❌ НЕТ |

**Проблема:** Эти компоненты имеют роутеры для бизнес-данных, но НЕ используют общую систему `component_status` для статусов.

**Решение:** Интегрировать их с общим роутером `/api/components/*` для статусов, сохранив специфичные роутеры для данных.

---

## 🎯 ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ

### **ЭТАП 1: ИНТЕГРАЦИЯ 7 УЖЕ ПОДКЛЮЧЕННЫХ КОМПОНЕНТОВ**

#### **Задача 1.1: Интегрировать 7 компонентов с общей системой component_status**

**Что нужно сделать:**

1. **Проверить, что общий роутер работает для этих компонентов:**
   ```bash
   # Тестировать endpoints:
   GET /api/components/status/dark_web_monitoring_agent
   POST /api/components/enable/dark_web_monitoring_agent
   POST /api/components/disable/dark_web_monitoring_agent
   ```

2. **Убедиться, что ViewModels используют общий роутер для статусов:**
   - `DarkWebMonitoringViewModel.swift` - использовать `/api/components/status/dark_web_monitoring_agent`
   - `IdentityTheftViewModel.swift` - использовать `/api/components/status/russian_identity_theft_protection_agent`
   - И т.д. для всех 7 компонентов

3. **Сохранить специфичные роутеры для бизнес-данных:**
   - `/api/reports/dark-web/*` - для утечек (оставить как есть)
   - `/api/reports/identity/*` - для попыток кражи (оставить как есть)
   - И т.д.

**Время:** 2-3 часа

---

### **ЭТАП 2: ПОДКЛЮЧЕНИЕ 5 КОМПОНЕНТОВ С РОУТЕРАМИ БЕЗ БД**

#### **Задача 2.1: crash_detection_agent**

**Текущее состояние:**
- ✅ Роутер существует: `crash_detection_router.py`
- ❌ Использует in-memory хранилище
- ❌ Нет подключения к `component_status`
- ❌ Нет таблицы для алертов

**Что нужно сделать:**

1. **Интегрировать с общим роутером для статусов:**
   - Использовать `/api/components/status/crash_detection_agent` для статусов
   - Удалить in-memory хранилище для статусов

2. **Создать специфичную таблицу для алертов:**
   ```sql
   CREATE TABLE IF NOT EXISTS crash_detection_alerts (
       id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
       user_id UUID NOT NULL,
       session_id VARCHAR(255) NOT NULL,
       latitude DECIMAL(10, 8) NOT NULL,
       longitude DECIMAL(11, 8) NOT NULL,
       severity VARCHAR(20) NOT NULL,
       accelerometer_data JSONB,
       gyroscope_data JSONB,
       speed DECIMAL(10, 2),
       timestamp TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
       created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
   );
   ```

3. **Обновить роутер для сохранения алертов в БД:**
   - Добавить `Depends(get_db)` в endpoints для алертов
   - Сохранять алерты в таблицу `crash_detection_alerts`

4. **Обновить ViewModel:**
   - Использовать `/api/components/status/crash_detection_agent` для статусов
   - Добавить обработку `unauthorized`

**Время:** 3-4 часа

---

#### **Задача 2.2: roadside_assistance_agent**

**Текущее состояние:**
- ✅ Роутер существует: `roadside_assistance_router.py`
- ❌ Использует агент напрямую, нет БД
- ❌ Нет подключения к `component_status`
- ❌ Нет таблицы для запросов

**Что нужно сделать:**

1. **Интегрировать с общим роутером для статусов:**
   - Использовать `/api/components/status/roadside_assistance_agent` для статусов

2. **Создать специфичную таблицу для запросов:**
   ```sql
   CREATE TABLE IF NOT EXISTS roadside_assistance_requests (
       id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
       user_id UUID NOT NULL,
       request_id VARCHAR(255) NOT NULL UNIQUE,
       problem_type VARCHAR(50) NOT NULL,
       latitude DECIMAL(10, 8) NOT NULL,
       longitude DECIMAL(11, 8) NOT NULL,
       address TEXT,
       description TEXT,
       vehicle_make VARCHAR(100),
       vehicle_model VARCHAR(100),
       vehicle_year INTEGER,
       vehicle_color VARCHAR(50),
       license_plate VARCHAR(50),
       vin VARCHAR(50),
       partner VARCHAR(100),
       status VARCHAR(50) NOT NULL DEFAULT 'pending',
       created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
       updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
   );
   ```

3. **Обновить роутер для сохранения запросов в БД:**
   - Добавить `Depends(get_db)` в endpoints для запросов
   - Сохранять запросы в таблицу `roadside_assistance_requests`

4. **Обновить ViewModel:**
   - Использовать `/api/components/status/roadside_assistance_agent` для статусов
   - Добавить обработку `unauthorized`

**Время:** 3-4 часа

---

#### **Задача 2.3: parental_control_bot**

**Текущее состояние:**
- ✅ Роутер существует: `parental_control_router.py`
- ❌ Использует mock данные
- ❌ Нет подключения к `component_status`
- ❌ Нет таблиц для статистики

**Что нужно сделать:**

1. **Интегрировать с общим роутером для статусов:**
   - Использовать `/api/components/status/parental_control_bot` для статусов

2. **Создать специфичные таблицы для статистики:**
   ```sql
   CREATE TABLE IF NOT EXISTS parental_control_stats (
       id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
       user_id UUID NOT NULL,
       child_id UUID NOT NULL,
       child_name VARCHAR(255),
       websites_blocked INTEGER NOT NULL DEFAULT 0,
       apps_blocked INTEGER NOT NULL DEFAULT 0,
       search_queries_blocked INTEGER NOT NULL DEFAULT 0,
       active_filters INTEGER NOT NULL DEFAULT 0,
       today_usage VARCHAR(50),
       today_limit VARCHAR(50),
       remaining VARCHAR(50),
       schedules_count INTEGER NOT NULL DEFAULT 0,
       current_location VARCHAR(255),
       geofences_count INTEGER NOT NULL DEFAULT 0,
       events_today INTEGER NOT NULL DEFAULT 0,
       sites_tracked INTEGER NOT NULL DEFAULT 0,
       apps_tracked INTEGER NOT NULL DEFAULT 0,
       contacts_tracked INTEGER NOT NULL DEFAULT 0,
       messages_monitored BOOLEAN NOT NULL DEFAULT FALSE,
       last_update TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
       created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
       UNIQUE(user_id, child_id)
   );

   CREATE TABLE IF NOT EXISTS parental_bypass_stats (
       id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
       user_id UUID NOT NULL,
       child_id UUID NOT NULL,
       today INTEGER NOT NULL DEFAULT 0,
       week INTEGER NOT NULL DEFAULT 0,
       blocked INTEGER NOT NULL DEFAULT 0,
       incognito INTEGER NOT NULL DEFAULT 0,
       tor INTEGER NOT NULL DEFAULT 0,
       proxy INTEGER NOT NULL DEFAULT 0,
       message TEXT,
       created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
       updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
       UNIQUE(user_id, child_id)
   );
   ```

3. **Обновить роутер для работы с БД:**
   - Заменить mock данные на SQL запросы
   - Добавить `Depends(get_db)` в endpoints

4. **Обновить ViewModel:**
   - Использовать `/api/components/status/parental_control_bot` для статусов
   - Добавить обработку `unauthorized`

**Время:** 4-5 часов

---

#### **Задача 2.4: subscription_manager**

**Текущее состояние:**
- ✅ Роутер существует: `subscription_sync_router.py`
- ✅ Таблица `subscriptions` существует
- ❌ Использует SFM adapter, не PostgreSQL напрямую
- ❌ Нет подключения к `component_status`

**Что нужно сделать:**

1. **Интегрировать с общим роутером для статусов:**
   - Использовать `/api/components/status/subscription_manager` для статусов

2. **Добавить fallback к PostgreSQL:**
   - Добавить `Depends(get_db)` как альтернативу SFM
   - Использовать PostgreSQL для fallback, если SFM недоступен

3. **Обновить ViewModel:**
   - Использовать `/api/components/status/subscription_manager` для статусов
   - Добавить обработку `unauthorized`

**Время:** 2-3 часа

---

#### **Задача 2.5: analytics_manager**

**Текущее состояние:**
- ✅ Роутер существует: `metrics_router.py`
- ❌ Нет таблицы для метрик
- ❌ Нет подключения к `component_status`

**Что нужно сделать:**

1. **Интегрировать с общим роутером для статусов:**
   - Использовать `/api/components/status/analytics_manager` для статусов

2. **Создать специфичную таблицу для метрик:**
   ```sql
   CREATE TABLE IF NOT EXISTS analytics_metrics (
       id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
       user_id UUID,
       device_id VARCHAR(255) NOT NULL,
       app_version VARCHAR(50),
       platform VARCHAR(50),
       metric_type VARCHAR(50) NOT NULL,
       timestamp TIMESTAMP NOT NULL,
       action VARCHAR(255),
       parameters JSONB,
       endpoint VARCHAR(255),
       method VARCHAR(10),
       response_time DECIMAL(10, 3),
       status_code INTEGER,
       success BOOLEAN,
       error_type VARCHAR(100),
       error_message TEXT,
       context JSONB,
       alert_id VARCHAR(255),
       alert_type VARCHAR(100),
       severity VARCHAR(50),
       message TEXT,
       status VARCHAR(50),
       uptime DECIMAL(10, 2),
       active_components INTEGER,
       total_components INTEGER,
       issues TEXT[],
       created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
   );
   ```

3. **Обновить роутер для сохранения метрик в БД:**
   - Добавить `Depends(get_db)` в endpoints
   - Сохранять метрики в таблицу `analytics_metrics`

4. **Обновить ViewModel:**
   - Использовать `/api/components/status/analytics_manager` для статусов
   - Добавить `guard AppConfig.authToken != nil`
   - Добавить обработку `unauthorized`

**Время:** 3-4 часа

**ИТОГО ЭТАП 2:** 15-20 часов (2-2.5 дня)

---

### **ЭТАП 3: ОБРАБОТКА UNAUTHORIZED ВО ВСЕХ VIEWMODELS**

#### **Задача 3.1: Добавить обработку unauthorized в ViewModels (30 компонентов)**

**Компоненты без обработки unauthorized:**

**NetworkProtectionScreen (8 компонентов):**
1. `NetworkProtectionViewModel.swift` - для 10 компонентов
2. `PhishingSettingsViewModel.swift`
3. `MalwareSettingsViewModel.swift`
4. `MobileSecuritySettingsViewModel.swift`
5. `NetworkSecuritySettingsViewModel.swift`
6. `IncidentResponseSettingsViewModel.swift`
7. `PasswordGeneratorSettingsViewModel.swift`

**ParentalControlScreen (4 компонента):**
8. `ParentalControlViewModel.swift` - для 5 компонентов

**AdvancedProtectionSettingsScreen (6 компонентов):**
9. `TelegramSecurityViewModel.swift` (если есть)
10. `WhatsAppSecurityViewModel.swift` (если есть)
11. `InstagramSecurityViewModel.swift` (если есть)
12. `MaxMessengerSecurityViewModel.swift` (если есть)
13. `GamingSecurityViewModel.swift` (если есть)
14. `BrowserSecurityViewModel.swift` (если есть)

**SettingsScreen (5 менеджеров):**
15. `EmergencyContactsViewModel.swift` (если есть)
16. `EmergencyNotificationsViewModel.swift`
17. `VoiceControlViewModel.swift` (если есть)
18. `RussianChildProtectionComplianceViewModel.swift` (если есть)
19. `RussianDataProtectionComplianceViewModel.swift` (если есть)

**Улучшение существующих (7 менеджеров):**
20. `FamilyNotificationViewModel.swift` (если есть)
21. `SmartNotificationViewModel.swift` (если есть)
22. `ChildInterfaceViewModel.swift` (если есть)
23. `ElderlyInterfaceViewModel.swift` (если есть)
24. `ReferralViewModel.swift` (если есть)
25. `QRPaymentViewModel.swift` (если есть)
26. `ReportViewModel.swift` (если есть)

**Что нужно сделать для каждого ViewModel:**

1. **Добавить проверку токена:**
   ```swift
   guard AppConfig.authToken != nil else {
       // Обработка отсутствия токена
       return
   }
   ```

2. **Добавить обработку unauthorized:**
   ```swift
   catch let networkError as NetworkError {
       if case .unauthorized(let message) = networkError {
           // Обработка unauthorized
           // Например: показать экран авторизации
       }
   }
   ```

3. **Добавить локализацию ошибок:**
   ```swift
   let errorMessage = LocalizationManager.shared.localizedString(
       forKey: "error.unauthorized",
       defaultValue: "Session expired. Please login again."
   )
   ```

**Время на ViewModel:** 15-30 минут  
**ИТОГО:** 6-10 часов (1 день)

---

### **ЭТАП 4: СОЗДАНИЕ СПЕЦИФИЧНЫХ ТАБЛИЦ ДЛЯ БИЗНЕС-ДАННЫХ**

#### **Задача 4.1: Создать специфичные таблицы для компонентов с данными**

**Таблицы, которые нужно создать:**

1. ✅ `crash_detection_alerts` - для crash_detection_agent
2. ✅ `roadside_assistance_requests` - для roadside_assistance_agent
3. ✅ `parental_control_stats` - для parental_control_bot
4. ✅ `parental_bypass_stats` - для parental_control_bot
5. ✅ `analytics_metrics` - для analytics_manager
6. ❓ `emergency_contacts` - для emergency_contacts_manager (если нужно)
7. ❓ `emergency_notifications` - для emergency_notifications_manager (если нужно)
8. ❓ `family_notifications` - для family_notification_manager (если нужно)
9. ❓ `smart_notifications` - для smart_notification_manager (если нужно)
10. ❓ `qr_payments` - для qr_payment_manager (если нужно)
11. ❓ `referral_codes` - для referral_manager (если нужно)
12. ❓ `referrals` - для referral_manager (если нужно)

**Что нужно сделать:**

1. **Создать SQL миграцию:**
   ```sql
   -- Файл: migrations/create_component_data_tables.sql
   -- Содержит все CREATE TABLE для специфичных таблиц
   ```

2. **Применить миграцию на сервере:**
   ```bash
   psql -U postgres -d aladdin_db -f migrations/create_component_data_tables.sql
   ```

3. **Проверить создание таблиц:**
   ```sql
   \dt  -- Список всех таблиц
   ```

**Время:** 2-3 часа

---

## 📋 TODO ЛИСТ ДЛЯ ОТСЛЕЖИВАНИЯ

### **ЭТАП 1: ИНТЕГРАЦИЯ 7 УЖЕ ПОДКЛЮЧЕННЫХ КОМПОНЕНТОВ**

- [ ] **Задача 1.1.1:** Проверить работу общего роутера для `dark_web_monitoring_agent`
  - [ ] Тест: `GET /api/components/status/dark_web_monitoring_agent`
  - [ ] Тест: `POST /api/components/enable/dark_web_monitoring_agent`
  - [ ] Тест: `POST /api/components/disable/dark_web_monitoring_agent`

- [ ] **Задача 1.1.2:** Проверить работу общего роутера для `russian_identity_theft_protection_agent`
  - [ ] Тест: `GET /api/components/status/russian_identity_theft_protection_agent`
  - [ ] Тест: `POST /api/components/enable/russian_identity_theft_protection_agent`
  - [ ] Тест: `POST /api/components/disable/russian_identity_theft_protection_agent`

- [ ] **Задача 1.1.3:** Проверить работу общего роутера для `location_bubble_agent`
  - [ ] Тест: `GET /api/components/status/location_bubble_agent`
  - [ ] Тест: `POST /api/components/enable/location_bubble_agent`
  - [ ] Тест: `POST /api/components/disable/location_bubble_agent`

- [ ] **Задача 1.1.4:** Проверить работу общего роутера для `personal_data_cleanup_agent`
  - [ ] Тест: `GET /api/components/status/personal_data_cleanup_agent`
  - [ ] Тест: `POST /api/components/enable/personal_data_cleanup_agent`
  - [ ] Тест: `POST /api/components/disable/personal_data_cleanup_agent`

- [ ] **Задача 1.1.5:** Проверить работу общего роутера для `anti_tracker_agent`
  - [ ] Тест: `GET /api/components/status/anti_tracker_agent`
  - [ ] Тест: `POST /api/components/enable/anti_tracker_agent`
  - [ ] Тест: `POST /api/components/disable/anti_tracker_agent`

- [ ] **Задача 1.1.6:** Проверить работу общего роутера для `ai_categories_agent`
  - [ ] Тест: `GET /api/components/status/ai_categories_agent`
  - [ ] Тест: `POST /api/components/enable/ai_categories_agent`
  - [ ] Тест: `POST /api/components/disable/ai_categories_agent`

- [ ] **Задача 1.1.7:** Проверить работу общего роутера для `driving_reports_agent`
  - [ ] Тест: `GET /api/components/status/driving_reports_agent`
  - [ ] Тест: `POST /api/components/enable/driving_reports_agent`
  - [ ] Тест: `POST /api/components/disable/driving_reports_agent`

- [ ] **Задача 1.2:** Обновить ViewModels для использования общего роутера
  - [ ] `DarkWebMonitoringViewModel.swift` - использовать `/api/components/status/dark_web_monitoring_agent`
  - [ ] `IdentityTheftViewModel.swift` - использовать `/api/components/status/russian_identity_theft_protection_agent`
  - [ ] `LocationBubbleViewModel.swift` - использовать `/api/components/status/location_bubble_agent`
  - [ ] `DataCleanupViewModel.swift` - использовать `/api/components/status/personal_data_cleanup_agent`
  - [ ] `AntiTrackerViewModel.swift` - использовать `/api/components/status/anti_tracker_agent`
  - [ ] `AICategoriesViewModel.swift` - использовать `/api/components/status/ai_categories_agent`
  - [ ] `DrivingReportsViewModel.swift` - использовать `/api/components/status/driving_reports_agent`

---

### **ЭТАП 2: ПОДКЛЮЧЕНИЕ 5 КОМПОНЕНТОВ С РОУТЕРАМИ БЕЗ БД**

- [ ] **Задача 2.1: crash_detection_agent**
  - [ ] Создать таблицу `crash_detection_alerts`
  - [ ] Обновить роутер для использования `component_status` (удалить in-memory)
  - [ ] Добавить `Depends(get_db)` в endpoints для алертов
  - [ ] Сохранять алерты в таблицу `crash_detection_alerts`
  - [ ] Обновить `NetworkProtectionViewModel.swift` для использования `/api/components/status/crash_detection_agent`
  - [ ] Добавить обработку `unauthorized` в ViewModel
  - [ ] Протестировать endpoints

- [ ] **Задача 2.2: roadside_assistance_agent**
  - [ ] Создать таблицу `roadside_assistance_requests`
  - [ ] Обновить роутер для использования `component_status`
  - [ ] Добавить `Depends(get_db)` в endpoints для запросов
  - [ ] Сохранять запросы в таблицу `roadside_assistance_requests`
  - [ ] Обновить `NetworkProtectionViewModel.swift` для использования `/api/components/status/roadside_assistance_agent`
  - [ ] Добавить обработку `unauthorized` в ViewModel
  - [ ] Протестировать endpoints

- [ ] **Задача 2.3: parental_control_bot**
  - [ ] Создать таблицы `parental_control_stats` и `parental_bypass_stats`
  - [ ] Обновить роутер для использования `component_status`
  - [ ] Заменить mock данные на SQL запросы
  - [ ] Добавить `Depends(get_db)` в endpoints
  - [ ] Обновить `ParentalControlViewModel.swift` для использования `/api/components/status/parental_control_bot`
  - [ ] Добавить обработку `unauthorized` в ViewModel
  - [ ] Протестировать endpoints

- [ ] **Задача 2.4: subscription_manager**
  - [ ] Проверить существование таблицы `subscriptions`
  - [ ] Добавить `Depends(get_db)` как fallback к SFM
  - [ ] Обновить роутер для использования `component_status`
  - [ ] Обновить `TariffsViewModel.swift` для использования `/api/components/status/subscription_manager`
  - [ ] Добавить обработку `unauthorized` в ViewModel
  - [ ] Протестировать endpoints

- [ ] **Задача 2.5: analytics_manager**
  - [ ] Создать таблицу `analytics_metrics`
  - [ ] Обновить роутер для использования `component_status`
  - [ ] Добавить `Depends(get_db)` в endpoints
  - [ ] Сохранять метрики в таблицу `analytics_metrics`
  - [ ] Обновить `AnalyticsViewModel.swift` для использования `/api/components/status/analytics_manager`
  - [ ] Добавить `guard AppConfig.authToken != nil`
  - [ ] Добавить обработку `unauthorized` в ViewModel
  - [ ] Протестировать endpoints

---

### **ЭТАП 3: ОБРАБОТКА UNAUTHORIZED ВО ВСЕХ VIEWMODELS**

- [ ] **Задача 3.1: NetworkProtectionScreen ViewModels**
  - [ ] `NetworkProtectionViewModel.swift` - добавить обработку unauthorized для всех 10 компонентов
  - [ ] `PhishingSettingsViewModel.swift` - добавить обработку unauthorized
  - [ ] `MalwareSettingsViewModel.swift` - добавить обработку unauthorized
  - [ ] `MobileSecuritySettingsViewModel.swift` - добавить обработку unauthorized
  - [ ] `NetworkSecuritySettingsViewModel.swift` - добавить обработку unauthorized
  - [ ] `IncidentResponseSettingsViewModel.swift` - добавить обработку unauthorized
  - [ ] `PasswordGeneratorSettingsViewModel.swift` - добавить обработку unauthorized

- [ ] **Задача 3.2: ParentalControlScreen ViewModels**
  - [ ] `ParentalControlViewModel.swift` - добавить обработку unauthorized для всех 5 компонентов

- [ ] **Задача 3.3: AdvancedProtectionSettingsScreen ViewModels**
  - [ ] Проверить существование ViewModels для мессенджеров
  - [ ] Добавить обработку unauthorized в существующие ViewModels

- [ ] **Задача 3.4: SettingsScreen ViewModels**
  - [ ] `EmergencyNotificationsViewModel.swift` - добавить обработку unauthorized
  - [ ] Проверить и обновить другие ViewModels

- [ ] **Задача 3.5: Улучшение существующих ViewModels**
  - [ ] Проверить существование ViewModels для менеджеров
  - [ ] Добавить обработку unauthorized в существующие ViewModels

---

### **ЭТАП 4: СОЗДАНИЕ СПЕЦИФИЧНЫХ ТАБЛИЦ**

- [ ] **Задача 4.1: Создать SQL миграцию**
  - [ ] Создать файл `migrations/create_component_data_tables.sql`
  - [ ] Добавить CREATE TABLE для `crash_detection_alerts`
  - [ ] Добавить CREATE TABLE для `roadside_assistance_requests`
  - [ ] Добавить CREATE TABLE для `parental_control_stats`
  - [ ] Добавить CREATE TABLE для `parental_bypass_stats`
  - [ ] Добавить CREATE TABLE для `analytics_metrics`
  - [ ] Добавить индексы для всех таблиц

- [ ] **Задача 4.2: Применить миграцию на сервере**
  - [ ] Подключиться к серверу
  - [ ] Применить миграцию: `psql -U postgres -d aladdin_db -f migrations/create_component_data_tables.sql`
  - [ ] Проверить создание таблиц: `\dt`

- [ ] **Задача 4.3: Проверить работу таблиц**
  - [ ] Тест: INSERT в каждую таблицу
  - [ ] Тест: SELECT из каждой таблицы
  - [ ] Тест: UPDATE в каждой таблице

---

## ⏱️ ОЦЕНКА ВРЕМЕНИ

| Этап | Задач | Время (часы) | Время (дни) |
|------|-------|--------------|-------------|
| **Этап 1: Интеграция 7 компонентов** | 8 | 2-3 | 0.5 |
| **Этап 2: Подключение 5 компонентов** | 5 | 15-20 | 2-2.5 |
| **Этап 3: Обработка unauthorized** | 5 | 6-10 | 1 |
| **Этап 4: Создание таблиц** | 3 | 2-3 | 0.5 |
| **ИТОГО** | **21** | **25-36** | **4-4.5** |

---

## ✅ КРИТЕРИИ УСПЕХА

### **Для каждого компонента:**

1. ✅ Статус компонента хранится в общей таблице `component_status`
2. ✅ Конфигурация компонента хранится в общей таблице `component_configuration`
3. ✅ ViewModel использует общий роутер `/api/components/status/{component_id}` для статусов
4. ✅ ViewModel обрабатывает `NetworkError.unauthorized`
5. ✅ Специфичные данные (если есть) хранятся в специфичных таблицах
6. ✅ Все endpoints протестированы

---

## 🎯 ПРИОРИТЕТЫ

1. **Высокий приоритет:** Этап 1 (интеграция 7 компонентов) - быстро даст результат
2. **Высокий приоритет:** Этап 2 (подключение 5 компонентов) - критично для функциональности
3. **Средний приоритет:** Этап 3 (обработка unauthorized) - важно для UX
4. **Низкий приоритет:** Этап 4 (создание таблиц) - можно делать параллельно

---

## 📝 ЗАМЕТКИ

1. **Общая архитектура правильная:** Не нужно создавать отдельные роутеры для каждого компонента
2. **Специфичные таблицы нужны только для бизнес-данных:** Статусы и конфигурации хранятся в общих таблицах
3. **SFM остается центральной системой:** Все компоненты работают через SFM для выполнения функций
4. **Общий роутер масштабируемый:** Легко добавить новые компоненты без создания новых роутеров

---

**Статус плана:** ✅ ГОТОВ К РЕАЛИЗАЦИИ  
**Следующий шаг:** Начать с Этапа 1 - интеграция 7 уже подключенных компонентов
