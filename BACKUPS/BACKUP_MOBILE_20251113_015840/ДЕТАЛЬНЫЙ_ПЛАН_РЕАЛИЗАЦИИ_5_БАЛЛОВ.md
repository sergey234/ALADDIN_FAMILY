# 🎯 ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ ДОСТИЖЕНИЯ 5.0/5.0

**Дата создания:** 2025-11-06  
**Текущая оценка:** 4.42 / 5.0 ⭐⭐⭐⭐  
**Целевая оценка:** 5.0 / 5.0 ⭐⭐⭐⭐⭐  
**Время реализации:** 20-30 часов

---

## 📋 ОБЩИЙ ПЛАН

### ✅ **ПРИОРИТЕТЫ:**
1. 🔴 **ВЫСОКИЙ:** SSL Pinning (2-3ч)
2. 🟡 **СРЕДНИЙ:** JWT токены (3-4ч)
3. 🟡 **СРЕДНИЙ:** Антивирус (4-5ч)
4. 🟡 **СРЕДНИЙ:** Родительский контроль (3-4ч)
5. 🟡 **СРЕДНИЙ:** Офлайн защита (2-3ч)
6. 🟡 **СРЕДНИЙ:** Безопасное кэширование (2-3ч)
7. 🟢 **НИЗКИЙ:** IoT защита (4-5ч)

---

## 1️⃣ SSL PINNING ⭐⭐⭐⭐ → ⭐⭐⭐⭐⭐

**Время:** 2-3 часа  
**Приоритет:** 🔴 ВЫСОКИЙ  
**Файлы для изменения:**
- `Core/Network/NetworkManager.swift`
- Добавить сертификаты в Bundle проекта

### ✅ **ШАГ 1: Добавить сертификаты в Bundle (30 минут)**

**Действия:**
1. Открыть Xcode проект
2. Перетащить файлы сертификатов в проект:
   - `ALADDIN/Certificates/aladdin_cert.cer`
   - `ALADDIN/Certificates/aladdin_cert_backup.cer`
3. Убедиться, что сертификаты добавлены в Target Membership
4. Проверить, что файлы видны в Bundle

**Проверка:**
```swift
// В NetworkManager.swift добавить проверку:
if let path = Bundle.main.path(forResource: "aladdin_cert", ofType: "cer") {
    print("✅ Сертификат найден: \(path)")
} else {
    print("❌ Сертификат не найден в Bundle!")
}
```

### ✅ **ШАГ 2: Обновить pinnedDomains (15 минут)**

**Файл:** `Core/Network/NetworkManager.swift`  
**Строки:** 46-49

**Было:**
```swift
self.pinnedDomains = Set([
    "api.aladdin.com",
    "api.example.com",
    "localhost"
])
```

**Должно быть:**
```swift
self.pinnedDomains = Set([
    "api.aladdin.family",  // Основной API
    "vpn.aladdin.family",  // VPN сервер
    "cdn.aladdin.family"   // CDN сервер
])
```

### ✅ **ШАГ 3: Добавить fallback механизм (45 минут)**

**Файл:** `Core/Network/NetworkManager.swift`  
**Метод:** `validateServerCertificate(_:for:)`

**Добавить:**
```swift
private func validateServerCertificate(_ serverTrust: SecTrust, for host: String) -> Bool {
    guard shouldPinCertificate(for: host) else {
        print("🔓 SSL Pinning: Домен \(host) не требует SSL Pinning")
        return true
    }
    
    // Проверяем, что у нас есть закрепленные сертификаты
    guard !pinnedCertificates.isEmpty else {
        print("⚠️ SSL Pinning: Нет закрепленных сертификатов - используем стандартную проверку")
        // FALLBACK: Используем стандартную проверку сертификата
        return performStandardCertificateValidation(serverTrust)
    }
    
    // ... существующий код проверки ...
}

private func performStandardCertificateValidation(_ serverTrust: SecTrust) -> Bool {
    var secresult = SecTrustResultType.invalid
    let status = SecTrustEvaluate(serverTrust, &secresult)
    
    if status == errSecSuccess {
        if secresult == .unspecified || secresult == .proceed {
            print("✅ Стандартная проверка сертификата успешна")
            return true
        }
    }
    
    print("❌ Стандартная проверка сертификата не прошла")
    return false
}
```

### ✅ **ШАГ 4: Протестировать на реальном сервере (30 минут)**

**Действия:**
1. Запустить приложение на симуляторе
2. Выполнить API запрос к реальному серверу
3. Проверить логи SSL Pinning
4. Убедиться, что сертификаты загружаются
5. Проверить fallback механизм

**Ожидаемый результат:** ⭐⭐⭐⭐⭐ (5/5)

---

## 2️⃣ JWT ТОКЕНЫ ⭐⭐⭐⭐ → ⭐⭐⭐⭐⭐

**Время:** 3-4 часа  
**Приоритет:** 🟡 СРЕДНИЙ  
**Файлы для изменения:**
- `Core/Network/NetworkManager.swift`
- `Core/Network/APIService.swift`
- `Core/Security/KeychainManager.swift`
- Создать новый файл: `Core/Security/JWTTokenManager.swift`

### ✅ **ШАГ 1: Создать JWTTokenManager (60 минут)**

**Новый файл:** `Core/Security/JWTTokenManager.swift`

```swift
import Foundation
import Security

/// 🔐 JWT Token Manager
/// Управление JWT токенами: проверка истечения, автоматическое обновление
class JWTTokenManager {
    static let shared = JWTTokenManager()
    
    private let keychainManager = KeychainManager.shared
    
    private init() {}
    
    // MARK: - Token Expiration Check
    
    /// Проверяет, истёк ли токен
    func isTokenExpired(_ token: String) -> Bool {
        guard let payload = decodeJWTPayload(token) else {
            return true // Если не можем декодировать, считаем истёкшим
        }
        
        guard let exp = payload["exp"] as? TimeInterval else {
            return true // Если нет поля exp, считаем истёкшим
        }
        
        let expirationDate = Date(timeIntervalSince1970: exp)
        let isExpired = expirationDate < Date()
        
        if isExpired {
            print("⚠️ JWT Token истёк: \(expirationDate)")
        } else {
            let timeUntilExpiration = expirationDate.timeIntervalSinceNow
            print("✅ JWT Token действителен ещё \(Int(timeUntilExpiration)) секунд")
        }
        
        return isExpired
    }
    
    /// Декодирует JWT payload
    private func decodeJWTPayload(_ token: String) -> [String: Any]? {
        let parts = token.components(separatedBy: ".")
        guard parts.count == 3 else { return nil }
        
        let payload = parts[1]
        
        // Добавляем padding если нужно
        var base64 = payload
            .replacingOccurrences(of: "-", with: "+")
            .replacingOccurrences(of: "_", with: "/")
        
        let remainder = base64.count % 4
        if remainder > 0 {
            base64 = base64.padding(toLength: base64.count + 4 - remainder, withPad: "=", startingAt: 0)
        }
        
        guard let data = Data(base64Encoded: base64) else { return nil }
        
        return try? JSONSerialization.jsonObject(with: data) as? [String: Any]
    }
    
    // MARK: - Token Refresh
    
    /// Проверяет и обновляет токен если нужно
    func refreshTokenIfNeeded() async -> Bool {
        guard let accessToken: String = keychainManager.load(String.self, forKey: .authToken) else {
            print("❌ JWT: Access token не найден в Keychain")
            return false
        }
        
        // Проверяем, не истёк ли токен
        if !isTokenExpired(accessToken) {
            print("✅ JWT: Access token действителен, обновление не требуется")
            return true
        }
        
        print("🔄 JWT: Access token истёк, обновляем...")
        
        // Получаем refresh token
        guard let refreshToken: String = keychainManager.load(String.self, forKey: .refreshToken) else {
            print("❌ JWT: Refresh token не найден в Keychain")
            return false
        }
        
        // Обновляем токен через API
        return await refreshAccessToken(refreshToken: refreshToken)
    }
    
    /// Обновляет access token используя refresh token
    private func refreshAccessToken(refreshToken: String) async -> Bool {
        // TODO: Реализовать API запрос для обновления токена
        // Пример:
        // let response = await apiService.refreshToken(refreshToken: refreshToken)
        // if let newAccessToken = response.accessToken {
        //     keychainManager.save(newAccessToken, forKey: .authToken)
        //     return true
        // }
        
        print("⚠️ JWT: Refresh token механизм требует реализации API")
        return false
    }
}
```

### ✅ **ШАГ 2: Добавить API метод для refresh токена (30 минут)**

**Файл:** `Core/Network/APIService.swift`

**Добавить:**
```swift
// MARK: - Auth API Methods

func refreshToken(refreshToken: String) async throws -> RefreshTokenResponse {
    struct RefreshTokenRequest: Codable {
        let refresh_token: String
    }
    
    let request = RefreshTokenRequest(refresh_token: refreshToken)
    return try await networkManager.post(endpoint: "/auth/refresh", body: request)
}

struct RefreshTokenResponse: Codable {
    let access_token: String
    let refresh_token: String?
    let expires_in: TimeInterval?
}
```

### ✅ **ШАГ 3: Интегрировать в NetworkManager (60 минут)**

**Файл:** `Core/Network/NetworkManager.swift`

**Добавить перед каждым запросом:**
```swift
// В методе performRequest добавить проверку токена:
private func performRequest<T: Codable>(
    request: URLRequest,
    completion: @escaping (Result<T, Error>) -> Void
) {
    // Проверяем и обновляем токен если нужно
    Task {
        let tokenValid = await JWTTokenManager.shared.refreshTokenIfNeeded()
        
        if !tokenValid {
            completion(.failure(NetworkError.tokenExpired))
            return
        }
        
        // Выполняем запрос
        session.dataTask(with: request) { [weak self] data, response, error in
            // ... существующий код ...
            
            // Обработка 401 ошибки
            if let httpResponse = response as? HTTPURLResponse,
               httpResponse.statusCode == 401 {
                // Пытаемся обновить токен и повторить запрос
                Task {
                    let refreshed = await JWTTokenManager.shared.refreshTokenIfNeeded()
                    if refreshed {
                        // Повторяем запрос с новым токеном
                        self?.performRequest(request: request, completion: completion)
                    } else {
                        completion(.failure(NetworkError.tokenExpired))
                    }
                }
                return
            }
            
            // ... остальной код ...
        }.resume()
    }
}
```

### ✅ **ШАГ 4: Протестировать обновление токенов (30 минут)**

**Ожидаемый результат:** ⭐⭐⭐⭐⭐ (5/5)

---

## 3️⃣ АНТИВИРУС ⭐⭐⭐⭐ → ⭐⭐⭐⭐⭐

**Время:** 4-5 часов  
**Приоритет:** 🟡 СРЕДНИЙ  
**Файлы для изменения:**
- `Core/Antivirus/AntivirusManager.swift`
- Создать новый файл: `Core/Antivirus/LocalThreatDatabase.swift`

### ✅ **ШАГ 1: Создать локальную базу сигнатур (90 минут)**

**Новый файл:** `Core/Antivirus/LocalThreatDatabase.swift`

```swift
import Foundation

/// 🛡️ Local Threat Database
/// Локальная база сигнатур для офлайн сканирования
class LocalThreatDatabase {
    static let shared = LocalThreatDatabase()
    
    private var threatSignatures: [ThreatSignature] = []
    private let databaseFile = "threat_database.json"
    
    private init() {
        loadDatabase()
    }
    
    // MARK: - Database Management
    
    /// Загружает базу сигнатур
    private func loadDatabase() {
        guard let url = Bundle.main.url(forResource: "threat_database", withExtension: "json"),
              let data = try? Data(contentsOf: url),
              let signatures = try? JSONDecoder().decode([ThreatSignature].self, from: data) else {
            print("⚠️ LocalThreatDatabase: База сигнатур не найдена, используем пустую")
            threatSignatures = []
            return
        }
        
        threatSignatures = signatures
        print("✅ LocalThreatDatabase: Загружено \(signatures.count) сигнатур")
    }
    
    /// Обновляет базу сигнатур с сервера
    func updateDatabase() async {
        // TODO: Загрузить обновления с сервера
        print("🔄 LocalThreatDatabase: Обновление базы сигнатур...")
    }
    
    // MARK: - Threat Detection
    
    /// Проверяет файл на наличие угроз в локальной базе
    func scanFile(fileURL: URL) -> ThreatLevel {
        // Проверка по расширению
        let fileExtension = fileURL.pathExtension.lowercased()
        if let threat = threatSignatures.first(where: { $0.dangerousExtensions.contains(fileExtension) }) {
            print("⚠️ LocalThreatDatabase: Обнаружена угроза по расширению: \(threat.name)")
            return .suspicious
        }
        
        // Проверка по имени файла
        let fileName = fileURL.lastPathComponent.lowercased()
        if let threat = threatSignatures.first(where: { signature in
            signature.dangerousNames.contains { fileName.contains($0.lowercased()) }
        }) {
            print("⚠️ LocalThreatDatabase: Обнаружена угроза по имени: \(threat.name)")
            return .suspicious
        }
        
        return .safe
    }
}

struct ThreatSignature: Codable {
    let id: String
    let name: String
    let type: String
    let dangerousExtensions: [String]
    let dangerousNames: [String]
    let severity: String
}
```

### ✅ **ШАГ 2: Добавить офлайн режим сканирования (60 минут)**

**Файл:** `Core/Antivirus/AntivirusManager.swift`

**Добавить метод:**
```swift
/// Офлайн сканирование файла
func scanOffline(fileURL: URL) async -> ScanResult {
    log("🛡️ Офлайн сканирование: \(fileURL.lastPathComponent)")
    
    // Используем локальную базу сигнатур
    let localThreatLevel = LocalThreatDatabase.shared.scanFile(fileURL: fileURL)
    
    if localThreatLevel == .suspicious {
        let result = ScanResult(
            filePath: fileURL.path,
            threatLevel: .suspicious,
            detectedThreats: ["Обнаружено локальной базой сигнатур"],
            scanTime: Date(),
            checksum: nil
        )
        
        // Кэшируем результат
        cacheScanResult(result)
        
        return result
    }
    
    // Если локальная база не нашла угроз, возвращаем safe
    let result = ScanResult(
        filePath: fileURL.path,
        threatLevel: .safe,
        detectedThreats: [],
        scanTime: Date(),
        checksum: nil
    )
    
    cacheScanResult(result)
    return result
}
```

### ✅ **ШАГ 3: Добавить кэширование результатов (60 минут)**

**Файл:** `Core/Antivirus/AntivirusManager.swift`

**Добавить:**
```swift
// MARK: - Scan Result Caching

private var scanResultsCache: [String: ScanResult] = [:]
private let cacheManager = CacheManager.shared

/// Кэширует результат сканирования
private func cacheScanResult(_ result: ScanResult) {
    // Используем путь файла как ключ
    let cacheKey = "scan_\(result.filePath)"
    
    // Сохраняем в CacheManager
    cacheManager.store(result, forKey: cacheKey, ttl: 3600, priority: .normal)
    
    // Сохраняем в локальный кэш
    scanResultsCache[cacheKey] = result
    
    log("💾 Результат сканирования закэширован: \(cacheKey)")
}

/// Получает результат сканирования из кэша
func getCachedScanResult(fileURL: URL) -> ScanResult? {
    let cacheKey = "scan_\(fileURL.path)"
    
    // Проверяем локальный кэш
    if let cached = scanResultsCache[cacheKey] {
        log("💾 Результат найден в локальном кэше")
        return cached
    }
    
    // Проверяем CacheManager
    if let cached: ScanResult = cacheManager.retrieve(ScanResult.self, forKey: cacheKey) {
        log("💾 Результат найден в CacheManager")
        scanResultsCache[cacheKey] = cached
        return cached
    }
    
    return nil
}

/// Проверяет, изменился ли файл (по checksum)
private func hasFileChanged(fileURL: URL, cachedResult: ScanResult) -> Bool {
    // TODO: Вычислить checksum файла и сравнить с cachedResult.checksum
    // Если checksum отличается, файл изменился
    return true // Пока всегда считаем, что файл мог измениться
}
```

### ✅ **ШАГ 4: Оптимизировать производительность (30 минут)**

**Файл:** `Core/Antivirus/AntivirusManager.swift`

**Добавить параллельную обработку:**
```swift
/// Сканирует несколько файлов параллельно
func scanFiles(_ fileURLs: [URL]) async -> [ScanResult] {
    log("🛡️ Параллельное сканирование \(fileURLs.count) файлов")
    
    return await withTaskGroup(of: ScanResult.self) { group in
        for fileURL in fileURLs {
            group.addTask {
                // Сначала проверяем кэш
                if let cached = self.getCachedScanResult(fileURL: fileURL),
                   !self.hasFileChanged(fileURL: fileURL, cachedResult: cached) {
                    return cached
                }
                
                // Если нет в кэше или файл изменился, сканируем
                return await self.performFullScan(fileURL: fileURL)
            }
        }
        
        var results: [ScanResult] = []
        for await result in group {
            results.append(result)
        }
        
        return results
    }
}
```

**Ожидаемый результат:** ⭐⭐⭐⭐⭐ (5/5)

---

## 4️⃣ РОДИТЕЛЬСКИЙ КОНТРОЛЬ ⭐⭐⭐⭐ → ⭐⭐⭐⭐⭐

**Время:** 3-4 часа  
**Приоритет:** 🟡 СРЕДНИЙ  
**Файлы для изменения:**
- `Core/Managers/ParentalControlManager.swift`
- Создать новый файл: `Core/Managers/ParentalControlReportsManager.swift`

### ✅ **ШАГ 1: Создать ReportsManager (90 минут)**

**Новый файл:** `Core/Managers/ParentalControlReportsManager.swift`

```swift
import Foundation

/// 📊 Parental Control Reports Manager
/// Управление детальными отчётами родительского контроля
class ParentalControlReportsManager {
    static let shared = ParentalControlReportsManager()
    
    private let apiService = APIService.shared
    
    private init() {}
    
    // MARK: - Daily Reports
    
    /// Получает ежедневный отчёт
    func getDailyReport(childId: String, date: Date = Date()) async -> Result<DailyReport, Error> {
        let dateString = formatDate(date)
        
        // TODO: Реализовать API запрос
        // return await apiService.getDailyReport(childId: childId, date: dateString)
        
        // Пока возвращаем мок данные
        let report = DailyReport(
            date: date,
            totalScreenTime: 3600, // 1 час
            appsUsage: [
                AppUsage(appName: "Instagram", timeSpent: 1800, limit: 1800),
                AppUsage(appName: "TikTok", timeSpent: 1200, limit: 1200)
            ],
            websitesBlocked: 5,
            threatsBlocked: 2
        )
        
        return .success(report)
    }
    
    // MARK: - Weekly Reports
    
    /// Получает еженедельный отчёт
    func getWeeklyReport(childId: String, weekStartDate: Date = Date()) async -> Result<WeeklyReport, Error> {
        // TODO: Реализовать API запрос
        return .failure(NSError(domain: "NotImplemented", code: 0))
    }
    
    // MARK: - Monthly Reports
    
    /// Получает ежемесячный отчёт
    func getMonthlyReport(childId: String, month: Int, year: Int) async -> Result<MonthlyReport, Error> {
        // TODO: Реализовать API запрос
        return .failure(NSError(domain: "NotImplemented", code: 0))
    }
    
    // MARK: - Export Reports
    
    /// Экспортирует отчёт в PDF
    func exportReportToPDF(_ report: DailyReport) -> URL? {
        // TODO: Реализовать экспорт в PDF
        return nil
    }
    
    /// Экспортирует отчёт в CSV
    func exportReportToCSV(_ report: DailyReport) -> URL? {
        // TODO: Реализовать экспорт в CSV
        return nil
    }
    
    private func formatDate(_ date: Date) -> String {
        let formatter = DateFormatter()
        formatter.dateFormat = "yyyy-MM-dd"
        return formatter.string(from: date)
    }
}

// MARK: - Report Models

struct DailyReport: Codable {
    let date: Date
    let totalScreenTime: TimeInterval
    let appsUsage: [AppUsage]
    let websitesBlocked: Int
    let threatsBlocked: Int
}

struct AppUsage: Codable {
    let appName: String
    let timeSpent: TimeInterval
    let limit: TimeInterval
}

struct WeeklyReport: Codable {
    let weekStartDate: Date
    let dailyReports: [DailyReport]
    let totalScreenTime: TimeInterval
    let averageScreenTime: TimeInterval
}

struct MonthlyReport: Codable {
    let month: Int
    let year: Int
    let weeklyReports: [WeeklyReport]
    let totalScreenTime: TimeInterval
    let topApps: [AppUsage]
}
```

### ✅ **ШАГ 2: Добавить статистику использования (60 минут)**

**Файл:** `Core/Managers/ParentalControlManager.swift`

**Добавить методы:**
```swift
// MARK: - Usage Statistics

/// Получает статистику использования по приложениям
func getAppUsageStatistics(childId: String, period: StatisticsPeriod = .today) async -> Result<[AppUsageStatistics], Error> {
    // TODO: Реализовать API запрос
    return .failure(NSError(domain: "NotImplemented", code: 0))
}

/// Получает статистику использования по сайтам
func getWebsiteUsageStatistics(childId: String, period: StatisticsPeriod = .today) async -> Result<[WebsiteUsageStatistics], Error> {
    // TODO: Реализовать API запрос
    return .failure(NSError(domain: "NotImplemented", code: 0))
}

/// Получает графики активности
func getActivityCharts(childId: String, period: StatisticsPeriod = .today) async -> Result<ActivityCharts, Error> {
    // TODO: Реализовать API запрос
    return .failure(NSError(domain: "NotImplemented", code: 0))
}

enum StatisticsPeriod {
    case today
    case week
    case month
    case custom(startDate: Date, endDate: Date)
}

struct AppUsageStatistics: Codable {
    let appName: String
    let timeSpent: TimeInterval
    let sessions: Int
    let limit: TimeInterval
}

struct WebsiteUsageStatistics: Codable {
    let website: String
    let visits: Int
    let timeSpent: TimeInterval
    let isBlocked: Bool
}

struct ActivityCharts: Codable {
    let hourlyActivity: [HourlyActivity]
    let dailyActivity: [DailyActivity]
}

struct HourlyActivity: Codable {
    let hour: Int
    let timeSpent: TimeInterval
}

struct DailyActivity: Codable {
    let date: Date
    let timeSpent: TimeInterval
}
```

### ✅ **ШАГ 3: Добавить гибкие настройки (30 минут)**

**Файл:** `Core/Managers/ParentalControlManager.swift`

**Добавить:**
```swift
// MARK: - Flexible Settings

/// Применяет гибкие настройки по возрасту
func applyAgeBasedSettings(childId: String, age: Int) {
    // Настройки для разных возрастов
    switch age {
    case 0..<6:
        // Дошкольники: строгие ограничения
        applyContentBlocking(childId: childId, websiteBlocking: true, appBlocking: true, searchBlocking: true, safesearch: true)
        applyTimeRestrictions(childId: childId, dailyLimit: 3600, bedtimeStart: "20:00", bedtimeEnd: "07:00")
    case 6..<13:
        // Младшие школьники: умеренные ограничения
        applyContentBlocking(childId: childId, websiteBlocking: true, appBlocking: false, searchBlocking: true, safesearch: true)
        applyTimeRestrictions(childId: childId, dailyLimit: 7200, bedtimeStart: "21:00", bedtimeEnd: "07:00")
    case 13..<18:
        // Подростки: мягкие ограничения
        applyContentBlocking(childId: childId, websiteBlocking: false, appBlocking: false, searchBlocking: false, safesearch: true)
        applyTimeRestrictions(childId: childId, dailyLimit: 10800, bedtimeStart: "22:00", bedtimeEnd: "07:00")
    default:
        break
    }
}

/// Применяет настройки по приложениям
func applyAppBasedSettings(childId: String, appSettings: [AppSetting]) {
    // TODO: Реализовать настройки по приложениям
}

struct AppSetting: Codable {
    let appName: String
    let isBlocked: Bool
    let timeLimit: TimeInterval?
}
```

**Ожидаемый результат:** ⭐⭐⭐⭐⭐ (5/5)

---

## 5️⃣ ОФЛАЙН ЗАЩИТА ⭐⭐⭐⭐ → ⭐⭐⭐⭐⭐

**Время:** 2-3 часа  
**Приоритет:** 🟡 СРЕДНИЙ  
**Файлы для изменения:**
- `Core/Offline/OfflineManager.swift`
- `Core/Offline/OfflineStorageManager.swift`

### ✅ **ШАГ 1: Реализовать конфликт-резолюшн (60 минут)**

**Файл:** `Core/Offline/OfflineStorageManager.swift`

**Добавить:**
```swift
// MARK: - Conflict Resolution

/// Обнаруживает конфликты при синхронизации
func detectConflicts() async -> [SyncConflict] {
    let context = persistentContainer.viewContext
    let request: NSFetchRequest<NSFetchRequestResult> = NSFetchRequest(entityName: "OfflineRecord")
    request.predicate = NSPredicate(format: "isSynced == NO")
    
    guard let unsyncedRecords = try? context.fetch(request) as? [OfflineRecord] else {
        return []
    }
    
    var conflicts: [SyncConflict] = []
    
    for record in unsyncedRecords {
        // Проверяем, есть ли на сервере более новая версия
        // TODO: Сравнить с серверной версией
        // Если есть конфликт, добавляем в список
    }
    
    return conflicts
}

/// Автоматически разрешает конфликты
func autoResolveConflicts(_ conflicts: [SyncConflict]) async {
    for conflict in conflicts {
        switch conflict.resolutionStrategy {
        case .serverWins:
            // Используем версию с сервера
            await applyServerVersion(conflict)
        case .clientWins:
            // Используем локальную версию
            await applyClientVersion(conflict)
        case .merge:
            // Объединяем изменения
            await mergeVersions(conflict)
        case .manual:
            // Требуется ручное разрешение
            await notifyManualResolution(conflict)
        }
    }
}

struct SyncConflict: Codable {
    let recordId: UUID
    let dataType: OfflineDataType
    let clientVersion: Date
    let serverVersion: Date
    let resolutionStrategy: ConflictResolutionStrategy
}

enum ConflictResolutionStrategy {
    case serverWins
    case clientWins
    case merge
    case manual
}
```

### ✅ **ШАГ 2: Добавить приоритизацию данных (45 минут)**

**Файл:** `Core/Offline/OfflineManager.swift`

**Добавить:**
```swift
// MARK: - Data Prioritization

enum DataPriority: Int, Comparable {
    case critical = 0  // Критические данные (высокий приоритет)
    case important = 1  // Важные данные (средний приоритет)
    case normal = 2  // Обычные данные (низкий приоритет)
    
    static func < (lhs: DataPriority, rhs: DataPriority) -> Bool {
        return lhs.rawValue < rhs.rawValue
    }
}

/// Выполняет синхронизацию с приоритетами
func syncWithPriorities() async {
    // Сначала синхронизируем критические данные
    await syncData(priority: .critical)
    
    // Затем важные данные
    await syncData(priority: .important)
    
    // В конце обычные данные
    await syncData(priority: .normal)
}

private func syncData(priority: DataPriority) async {
    // TODO: Реализовать синхронизацию по приоритету
}
```

### ✅ **ШАГ 3: Улучшить синхронизацию (30 минут)**

**Файл:** `Core/Offline/OfflineStorageManager.swift`

**Улучшить метод `sync`:**
```swift
func sync(
    dataType: OfflineDataType,
    onlyChanged: Bool = true  // Синхронизировать только изменённые данные
) async -> Result<Void, NetworkError> {
    // ... существующий код ...
    
    if onlyChanged {
        // Синхронизируем только изменённые записи
        request.predicate = NSPredicate(format: "dataType == %@ AND isSynced == NO AND isModified == YES", dataType.rawValue)
    }
    
    // ... остальной код ...
}
```

**Ожидаемый результат:** ⭐⭐⭐⭐⭐ (5/5)

---

## 6️⃣ БЕЗОПАСНОЕ КЭШИРОВАНИЕ ⭐⭐⭐⭐ → ⭐⭐⭐⭐⭐

**Время:** 2-3 часа  
**Приоритет:** 🟡 СРЕДНИЙ  
**Файлы для изменения:**
- `Core/Cache/CacheManager.swift`
- `Core/Security/SecurityManager.swift`

### ✅ **ШАГ 1: Добавить шифрование кэша (60 минут)**

**Файл:** `Core/Cache/CacheManager.swift`

**Изменить метод `store`:**
```swift
func store<T: Codable>(
    _ data: T,
    forKey key: String,
    ttl: TimeInterval? = nil,
    priority: CachePriority = .normal,
    encrypt: Bool = true  // По умолчанию шифруем
) {
    // Кодируем данные
    guard let encodedData = try? JSONEncoder().encode(data) else {
        print("❌ Cache: Не удалось закодировать данные для \(key)")
        return
    }
    
    // Шифруем данные если нужно
    let finalData: Data
    if encrypt {
        guard let encryptedData = SecurityManager.shared.encryptData(encodedData) else {
            print("❌ Cache: Не удалось зашифровать данные для \(key)")
            return
        }
        finalData = encryptedData
    } else {
        finalData = encodedData
    }
    
    // Создаём запись с зашифрованными данными
    let entry = CacheEntry(
        data: finalData,  // Теперь это зашифрованные данные
        key: key,
        ttl: ttl ?? defaultTTL,
        priority: priority,
        createdAt: Date(),
        lastAccessed: Date(),
        isEncrypted: encrypt
    )
    
    // ... остальной код ...
}
```

**Изменить метод `retrieve`:**
```swift
func retrieve<T: Codable>(_ type: T.Type, forKey key: String) -> T? {
    guard let entry = cache[key] else {
        statistics.totalMisses += 1
        return nil
    }
    
    // Проверяем, не устарели ли данные
    if entry.isExpired {
        remove(key: key)
        statistics.totalMisses += 1
        statistics.expiredEntries += 1
        return nil
    }
    
    // Дешифруем данные если нужно
    let decryptedData: Data
    if entry.isEncrypted {
        guard let decrypted = SecurityManager.shared.decryptData(entry.data as! Data) else {
            print("❌ Cache: Не удалось расшифровать данные для \(key)")
            return nil
        }
        decryptedData = decrypted
    } else {
        decryptedData = entry.data as! Data
    }
    
    // Декодируем данные
    guard let decoded = try? JSONDecoder().decode(type, from: decryptedData) else {
        print("❌ Cache: Не удалось декодировать данные для \(key)")
        return nil
    }
    
    // ... остальной код ...
}
```

**Обновить `CacheEntry`:**
```swift
struct CacheEntry {
    let data: Any
    let key: String
    let ttl: TimeInterval
    let priority: CachePriority
    var createdAt: Date
    var lastAccessed: Date
    let isEncrypted: Bool  // Добавить это поле
    
    // ... остальные поля ...
}
```

### ✅ **ШАГ 2: Добавить изоляцию кэша (45 минут)**

**Файл:** `Core/Cache/CacheManager.swift`

**Добавить:**
```swift
// MARK: - Cache Isolation

private var userCachePrefix: String {
    // Используем user ID для изоляции между пользователями
    return "user_\(getCurrentUserId())_"
}

private var sessionCachePrefix: String {
    // Используем session ID для изоляции между сессиями
    return "session_\(getCurrentSessionId())_"
}

private func getCurrentUserId() -> String {
    // TODO: Получить текущий user ID
    return "default"
}

private func getCurrentSessionId() -> String {
    // TODO: Получить текущий session ID
    return UUID().uuidString
}

/// Изолированный ключ для кэша
private func isolatedKey(_ key: String) -> String {
    return "\(userCachePrefix)\(sessionCachePrefix)\(key)"
}

/// Очищает кэш для текущего пользователя
func clearUserCache() {
    let userPrefix = userCachePrefix
    let keysToRemove = cache.keys.filter { $0.hasPrefix(userPrefix) }
    
    for key in keysToRemove {
        remove(key: key)
    }
    
    print("💾 Cache: Очищен кэш для пользователя")
}

/// Очищает кэш для текущей сессии
func clearSessionCache() {
    let sessionPrefix = sessionCachePrefix
    let keysToRemove = cache.keys.filter { $0.hasPrefix(sessionPrefix) }
    
    for key in keysToRemove {
        remove(key: key)
    }
    
    print("💾 Cache: Очищен кэш для сессии")
}
```

### ✅ **ШАГ 3: Улучшить автоматическую очистку (30 минут)**

**Файл:** `Core/Cache/CacheManager.swift`

**Добавить:**
```swift
// MARK: - Automatic Cleanup

/// Очищает кэш при выходе из приложения
func cleanupOnAppTermination() {
    // Очищаем все не критичные данные
    let criticalKeys = cache.keys.filter { key in
        guard let entry = cache[key] else { return false }
        return entry.priority == .critical
    }
    
    // Сохраняем только критичные данные
    let criticalCache = criticalKeys.compactMap { key -> (String, CacheEntry)? in
        guard let entry = cache[key] else { return nil }
        return (key, entry)
    }
    
    // Очищаем весь кэш
    clear()
    
    // Восстанавливаем критичные данные
    for (key, entry) in criticalCache {
        cache[key] = entry
    }
    
    print("💾 Cache: Очистка при выходе из приложения завершена")
}

/// Очищает кэш при смене пользователя
func cleanupOnUserChange() {
    clearUserCache()
    print("💾 Cache: Очистка при смене пользователя завершена")
}
```

**Ожидаемый результат:** ⭐⭐⭐⭐⭐ (5/5)

---

## 7️⃣ IoT ЗАЩИТА ⭐⭐⭐⭐ → ⭐⭐⭐⭐⭐

**Время:** 4-5 часов  
**Приоритет:** 🟢 НИЗКИЙ  
**Файлы для изменения:**
- `Core/IoT/IoTSecurityModule.swift`

### ✅ **ШАГ 1: Улучшить обнаружение устройств (90 минут)**

**Файл:** `Core/IoT/IoTSecurityModule.swift`

**Добавить:**
```swift
// MARK: - Device Discovery

/// Сканирует сеть для обнаружения устройств
func scanNetwork() async -> [IoTDevice] {
    // TODO: Реализовать сканирование сети
    // Использовать Network.framework для сканирования
    return []
}

/// Обнаруживает устройства по MAC адресам
func discoverByMACAddresses(_ macAddresses: [String]) async -> [IoTDevice] {
    // TODO: Реализовать обнаружение по MAC адресам
    return []
}

/// Обнаруживает устройства по портам
func discoverByPorts(_ ports: [Int]) async -> [IoTDevice] {
    // TODO: Реализовать обнаружение по портам
    return []
}

/// Обнаруживает устройства по протоколам
func discoverByProtocols(_ protocols: [String]) async -> [IoTDevice] {
    // TODO: Реализовать обнаружение по протоколам
    return []
}
```

### ✅ **ШАГ 2: Добавить поддержку разных типов устройств (60 минут)**

**Добавить модели:**
```swift
enum IoTDeviceType: String, Codable {
    case smartLight = "smart_light"
    case smartOutlet = "smart_outlet"
    case smartCamera = "smart_camera"
    case smartLock = "smart_lock"
    case smartThermostat = "smart_thermostat"
    case smartTV = "smart_tv"
    case smartSpeaker = "smart_speaker"
    case unknown = "unknown"
}

struct IoTDevice: Codable, Identifiable {
    let id: String
    let name: String
    let type: IoTDeviceType
    let macAddress: String?
    let ipAddress: String?
    let isSecure: Bool
    let threatLevel: ThreatLevel
}
```

### ✅ **ШАГ 3: Добавить анализ трафика (60 минут)**

**Добавить:**
```swift
// MARK: - Traffic Analysis

/// Анализирует сетевой трафик устройства
func analyzeTraffic(for device: IoTDevice) async -> TrafficAnalysisResult {
    // TODO: Реализовать анализ трафика
    return TrafficAnalysisResult(
        deviceId: device.id,
        suspiciousActivity: false,
        blockedConnections: 0,
        totalConnections: 0
    )
}

/// Обнаруживает подозрительную активность
func detectSuspiciousActivity(for device: IoTDevice) async -> Bool {
    // TODO: Реализовать обнаружение подозрительной активности
    return false
}

/// Блокирует небезопасные соединения
func blockUnsafeConnections(for device: IoTDevice) async {
    // TODO: Реализовать блокировку небезопасных соединений
}

struct TrafficAnalysisResult: Codable {
    let deviceId: String
    let suspiciousActivity: Bool
    let blockedConnections: Int
    let totalConnections: Int
}
```

### ✅ **ШАГ 4: Реализовать автоматическую блокировку (30 минут)**

**Добавить:**
```swift
// MARK: - Automatic Blocking

/// Автоматически блокирует небезопасные устройства
func autoBlockUnsafeDevices() async {
    let devices = await scanNetwork()
    
    for device in devices {
        if !device.isSecure {
            await blockDevice(device)
            await notifySuspiciousDevice(device)
        }
    }
}

/// Блокирует устройство
private func blockDevice(_ device: IoTDevice) async {
    // TODO: Реализовать блокировку устройства
}

/// Уведомляет о подозрительном устройстве
private func notifySuspiciousDevice(_ device: IoTDevice) async {
    // TODO: Отправить уведомление
}
```

**Ожидаемый результат:** ⭐⭐⭐⭐⭐ (5/5)

---

## 📊 ИТОГОВАЯ ТАБЛИЦА РЕАЛИЗАЦИИ

| № | Улучшение | Время | Приоритет | Статус |
|---|-----------|-------|-----------|--------|
| 1 | SSL Pinning | 2-3ч | 🔴 Высокий | ⏳ Ожидает |
| 2 | JWT токены | 3-4ч | 🟡 Средний | ⏳ Ожидает |
| 3 | Антивирус | 4-5ч | 🟡 Средний | ⏳ Ожидает |
| 4 | Родительский контроль | 3-4ч | 🟡 Средний | ⏳ Ожидает |
| 5 | Офлайн защита | 2-3ч | 🟡 Средний | ⏳ Ожидает |
| 6 | Безопасное кэширование | 2-3ч | 🟡 Средний | ⏳ Ожидает |
| 7 | IoT защита | 4-5ч | 🟢 Низкий | ⏳ Ожидает |

**Итого:** 20-30 часов работы

---

## 🎯 ПОРЯДОК ВЫПОЛНЕНИЯ

### **Неделя 1 (Высокий приоритет):**
- День 1-2: SSL Pinning (2-3ч)
- День 3-4: JWT токены (3-4ч)
- День 5: Тестирование (2ч)

### **Неделя 2 (Средний приоритет):**
- День 1-2: Антивирус (4-5ч)
- День 3-4: Родительский контроль (3-4ч)
- День 5: Офлайн защита + Безопасное кэширование (4-6ч)

### **Неделя 3 (Низкий приоритет):**
- День 1-2: IoT защита (4-5ч)
- День 3: Финальное тестирование (2ч)

---

**Дата создания:** 2025-11-06  
**Статус:** ✅ ПЛАН ГОТОВ К ВЫПОЛНЕНИЮ  
**Результат:** 5.0 / 5.0 ⭐⭐⭐⭐⭐

