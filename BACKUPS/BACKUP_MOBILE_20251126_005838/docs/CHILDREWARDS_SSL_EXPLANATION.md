# 📖 Объяснение: ChildRewardsService API и SSL-сертификаты

**Дата создания:** 2025-11-11

---

## 1️⃣ Что такое "Описание API для ChildRewardsService"?

### Простыми словами:

**API (Application Programming Interface)** — это способ, как приложение общается с сервером.

**ChildRewardsService** — это сервис, который должен получать данные о наградах для детей с сервера.

### Что нужно получить:

**Описание API** — это документ, который объясняет:
- Какие запросы можно отправлять на сервер
- Какие ответы приходят обратно
- Какие данные нужны для запросов

### Пример того, что нужно:

```
GET /api/rewards/balance
Ответ: { "balance": 245, "weeklyEarned": 128 }

GET /api/rewards/history
Ответ: [ { "id": "1", "title": "Домашнее задание", "amount": 10, "date": "2025-11-11" } ]

POST /api/rewards/give
Запрос: { "childId": "123", "reason": "homework", "amount": 10 }
Ответ: { "success": true, "newBalance": 255 }
```

### Где получить:

- От backend разработчиков
- Из документации API (Swagger, Postman)
- Из существующих эндпоинтов (если уже есть)

### Что делать, если нет:

- Можно использовать моковые данные (как сейчас)
- Можно создать временный API на сервере
- Можно отложить до получения описания

---

## 2️⃣ Что такое "Файлы SSL-сертификатов"?

### Простыми словами:

**SSL-сертификат** — это "паспорт" сервера, который подтверждает, что сервер настоящий и безопасный.

**SSL Pinning** — это защита, которая проверяет, что приложение общается именно с нашим сервером, а не с поддельным.

### Что нужно получить:

**Файлы сертификатов:**
- `aladdin_cert.cer` — основной сертификат
- `aladdin_cert_backup.cer` — резервный сертификат

### Где получить:

- От backend разработчиков
- От DevOps инженера
- Из папки сервера (если есть доступ)

### Что делать, если нет:

- Можно временно отключить SSL Pinning (только для разработки!)
- Можно использовать самоподписанный сертификат (для тестов)
- Можно отложить до получения файлов

---

## 📋 Текущее состояние

### ChildRewardsService:

**Статус:** ⏳ Используются моковые данные

**Где используется:**
- `Screens/ChildRewardsScreen.swift` — экран наград
- `ViewModels/ChildRewardsViewModel.swift` — логика наград

**Что работает:**
- ✅ UI отображается
- ✅ Локальные данные (UserDefaults)
- ✅ Моковые награды и история

**Что не работает:**
- ❌ Реальные данные с сервера
- ❌ Синхронизация между устройствами
- ❌ История наград с сервера

---

### SSL-сертификаты:

**Статус:** ⚠️ Сертификаты не найдены

**Где проверяется:**
- `Core/Network/NetworkManager.swift` — загрузка сертификатов

**Что видно в логах:**
```
⚠️ SSL Pinning: Сертификат aladdin_cert не найден
⚠️ SSL Pinning: Сертификат aladdin_cert_backup не найден
⚠️ SSL Pinning: Сертификаты не найдены в Bundle!
```

**Что работает:**
- ✅ NetworkManager работает без сертификатов (для разработки)
- ✅ Запросы к API проходят

**Что не работает:**
- ❌ SSL Pinning (защита от поддельных серверов)
- ❌ Проверка подлинности сервера

---

## ✅ Что можно сделать сейчас

### Вариант 1: Продолжить с моками

**Для ChildRewardsService:**
- Оставить моковые данные
- Работать над UI и логикой
- Подключить реальный API позже

**Для SSL-сертификатов:**
- Продолжить разработку без SSL Pinning
- Добавить сертификаты перед релизом

### Вариант 2: Создать временные решения

**Для ChildRewardsService:**
- Создать временный API на тестовом сервере
- Использовать простые эндпоинты

**Для SSL-сертификатов:**
- Создать самоподписанный сертификат для тестов
- Заменить на реальный перед релизом

---

## 📝 Рекомендации

1. **ChildRewardsService:**
   - Связаться с backend командой
   - Получить описание API
   - Или создать временный API для тестов

2. **SSL-сертификаты:**
   - Связаться с DevOps
   - Получить файлы сертификатов
   - Или отложить до получения файлов

3. **Приоритет:**
   - Можно продолжать разработку без них
   - Добавить перед релизом в продакшен

---

**Дата создания:** 2025-11-11  
**Статус:** Актуально

