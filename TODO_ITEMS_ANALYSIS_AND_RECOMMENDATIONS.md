# 📋 АНАЛИЗ TODO И ТЕСТИРОВАНИЯ

**Дата:** 2026-02-09  
**Статус:** ✅ **ПОЛНЫЙ АНАЛИЗ ЗАВЕРШЕН**

---

## 🔍 ЧТО НУЖНО СДЕЛАТЬ?

### **1. Интеграционное тестирование на устройстве**

**Статус:** ⚠️ **НЕ КОД - РУЧНОЕ ТЕСТИРОВАНИЕ**

**Что это:**
- Это **НЕ код**, который нужно писать
- Это **процесс ручного тестирования** на реальном устройстве
- Требует работающий backend сервер

**Что нужно сделать:**
1. ✅ Установить приложение на реальное устройство (iPhone)
2. ✅ Создать семью через UI
3. ✅ Проверить, что токены сохраняются в Keychain
4. ✅ Проверить, что API запросы работают после авторизации
5. ✅ Проверить, что 403/404 ошибки исчезли

**Когда делать:**
- ⏳ После того, как backend будет готов
- ⏳ Перед релизом в App Store

**Критичность:** ⚠️ **СРЕДНЯЯ** - нужно для проверки, но не блокирует код

---

### **2. Тестирование API после авторизации**

**Статус:** ⚠️ **НЕ КОД - РУЧНОЕ ТЕСТИРОВАНИЕ**

**Что это:**
- Это **НЕ код**, который нужно писать
- Это **процесс ручного тестирования** API запросов
- Требует работающий backend сервер

**Что нужно сделать:**
1. ✅ Авторизоваться (создать семью)
2. ✅ Проверить, что токены в Keychain
3. ✅ Выполнить несколько API запросов (например, `/user/profile`)
4. ✅ Проверить, что запросы возвращают 200 OK (не 403/404)
5. ✅ Проверить, что данные загружаются корректно

**Когда делать:**
- ⏳ После того, как backend будет готов
- ⏳ Вместе с интеграционным тестированием

**Критичность:** ⚠️ **СРЕДНЯЯ** - нужно для проверки, но не блокирует код

---

## 📝 TODO: СТРОКА 189 - Keychain для согласия

**Файл:** `ViewModels/FamilyRegistrationViewModel.swift`  
**Строка:** 189  
**Текущий код:**
```swift
// TODO: В будущем заменить на Keychain для безопасности
UserDefaults.standard.set(true, forKey: AppConfig.UserDefaultsKeys.consentAccepted)
```

**Что это:**
- Сейчас согласие сохраняется в `UserDefaults` (небезопасно)
- TODO предлагает перейти на `Keychain` (безопаснее)

**Нужно ли делать:**
- ⚠️ **НЕ КРИТИЧНО** для авторизации
- ✅ **ЖЕЛАТЕЛЬНО** для безопасности (но не блокирует)

**Анализ:**
- ✅ Согласие - это не sensitive данные (это просто флаг true/false)
- ✅ UserDefaults достаточно для этого случая
- ⚠️ Keychain будет избыточен (но более безопасен)

**Рекомендация:**
- ⚠️ **МОЖНО ОСТАВИТЬ** как есть (UserDefaults достаточно)
- ✅ **ИЛИ СДЕЛАТЬ** для улучшения безопасности (но не обязательно)

**Критичность:** ⚠️ **НИЗКАЯ** - не блокирует авторизацию

---

## 📝 TODO: СТРОКА 517 - recoverFamily метод

**Файл:** `ViewModels/FamilyRegistrationViewModel.swift`  
**Строка:** 517  
**Текущий код:**
```swift
func recoverAccess(withCode code: String) {
    isLoading = true

    // TODO: Implement recoverFamily method in APIService
    DispatchQueue.main.async {
        self.isLoading = false
        self.errorMessage = "Функция восстановления доступа временно недоступна"
    }
}
```

**Что это:**
- Метод `recoverAccess()` не реализован
- Показывает ошибку "Функция восстановления доступа временно недоступна"

**Проверка:**
- ✅ Метод `recoverFamily()` **УЖЕ РЕАЛИЗОВАН** в `NetworkManager` (строка 788)
- ✅ Метод `recoverFamily()` **УЖЕ РЕАЛИЗОВАН** в `NetworkManager` extension (строка 788)
- ❌ Но `recoverAccess()` **НЕ ИСПОЛЬЗУЕТ** его

**Где используется:**
- ✅ `BackupRecoveryModal.swift` - вызывает `recoverAccess()` (строка 171)
- ✅ `InvitationCodeInputModal.swift` - вызывает `recoverAccess()` (строка 147)

**Нужно ли делать:**
- ✅ **ДА, НУЖНО** - функция восстановления используется в UI
- ✅ Метод уже есть, нужно просто подключить его

**Что нужно сделать:**
1. ✅ Использовать `apiService` для вызова `recoverFamily()`
2. ✅ Или использовать `networkManager.recoverFamily()` напрямую
3. ✅ Обработать успех/ошибку
4. ✅ Обновить UI

**Критичность:** ⚠️ **СРЕДНЯЯ** - функция используется в UI, но не блокирует авторизацию

---

## ✅ РЕКОМЕНДАЦИИ

### **1. Интеграционное тестирование**
- ⚠️ **НЕ КОД** - это ручной процесс
- ⏳ Делать после готовности backend
- ⚠️ Не блокирует код

### **2. Тестирование API**
- ⚠️ **НЕ КОД** - это ручной процесс
- ⏳ Делать после готовности backend
- ⚠️ Не блокирует код

### **3. TODO: Keychain для согласия (строка 189)**
- ⚠️ **НЕ КРИТИЧНО** - можно оставить как есть
- ✅ **ЖЕЛАТЕЛЬНО** - но не обязательно
- ⚠️ Не блокирует авторизацию

### **4. TODO: recoverFamily (строка 517)**
- ✅ **НУЖНО СДЕЛАТЬ** - функция используется в UI
- ✅ Метод уже реализован, нужно подключить
- ⚠️ Не блокирует авторизацию, но блокирует функцию восстановления

---

## 🎯 ПРИОРИТЕТЫ

### **Высокий приоритет:**
1. ✅ **TODO: recoverFamily (строка 517)** - функция используется в UI

### **Средний приоритет:**
2. ⚠️ **Интеграционное тестирование** - после готовности backend
3. ⚠️ **Тестирование API** - после готовности backend

### **Низкий приоритет:**
4. ⚠️ **TODO: Keychain для согласия** - не критично

---

## 📊 ИТОГОВАЯ ТАБЛИЦА

| Задача | Тип | Критичность | Статус | Нужно делать? |
|--------|-----|-------------|--------|---------------|
| Интеграционное тестирование | Ручное тестирование | Средняя | ⏳ Ожидает backend | ⚠️ После backend |
| Тестирование API | Ручное тестирование | Средняя | ⏳ Ожидает backend | ⚠️ После backend |
| TODO: Keychain для согласия | Код | Низкая | ⚠️ Не сделано | ⚠️ Не обязательно |
| TODO: recoverFamily | Код | Средняя | ❌ Не сделано | ✅ **ДА, НУЖНО** |

---

## ✅ ЗАКЛЮЧЕНИЕ

**Что нужно сделать СЕЙЧАС:**
1. ✅ **TODO: recoverFamily (строка 517)** - подключить существующий метод

**Что можно сделать ПОЗЖЕ:**
2. ⚠️ **TODO: Keychain для согласия** - не критично
3. ⚠️ **Интеграционное тестирование** - после backend
4. ⚠️ **Тестирование API** - после backend

**Авторизация:**
- ✅ **100% ГОТОВА** - все основные задачи выполнены
- ⚠️ Осталось только подключить `recoverFamily()` (не блокирует авторизацию)
