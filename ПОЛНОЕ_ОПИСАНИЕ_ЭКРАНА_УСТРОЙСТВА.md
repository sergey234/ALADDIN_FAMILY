# 📱 ПОЛНОЕ ОПИСАНИЕ ЭКРАНА "УСТРОЙСТВА"

## 🎯 НАЗНАЧЕНИЕ:

Экран **"Устройства"** (`DevicesScreen`) — это **центральный пункт управления всеми устройствами семьи** в приложении ALADDIN Family. Здесь родители могут видеть все подключённые устройства, их статус защиты, фильтровать по категориям и управлять защитой.

---

## 📍 РАСПОЛОЖЕНИЕ:

**Путь в приложении:**
```
Главная страница (MainScreen)
    ↓
Нижнее меню → 📱 "Устройства"
    ↓
DevicesScreen
```

**Код навигации:**
```swift
// Screens/01_MainScreen.swift, строка 189
NavigationLink(destination: DevicesScreen()) {
    VStack(spacing: 4) {
        Text("📱")
        Text("Устройства")
    }
}
```

---

## 🎨 ЧТО ОТОБРАЖАЕТСЯ НА ЭКРАНЕ:

### 1️⃣ **НАВИГАЦИОННАЯ ПАНЕЛЬ (верхняя часть)**

**Элементы:**
- **Заголовок:** "УСТРОЙСТВА"
- **Подзаголовок:** "{количество} устройств под защитой" (динамический)
- **Кнопка "Назад"** (⬅️) — возврат на главную
- **Кнопка "Добавить"** (+) — открывает модальное окно для добавления устройства

**Код:**
```swift
ALADDINNavigationBar(
    title: "УСТРОЙСТВА",
    subtitle: "\(devices.count) устройств под защитой",
    showBackButton: true,
    showAddButton: true,
    ...
)
```

---

### 2️⃣ **СТАТИСТИКА УСТРОЙСТВ** 📊

**Блок с тремя метриками:**

#### ✅ **Защищённые устройства** 🛡️
- **Иконка:** 🛡️
- **Число:** Количество устройств со статусом `.protected`
- **Цвет:** Зелёный (`.successGreen`)

#### ⚠️ **Предупреждения**
- **Иконка:** ⚠️
- **Число:** Количество устройств со статусом `.warning`
- **Цвет:** Оранжевый (`.warningOrange`)

#### 🔴 **Опасность**
- **Иконка:** 🔴
- **Число:** Количество устройств со статусом `.danger`
- **Цвет:** Красный (`.dangerRed`)

**Расположение:** Горизонтально, три колонки с равной шириной

**Пример:**
```
📊 СТАТИСТИКА
┌──────────┬──────────┬──────────┐
│ 🛡️ 5     │ ⚠️ 2     │ 🔴 1     │
│ Защищённые│Предупреждения│Опасность│
└──────────┴──────────┴──────────┘
```

---

### 3️⃣ **ФИЛЬТРЫ** 🔍

**Горизонтальная прокрутка с кнопками-фильтрами:**

**Доступные фильтры:**
1. **"Все"** (`.all`) — показать все устройства
2. **"Защищённые"** (`.protected`) — только защищённые
3. **"Предупреждения"** (`.warning`) — устройства с предупреждениями
4. **"Опасность"** (`.danger`) — устройства в опасности
5. **"Неактивные"** (`.inactive`) — устройства без активности

**Визуал:**
- Выбранный фильтр: Синий фон, белый текст
- Невыбранный фильтр: Полупрозрачный фон, серый текст
- Горизонтальная прокрутка (если не помещается)

---

### 4️⃣ **СПИСОК УСТРОЙСТВ** 📱

**Карточка каждого устройства содержит:**

#### **Иконка устройства:**
- **iPhone:** 🟦 Синяя иконка "iphone"
- **iPad:** 🟪 Фиолетовая иконка "ipad"
- **Mac:** ⚫ Чёрная иконка "laptopcomputer"
- **Android:** 🟢 Зелёная иконка "phone"

#### **Информация об устройстве:**
- **Название:** Например, "iPhone 14 Pro"
- **Владелец:** Имя члена семьи (например, "Сергей", "Маша")
- **Последняя активность:** "Сейчас", "5 мин назад", "1 час назад"

#### **Статус защиты:**
- **Индикатор:** Цветной кружок (🟢/🟠/🔴/⚪)
- **Текст статуса:** "Защищён", "Предупреждение", "Опасность", "Неактивен"
- **Цвет текста:** Соответствует статусу

**Пример карточки:**
```
┌─────────────────────────────────┐
│ 📱  iPhone 14 Pro              │
│     Сергей                      │
│     Сейчас                      │
│                    🟢 Защищён   │
└─────────────────────────────────┘
```

**Счётчик:**
Вверху списка показывается: **"{количество отфильтрованных} из {общее количество}"**

---

## 🔄 КАК ЭТО РАБОТАЕТ:

### **1. ЗАГРУЗКА ДАННЫХ:**

**При открытии экрана:**
```swift
.onAppear {
    loadDevices()
}
```

**Процесс загрузки:**
1. Показывается индикатор загрузки (`ProgressView`)
2. Вызывается `apiService.getDevices()`
3. Получаются данные с сервера (или моки)
4. Конвертируются в модель `Device`
5. Обновляется массив `devices`
6. Скрывается индикатор загрузки

**Код:**
```swift
private func loadDevices() {
    isLoading = true
    errorMessage = nil
    
    apiService.getDevices { result in
        DispatchQueue.main.async {
            isLoading = false
            
            switch result {
            case .success(let deviceResponses):
                devices = deviceResponses.map { convertToDevice($0) }
            case .failure(let error):
                errorMessage = error.localizedDescription
            }
        }
    }
}
```

---

### **2. ФИЛЬТРАЦИЯ:**

**При выборе фильтра:**
1. Изменяется `selectedFilter`
2. SwiftUI автоматически пересчитывает `filteredDevices`
3. Обновляется список устройств

**Логика фильтрации:**
```swift
private var filteredDevices: [Device] {
    switch selectedFilter {
    case .all: return devices
    case .protected: return devices.filter { $0.status == .protected }
    case .warning: return devices.filter { $0.status == .warning }
    case .danger: return devices.filter { $0.status == .danger }
    case .inactive: return devices.filter { $0.status == .inactive }
    }
}
```

---

### **3. ДОБАВЛЕНИЕ УСТРОЙСТВА:**

**Процесс:**
1. Пользователь нажимает кнопку "+" в навигационной панели
2. Открывается модальное окно `AddDeviceView`
3. Пользователь вводит данные устройства
4. Устройство добавляется через API
5. Список автоматически обновляется

**Текущая реализация:**
```swift
.sheet(isPresented: $showAddDevice) {
    AddDeviceView()  // ⚠️ Пока только заглушка
}
```

---

### **4. ОБРАБОТКА ОШИБОК:**

**При ошибке загрузки:**
- Показывается `Alert` с текстом ошибки
- Кнопка "ОК" закрывает алерт
- Пользователь может попробовать снова

---

## 📊 МОДЕЛЬ ДАННЫХ:

### **Device:**
```swift
struct Device: Identifiable {
    let id: UUID              // Уникальный идентификатор
    let name: String          // Название (например, "iPhone 14 Pro")
    let owner: String         // Владелец (имя члена семьи)
    let type: DeviceType      // Тип устройства
    let status: DeviceStatus   // Статус защиты
    let lastActive: String    // Последняя активность ("Сейчас", "5 мин назад")
}
```

### **DeviceType:**
```swift
enum DeviceType {
    case iphone    // iPhone
    case ipad      // iPad
    case mac       // MacBook
    case android   // Android устройство
}
```

### **DeviceStatus:**
```swift
enum DeviceStatus {
    case protected  // 🟢 Защищён — всё в порядке
    case warning    // 🟠 Предупреждение — нужно внимание
    case danger     // 🔴 Опасность — требуется действие
    case inactive   // ⚪ Неактивен — устройство не используется
}
```

---

## 🔗 СВЯЗЬ С ДРУГИМИ ЭКРАНАМИ:

### **DeviceDetailScreen** (Детали устройства):

**Навигация:**
- При нажатии на карточку устройства → открывается `DeviceDetailScreen`

**Что показывается:**
- Полная информация об устройстве
- Статистика угроз
- История активности
- Настройки защиты
- Возможность блокировки устройства

**Вкладки в деталях:**
1. **Инфо** — основная информация
2. **Статистика** — метрики защиты
3. **Угрозы** — заблокированные угрозы
4. **Настройки** — параметры защиты

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ:

### **API Интеграция:**
- **Метод:** `apiService.getDevices()`
- **Тип запроса:** Асинхронный callback
- **Формат ответа:** `[DeviceResponse]`

### **Преобразование данных:**
```swift
private func convertToDevice(_ response: DeviceResponse) -> Device {
    // Конвертация типа устройства
    let deviceType: DeviceType
    switch response.type.lowercased() {
    case "iphone": deviceType = .iphone
    case "ipad": deviceType = .ipad
    case "mac": deviceType = .mac
    case "android": deviceType = .android
    default: deviceType = .iphone
    }
    
    // Конвертация статуса
    let deviceStatus: DeviceStatus
    switch response.status.lowercased() {
    case "protected": deviceStatus = .protected
    case "warning": deviceStatus = .warning
    case "danger": deviceStatus = .danger
    case "inactive": deviceStatus = .inactive
    default: deviceStatus = .inactive
    }
    
    return Device(...)
}
```

---

## 📈 СТАТИСТИКА (АВТОМАТИЧЕСКИЙ ПОДСЧЁТ):

**Все метрики рассчитываются динамически из массива `devices`:**

```swift
// Защищённые
devices.filter { $0.status == .protected }.count

// Предупреждения
devices.filter { $0.status == .warning }.count

// Опасность
devices.filter { $0.status == .danger }.count
```

**Преимущества:**
- ✅ Всегда актуальные данные
- ✅ Автоматическое обновление при изменении списка
- ✅ Не требует ручного подсчёта

---

## 🎯 ОСНОВНЫЕ ФУНКЦИИ:

### ✅ **Что работает сейчас:**

1. **Отображение списка устройств** — работает
2. **Фильтрация по статусам** — работает
3. **Статистика устройств** — работает (автоматический подсчёт)
4. **Индикатор загрузки** — работает
5. **Обработка ошибок** — работает
6. **Навигация назад** — работает

### ⚠️ **Что требует доработки:**

1. **Добавление устройства:**
   - `AddDeviceView` — сейчас только заглушка
   - Нужна форма ввода данных
   - Интеграция с API для добавления

2. **Детали устройства:**
   - Навигация на `DeviceDetailScreen` не реализована
   - Нужно добавить `.onTapGesture` на карточку

3. **Обновление данных:**
   - Нет функции "Потянуть для обновления" (pull-to-refresh)
   - Нет автоматического обновления по таймеру

---

## 🔄 ПОТОК ДАННЫХ:

```
1. Открытие экрана
   ↓
2. .onAppear → loadDevices()
   ↓
3. apiService.getDevices()
   ↓
4. Получение DeviceResponse[]
   ↓
5. convertToDevice() → Device[]
   ↓
6. Обновление @State devices
   ↓
7. SwiftUI перерисовывает UI
   ↓
8. Отображение списка + статистики
```

---

## 📱 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:

### **Сценарий 1: Просмотр всех устройств**
```
1. Открыть "Устройства"
2. Выбрать фильтр "Все"
3. Видеть все устройства семьи
```

### **Сценарий 2: Проверка проблемных устройств**
```
1. Открыть "Устройства"
2. Выбрать фильтр "Опасность"
3. Видеть устройства, требующие внимания
4. Нажать на устройство → открыть детали
```

### **Сценарий 3: Добавление нового устройства**
```
1. Открыть "Устройства"
2. Нажать кнопку "+"
3. Заполнить форму
4. Сохранить
5. Устройство появляется в списке
```

---

## ✅ ИТОГОВАЯ СВОДКА:

**Экран "Устройства" — это:**

✅ **Центр управления устройствами семьи**
✅ **Мониторинг статуса защиты всех устройств**
✅ **Быстрый доступ к проблемным устройствам** (через фильтры)
✅ **Наглядная статистика защиты** (защищённые/предупреждения/опасность)
✅ **Удобная фильтрация** по статусам
✅ **Интеграция с API** для загрузки данных

**Основная цель:** Дать родителям полный контроль и видимость над всеми устройствами семьи для обеспечения безопасности.

---

**Файл:** `Screens/20_DevicesScreen.swift`  
**Модель:** `Shared/Models/Device.swift`  
**Детали:** `Screens/22_DeviceDetailScreen.swift`

